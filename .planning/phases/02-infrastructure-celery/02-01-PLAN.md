---
phase: 02-infrastructure-celery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/debug_series_test.py
  - backend/tests/focused_series_test.py
  - backend/tests/backend_test_sprint7.py
  - backend/tests/backend_test_focused.py
  - backend/tests/targeted_test.py
  - backend/tests/conftest.py
  - backend/celery_app.py
  - backend/celeryconfig.py
  - backend/tasks/content_tasks.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "Celery worker starts with all 4 queues (default, media, content, video) logged"
    - "Celery beat starts and registers all 7 periodic tasks in its schedule"
    - "All baseline tests pass with zero collection errors"
    - "Test suite collects 90+ tests without sys.exit or missing-module failures"
  artifacts:
    - path: "backend/celery_app.py"
      provides: "Celery app instance with Redis broker and 2 task modules"
      contains: "include=["
    - path: "backend/celeryconfig.py"
      provides: "Beat schedule with 7 periodic tasks and queue routing"
      contains: "beat_schedule"
    - path: "backend/tests/conftest.py"
      provides: "Shared test fixtures and pytest configuration"
      contains: "def "
  key_links:
    - from: "backend/celery_app.py"
      to: "backend/celeryconfig.py"
      via: "config_from_object"
      pattern: "config_from_object.*celeryconfig"
    - from: "backend/celeryconfig.py"
      to: "backend/tasks/content_tasks.py"
      via: "beat_schedule task names"
      pattern: "tasks\\.content_tasks\\."
---

<objective>
Fix the test suite so all tests collect and pass, and verify the Celery worker/beat configuration is complete and functional.

Purpose: INFRA-01 requires Celery to start cleanly; INFRA-02 requires all baseline tests to pass. The test suite currently has 4 files with `sys.exit(1)` at module level that crash pytest collection, and at least one file importing `aiohttp` which is not installed. The Celery config files already exist but need verification that all task names match actual function names.
Output: A passing test suite and verified Celery configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/celery_app.py
@backend/celeryconfig.py
@backend/tasks/content_tasks.py
@backend/tasks/media_tasks.py
@backend/Procfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test suite collection errors and ensure all tests pass</name>
  <files>
    backend/tests/debug_series_test.py,
    backend/tests/focused_series_test.py,
    backend/tests/backend_test_sprint7.py,
    backend/tests/backend_test_focused.py,
    backend/tests/targeted_test.py,
    backend/tests/auth_isolation_test.py,
    backend/tests/backend_test.py,
    backend/tests/production_deployment_test.py,
    backend/tests/conftest.py
  </files>
  <read_first>
    backend/tests/debug_series_test.py,
    backend/tests/focused_series_test.py,
    backend/tests/backend_test_sprint7.py,
    backend/tests/backend_test_focused.py,
    backend/tests/targeted_test.py,
    backend/tests/auth_isolation_test.py,
    backend/tests/backend_test.py,
    backend/tests/production_deployment_test.py,
    backend/tests/conftest.py
  </read_first>
  <action>
    Four test files contain `sys.exit(1)` at module level, which crashes pytest collection for the entire suite:
    - backend/tests/debug_series_test.py
    - backend/tests/focused_series_test.py
    - backend/tests/backend_test_sprint7.py
    - backend/tests/backend_test_focused.py

    For each of these files:
    1. Remove the `sys.exit(1)` call and surrounding guard logic
    2. If the file has a `if __name__ != "__main__": sys.exit(1)` pattern, remove that block entirely
    3. Ensure the test functions inside still have valid pytest-compatible signatures (def test_* or async def test_*)
    4. If the file has `import aiohttp` and aiohttp is not in requirements.txt, replace aiohttp usage with httpx (which IS in requirements.txt: httpx 0.28.1). Use `httpx.AsyncClient` instead of `aiohttp.ClientSession`.

    For backend/tests/targeted_test.py:
    - It imports `aiohttp` which is not installed. Replace `import aiohttp` with `import httpx` and update any `aiohttp.ClientSession` usage to `httpx.AsyncClient`.

    For backend/tests/auth_isolation_test.py, backend/tests/backend_test.py, backend/tests/production_deployment_test.py:
    - Read these files to identify their collection errors and fix them (likely also aiohttp imports or sys.exit patterns).

    If conftest.py does not exist, create it with a basic fixture for the test client:
    ```python
    import pytest
    from unittest.mock import AsyncMock, patch

    @pytest.fixture
    def mock_db():
        """Mock MongoDB database for unit tests."""
        with patch("database.db") as mock:
            yield mock
    ```

    After fixes, run: `cd backend && python3 -m pytest --collect-only` to verify all tests collect without errors.
    Then run: `cd backend && python3 -m pytest -x -q` to verify tests pass.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest --collect-only 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest --collect-only` exits with code 0 and shows "X tests collected" with no errors
    - No test file contains `sys.exit(1)` at module level
    - No test file imports `aiohttp` (all replaced with httpx)
    - `python3 -m pytest -x -q` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>All tests collect without errors and pass. Zero regressions from Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Celery configuration completeness and task name alignment</name>
  <files>
    backend/celery_app.py,
    backend/celeryconfig.py,
    backend/tasks/content_tasks.py,
    backend/tasks/media_tasks.py
  </files>
  <read_first>
    backend/celery_app.py,
    backend/celeryconfig.py,
    backend/tasks/content_tasks.py,
    backend/tasks/media_tasks.py,
    backend/Procfile
  </read_first>
  <action>
    Verify that every task name in celeryconfig.py beat_schedule matches an actual `@shared_task` decorated function in the tasks modules:

    Expected beat_schedule task names and their targets:
    - `tasks.content_tasks.process_scheduled_posts` -> must exist as `@shared_task(name='tasks.content_tasks.process_scheduled_posts')` in content_tasks.py
    - `tasks.content_tasks.reset_daily_limits` -> same pattern
    - `tasks.content_tasks.refresh_monthly_credits` -> same pattern
    - `tasks.content_tasks.cleanup_old_jobs` -> same pattern
    - `tasks.content_tasks.cleanup_expired_shares` -> same pattern
    - `tasks.content_tasks.aggregate_daily_analytics` -> same pattern
    - `tasks.content_tasks.cleanup_stale_running_jobs` -> same pattern

    For each task in the beat_schedule, grep content_tasks.py for the matching `@shared_task(name=...)` decorator or function name. If any task name in celeryconfig.py does NOT match, fix the mismatch.

    Verify celery_app.py:
    - `include` list contains `["tasks.content_tasks", "tasks.media_tasks"]`
    - broker and backend use `settings.app.redis_url` with fallback to `redis://localhost:6379/0`
    - The module exports both `celery_app` and `app` (alias for CLI discovery)

    Verify Procfile has these 3 entries:
    - `web: uvicorn server:app --host 0.0.0.0 --port $PORT`
    - `worker: celery -A celery_app:celery_app worker --loglevel=info --concurrency=2 -Q default,media,content,video`
    - `beat: celery -A celery_app:celery_app beat --loglevel=info --scheduler celery.beat:PersistentScheduler`

    Verify celeryconfig.py task_routes:
    - `tasks.media_tasks.*` routes to `media` queue
    - `tasks.content_tasks.*` routes to `content` queue
    - Default queue is implicitly `default` (Celery default)

    If any `@shared_task` in content_tasks.py does NOT have an explicit `name=` kwarg, add one matching the pattern `tasks.content_tasks.{function_name}` to prevent auto-naming issues.

    Also verify that video queue is used: check if any task routes to `video` queue or if media_tasks.py has video-related tasks that should route there. If no task routes to `video`, add a route entry in celeryconfig.py: `"tasks.media_tasks.generate_video*": {"queue": "video"}` (matching any video generation task names).
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -c "from celery_app import celery_app; print('Celery app OK:', celery_app.main); from celeryconfig import beat_schedule; print('Beat tasks:', len(beat_schedule))"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "from celery_app import celery_app; print(celery_app.main)"` prints `thookai` without errors
    - `python3 -c "from celeryconfig import beat_schedule; print(len(beat_schedule))"` prints `7`
    - Every task name in beat_schedule has a matching `@shared_task` function in content_tasks.py
    - Procfile contains exactly 3 lines: web, worker, beat
    - celeryconfig.py `task_routes` contains entries for `tasks.media_tasks.*` and `tasks.content_tasks.*`
    - celery_app.py `include` list contains `tasks.content_tasks` and `tasks.media_tasks`
  </acceptance_criteria>
  <done>Celery app imports cleanly, all 7 beat tasks have matching function definitions, Procfile has worker+beat entries, and task routing covers all queues.</done>
</task>

</tasks>

<verification>
1. `cd backend && python3 -m pytest -x -q` exits 0 with all tests passing
2. `cd backend && python3 -c "from celery_app import celery_app; print(celery_app.main)"` prints `thookai`
3. `cd backend && python3 -c "from celeryconfig import beat_schedule; [print(k) for k in beat_schedule]"` lists all 7 tasks
4. `grep -c 'sys.exit' backend/tests/*.py` returns 0
</verification>

<success_criteria>
- All tests collect and pass (INFRA-02)
- Celery app, config, beat schedule, and Procfile are verified correct (INFRA-01)
- No test file crashes pytest collection
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-celery/02-01-SUMMARY.md`
</output>
