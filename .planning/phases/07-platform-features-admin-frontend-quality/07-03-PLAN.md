---
phase: 07-platform-features-admin-frontend-quality
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_admin_agency.py
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04

must_haves:
  truths:
    - "Admin stats overview endpoint returns real user count, active subscriptions, and recent job counts from database queries"
    - "Admin user list endpoint returns paginated users with filtering by tier and search by email"
    - "Agency workspace creation requires Studio+ tier and creates workspace with workspace_id"
    - "Workspace invitation sends email via Resend and creates pending member record"
    - "Member role updates enforce valid roles (creator, manager, admin) and only workspace owner/admin can change roles"
  artifacts:
    - path: "backend/tests/test_admin_agency.py"
      provides: "Tests for ADMIN-01 through ADMIN-04"
      min_lines: 200
  key_links:
    - from: "backend/routes/admin.py"
      to: "db.users"
      via: "count_documents, aggregate"
      pattern: "db\\.users\\."
    - from: "backend/routes/admin.py"
      to: "auth_utils.require_admin"
      via: "Depends(require_admin)"
      pattern: "require_admin"
    - from: "backend/routes/agency.py"
      to: "db.workspaces"
      via: "Motor async CRUD"
      pattern: "db\\.workspaces"
    - from: "backend/routes/agency.py"
      to: "services/email_service.send_workspace_invite_email"
      via: "async function call"
      pattern: "send_workspace_invite_email"
---

<objective>
Verify admin dashboard and agency workspace endpoints work correctly via automated tests.

Purpose: Admin dashboard must show real platform stats (not placeholders), and agency workspace CRUD + invitation + role management must enforce tier restrictions and send emails. Tests prove these features work end-to-end with mocked database and email service.
Output: backend/tests/test_admin_agency.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/admin.py
@backend/routes/agency.py
@backend/auth_utils.py
@backend/services/email_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test admin dashboard stats and user management endpoints</name>
  <files>backend/tests/test_admin_agency.py</files>
  <read_first>
    backend/routes/admin.py
    backend/auth_utils.py
    backend/tests/conftest.py
  </read_first>
  <behavior>
    ADMIN-01 Admin Dashboard:
    - GET /api/admin/stats/overview returns total_users, active_subscriptions, jobs_today, jobs_this_week as integers from real DB queries
    - GET /api/admin/stats/overview without admin role returns 403
    - GET /api/admin/users returns paginated user list with user_id, email, subscription_tier fields
    - GET /api/admin/users?tier=pro filters by tier
    - GET /api/admin/users?search=test@example filters by email substring
    - PUT /api/admin/users/{user_id}/tier with body {"tier": "pro"} updates user tier
    - POST /api/admin/users/{user_id}/credits with body {"credits": 100, "reason": "manual grant"} adds credits
  </behavior>
  <action>
    Create backend/tests/test_admin_agency.py with test class:

    1. TestAdminDashboard:
       - Read admin.py fully to understand all endpoint paths and the require_admin dependency
       - Override require_admin dependency (not get_current_user): app.dependency_overrides[require_admin] = lambda: {"user_id": "admin-user"}
       - Import require_admin from auth_utils
       - Patch "database.db" with AsyncMock:
         - db.users.count_documents as AsyncMock returning 150
         - db.content_jobs.count_documents as AsyncMock returning 42
         - db.users.find().skip().limit().to_list() returning sample users list
         - db.users.find_one returning user doc
         - db.users.update_one as AsyncMock
       - Test GET /api/admin/stats/overview → assert 200 with keys: total_users, active_subscriptions (or similar stat keys from admin.py)
       - Test non-admin access: temporarily remove override, patch get_current_user to return regular user, mock db.users.find_one to return {"user_id": "regular", "role": "user"} → GET /api/admin/stats/overview returns 403
       - Test GET /api/admin/users → assert 200 with users list
       - Test GET /api/admin/users?tier=pro → verify tier filter applied
       - Test PUT /api/admin/users/{id}/tier with {"tier": "pro"} → assert 200
       - Test POST /api/admin/users/{id}/credits with {"credits": 100, "reason": "bonus"} → assert 200

    Important: Read admin.py completely first to get the exact endpoint paths — they may use /stats/overview, /users, /users/{user_id}/tier, /users/{user_id}/credits patterns. The router has no prefix (tags=["admin"]) so paths under /api/admin/ depend on how it's mounted in server.py.

    Check server.py for how admin_router is mounted — look for the include_router call to determine the full API path prefix.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_admin_agency.py::TestAdminDashboard -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_admin_agency.py exists
    - grep "class TestAdminDashboard" backend/tests/test_admin_agency.py returns a match
    - grep "require_admin" backend/tests/test_admin_agency.py returns a match
    - grep "total_users" backend/tests/test_admin_agency.py returns a match
    - All tests in TestAdminDashboard pass with 0 failures
  </acceptance_criteria>
  <done>Admin dashboard stats show real counts from DB queries, user management CRUD works, admin role enforcement verified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test agency workspace CRUD, invitations, and role management</name>
  <files>backend/tests/test_admin_agency.py</files>
  <read_first>
    backend/routes/agency.py
    backend/services/email_service.py
    backend/tests/test_admin_agency.py
  </read_first>
  <behavior>
    ADMIN-02 Agency Workspace:
    - POST /api/agency/workspaces creates workspace when user has Studio+ tier
    - POST /api/agency/workspaces returns 403 when user has free/starter tier
    - GET /api/agency/workspaces returns list of user's workspaces
    - GET /api/agency/workspaces/{id} returns single workspace with members

    ADMIN-03 Workspace Invitations:
    - POST /api/agency/workspaces/{id}/invite sends invitation email via send_workspace_invite_email
    - POST /api/agency/workspaces/{id}/invite creates pending member record in db.workspace_members

    ADMIN-04 Member Roles:
    - PUT /api/agency/workspaces/{id}/members/{user_id}/role updates member role
    - PUT with invalid role returns 400
    - Only workspace owner or admin can change roles (non-owner/admin gets 403)
    - DELETE /api/agency/workspaces/{id}/members/{user_id} removes member
  </behavior>
  <action>
    Append two test classes to backend/tests/test_admin_agency.py:

    2. TestAgencyWorkspace:
       - Override get_current_user: {"user_id": "agency-owner"}
       - Patch "database.db":
         - db.users.find_one returns {"user_id": "agency-owner", "subscription_tier": "agency", "email": "owner@test.com"}
         - db.workspaces.insert_one as AsyncMock
         - db.workspaces.find_one returns workspace doc with workspace_id, owner_id, name
         - db.workspaces.find().to_list() returns list of workspaces
         - db.workspace_members.find().to_list() returns member list
       - Test POST /api/agency/workspaces with {"name": "Test Agency"} → assert 200/201 with workspace_id
       - Test POST /api/agency/workspaces when tier is "starter" → mock db.users.find_one to return starter tier → assert 403 with "paid plan" in detail
       - Test GET /api/agency/workspaces → assert 200 with workspaces list

    3. TestWorkspaceInvitations:
       - Same user override as above
       - Patch "services.email_service.send_workspace_invite_email" as AsyncMock
       - Mock db.workspaces.find_one to return workspace owned by agency-owner
       - Mock db.workspace_members.find_one to return None (not already invited)
       - Mock db.workspace_members.insert_one as AsyncMock
       - Test POST /api/agency/workspaces/{id}/invite with {"email": "new@test.com", "role": "creator"} → assert 200, verify send_workspace_invite_email was called with "new@test.com"
       - Test role update: mock db.workspace_members.find_one returns member doc → PUT /api/agency/workspaces/{id}/members/{user_id}/role with {"role": "manager"} → assert 200
       - Test invalid role: PUT with {"role": "superadmin"} → assert 400

    Read agency.py fully before writing tests to understand exact endpoint paths and the check_agency_tier helper. The router prefix is "/agency", so full paths are /api/agency/workspaces, /api/agency/workspaces/{id}/invite, etc.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_admin_agency.py::TestAgencyWorkspace tests/test_admin_agency.py::TestWorkspaceInvitations -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "class TestAgencyWorkspace" backend/tests/test_admin_agency.py returns a match
    - grep "class TestWorkspaceInvitations" backend/tests/test_admin_agency.py returns a match
    - grep "send_workspace_invite_email" backend/tests/test_admin_agency.py returns a match
    - grep "403" backend/tests/test_admin_agency.py returns a match (tier enforcement test)
    - All tests in these two classes pass with 0 failures
  </acceptance_criteria>
  <done>Agency workspace CRUD enforces tier restrictions, invitations send emails via Resend, role management validates roles and permissions</done>
</task>

</tasks>

<verification>
All 4 ADMIN requirements have passing automated tests:
- ADMIN-01: Admin dashboard shows real stats from DB queries
- ADMIN-02: Workspace CRUD with tier enforcement
- ADMIN-03: Invitation emails sent via Resend service
- ADMIN-04: Role management with permission checks

```bash
cd backend && python -m pytest tests/test_admin_agency.py -x -v
```
</verification>

<success_criteria>
- backend/tests/test_admin_agency.py exists with 3 test classes
- All tests pass (0 failures)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-features-admin-frontend-quality/07-03-SUMMARY.md`
</output>
