---
phase: 22-apifetch-migration-error-handling
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - frontend/src/lib/campaignsApi.js
  - frontend/src/lib/templatesApi.js
  - frontend/src/hooks/useStrategyFeed.js
  - frontend/src/hooks/useNotifications.js
  - frontend/src/components/MediaUploader.jsx
  - frontend/src/components/VoiceCloneCard.jsx
  - frontend/src/components/PersonaShareModal.jsx
  - frontend/src/context/AuthContext.jsx
  - frontend/src/pages/AuthPage.jsx
  - frontend/src/pages/ResetPasswordPage.jsx
  - frontend/src/pages/Public/PersonaCardPublic.jsx
  - frontend/src/pages/Onboarding/index.jsx
  - frontend/src/pages/Onboarding/PhaseOne.jsx
  - frontend/src/pages/ViralCard.jsx
autonomous: true
requirements: [API-05]

must_haves:
  truths:
    - "All lib/ API modules (campaignsApi, templatesApi) use apiFetch instead of raw fetch"
    - "All hooks (useStrategyFeed, useNotifications) use apiFetch instead of raw fetch"
    - "All non-Dashboard page components (AuthPage, Onboarding, ViralCard, etc.) use apiFetch instead of raw fetch"
    - "All shared components (MediaUploader, VoiceCloneCard, PersonaShareModal) use apiFetch instead of raw fetch"
    - "AuthContext uses apiFetch for logout and cookie-check calls"
    - "No file in this batch contains a raw fetch() call or localStorage.getItem('thook_token')"
  artifacts:
    - path: "frontend/src/lib/campaignsApi.js"
      provides: "Campaign CRUD using apiFetch"
      contains: "apiFetch"
    - path: "frontend/src/lib/templatesApi.js"
      provides: "Template CRUD using apiFetch"
      contains: "apiFetch"
    - path: "frontend/src/hooks/useStrategyFeed.js"
      provides: "Strategy feed hook using apiFetch"
      contains: "apiFetch"
    - path: "frontend/src/hooks/useNotifications.js"
      provides: "Notifications hook using apiFetch"
      contains: "apiFetch"
  key_links:
    - from: "frontend/src/lib/campaignsApi.js"
      to: "frontend/src/lib/api.js"
      via: "import apiFetch"
      pattern: "import.*apiFetch.*from.*api"
    - from: "frontend/src/lib/templatesApi.js"
      to: "frontend/src/lib/api.js"
      via: "import apiFetch"
      pattern: "import.*apiFetch.*from.*api"
---

<objective>
Migrate the first batch of files (14 files, ~45 fetch calls) from raw fetch() + localStorage Bearer auth to apiFetch. This covers all lib/ modules, hooks, shared components, non-Dashboard pages, and AuthContext.

Purpose: These files are the smaller, self-contained modules where migration is mechanical. Getting them done first reduces the total fetch count by ~40% and validates the apiFetch contract before the larger Dashboard migration in Plan 03.

Output: 14 files migrated, zero raw fetch() calls in any of them, zero localStorage references for auth tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-apifetch-migration-error-handling/22-01-PLAN.md

<interfaces>
<!-- apiFetch contract from Plan 01 -->
From frontend/src/lib/api.js (after Plan 01):
```javascript
import { API_BASE_URL, DEFAULT_TIMEOUT_MS, MAX_RETRIES, RETRY_BACKOFF_MS } from './constants';

/**
 * @param {string} path - API path (e.g., "/api/content/create")
 * @param {object} options - fetch options + optional timeout override
 * @returns {Promise<Response>} - raw Response object
 */
export async function apiFetch(path, options = {})
```

From frontend/src/lib/constants.js (after Plan 01):
```javascript
export const API_BASE_URL = process.env.REACT_APP_BACKEND_URL || '';
```

Key behaviors:
- credentials: 'include' is automatic (cookies sent)
- CSRF token header injected on POST/PUT/PATCH/DELETE
- Content-Type: application/json set unless body is FormData
- 401 auto-redirects to /auth
- 403 shows permission toast
- 5xx retries once then shows error toast
- Returns raw Response (caller calls .json(), checks .ok, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lib/ API modules and hooks to apiFetch</name>
  <read_first>frontend/src/lib/campaignsApi.js, frontend/src/lib/templatesApi.js, frontend/src/hooks/useStrategyFeed.js, frontend/src/hooks/useNotifications.js</read_first>
  <files>frontend/src/lib/campaignsApi.js, frontend/src/lib/templatesApi.js, frontend/src/hooks/useStrategyFeed.js, frontend/src/hooks/useNotifications.js</files>
  <action>
**For each file, apply this migration pattern:**

1. **Remove** `const BACKEND_URL = process.env.REACT_APP_BACKEND_URL;` line
2. **Remove** the `getAuthHeaders()` / `authHeaders()` helper function entirely (apiFetch handles auth via cookies + CSRF)
3. **Add** `import { apiFetch } from '@/lib/api';` at top (or `'../lib/api'` depending on relative path)
4. **Replace every** `fetch(\`${BACKEND_URL}/api/...\`, { ... })` with `apiFetch('/api/...', { ... })`
5. **Remove** `credentials: "include"` from options (apiFetch adds it)
6. **Remove** `headers: getAuthHeaders()` or `headers: { Authorization: ... }` (apiFetch handles auth)
7. **Keep** any `method`, `body`, and custom headers that are NOT auth-related
8. **Keep** `Content-Type: "application/json"` only if body is JSON (apiFetch adds it, but keeping is harmless — actually apiFetch already sets it, so remove to avoid duplication)
9. **Keep** all response handling logic (`res.ok`, `res.json()`, error throwing) — apiFetch returns raw Response

**Specific file notes:**

**campaignsApi.js** (8 fetch calls):
- Remove `getAuthHeaders()` function (lines 3-9)
- All 8 functions switch from `fetch(\`${BACKEND_URL}/api/campaigns...\`, { credentials: "include", headers: getAuthHeaders(), ... })` to `apiFetch('/api/campaigns...', { ... })`
- Keep `method`, `body: JSON.stringify(...)` on POST/PUT/DELETE calls
- Remove `"Content-Type": "application/json"` from headers (apiFetch sets it)

**templatesApi.js** (10 fetch calls):
- Remove `getAuthHeaders()` function (lines 3-9)
- All 10 functions switch identically
- Pattern: `fetch(\`${BACKEND_URL}/api/templates...\`)` becomes `apiFetch('/api/templates...')`

**useStrategyFeed.js** (5 fetch calls):
- Remove `getAuthHeaders` useCallback (lines 20-26)
- Remove `getAuthHeaders` from all useCallback dependency arrays
- 5 fetch calls become apiFetch calls
- For `dismissCard`: the custom `Content-Type` header is only needed when body exists — apiFetch handles this, so just pass `{ method: "POST", body }` when body is truthy
- Keep all the `setActiveCards`, `setHistoryCards` state management
- In `fetchHistoryCards`: the two parallel fetches become `apiFetch` calls inside `Promise.all`

**useNotifications.js** (4 fetch calls):
- Remove `getAuthHeaders` useCallback (lines 19-25)
- Remove `getAuthHeaders` from all useCallback dependency arrays
- 4 fetch calls become apiFetch calls
- **SSE EventSource (line 122)**: Keep the EventSource as-is — EventSource is NOT a fetch() call and cannot use apiFetch. It uses `withCredentials: true` which sends cookies. This is correct and should NOT be changed.
- Remove `console.error` statements where apiFetch's global error handling covers the case (or keep them for SSE-specific errors which are outside apiFetch)
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production && grep -c "raw fetch" frontend/src/lib/campaignsApi.js frontend/src/lib/templatesApi.js frontend/src/hooks/useStrategyFeed.js frontend/src/hooks/useNotifications.js 2>/dev/null; for f in frontend/src/lib/campaignsApi.js frontend/src/lib/templatesApi.js frontend/src/hooks/useStrategyFeed.js frontend/src/hooks/useNotifications.js; do echo "--- $f ---"; grep -n "^[^/]*fetch(" "$f" 2>/dev/null || echo "NO RAW FETCH - OK"; grep -c "apiFetch" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "^[^/]*[^a]fetch(" frontend/src/lib/campaignsApi.js` returns no matches (zero raw fetch)
    - `grep -rn "^[^/]*[^a]fetch(" frontend/src/lib/templatesApi.js` returns no matches
    - `grep -rn "^[^/]*[^a]fetch(" frontend/src/hooks/useStrategyFeed.js` returns no matches (EventSource is not fetch)
    - `grep -rn "^[^/]*[^a]fetch(" frontend/src/hooks/useNotifications.js` returns no matches
    - `grep -c "localStorage" frontend/src/lib/campaignsApi.js frontend/src/lib/templatesApi.js frontend/src/hooks/useStrategyFeed.js frontend/src/hooks/useNotifications.js` returns 0 for each
    - `grep -c "apiFetch" frontend/src/lib/campaignsApi.js` returns >= 8
    - `grep -c "apiFetch" frontend/src/lib/templatesApi.js` returns >= 10
  </acceptance_criteria>
  <done>All 4 lib/hook files migrated: zero raw fetch(), zero localStorage, all using apiFetch with cookie auth</done>
</task>

<task type="auto">
  <name>Task 2: Migrate components, AuthContext, and non-Dashboard pages to apiFetch</name>
  <read_first>frontend/src/context/AuthContext.jsx, frontend/src/pages/AuthPage.jsx, frontend/src/components/MediaUploader.jsx, frontend/src/components/VoiceCloneCard.jsx, frontend/src/components/PersonaShareModal.jsx, frontend/src/pages/ResetPasswordPage.jsx, frontend/src/pages/Public/PersonaCardPublic.jsx, frontend/src/pages/Onboarding/index.jsx, frontend/src/pages/Onboarding/PhaseOne.jsx, frontend/src/pages/ViralCard.jsx</read_first>
  <files>frontend/src/context/AuthContext.jsx, frontend/src/pages/AuthPage.jsx, frontend/src/components/MediaUploader.jsx, frontend/src/components/VoiceCloneCard.jsx, frontend/src/components/PersonaShareModal.jsx, frontend/src/pages/ResetPasswordPage.jsx, frontend/src/pages/Public/PersonaCardPublic.jsx, frontend/src/pages/Onboarding/index.jsx, frontend/src/pages/Onboarding/PhaseOne.jsx, frontend/src/pages/ViralCard.jsx</files>
  <action>
Apply the same migration pattern as Task 1 to these 10 files. Special handling needed:

**AuthContext.jsx** (3 fetch calls):
- Add `import { apiFetch } from '@/lib/api';` (or use relative path `'../lib/api'`)
- Import `API_BASE_URL` from `'@/lib/constants'` for the one Google OAuth case
- Remove `const BACKEND_URL = process.env.REACT_APP_BACKEND_URL;`
- `checkAuth` (line 23): Change `fetch(\`${BACKEND_URL}/api/auth/me\`, { credentials: "include" })` to `apiFetch('/api/auth/me')`
- **IMPORTANT**: In `checkAuth`, apiFetch will auto-redirect on 401. But checkAuth should NOT redirect — it should just set user to null. Solution: catch the 401 error thrown by apiFetch and set user to null. Alternatively, pass a custom option to suppress 401 redirect: add `{ _skipAuthRedirect: true }` and handle in apiFetch. BETTER approach: just wrap in try/catch and catch the "Session expired" error — the redirect will still happen but that's actually correct behavior (if the session cookie is truly expired, redirect is appropriate). Keep it simple: use apiFetch and let the existing try/catch handle errors by setting user to null.
- Actually, the 401 redirect in apiFetch will fire before the catch block can suppress it. This is fine for checkAuth — if the cookie session is expired, redirecting to /auth is the correct behavior. The catch block in checkAuth will still fire (apiFetch throws after redirect), setting user to null.
- Google OAuth branch (line 50): Keep the `Authorization: Bearer ${token}` header for the one-time token validation. Use `apiFetch('/api/auth/me', { headers: { Authorization: \`Bearer ${token}\` } })`. apiFetch will merge this header with its CSRF header.
- `logout` (line 73): Change to `apiFetch('/api/auth/logout', { method: "POST" })`
- Remove `getCsrfTokenFromCookie` from AuthContext — it's already in api.js. Remove it from the Provider value too. (Phase 21 exported it but no consumer uses it — apiFetch handles CSRF internally.)

**AuthPage.jsx** (2 fetch calls):
- Remove `const BACKEND_URL = ...`
- Import `{ API_BASE_URL }` from `'@/lib/constants'` for the Google OAuth redirect URL (`window.location.href = \`${API_BASE_URL}/api/auth/google\``)
- `handleForgotPassword` (line 36): `apiFetch('/api/auth/forgot-password', { method: "POST", body: ... })`
- `handleSubmit` (line 61): `apiFetch(endpoint, { method: "POST", body: ... })` — keep the `credentials: "include"` removal since apiFetch handles it
- Note: AuthPage login/register should NOT auto-redirect on 401 (you're trying to log in!). But these endpoints won't return 401 — they return 200 or 400/422. So no issue.

**MediaUploader.jsx** (1 fetch + 1 XHR):
- The `fetch` call (line 126, URL upload) becomes `apiFetch('/api/uploads/url', { method: "POST", body: ... })`
- The XHR upload (line 90) stays as XHR — apiFetch is for fetch(), not XHR. XHR is needed for upload progress tracking (`xhr.upload.onprogress`). BUT update the XHR to use cookie auth: remove the `Authorization` header injection and keep `xhr.withCredentials = true` (already present). Since backend accepts both Bearer and cookie, and the cookie is now the primary, this works. Remove the `authHeaders()` function and the `if (headers.Authorization) xhr.setRequestHeader(...)` block.
- Import `{ API_BASE_URL }` from constants for the XHR URL
- Remove `const BACKEND_URL = ...` and `authHeaders()` function
- Remove `localStorage.getItem("thook_token")` reference

**VoiceCloneCard.jsx** (4 fetch calls):
- Remove `const BACKEND_URL = ...`
- All 4 fetch calls become apiFetch calls with same method/body
- Remove all `Authorization: \`Bearer ${token}\`` headers

**PersonaShareModal.jsx** (2 fetch calls):
- Remove `const BACKEND_URL = ...`
- 2 fetch calls become apiFetch
- The GET to load share status + POST to create share

**ResetPasswordPage.jsx** (1 fetch call):
- Remove `const BACKEND_URL = ...`
- 1 fetch becomes apiFetch

**PersonaCardPublic.jsx** (1 fetch call):
- Remove `const BACKEND_URL = ...`
- This is a PUBLIC page (no auth). The fetch call `fetch(\`${BACKEND_URL}/api/persona/public/${shareToken}\`)` has no auth headers.
- Use `apiFetch('/api/persona/public/${shareToken}')` — the CSRF header won't be injected (GET request), and credentials:include is harmless on a public endpoint
- Note: apiFetch will redirect to /auth on 401. Public endpoints shouldn't return 401, so this is fine.

**Onboarding/index.jsx** (1 fetch call):
- Remove `const BACKEND_URL = ...`
- POST to generate-persona becomes apiFetch

**Onboarding/PhaseOne.jsx** (1 fetch call):
- Remove `const BACKEND_URL = ...`
- POST to analyze-posts becomes apiFetch

**ViralCard.jsx** (2 fetch calls):
- Remove `const BACKEND_URL = ...`
- GET viral card + POST analyze become apiFetch

After all migrations, run `CI=false npm run build` to confirm zero compilation errors.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production && for f in frontend/src/context/AuthContext.jsx frontend/src/pages/AuthPage.jsx frontend/src/components/MediaUploader.jsx frontend/src/components/VoiceCloneCard.jsx frontend/src/components/PersonaShareModal.jsx frontend/src/pages/ResetPasswordPage.jsx frontend/src/pages/Public/PersonaCardPublic.jsx frontend/src/pages/Onboarding/index.jsx frontend/src/pages/Onboarding/PhaseOne.jsx frontend/src/pages/ViralCard.jsx; do count=$(grep -c "localStorage" "$f" 2>/dev/null || echo 0); echo "$f localStorage=$count"; done && CI=false npx --prefix frontend react-scripts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "localStorage" frontend/src/context/AuthContext.jsx` returns 0
    - `grep -c "localStorage" frontend/src/pages/AuthPage.jsx` returns 0
    - `grep -c "localStorage" frontend/src/components/MediaUploader.jsx` returns 0
    - `grep -c "localStorage" frontend/src/components/VoiceCloneCard.jsx` returns 0
    - `grep -rn "^[^/]*[^a]fetch(" frontend/src/components/PersonaShareModal.jsx` returns no matches
    - `grep -rn "^[^/]*[^a]fetch(" frontend/src/pages/ResetPasswordPage.jsx` returns no matches
    - Each of the 10 files contains zero raw `fetch(` calls (excluding comments and the EventSource in useNotifications)
    - `CI=false npm run build` in frontend/ exits 0
  </acceptance_criteria>
  <done>All 10 component/page/context files migrated: zero raw fetch(), zero localStorage.getItem("thook_token"), all using apiFetch with cookie auth. Frontend builds cleanly.</done>
</task>

</tasks>

<verification>
1. Zero raw `fetch(` in any of the 14 files in this plan (excluding EventSource which is not fetch)
2. Zero `localStorage.getItem("thook_token")` in any of the 14 files
3. Zero `BACKEND_URL = process.env.REACT_APP_BACKEND_URL` declarations in any of the 14 files
4. All files import from `@/lib/api` or `@/lib/constants`
5. `CI=false npm run build` passes
</verification>

<success_criteria>
- 14 files migrated from raw fetch() to apiFetch
- ~45 individual fetch calls replaced
- Zero localStorage auth token references in migrated files
- Frontend compiles without errors
- API-05 partially fulfilled (first batch complete)
</success_criteria>

<output>
After completion, create `.planning/phases/22-apifetch-migration-error-handling/22-02-SUMMARY.md`
</output>
