---
phase: 34-security-gdpr
plan: 08
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/pages/PrivacyPolicy.jsx
  - frontend/src/App.js
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SECR-12
  - SECR-13
must_haves:
  truths:
    - "/privacy route renders PrivacyPolicy.jsx without authentication — accessible in incognito"
    - "/terms route renders TermsOfService.jsx without authentication — accessible in incognito"
    - "PrivacyPolicy.jsx section 6 (Cookies) references the in-app cookie consent banner and the ability to withdraw consent via Settings → Data"
    - "Both /privacy and /terms routes are in App.js as public routes (outside ProtectedRoute)"
    - "ROADMAP.md Phase 34 success criterion 4 reads 'POST /api/auth/delete-account' (not 'DELETE /api/auth/account')"
  artifacts:
    - path: "frontend/src/pages/PrivacyPolicy.jsx"
      provides: "Updated cookies section mentioning in-app banner + Settings > Data withdrawal path"
      contains: "cookie consent banner\|Settings.*Data\|thookai_cookie_consent"
    - path: "frontend/src/App.js"
      provides: "Confirmed /privacy and /terms as public routes accessible without auth"
      contains: "PrivacyPolicy\|TermsOfService"
    - path: ".planning/ROADMAP.md"
      provides: "Success criterion 4 updated to match POST /api/auth/delete-account implementation"
      contains: "POST /api/auth/delete-account"
  key_links:
    - from: "frontend/src/App.js"
      to: "frontend/src/pages/PrivacyPolicy.jsx"
      via: "Route path='/privacy'"
      pattern: "path.*privacy\|PrivacyPolicy"
    - from: "frontend/src/App.js"
      to: "frontend/src/pages/TermsOfService.jsx"
      via: "Route path='/terms'"
      pattern: "path.*terms\|TermsOfService"
---

<objective>
Verify and fix the Privacy Policy and Terms of Service pages: confirm both routes are public in App.js, update PrivacyPolicy.jsx to reference the in-app cookie consent banner (not just browser settings), and update ROADMAP.md success criterion 4 to reflect the correct endpoint path.

Purpose: SECR-12 and SECR-13 are substantially complete — the pages exist with real content. This plan handles the remaining gap: the Privacy Policy's cookies section says "manage cookie preferences in your browser settings" but should reference the in-app banner. Also fixes the ROADMAP.md path discrepancy documented in plan 34-05.

Output: `PrivacyPolicy.jsx` with updated cookies section, `App.js` verified to have public routes (no edits needed if routes are already public), and `ROADMAP.md` success criterion 4 corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-security-gdpr/34-RESEARCH.md

<interfaces>
<!-- Current PrivacyPolicy.jsx cookies section gap (from research 1.11) -->
<!-- Section 6 currently says: "You can manage cookie preferences in your browser settings" -->
<!-- Should say: reference the in-app cookie consent banner + Settings > Data path -->

<!-- Desired text for cookies section: -->
<!--
Section: Cookies and Tracking
We use analytics cookies (PostHog) to improve the platform experience. You can:
- Manage your cookie preferences using the consent banner shown on your first visit
- Change your choice anytime via Settings → Data → Cookie Preferences (coming soon)
- Withdraw analytics consent by clearing your browser's localStorage (key: thookai_cookie_consent)

Analytics tracking (PostHog) only activates after you click "Accept" in the consent banner.
Essential cookies (authentication session) are required for the platform to function.
-->

<!-- TermsOfService.jsx — research found no specific gaps, no changes needed -->
<!-- App.js routing — /privacy and /terms must be public routes (outside ProtectedRoute) -->
<!-- If they are already public, no change to App.js needed — document as verified -->

<!-- ROADMAP.md line ~569 success criterion 4: -->
<!-- Current: "DELETE /api/auth/account" -->
<!-- Fix to: "POST /api/auth/delete-account" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify public routes + update PrivacyPolicy.jsx cookies section</name>
  <files>
    frontend/src/pages/PrivacyPolicy.jsx
    frontend/src/App.js
  </files>
  <read_first>
    - frontend/src/App.js (full file — locate /privacy and /terms routes and confirm they are outside ProtectedRoute)
    - frontend/src/pages/PrivacyPolicy.jsx (full file — locate section 6 Cookies content)
    - frontend/src/pages/TermsOfService.jsx (first 30 lines — confirm file exists and is not Lorem Ipsum)
  </read_first>
  <action>
**Step 1 — Verify App.js routing (read-only):**
Read `frontend/src/App.js`. Confirm that `/privacy` and `/terms` routes are defined as public routes — i.e., they are NOT wrapped in a `ProtectedRoute` component (or equivalent auth check). If they are already public, NO changes to App.js are needed — document as "verified public."

If `/privacy` or `/terms` routes are accidentally inside a `ProtectedRoute`, move them outside to the public section. The routes should render `PrivacyPolicy` and `TermsOfService` components respectively, reachable by unauthenticated users.

**Step 2 — Update PrivacyPolicy.jsx cookies section:**
Locate section 6 (or equivalent cookies section) in `PrivacyPolicy.jsx`. Replace the phrase "manage cookie preferences in your browser settings" (or equivalent) with content that references:
1. The in-app consent banner: "You can manage your cookie preferences using the consent banner shown on your first visit"
2. The Settings > Data path for later changes (mark as coming soon if the exact settings path isn't finalized yet)
3. The ability to withdraw consent via localStorage: "You can also withdraw consent at any time by clearing the `thookai_cookie_consent` key from your browser's local storage"
4. That analytics (PostHog) only activates after accepting: "Analytics tracking only activates after you explicitly accept cookies"

Keep the section title unchanged. Maintain the existing JSX structure and design classes. The new text should fit naturally within the existing section layout — do not add new sections or restructure the page.

**Step 3 — Verify TermsOfService.jsx:**
Read the first 30 lines of `TermsOfService.jsx`. If the file exists with real content (no Lorem Ipsum), document as "VERIFIED — no changes needed." If any Lorem Ipsum exists (grep result), replace it with appropriate placeholder text in the affected sections — but the research confirmed there is none.

Do NOT change any branding, contact email addresses, dates, or legal content unless instructed by this plan.
  </action>
  <verify>
    <automated>grep -n "thookai_cookie_consent\|consent banner\|Analytics tracking" /Users/kuldeepsinhparmar/thookAI-production/frontend/src/pages/PrivacyPolicy.jsx | head -5</automated>
  </verify>
  <done>PrivacyPolicy.jsx cookies section references the in-app consent banner and the thookai_cookie_consent localStorage key. App.js routes for /privacy and /terms are confirmed public (outside ProtectedRoute). TermsOfService.jsx verified as having real content.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md success criterion 4 to match implementation</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 559-575 — locate Phase 34 success criteria)
  </read_first>
  <action>
Read ROADMAP.md lines 559-575 (Phase 34 section). Locate success criterion 4 which currently reads something like:
"A user can delete their account at DELETE /api/auth/account..."

Replace `DELETE /api/auth/account` with `POST /api/auth/delete-account` and replace any reference to "DELETE /api/auth/account" with the correct path. The updated criterion should read:

"A user can delete their account at **POST /api/auth/delete-account** with body `{"confirm": "DELETE"}` — within 5 seconds, their email is anonymized in the users collection, and their persona/content/scheduled posts are removed — the user can no longer log in"

Also update the description in the same criterion to note: "Note: implementation uses POST with confirmation body (safer than bare DELETE — prevents accidental deletion from CSRF)."

Do NOT modify any other line in ROADMAP.md. Do NOT change success criteria 1, 2, 3, 5, or 6. Do NOT change the Goal, Requirements, Depends on, or Plans fields.
  </action>
  <verify>
    <automated>grep -n "POST.*delete-account\|DELETE.*api/auth/account" /Users/kuldeepsinhparmar/thookAI-production/.planning/ROADMAP.md | head -5</automated>
  </verify>
  <done>ROADMAP.md Phase 34 success criterion 4 reads 'POST /api/auth/delete-account' (not 'DELETE /api/auth/account'). No other ROADMAP.md lines were modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public routes→unauthenticated users | /privacy and /terms must be accessible without login — legal requirement |
| planning docs→implementation | ROADMAP.md endpoint spec must match implementation to prevent future confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08-01 | Information Disclosure | /privacy or /terms accidentally auth-protected | mitigate | Verify App.js routing — both routes confirmed outside ProtectedRoute |
| T-34-08-02 | Information Disclosure | Privacy policy misleads users about cookie control | mitigate | Update cookies section to reference in-app consent banner and localStorage key |
</threat_model>

<verification>
```bash
# 1. PrivacyPolicy.jsx references consent banner
grep -n "consent banner\|thookai_cookie_consent\|Analytics tracking" \
  /Users/kuldeepsinhparmar/thookAI-production/frontend/src/pages/PrivacyPolicy.jsx
# Expected: 2-3 lines

# 2. App.js has /privacy and /terms as routes (verify they exist)
grep -n "privacy\|PrivacyPolicy\|terms\|TermsOfService" \
  /Users/kuldeepsinhparmar/thookAI-production/frontend/src/App.js
# Expected: at least 2 lines (one for each route)

# 3. App.js routes are NOT inside ProtectedRoute
# (manual check — read the surrounding context in App.js output above)

# 4. ROADMAP.md success criterion 4 updated
grep -n "POST.*delete-account" /Users/kuldeepsinhparmar/thookAI-production/.planning/ROADMAP.md
# Expected: 1 line in Phase 34 section

# 5. No Lorem Ipsum in either legal page
grep -i "lorem ipsum" \
  /Users/kuldeepsinhparmar/thookAI-production/frontend/src/pages/PrivacyPolicy.jsx \
  /Users/kuldeepsinhparmar/thookAI-production/frontend/src/pages/TermsOfService.jsx
# Expected: zero output

# 6. Frontend builds without errors
cd /Users/kuldeepsinhparmar/thookAI-production/frontend && \
  npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- PrivacyPolicy.jsx cookies section references the in-app consent banner, thookai_cookie_consent localStorage key, and states analytics only activates after Accept
- App.js routes for /privacy and /terms are confirmed as public (outside ProtectedRoute)
- TermsOfService.jsx has real content — no Lorem Ipsum
- ROADMAP.md Phase 34 success criterion 4 reads "POST /api/auth/delete-account" not "DELETE /api/auth/account"
- npm run build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/34-security-gdpr/34-08-SUMMARY.md` with:
- App.js routing verdict (VERIFIED PUBLIC or FIXED with line numbers)
- PrivacyPolicy.jsx cookies section: lines changed (before/after)
- TermsOfService.jsx verdict: VERIFIED CLEAN or FIXED
- ROADMAP.md change: old text vs new text in success criterion 4
- npm build result
</output>
