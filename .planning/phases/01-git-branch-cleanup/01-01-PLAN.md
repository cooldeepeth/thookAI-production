---
phase: 01-git-branch-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [GIT-01]
must_haves:
  truths:
    - "git branch -a shows zero worktree-agent-* branches"
    - "git branch shows zero stale feature/fix branches that are already merged to dev"
  artifacts:
    - path: "(git branch -a output)"
      provides: "proof that no worktree-agent-* branches remain locally"
      contains: "no lines matching worktree-agent-"
  key_links:
    - from: "local worktree-agent-* branches"
      to: "deleted"
      via: "git branch -D"
      pattern: "worktree-agent-"
    - from: "merged feature/fix branches"
      to: "deleted"
      via: "git branch -D (after confirming merged into dev)"
      pattern: "git branch --merged dev"
---

<objective>
Delete all 20 worktree-agent-* branches (local only — confirmed none exist on remote) and all
stale feature/fix/docs/infra branches that have already been merged into dev. This clears the
local branch namespace so Phase 1 ends with a clean slate.

Purpose: 20 abandoned agent worktrees are polluting the branch list. Stale merged branches create
noise and risk accidental checkouts. Removing them is a prerequisite for GIT-01 and makes the
git state legible before the merge work in Plan 02.

Output: A local repo where `git branch` shows only: feat/post-launch-sprint (current), dev, main,
and the few fix branches not yet merged to dev.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all worktree-agent-* branches (local)</name>
  <files>(no files — git operations only)</files>
  <read_first>
    Run this before doing anything:
      git branch -a
    Confirm the exact list of worktree-agent-* branch names. There should be 20.
    Confirm none exist on remote (no "remotes/origin/worktree-agent-*" lines).
  </read_first>
  <action>
    Delete every worktree-agent-* branch locally. These are all abandoned — confirmed no open PRs,
    no remote counterparts.

    Run:
      git branch | grep "worktree-agent-" | xargs git branch -D

    If xargs fails on empty input, verify manually:
      git branch -a | grep worktree-agent-

    Expected: 20 branches deleted (worktree-agent-a113a0c0, worktree-agent-a189d258,
    worktree-agent-a25a8804, worktree-agent-a35e8c4d, worktree-agent-a51335e9,
    worktree-agent-a5734e73, worktree-agent-a677a9fb, worktree-agent-a6e4ec4e,
    worktree-agent-a6e789a6, worktree-agent-a74b1a00, worktree-agent-a85b519b,
    worktree-agent-a873d232, worktree-agent-ab94d4ed, worktree-agent-abd47760,
    worktree-agent-ac842369, worktree-agent-ad0bf75e, worktree-agent-ad238b09,
    worktree-agent-ad5ed8df, worktree-agent-ad8484eb, worktree-agent-ae684591).

    Do NOT run git push --delete for any of these — they have no remote counterparts.
    Do NOT touch: feat/post-launch-sprint, dev, main.
  </action>
  <verify>
    <automated>git branch -a | grep worktree-agent- ; echo "worktree count: $(git branch | grep -c worktree-agent- || true)"</automated>
  </verify>
  <acceptance_criteria>
    - `git branch -a | grep worktree-agent-` returns no output (exit 1 is acceptable — means no matches)
    - `git branch | grep -c worktree-agent-` returns 0
  </acceptance_criteria>
  <done>Zero worktree-agent-* branches in local or remote branch list</done>
</task>

<task type="auto">
  <name>Task 2: Identify and delete stale merged branches</name>
  <files>(no files — git operations only)</files>
  <read_first>
    Run these to understand what is merged and what is not:
      git branch --merged dev
      git log --oneline dev | head -10
    The --merged dev output shows every local branch whose tip is reachable from dev.
    Any branch in that list (except dev itself and feat/post-launch-sprint) is safe to delete.
  </read_first>
  <action>
    Step 1 — Generate the safe-to-delete list:
      git branch --merged dev | grep -v "^\* \|  dev$\|  main$\|  feat/post-launch-sprint$"

    This will list branches already fully merged into dev. Examples expected to appear:
      docs/deployment-guide
      feat/m1-m3-foundation-orchestration-uom
      fix/celery-config-and-onboarding-model
      fix/cors-and-deploy-final
      fix/full-stack-audit-and-hardening
      fix/model-name-onboarding
      fix/ship-ready-blockers
      infra/celery-setup
      (and others)

    Step 2 — Delete them all:
      git branch --merged dev | grep -v "^\* \|  dev$\|  main$\|  feat/post-launch-sprint$" | xargs git branch -d

    Use -d (not -D) here — if git refuses to delete a branch claiming it's not merged, that's a
    signal to STOP and report rather than force-delete.

    Step 3 — For branches NOT in --merged dev (the + branches with remote counterparts not yet
    merged into dev), leave them alone. These include:
      feat/admin-dashboard, feat/campaign-grouping, feat/content-export, feat/email-service,
      feat/notification-system, feat/outbound-webhooks, feat/performance-intelligence,
      feat/persona-sharing-ui, feat/post-history-import, feat/real-analytics-ingestion,
      feat/template-marketplace-frontend, feat/template-seed-data, feat/vector-store-integration,
      feat/video-pipeline-wiring, feat/voice-clone-flow, fix/billing-hardening,
      fix/designer-async-fix, fix/fatigue-shield-unification, fix/google-auth-config,
      fix/media-storage-hardening, claude/funny-brattain
    These have remote counterparts and may represent work not yet decided upon — do not delete.

    IMPORTANT: Remote branches (remotes/origin/*) for the already-merged set should also be
    cleaned up. For each branch deleted locally that also has a remote:
      git push origin --delete <branch-name>
    Run this for each merged-to-dev branch that shows a remotes/origin/<branch-name> entry.
    Branches confirmed to have remotes (from earlier inspection):
      docs/deployment-guide, fix/celery-config-and-onboarding-model, fix/cors-and-deploy-final,
      fix/full-stack-audit-and-hardening, fix/model-name-onboarding, fix/ship-ready-blockers,
      infra/celery-setup, feat/m1-m3-foundation-orchestration-uom
  </action>
  <verify>
    <automated>git branch --merged dev | grep -v "^\* \|  dev$\|  main$\|  feat/post-launch-sprint$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --merged dev | grep -v "^\* \|  dev$\|  main$\|  feat/post-launch-sprint$"` returns no output
    - `git branch -a | grep worktree-agent-` returns no output
    - `git branch` shows: feat/post-launch-sprint (current), dev, main, plus any unmerged feature branches
    - No git errors during deletion (if -d refuses a branch, report the branch name and reason)
  </acceptance_criteria>
  <done>All merged-to-dev branches deleted locally and from remote. Only active/unmerged branches remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Deleted all 20 worktree-agent-* branches (local only — none were on remote).
    Deleted all branches that were fully merged into dev (local + remote).
    Branches with unmerged work on remote (feat/admin-dashboard, etc.) were intentionally preserved.
  </what-built>
  <how-to-verify>
    Run these commands and confirm outputs:

    1. `git branch -a | grep worktree-agent-`
       Expected: no output (or "grep: no match")

    2. `git branch`
       Expected: feat/post-launch-sprint (starred), dev, main, plus remaining unmerged feat/fix branches

    3. `git branch -a | grep remotes/origin/ | grep -v "HEAD\|dev\|main\|feat/post-launch-sprint"`
       Expected: only the unmerged feature branches remain on remote (feat/admin-dashboard, etc.)
       Merged branches (fix/model-name-onboarding, fix/cors-and-deploy-final, etc.) should NOT appear.

    4. `git branch --merged dev`
       Expected: only dev, main, and feat/post-launch-sprint (and maybe main depending on its state)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 02, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
GIT-01 success: `git branch -a` shows no lines matching `worktree-agent-*` and no merged feature
branches other than dev/main/feat/post-launch-sprint.
</verification>

<success_criteria>
- Zero worktree-agent-* branches locally or remotely
- All branches merged to dev have been deleted (local + remote)
- feat/post-launch-sprint, dev, main, and unmerged branches are intact
- Human has verified via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-git-branch-cleanup/01-01-SUMMARY.md`
</output>
