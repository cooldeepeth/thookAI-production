---
phase: 03-auth-onboarding-email
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/email_service.py
  - backend/routes/password_reset.py
  - backend/tests/test_email_password_reset.py
autonomous: true
requirements: [AUTH-04]

must_haves:
  truths:
    - "POST /auth/forgot-password generates a token, stores hash in DB, and calls send_password_reset_email"
    - "POST /auth/reset-password with valid token updates the user's password"
    - "POST /auth/reset-password with expired or used token returns 400"
    - "Email service gracefully handles missing RESEND_API_KEY by logging warning and returning False"
    - "Password reset email contains a link with the correct frontend URL and URL-encoded token"
    - "Workspace invite email contains workspace name and invite link"
  artifacts:
    - path: "backend/services/email_service.py"
      provides: "send_password_reset_email, send_workspace_invite_email, send_content_published_email"
      contains: "resend.Emails.send"
    - path: "backend/routes/password_reset.py"
      provides: "POST /auth/forgot-password, POST /auth/reset-password"
      contains: "send_password_reset_email"
    - path: "backend/tests/test_email_password_reset.py"
      provides: "Unit tests for email service and password reset flow"
      exports: ["TestEmailService", "TestPasswordResetFlow"]
  key_links:
    - from: "backend/routes/password_reset.py"
      to: "backend/services/email_service.py"
      via: "send_password_reset_email import"
      pattern: "from services.email_service import send_password_reset_email"
    - from: "backend/services/email_service.py"
      to: "config settings"
      via: "settings.email.is_configured() and settings.email.resend_api_key"
      pattern: "settings.email"
---

<objective>
Verify email service and password reset flow work end-to-end.

Purpose: Users who forget their password must receive a working reset email via Resend, and the token-based reset must securely update their password. Agency workspace invitations must also send emails.
Output: Tested email service and password reset flow with unit tests proving token generation, email dispatch, and password update all work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/services/email_service.py
@backend/routes/password_reset.py
@backend/routes/agency.py
@backend/config.py
@backend/auth_utils.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/services/email_service.py:
```python
def send_password_reset_email(to_email: str, reset_token: str) -> bool:
def send_workspace_invite_email(to_email: str, workspace_name: str, invite_token: str, inviter_name: str) -> bool:
def send_content_published_email(to_email: str, platform: str, content_preview: str) -> bool:
```

From backend/routes/password_reset.py:
```python
class ForgotPasswordRequest(BaseModel):
    email: EmailStr

class ResetPasswordRequest(BaseModel):
    token: str
    new_password: str
```

From backend/config.py:
```python
class EmailConfig:
    resend_api_key: Optional[str]
    from_email: str
    frontend_url: str
    def is_configured(self) -> bool:
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for email service and password reset flow</name>
  <files>backend/tests/test_email_password_reset.py</files>
  <read_first>
    - backend/services/email_service.py
    - backend/routes/password_reset.py
    - backend/auth_utils.py
    - backend/config.py
    - backend/tests/conftest.py
  </read_first>
  <behavior>
    - Test: _send_email returns False and logs warning when RESEND_API_KEY is not configured
    - Test: _send_email calls resend.Emails.send with correct from, to, subject, html when configured
    - Test: _send_email returns False and does not raise when resend.Emails.send throws
    - Test: send_password_reset_email builds reset link with frontend_url and URL-encoded token
    - Test: send_workspace_invite_email builds invite link and HTML-escapes workspace_name and inviter_name
    - Test: POST /auth/forgot-password with existing email-auth user creates token in password_resets collection
    - Test: POST /auth/forgot-password with nonexistent email returns same 200 response (no email enumeration)
    - Test: POST /auth/forgot-password with Google-auth user returns same 200 response (no email for OAuth users)
    - Test: POST /auth/reset-password with valid token updates password hash in users collection
    - Test: POST /auth/reset-password with used token returns 400
    - Test: POST /auth/reset-password with expired token returns 400
    - Test: POST /auth/reset-password with password < 8 chars returns 400
  </behavior>
  <action>
    Create `backend/tests/test_email_password_reset.py` with pytest + pytest-asyncio tests.

    For email service tests (TestEmailService class):
    - Mock `settings.email.is_configured()` to test both configured and unconfigured paths
    - Mock `resend.Emails.send` to verify it receives the correct payload without actually sending email
    - Mock `settings.email.resend_api_key` and `settings.email.from_email` for configured tests
    - Verify `send_password_reset_email` builds the reset link as: `{settings.email.frontend_url}/reset-password?token={url_encoded_token}`
    - Verify `send_workspace_invite_email` HTML-escapes workspace_name and inviter_name (test with `<script>alert(1)</script>` as workspace_name)

    For password reset flow tests (TestPasswordResetFlow class):
    - Use FastAPI TestClient with `httpx.AsyncClient(app=app, base_url="http://test")`
    - Create a test user in the DB before forgot-password tests
    - Mock `send_password_reset_email` to capture the token without sending real email
    - Verify token hash is stored in `db.password_resets` collection
    - Use the captured token to test reset-password endpoint
    - Verify password is actually updated by logging in with the new password
    - Test expired token by manually setting `expires_at` to past datetime
    - Test used token by setting `used: True` in the DB

    IMPORTANT: Do NOT use live Resend API. All email sends must be mocked.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_email_password_reset.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_email_password_reset.py` exists
    - `grep -c "async def test_\|def test_" backend/tests/test_email_password_reset.py` returns at least 10
    - `grep "resend" backend/tests/test_email_password_reset.py` matches (mocking resend verified)
    - `grep "is_configured" backend/tests/test_email_password_reset.py` matches (email config checked)
    - `grep "html.escape\|html_lib.escape\|XSS\|script" backend/tests/test_email_password_reset.py` matches (XSS prevention tested)
    - `grep "forgot-password\|reset-password" backend/tests/test_email_password_reset.py` matches (reset flow tested)
    - All tests pass with `pytest tests/test_email_password_reset.py -x`
  </acceptance_criteria>
  <done>All email and password reset unit tests pass: email service handles configured/unconfigured states, password reset flow creates tokens, validates expiry, and updates passwords securely.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any issues found in email service or password reset</name>
  <files>backend/services/email_service.py, backend/routes/password_reset.py</files>
  <read_first>
    - backend/services/email_service.py
    - backend/routes/password_reset.py
    - backend/config.py
  </read_first>
  <action>
    After running tests from Task 1, fix any failures. Known areas to verify:

    1. `password_reset.py` imports `send_password_reset_email` from `services.email_service` — verify this import works when running tests (may need path adjustments in test setup).

    2. The `forgot-password` endpoint uses `BackgroundTasks` to send email asynchronously. Verify the background task receives the correct arguments: `(data.email, token)` where `token` is the raw unhashed token.

    3. The `reset-password` endpoint enforces minimum 8-char password. Verify it also rejects empty strings.

    4. Verify `password_resets` collection name is consistent between `password_reset.py` (uses `db.password_resets`) and `db_indexes.py`.

    5. If `send_workspace_invite_email` in `agency.py` passes keyword arguments (`to_email=`, `workspace_name=`, `invite_token=`, `inviter_name=`), verify `email_service.py` function signature matches.

    If all tests pass without changes, skip this task.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_email_password_reset.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All tests in `test_email_password_reset.py` pass
    - `grep "send_password_reset_email" backend/routes/password_reset.py` matches (email function called)
    - `grep "background_tasks.add_task" backend/routes/password_reset.py` matches (async email send)
    - No regressions in existing test suite
  </acceptance_criteria>
  <done>Email service and password reset flow fully functional. Emails send via Resend when configured, gracefully degrade when not. Reset tokens are hashed, time-limited, and single-use.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_email_password_reset.py -v` shows all tests passing
- Email service returns False (not raises) when Resend is not configured
- Password reset tokens are SHA-256 hashed before storage
- Reset tokens expire after 1 hour
- Used tokens cannot be reused
- No email enumeration via forgot-password endpoint
</verification>

<success_criteria>
- 10+ unit tests for email and password reset all passing
- Password reset email sends via Resend with correct reset link (AUTH-04)
- Token-based password reset securely updates the password
- Email service handles unconfigured state gracefully
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-onboarding-email/03-02-SUMMARY.md`
</output>
