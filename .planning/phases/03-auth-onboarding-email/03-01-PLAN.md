---
phase: 03-auth-onboarding-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/auth.py
  - backend/routes/auth_google.py
  - backend/tests/test_auth_core.py
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "New user registration returns 200 with token and 200 starter credits"
    - "Login with valid credentials returns JWT token and sets httpOnly cookie"
    - "GET /auth/me with valid token returns user data without hashed_password or _id"
    - "Invalid credentials return 401"
    - "Google OAuth returns 503 with clear message when not configured"
    - "Google OAuth callback creates new user with 200 credits or links to existing user"
  artifacts:
    - path: "backend/routes/auth.py"
      provides: "Registration, login, logout, me endpoints"
      contains: "credits.*200"
    - path: "backend/routes/auth_google.py"
      provides: "Google OAuth login and callback"
      contains: "credits.*200"
    - path: "backend/tests/test_auth_core.py"
      provides: "Unit tests for auth endpoints"
      exports: ["TestRegistration", "TestLogin", "TestSessionPersistence", "TestGoogleOAuth"]
  key_links:
    - from: "backend/routes/auth.py"
      to: "backend/auth_utils.py"
      via: "hash_password, verify_password, get_current_user imports"
      pattern: "from auth_utils import"
    - from: "backend/routes/auth_google.py"
      to: "backend/routes/auth.py"
      via: "create_jwt_token, set_auth_cookie imports"
      pattern: "from routes.auth import create_jwt_token"
---

<objective>
Verify and harden auth registration, login, session persistence, and Google OAuth flows.

Purpose: Ensure users can register with 200 starter credits, log in, maintain sessions across refresh, and use Google OAuth — the entry gate to the entire platform.
Output: Verified auth endpoints with comprehensive unit tests proving all flows work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/auth.py
@backend/routes/auth_google.py
@backend/auth_utils.py
@backend/config.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for auth core (register, login, session, Google OAuth)</name>
  <files>backend/tests/test_auth_core.py</files>
  <read_first>
    - backend/routes/auth.py
    - backend/routes/auth_google.py
    - backend/auth_utils.py
    - backend/config.py
    - backend/tests/conftest.py
    - backend/tests/test_auth.py
  </read_first>
  <behavior>
    - Test: POST /auth/register with valid data returns 200, token, user_id, credits=200, onboarding_completed=False
    - Test: POST /auth/register response does NOT contain hashed_password or _id
    - Test: POST /auth/register with duplicate email returns 400
    - Test: POST /auth/register sets httpOnly cookie named "session_token"
    - Test: POST /auth/login with valid credentials returns 200 with token
    - Test: POST /auth/login with wrong password returns 401
    - Test: POST /auth/login with nonexistent email returns 401
    - Test: POST /auth/login for Google-auth user returns 401 (cannot email-login)
    - Test: GET /auth/me with valid Bearer token returns user data
    - Test: GET /auth/me without token returns 401
    - Test: GET /auth/me with expired token returns 401
    - Test: POST /auth/logout clears session_token cookie
    - Test: GET /auth/google without config returns 503 with error "google_auth_unavailable"
    - Test: Google OAuth callback with existing email updates name/picture, does not create duplicate
    - Test: Google OAuth callback with new email creates user with credits=200, auth_method="google"
  </behavior>
  <action>
    Create `backend/tests/test_auth_core.py` with pytest + pytest-asyncio tests using httpx.AsyncClient against a FastAPI TestClient.

    Use the existing conftest.py pattern from Phase 2. Import the FastAPI app from server.py using `from server import app`. Use `httpx.AsyncClient(app=app, base_url="http://test")` for async test client.

    For Google OAuth tests, mock the `_oauth.google` authlib client to avoid real Google API calls. Use `unittest.mock.patch` to mock `_oauth.google.authorize_redirect` and `_oauth.google.authorize_access_token`.

    For database isolation: use a unique email per test (e.g., `f"test_auth_{uuid.uuid4().hex[:8]}@test.com"`).

    Test classes:
    - `TestRegistration` — register success, duplicate, cookie set, credits=200
    - `TestLogin` — success, wrong password, nonexistent, google-user-blocked
    - `TestSessionPersistence` — /me with valid token, without token, with expired token
    - `TestGoogleOAuth` — 503 when unconfigured, callback creates/links user

    IMPORTANT: The existing `test_auth.py` uses `requests` against a live server (integration tests). These new tests must be proper unit tests using FastAPI TestClient that run WITHOUT a live server.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_auth_core.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_auth_core.py` exists
    - `grep -c "async def test_" backend/tests/test_auth_core.py` returns at least 12
    - `grep "credits.*200" backend/tests/test_auth_core.py` matches (verifies starter credits checked)
    - `grep "hashed_password" backend/tests/test_auth_core.py` matches (verifies exclusion checked)
    - `grep "session_token" backend/tests/test_auth_core.py` matches (verifies cookie checked)
    - `grep "google_auth_unavailable" backend/tests/test_auth_core.py` matches (verifies OAuth 503 checked)
    - All tests pass with `pytest tests/test_auth_core.py -x`
  </acceptance_criteria>
  <done>All auth core unit tests pass: registration gives 200 credits, login returns JWT, session persists via /me, Google OAuth returns 503 when unconfigured and creates/links users when configured.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any auth issues found by tests</name>
  <files>backend/routes/auth.py, backend/routes/auth_google.py</files>
  <read_first>
    - backend/routes/auth.py
    - backend/routes/auth_google.py
    - backend/auth_utils.py
  </read_first>
  <action>
    After running the tests from Task 1, fix any failures discovered. Known areas to verify and fix if broken:

    1. Registration MUST return `credits: 200` in the response (currently does via `safe_user` which includes all fields except hashed_password and _id — verify credits field is present).

    2. Login for a Google OAuth user (auth_method="google") MUST return 401, not leak the fact that the email exists. Current code checks `user.get("auth_method") == "google"` on line 73 — this correctly returns 401 but with same message as "invalid credentials". This is correct security behavior.

    3. The `set_auth_cookie` function sets `secure=True` and `samesite="none"`. For local development (non-HTTPS), this may prevent cookies from being set. Add a check: if `settings.app.is_development`, set `secure=False` and `samesite="lax"`.

    4. Google OAuth callback creates user with `credits: 200` — verify this on line 139 of auth_google.py (currently correct).

    5. Ensure `EXPIRE_DAYS` in auth.py uses `settings.security.jwt_expire_days` instead of hardcoded `7` for consistency with auth_utils.py.

    If all tests pass without changes, skip this task (no modifications needed).
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_auth_core.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "settings.security.jwt_expire_days\|EXPIRE_DAYS.*settings" backend/routes/auth.py` matches (JWT expiry from config)
    - `grep "settings.app.is_development\|is_production" backend/routes/auth.py` matches (cookie secure flag conditional)
    - All tests in `test_auth_core.py` pass
    - `cd backend && python -m pytest tests/ -x --ignore=tests/test_e2e_ship.py --ignore=tests/auth_isolation_test.py --ignore=tests/backend_test.py --ignore=tests/backend_test_focused.py --ignore=tests/backend_test_sprint7.py --ignore=tests/debug_public_persona.py --ignore=tests/debug_series_test.py --ignore=tests/focused_series_test.py --ignore=tests/production_deployment_test.py --ignore=tests/targeted_test.py` shows zero failures
  </acceptance_criteria>
  <done>Auth routes use config-driven JWT expiry and development-safe cookie settings. All auth tests pass with no regressions.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_auth_core.py -v` shows all tests passing
- Registration response includes `credits: 200` and no `hashed_password`
- Login returns JWT token
- /me endpoint returns user data with valid token, 401 without
- Google OAuth returns 503 when unconfigured
- No regressions in existing test suite
</verification>

<success_criteria>
- 12+ unit tests for auth core all passing
- Registration gives exactly 200 starter credits (AUTH-01)
- Login returns JWT and cookie persists session (AUTH-02)
- Google OAuth creates/links users correctly (AUTH-03)
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-onboarding-email/03-01-SUMMARY.md`
</output>
