---
phase: 03-auth-onboarding-email
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/routes/onboarding.py
  - backend/tests/test_onboarding_core.py
autonomous: true
requirements: [AUTH-05, AUTH-06]

must_haves:
  truths:
    - "Onboarding generate-persona uses model claude-sonnet-4-20250514, not the wrong claude-4-sonnet-20250514"
    - "Persona card generated via LLM contains personalized fields based on interview answers, not generic templates"
    - "Smart fallback persona generator produces meaningful persona from answers when LLM is unavailable"
    - "Persona Engine document in DB has voice_fingerprint, content_identity, uom, and learning_signals populated"
    - "Onboarding completion sets user.onboarding_completed to True"
  artifacts:
    - path: "backend/routes/onboarding.py"
      provides: "Onboarding questions, post analysis, persona generation"
      contains: "claude-sonnet-4-20250514"
    - path: "backend/tests/test_onboarding_core.py"
      provides: "Unit tests for onboarding and persona generation"
      exports: ["TestOnboardingQuestions", "TestPersonaGeneration", "TestSmartFallback"]
  key_links:
    - from: "backend/routes/onboarding.py"
      to: "backend/services/llm_client.py"
      via: "LlmChat class for persona generation"
      pattern: "from services.llm_client import LlmChat"
    - from: "backend/routes/onboarding.py"
      to: "database"
      via: "db.persona_engines.update_one and db.users.update_one"
      pattern: "db.persona_engines.update_one"
---

<objective>
Verify onboarding uses the correct Claude model and produces real personalized Persona Engines.

Purpose: The onboarding interview is ThookAI's core product value. If it uses the wrong model name, every user gets a generic mock persona instead of a personalized voice fingerprint. This plan ensures AUTH-05 (correct model) and AUTH-06 (real personalized persona).
Output: Verified onboarding with unit tests proving model correctness, persona personalization, and fallback quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/onboarding.py
@backend/services/llm_client.py
@backend/services/llm_keys.py
@backend/config.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/routes/onboarding.py:
```python
INTERVIEW_QUESTIONS = [...]  # 7 questions, ids 0-6

class GeneratePersonaRequest(BaseModel):
    answers: List[Dict[str, Any]]
    posts_analysis: Optional[str] = None

# Persona card expected structure:
# writing_voice_descriptor, content_niche_signature, inferred_audience_profile,
# top_content_format, personality_archetype, tone, regional_english, hook_style,
# focus_platforms, content_pillars, content_goal, burnout_risk, risk_tolerance,
# strategy_maturity, writing_style_notes
```

From backend/services/llm_client.py:
```python
class LlmChat:
    def __init__(self, api_key, session_id, system_message):
    def with_model(self, provider, model_name) -> "LlmChat":
    async def send_message(self, message: UserMessage) -> str:
```

From backend/services/llm_keys.py:
```python
def anthropic_available() -> bool:
def chat_constructor_key() -> str:
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for onboarding and persona generation</name>
  <files>backend/tests/test_onboarding_core.py</files>
  <read_first>
    - backend/routes/onboarding.py
    - backend/services/llm_client.py
    - backend/services/llm_keys.py
    - backend/config.py
    - backend/tests/conftest.py
  </read_first>
  <behavior>
    - Test: GET /onboarding/questions returns exactly 7 questions with id, type, question, hint fields
    - Test: Questions include both "text" and "multi_choice" types
    - Test: The LlmChat.with_model call in generate-persona uses provider="anthropic" and model="claude-sonnet-4-20250514" (NOT "claude-4-sonnet-20250514")
    - Test: When anthropic_available() returns True, generate-persona calls LlmChat with correct model and system_message
    - Test: When LLM returns valid JSON persona card, it is stored in db.persona_engines with voice_fingerprint, content_identity, uom fields
    - Test: When LLM fails (exception), smart fallback _generate_smart_persona is used
    - Test: Smart fallback with "I am a SaaS founder" answer produces personality_archetype="Builder"
    - Test: Smart fallback with "Bold, provocative" style words produces personality_archetype="Provocateur"
    - Test: Smart fallback with "Under 1 hour" time produces burnout_risk="high"
    - Test: Smart fallback with "All three" platform produces focus_platforms=["LinkedIn", "X (Twitter)", "Instagram"]
    - Test: Persona doc stored in DB has uom.burnout_risk, uom.risk_tolerance, uom.strategy_maturity populated
    - Test: After generate-persona, user.onboarding_completed is set to True
    - Test: generate-persona requires authentication (401 without token)
  </behavior>
  <action>
    Create `backend/tests/test_onboarding_core.py` with pytest + pytest-asyncio tests.

    Test structure:

    **TestOnboardingQuestions class:**
    - Test GET /onboarding/questions endpoint via TestClient
    - Verify 7 questions with correct fields

    **TestModelCorrectness class (AUTH-05):**
    - Import `onboarding` module and inspect the code path
    - Mock `anthropic_available()` to return True
    - Mock `LlmChat` class to capture the `.with_model()` call arguments
    - Call the generate-persona endpoint and verify `.with_model("anthropic", "claude-sonnet-4-20250514")` was called
    - This is the critical AUTH-05 verification: the model name must be exactly `claude-sonnet-4-20250514`

    **TestPersonaGeneration class (AUTH-06):**
    - Mock LlmChat to return a valid JSON persona card string
    - Call generate-persona with sample answers
    - Verify persona_engines collection contains the document with:
      - `card` field matching the LLM response
      - `voice_fingerprint` with `sentence_length_distribution`, `vocabulary_complexity`
      - `content_identity` with `topic_clusters`, `tone`, `regional_english`
      - `uom` with `burnout_risk`, `risk_tolerance`, `strategy_maturity`, `trust_in_thook`
      - `learning_signals` initialized with empty lists
    - Verify `users.onboarding_completed` is True after generation

    **TestSmartFallback class:**
    - Call `_generate_smart_persona` directly (import from onboarding module)
    - Test with various answer combinations to verify archetype detection:
      - "founder" in about -> "Builder"
      - "story" in about -> "Storyteller"
      - "bold" in style -> "Provocateur"
      - default -> "Educator"
    - Test burnout_risk: "Under 1 hour" -> "high", "1-3 hours" -> "medium", "5+ hours" -> "low"
    - Test platform parsing: "All three", "LinkedIn + X", single platform

    Use `from server import app` and `httpx.AsyncClient(app=app)` for endpoint tests.
    Import `_generate_smart_persona` from `routes.onboarding` for direct fallback tests.

    For the model name test (AUTH-05), use `unittest.mock.patch("routes.onboarding.LlmChat")` to capture the with_model arguments.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_onboarding_core.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_onboarding_core.py` exists
    - `grep -c "async def test_\|def test_" backend/tests/test_onboarding_core.py` returns at least 12
    - `grep "claude-sonnet-4-20250514" backend/tests/test_onboarding_core.py` matches (model name verified)
    - `grep "voice_fingerprint" backend/tests/test_onboarding_core.py` matches (persona structure checked)
    - `grep "uom" backend/tests/test_onboarding_core.py` matches (UOM fields checked)
    - `grep "_generate_smart_persona\|smart_persona" backend/tests/test_onboarding_core.py` matches (fallback tested)
    - `grep "onboarding_completed" backend/tests/test_onboarding_core.py` matches (flag update checked)
    - All tests pass with `pytest tests/test_onboarding_core.py -x`
  </acceptance_criteria>
  <done>All onboarding tests pass: correct Claude model used (AUTH-05), persona has personalized fields from LLM or smart fallback (AUTH-06), UOM fields populated, onboarding flag set.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any onboarding issues found by tests</name>
  <files>backend/routes/onboarding.py</files>
  <read_first>
    - backend/routes/onboarding.py
    - backend/services/llm_client.py
    - backend/services/llm_keys.py
  </read_first>
  <action>
    After running tests from Task 1, fix any failures. Known areas to verify:

    1. **Model name (AUTH-05 critical):** Lines ~167 in onboarding.py must use `.with_model("anthropic", "claude-sonnet-4-20250514")`. The CLAUDE.md says there's a bug with `"claude-4-sonnet-20250514"` — the current code appears already fixed but VERIFY by grepping. If any instance of the wrong model name exists, replace with `"claude-sonnet-4-20250514"`.

    Run: `grep -n "claude-4-sonnet\|claude-sonnet-4" backend/routes/onboarding.py` to confirm.

    2. **Analyze-posts model (line ~124):** Also verify the analyze-posts endpoint uses the correct model name `claude-sonnet-4-20250514`.

    3. **Fallback transparency:** When the smart fallback is used, the response currently does NOT indicate to the frontend that it was a fallback. Consider adding a `"demo_mode": True` or `"llm_generated": False` field to the response when fallback is used, so the frontend can optionally show a warning. Add a field `"source": "llm"` or `"source": "smart_fallback"` to the response dict.

    4. **Edge case:** If `data.answers` is empty, `_generate_smart_persona` should still produce a valid persona (verify the `get_ans` lambda handles missing question_ids gracefully).

    If all tests pass and model name is confirmed correct, the only change needed is adding the `source` field to the response.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && grep -n "claude-4-sonnet" routes/onboarding.py; echo "Exit: $?"; python -m pytest tests/test_onboarding_core.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claude-4-sonnet-20250514" backend/routes/onboarding.py` returns 0 (wrong model name absent)
    - `grep -c "claude-sonnet-4-20250514" backend/routes/onboarding.py` returns 2 (correct model in both analyze-posts and generate-persona)
    - All tests in `test_onboarding_core.py` pass
    - No regressions in existing test suite
  </acceptance_criteria>
  <done>Onboarding uses correct Claude model name everywhere (AUTH-05). Persona generation produces personalized results from LLM or quality fallback (AUTH-06). Source field indicates generation method.</done>
</task>

</tasks>

<verification>
- `grep "claude-sonnet-4-20250514" backend/routes/onboarding.py` returns 2 matches
- `grep "claude-4-sonnet" backend/routes/onboarding.py` returns 0 matches
- `pytest tests/test_onboarding_core.py -v` shows all tests passing
- Smart fallback produces archetype-specific personas
- UOM fields populated in persona doc
- No regressions in existing test suite
</verification>

<success_criteria>
- 12+ unit tests for onboarding all passing
- Model name is exactly "claude-sonnet-4-20250514" in both endpoints (AUTH-05)
- Persona Engine has personalized voice_fingerprint, content_identity, and UOM (AUTH-06)
- Smart fallback produces meaningful personas from interview answers
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-onboarding-email/03-03-SUMMARY.md`
</output>
