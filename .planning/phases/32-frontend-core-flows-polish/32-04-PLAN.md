---
phase: 32-frontend-core-flows-polish
plan: 04
type: execute
wave: 2
depends_on:
  - 32-03
files_modified:
  - frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx
  - frontend/src/pages/Dashboard/ContentStudio/index.jsx
  - frontend/src/pages/Dashboard/ContentStudio/AgentPipeline.jsx
  - frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx
autonomous: true
requirements:
  - FEND-03
  - FEND-06
  - FEND-07
must_haves:
  truths:
    - "Video toggle in InputPanel has role=switch, aria-checked, and responds to Space key"
    - "ContentStudio outer container has overflow-x-hidden to prevent 375px horizontal scroll"
    - "Left panel (InputPanel) has h-auto on mobile and h-[calc(100vh-4rem)] on md+"
    - "AgentPipeline progress region has aria-live=polite"
    - "All custom buttons in ContentStudio have type=button and focus-ring"
    - "ContentOutput schedule action UI (schedule-content-btn, schedule-datetime-input, schedule-submit-btn) is present and wired to PATCH /api/dashboard/schedule/content"
  artifacts:
    - path: "frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx"
      provides: "InputPanel with accessible video toggle"
      contains: "role=\"switch\"\|aria-checked"
    - path: "frontend/src/pages/Dashboard/ContentStudio/index.jsx"
      provides: "ContentStudio with responsive layout fix"
      contains: "overflow-x-hidden\|h-auto md:"
    - path: "frontend/src/pages/Dashboard/ContentStudio/AgentPipeline.jsx"
      provides: "Pipeline with aria-live region"
      contains: "aria-live=\"polite\""
    - path: "frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx"
      provides: "ContentOutput with schedule action data-testids (implemented in Phase 28)"
      contains: "schedule-content-btn\|schedule-datetime-input\|schedule-submit-btn"
  key_links:
    - from: "frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx"
      to: "onGenerateVideoChange callback"
      via: "role=switch button onClick + onKeyDown"
      pattern: "role=\"switch\""
    - from: "frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx"
      to: "/api/dashboard/schedule/content"
      via: "PATCH apiFetch with JSON body"
      pattern: "schedule/content"
---

<objective>
Fix ContentStudio for FEND-03/06/07: add role=switch keyboard handler to video toggle in InputPanel, fix responsive layout to prevent 375px horizontal scroll, add aria-live=polite to AgentPipeline progress display, and verify ContentOutput's schedule action (implemented in Phase 28) is still present.

Purpose: ContentStudio's custom video toggle has no keyboard support (FEND-07). The layout overflows at 375px (FEND-06). The pipeline progress area is not announced to screen readers (FEND-05/07). RESEARCH.md flags 'schedule action in ContentOutput' as a gap — Phase 28 (CONT-11) already implemented this; this plan verifies the implementation is intact rather than re-implementing it.

Output: Three files updated with targeted ARIA, keyboard, and responsive fixes. ContentOutput.jsx verified (read-only) to confirm Phase 28's schedule action data-testids are present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-frontend-core-flows-polish/32-RESEARCH.md

<interfaces>
<!-- Video toggle pattern from RESEARCH.md Pattern 3 -->
The video toggle button MUST be updated to:
```jsx
<button
  type="button"
  role="switch"
  aria-checked={generateVideo}
  aria-label="Generate video with content"
  onClick={() => videoEnabled && onGenerateVideoChange?.(!generateVideo)}
  onKeyDown={(e) => {
    if ((e.key === 'Enter' || e.key === ' ') && videoEnabled) {
      e.preventDefault();
      onGenerateVideoChange?.(!generateVideo);
    }
  }}
  disabled={!videoEnabled}
  className={`relative w-10 h-5 rounded-full transition-colors ${
    generateVideo && videoEnabled ? "bg-violet" : "bg-white/10"
  } ${!videoEnabled ? "opacity-40 cursor-not-allowed" : "cursor-pointer focus-ring"}`}
  data-testid="video-toggle"
>
```
aria-checked must be the boolean value of generateVideo (not string "true"/"false")

<!-- ContentStudio outer layout fix from RESEARCH.md Pattern 4 -->
Replace the outer flex container classes with:
  "flex flex-col md:flex-row overflow-x-hidden"
Left panel div:
  "w-full md:w-[400px] flex-shrink-0 h-auto md:h-[calc(100vh-4rem)] overflow-y-auto border-b md:border-b-0 md:border-r border-white/5"
Right panel div:
  "flex-1 min-h-[50vh] md:h-[calc(100vh-4rem)] overflow-y-auto"

<!-- AgentPipeline aria-live from RESEARCH.md Pattern 6 -->
Wrap the pipeline status cards in:
  <div aria-live="polite" aria-label="Generation progress">
    ...agent status cards...
  </div>

<!-- Schedule action — Phase 28 (CONT-11) implementation to verify -->
ContentOutput.jsx should already contain (from Phase 28):
  data-testid="schedule-content-btn"   — button to open schedule UI
  data-testid="schedule-datetime-input" — datetime input for scheduled time
  data-testid="schedule-submit-btn"    — submit button that fires PATCH /api/dashboard/schedule/content
If any of these are missing, add them. Do NOT re-implement the full schedule flow — only add missing data-testid attributes or the PATCH call if the button exists but lacks the correct endpoint.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read all four ContentStudio files</name>
  <files>
    frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx
    frontend/src/pages/Dashboard/ContentStudio/index.jsx
    frontend/src/pages/Dashboard/ContentStudio/AgentPipeline.jsx
    frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx
  </files>
  <read_first>
    - frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx (full file)
    - frontend/src/pages/Dashboard/ContentStudio/index.jsx (full file)
    - frontend/src/pages/Dashboard/ContentStudio/AgentPipeline.jsx (full file)
    - frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx (full file)
  </read_first>
  <action>
Read all four files. For InputPanel: find the video toggle button and note its current classes and event handlers. For index.jsx: find the outer flex container and the left/right panel divs, note their current className values. For AgentPipeline: find where agent status cards are rendered and whether any aria-live exists. For ContentOutput: check whether data-testid="schedule-content-btn", data-testid="schedule-datetime-input", and data-testid="schedule-submit-btn" are present, and whether the schedule submit calls PATCH /api/dashboard/schedule/content. Note all findings — do NOT modify yet.
  </action>
  <verify>
    <automated>grep -n "role=\|aria-\|overflow-x\|video-toggle\|schedule-content-btn\|schedule-datetime\|schedule-submit" /Users/kuldeepsinhparmar/thookAI-production/frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx | head -15</automated>
  </verify>
  <done>Audit complete — current state of all four files documented, including whether Phase 28 schedule data-testids are present in ContentOutput.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix video toggle keyboard accessibility in InputPanel.jsx</name>
  <files>frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx</files>
  <read_first>
    - frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx (full file — re-read before editing)
  </read_first>
  <action>
In InputPanel.jsx, find the video toggle button element (look for the toggle that controls `generateVideo` state or prop). Apply these changes to it:

1. Add `role="switch"` attribute
2. Add `aria-checked={generateVideo}` (boolean — NOT string)
3. Add `aria-label="Generate video with content"` if no existing aria-label
4. Add `data-testid="video-toggle"` if not present
5. Add `type="button"` if not present
6. Add `onKeyDown` handler:
```jsx
onKeyDown={(e) => {
  if ((e.key === 'Enter' || e.key === ' ') && videoEnabled) {
    e.preventDefault();
    onGenerateVideoChange?.(!generateVideo);
  }
}}
```
(Adapt variable names to match actual props/state in the file — `generateVideo`, `videoEnabled`, `onGenerateVideoChange` may be named differently)

7. Add `focus-ring` class to the button's className (inside the className string)

Also: scan the file for any other `<button>` elements missing `type="button"` or `focus-ring`, and add them.

Constraints: Do NOT change the visual appearance of the toggle beyond adding focus-ring. Do NOT refactor surrounding code.
  </action>
  <acceptance_criteria>
    grep -n "role=\"switch\"" frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx
    grep -n "aria-checked" frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx
    grep -n "onKeyDown" frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx
    grep -n "video-toggle" frontend/src/pages/Dashboard/ContentStudio/InputPanel.jsx
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/frontend && grep -n "role=\"switch\"\|aria-checked\|onKeyDown.*Enter\|video-toggle" src/pages/Dashboard/ContentStudio/InputPanel.jsx && echo "VIDEO TOGGLE KEYBOARD OK"</automated>
  </verify>
  <done>Video toggle has role=switch, aria-checked={boolean}, onKeyDown with Enter/Space handler, data-testid=video-toggle, type=button, focus-ring class.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix ContentStudio responsive layout and add aria-live to AgentPipeline</name>
  <files>
    frontend/src/pages/Dashboard/ContentStudio/index.jsx
    frontend/src/pages/Dashboard/ContentStudio/AgentPipeline.jsx
  </files>
  <read_first>
    - frontend/src/pages/Dashboard/ContentStudio/index.jsx (full file — re-read before editing)
    - frontend/src/pages/Dashboard/ContentStudio/AgentPipeline.jsx (full file — re-read before editing)
  </read_first>
  <action>
**In ContentStudio/index.jsx:**

Find the outermost container div that wraps InputPanel and the content panel. Update its className to include `overflow-x-hidden`. Then find the left panel (InputPanel wrapper div) and right panel div:

Left panel div — update className to:
```
"w-full md:w-[400px] flex-shrink-0 h-auto md:h-[calc(100vh-4rem)] overflow-y-auto border-b md:border-b-0 md:border-r border-white/5"
```
(Replace any existing `h-[calc(100vh-4rem)]` that lacks the `h-auto` mobile prefix)

Right panel div — update className to:
```
"flex-1 min-h-[50vh] md:h-[calc(100vh-4rem)] overflow-y-auto"
```
(Replace any `h-[calc(100vh-4rem)]` that lacks the `min-h-[50vh]` mobile prefix)

**In AgentPipeline.jsx:**

Find the container element that renders the agent status cards/steps. Wrap it (or add attribute to it) with:
```jsx
aria-live="polite"
aria-label="Generation progress"
```
If the container is already a div, add these two props to it. If the cards are in a list, add to the `<ul>` or wrapper div.

Also scan AgentPipeline for any buttons missing `type="button"` and add it.

Constraints: Minimal changes — only add/modify className values and aria attributes. Do NOT restructure components.
  </action>
  <acceptance_criteria>
    grep -n "overflow-x-hidden" frontend/src/pages/Dashboard/ContentStudio/index.jsx
    grep -n "h-auto md:h-" frontend/src/pages/Dashboard/ContentStudio/index.jsx
    grep -n "aria-live=\"polite\"" frontend/src/pages/Dashboard/ContentStudio/AgentPipeline.jsx
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/frontend && grep -n "overflow-x-hidden" src/pages/Dashboard/ContentStudio/index.jsx && grep -n "h-auto md:h-" src/pages/Dashboard/ContentStudio/index.jsx && grep -n "aria-live" src/pages/Dashboard/ContentStudio/AgentPipeline.jsx && echo "RESPONSIVE + ARIA OK"</automated>
  </verify>
  <done>ContentStudio index.jsx has overflow-x-hidden on outer container, left panel has h-auto md:h-[calc(100vh-4rem)], right panel has min-h-[50vh] md:h-[calc(100vh-4rem)]. AgentPipeline wrapper has aria-live=polite and aria-label=Generation progress.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verify ContentOutput schedule action from Phase 28 is intact</name>
  <files>frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx</files>
  <read_first>
    - frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx (full file — re-read before any edits)
  </read_first>
  <action>
Read ContentOutput.jsx. Check for all three Phase 28 schedule data-testids:
  - data-testid="schedule-content-btn"
  - data-testid="schedule-datetime-input"
  - data-testid="schedule-submit-btn"

And verify the schedule submit action calls PATCH /api/dashboard/schedule/content with a JSON body.

**If all three data-testids are already present and the PATCH endpoint is correct:** make NO changes. The verification grep below will confirm.

**If any data-testid is missing:** add only the missing `data-testid` attribute to the appropriate existing element — do NOT re-implement the schedule flow. Look for:
  - A button that opens/shows the schedule UI → add data-testid="schedule-content-btn"
  - A datetime `<input>` for the scheduled time → add data-testid="schedule-datetime-input"
  - A submit/confirm button for scheduling → add data-testid="schedule-submit-btn"

**If the PATCH body is incorrect (wrong URL or method):** fix only the fetch call URL/method to match PATCH /api/dashboard/schedule/content.

Constraints:
- Do NOT re-implement the schedule feature — it was already built in Phase 28
- Do NOT change any logic or JSX structure beyond adding missing data-testid attributes
- If the file already has all three data-testids, exit this task immediately with no changes
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/frontend && grep -n "schedule-content-btn\|schedule-datetime-input\|schedule-submit-btn" src/pages/Dashboard/ContentStudio/ContentOutput.jsx && grep -n "schedule/content" src/pages/Dashboard/ContentStudio/ContentOutput.jsx && echo "SCHEDULE ACTION OK"</automated>
  </verify>
  <done>ContentOutput.jsx has data-testid=schedule-content-btn, data-testid=schedule-datetime-input, data-testid=schedule-submit-btn, and PATCH /api/dashboard/schedule/content is the submit endpoint. All from Phase 28 — no re-implementation needed.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/kuldeepsinhparmar/thookAI-production/frontend
grep -n "role=\"switch\"\|aria-checked\|video-toggle" src/pages/Dashboard/ContentStudio/InputPanel.jsx
grep -n "overflow-x-hidden\|h-auto md:" src/pages/Dashboard/ContentStudio/index.jsx
grep -n "aria-live" src/pages/Dashboard/ContentStudio/AgentPipeline.jsx
grep -n "schedule-content-btn\|schedule-datetime-input\|schedule-submit-btn" src/pages/Dashboard/ContentStudio/ContentOutput.jsx
npm test -- --watchAll=false --testPathPattern="ContentStudio" --passWithNoTests 2>&1 | tail -5
```
</verification>

<success_criteria>
- InputPanel video toggle: role=switch, aria-checked={bool}, onKeyDown Space+Enter handler, data-testid=video-toggle, type=button, focus-ring
- ContentStudio outer container: overflow-x-hidden
- ContentStudio left panel: h-auto md:h-[calc(100vh-4rem)]
- ContentStudio right panel: min-h-[50vh] md:h-[calc(100vh-4rem)]
- AgentPipeline: aria-live=polite on status cards container
- ContentOutput: data-testid=schedule-content-btn, schedule-datetime-input, schedule-submit-btn all present; PATCH /api/dashboard/schedule/content is the submit endpoint
- No console.log added; no raw hex values
</success_criteria>

<output>
After completion, create `.planning/phases/32-frontend-core-flows-polish/32-04-SUMMARY.md` with files modified, changes made, and grep output confirming role=switch, overflow-x-hidden, aria-live, and all three schedule data-testids are present.
</output>
