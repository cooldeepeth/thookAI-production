---
phase: 32-frontend-core-flows-polish
plan: 05
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - frontend/src/components/ErrorBoundary.jsx
autonomous: true
requirements:
  - FEND-05
  - FEND-07
must_haves:
  truths:
    - "ErrorBoundary uses design system tokens (bg-[#050505], text-lime not text-lime-400)"
    - "ErrorBoundary reset button has type=button and focus-ring"
    - "ErrorBoundary has an AlertTriangle icon instead of the Warning emoji placeholder"
    - "ErrorBoundary calls componentDidCatch to log errors"
  artifacts:
    - path: "frontend/src/components/ErrorBoundary.jsx"
      provides: "Polished error boundary using design system tokens"
      contains: "AlertTriangle\|focus-ring\|bg-\\[#050505\\]"
  key_links:
    - from: "frontend/src/components/ErrorBoundary.jsx"
      to: "lucide-react"
      via: "import { AlertTriangle, RefreshCw }"
      pattern: "AlertTriangle"
---

<objective>
Fix ErrorBoundary.jsx to use correct design system tokens, replace the "Warning" text placeholder with AlertTriangle icon, and add focus-ring to the reset button.

Purpose: FEND-05 requires every page to handle error states — the global ErrorBoundary is the last line of defense. Currently it uses `text-lime-400` (not in config), `bg-[#0a0a0b]` (not the app background), and renders "Warning" as a text placeholder. These are design bugs noted in RESEARCH.md Pitfall 1.

Output: Updated ErrorBoundary.jsx using correct tokens and proper icon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-frontend-core-flows-polish/32-RESEARCH.md

<interfaces>
<!-- Correct token values from design-system.md -->
App background: bg-[#050505]  (NOT bg-[#0a0a0b])
Lime accent: text-lime  (NOT text-lime-400 — lime-400 is NOT in tailwind config)
Button: className="bg-lime text-black font-bold px-4 py-2 rounded-lg focus-ring type-button"

Icons: import { AlertTriangle, RefreshCw } from 'lucide-react';

componentDidCatch pattern (class component):
  componentDidCatch(error, info) {
    // Log to console in dev only — no console.log in production
    // If Sentry is configured: Sentry.captureException(error, { extra: info });
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read and fix ErrorBoundary.jsx</name>
  <files>frontend/src/components/ErrorBoundary.jsx</files>
  <read_first>
    - frontend/src/components/ErrorBoundary.jsx (full file)
  </read_first>
  <action>
Read ErrorBoundary.jsx, then apply these targeted fixes:

1. **Background color** — replace `bg-[#0a0a0b]` (or whatever incorrect background token exists) with `bg-[#050505]`.

2. **Lime text class** — replace `text-lime-400` with `text-lime` everywhere in this file.

3. **Icon** — find where "Warning" text or emoji appears. Replace with:
```jsx
import { AlertTriangle, RefreshCw } from 'lucide-react';
// In render:
<AlertTriangle size={32} className="text-red-400 mx-auto mb-4" />
```

4. **Reset button** — find the button that resets the error state (calls `this.setState({ hasError: false })`). Ensure it has:
   - `type="button"`
   - `className` includes `focus-ring`
   - A sensible label: "Try again" or "Reload page"

5. **componentDidCatch** — add or update:
```jsx
componentDidCatch(error, info) {
  // Intentionally left for Sentry integration in Phase 35
  // Do not console.log in production
}
```
(If componentDidCatch already exists and has console.error, replace console.error with a comment)

6. **data-testid** — add `data-testid="error-boundary-screen"` to the outermost div of the error fallback render.

Constraints:
- This is a class component — do NOT convert to function component
- Keep the `hasError`, `error`, `errorInfo` state structure as-is
- Do NOT add new npm imports
  </action>
  <acceptance_criteria>
    grep -n "text-lime-400" frontend/src/components/ErrorBoundary.jsx  # should return nothing
    grep -n "text-lime\b" frontend/src/components/ErrorBoundary.jsx   # should have matches
    grep -n "bg-\[#050505\]" frontend/src/components/ErrorBoundary.jsx
    grep -n "AlertTriangle\|focus-ring\|error-boundary-screen" frontend/src/components/ErrorBoundary.jsx
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/frontend && ! grep -q "text-lime-400\|#0a0a0b" src/components/ErrorBoundary.jsx && grep -q "AlertTriangle\|focus-ring\|error-boundary-screen" src/components/ErrorBoundary.jsx && echo "ERRORBOUNDARY OK" || echo "ERRORBOUNDARY NEEDS FIXES"</automated>
  </verify>
  <done>ErrorBoundary uses bg-[#050505], text-lime (not text-lime-400), AlertTriangle icon (not Warning text), reset button has type=button and focus-ring, data-testid=error-boundary-screen on fallback root, componentDidCatch has no console.error.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/kuldeepsinhparmar/thookAI-production/frontend
# Verify no old incorrect tokens
! grep -q "text-lime-400\|bg-\[#0a0a0b\]" src/components/ErrorBoundary.jsx && echo "NO BAD TOKENS"
# Verify new correct tokens and patterns
grep -n "AlertTriangle\|focus-ring\|error-boundary-screen\|bg-\[#050505\]" src/components/ErrorBoundary.jsx
npm test -- --watchAll=false --passWithNoTests 2>&1 | tail -3
```
</verification>

<success_criteria>
- No text-lime-400 or bg-[#0a0a0b] in ErrorBoundary.jsx
- AlertTriangle icon from lucide-react used in fallback render
- Reset button has type="button" and focus-ring class
- Fallback root div has data-testid="error-boundary-screen"
- componentDidCatch present with no console.error
- bg-[#050505] used as background in fallback render
</success_criteria>

<output>
After completion, create `.planning/phases/32-frontend-core-flows-polish/32-05-SUMMARY.md` with files modified, old vs new tokens, and grep output confirming no bad tokens remain.
</output>
