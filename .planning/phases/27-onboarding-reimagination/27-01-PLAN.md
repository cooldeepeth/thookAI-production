---
phase: 27-onboarding-reimagination
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_onboarding_core.py
  - backend/routes/onboarding.py
autonomous: true
requirements:
  - ONBD-05
  - ONBD-06
  - ONBD-08
must_haves:
  truths:
    - "pytest test_onboarding_core.py::TestModelCorrectness passes — correct model name verified"
    - "pytest TestNewPersonaFields passes — all four new fields present in persona_doc"
    - "generate_persona accepts voice_sample_url, visual_preference, writing_samples without error"
    - "Smart fallback includes personality_traits and voice_style in its output"
  artifacts:
    - path: "backend/tests/test_onboarding_core.py"
      provides: "Test classes TestNewPersonaFields, TestGeneratePersonaExtendedRequest, TestSmartFallbackNewFields"
      exports: ["TestNewPersonaFields", "TestGeneratePersonaExtendedRequest", "TestSmartFallbackNewFields"]
    - path: "backend/routes/onboarding.py"
      provides: "Extended GeneratePersonaRequest with new fields; persona_doc with four new fields; extended PERSONA_PROMPT"
      contains: "voice_sample_url, visual_preference, writing_samples, personality_traits, voice_style"
  key_links:
    - from: "backend/routes/onboarding.py"
      to: "backend/tests/test_onboarding_core.py"
      via: "pytest TestNewPersonaFields._run_generate_persona mock"
      pattern: "TestNewPersonaFields"
    - from: "backend/routes/onboarding.py"
      to: "backend/db.persona_engines"
      via: "update_one $set payload"
      pattern: "personality_traits.*voice_style.*visual_preferences.*writing_samples"
---

<objective>
Extend the backend onboarding route to accept and store four new persona fields, write TDD tests first (RED), then implement (GREEN).

Purpose: Persona generation must capture voice_style, visual_preferences, writing_samples, and personality_traits per ONBD-05/ONBD-06. Tests verify both the LLM path and smart fallback. ONBD-08 model name verification is already passing — tests confirm and are preserved.

Output: Extended GeneratePersonaRequest model, extended persona_doc, extended PERSONA_PROMPT JSON schema, extended _generate_smart_persona fallback, full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-onboarding-reimagination/27-RESEARCH.md
@.planning/phases/27-onboarding-reimagination/27-VALIDATION.md

<interfaces>
<!-- Current GeneratePersonaRequest (backend/routes/onboarding.py lines 101-103) -->
```python
class GeneratePersonaRequest(BaseModel):
    answers: List[Dict[str, Any]] = Field(min_length=1, description="At least one answer required")
    posts_analysis: Optional[str] = None
```

<!-- Current persona_doc structure (lines 196-230) — top-level keys -->
# persona_doc keys today: user_id, card, voice_fingerprint, content_identity,
# performance_intelligence, learning_signals, uom, onboarding_answers,
# created_at, updated_at
# MISSING: voice_style, visual_preferences, writing_samples, personality_traits

<!-- Current _generate_smart_persona signature (line 287) -->
def _generate_smart_persona(answers: list) -> dict:
# Returns dict with keys: writing_voice_descriptor, content_niche_signature,
# inferred_audience_profile, top_content_format, personality_archetype, tone,
# regional_english, hook_style, focus_platforms, content_pillars,
# content_goal, burnout_risk, risk_tolerance, strategy_maturity, writing_style_notes
# MISSING from return: personality_traits, voice_style

<!-- Current PERSONA_PROMPT JSON schema fields (lines 62-88) -->
# writing_voice_descriptor, content_niche_signature, inferred_audience_profile,
# top_content_format, personality_archetype, tone, regional_english, hook_style,
# focus_platforms, content_pillars, content_goal, burnout_risk, risk_tolerance,
# strategy_maturity, writing_style_notes
# MISSING: personality_traits, voice_style

<!-- Existing test file class structure -->
# TestOnboardingQuestions — 5 tests
# TestModelCorrectness — 3 tests (ONBD-08, already passing)
# TestPersonaGeneration — 11 tests (existing fields)
# TestSmartFallback — 12 tests (existing fields)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for new persona fields (RED phase)</name>
  <files>backend/tests/test_onboarding_core.py</files>
  <read_first>
    - backend/tests/test_onboarding_core.py (read entire file — understand existing test structure, MOCK_PERSONA_CARD, SAMPLE_ANSWERS fixtures, _run_generate_persona helper pattern)
    - .planning/phases/27-onboarding-reimagination/27-RESEARCH.md (Pattern 4 — backend schema extension, exact field names and types)
    - .planning/phases/27-onboarding-reimagination/27-VALIDATION.md (Wave 0 gaps — exact test names required)
  </read_first>
  <behavior>
    Append three new test classes to test_onboarding_core.py:

    TestGeneratePersonaExtendedRequest:
    - test_generate_persona_accepts_voice_sample_url: FakeRequest with voice_sample_url="https://r2.example.com/voice.webm" — call generate_persona, assert no exception raised
    - test_generate_persona_accepts_visual_preference: FakeRequest with visual_preference="bold" — call generate_persona, assert no exception raised
    - test_generate_persona_accepts_writing_samples: FakeRequest with writing_samples=["post1", "post2"] — call generate_persona, assert no exception raised
    - test_generate_persona_accepts_all_new_fields_together: All three new fields set simultaneously — assert no exception

    TestNewPersonaFields (extends _run_generate_persona helper pattern from TestPersonaGeneration):
    - test_persona_doc_has_voice_style: doc["voice_style"] must exist and be a str
    - test_persona_doc_has_visual_preferences: doc["visual_preferences"] must exist and equal the visual_preference passed in (e.g. "bold")
    - test_persona_doc_has_writing_samples: doc["writing_samples"] must exist and equal the writing_samples list passed in
    - test_persona_doc_has_personality_traits: doc["personality_traits"] must exist and be a list
    - test_visual_preferences_defaults_to_minimal_when_none: FakeRequest with visual_preference=None — doc["visual_preferences"] must equal "minimal"
    - test_writing_samples_defaults_to_empty_list_when_none: FakeRequest with writing_samples=None — doc["writing_samples"] must equal []

    TestSmartFallbackNewFields:
    - test_smart_fallback_includes_personality_traits: _generate_smart_persona(SAMPLE_ANSWERS) — result has "personality_traits" key and it is a list with at least one element
    - test_smart_fallback_includes_voice_style: result has "voice_style" key and it is a non-empty str
    - test_smart_fallback_personality_traits_are_strings: each element of personality_traits is a str

    IMPORTANT: The _run_generate_persona helper in TestNewPersonaFields must be extended to accept optional new fields:
    def _run_extended(self, visual_preference="bold", writing_samples=None, voice_sample_url=None):
        # Same pattern as TestPersonaGeneration._run_generate_persona but FakeRequest includes new fields
  </behavior>
  <action>
    Open backend/tests/test_onboarding_core.py. After the last class (TestSmartFallback), append the three new test classes.

    Use this exact MOCK_PERSONA_CARD_EXTENDED for the new tests (add personality_traits and voice_style to the existing MOCK_PERSONA_CARD dict at top of file):
    ```python
    MOCK_PERSONA_CARD_EXTENDED = {
        **MOCK_PERSONA_CARD,
        "personality_traits": ["Analytical", "Strategic", "Authentic"],
        "voice_style": "Direct professional voice with numbered frameworks and data-backed insights",
    }
    ```

    FakeRequest pattern for extended fields:
    ```python
    class FakeExtendedRequest(BaseModel):
        answers: List[Dict[str, Any]]
        posts_analysis: Optional[str] = None
        voice_sample_url: Optional[str] = None
        visual_preference: Optional[str] = None
        writing_samples: Optional[List[str]] = None
    ```

    After appending, run: `cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -k "TestNewPersonaFields or TestGeneratePersonaExtendedRequest or TestSmartFallbackNewFields" -x -q 2>&1 | head -40`
    Tests MUST fail (RED) because GeneratePersonaRequest does not yet accept new fields and persona_doc does not yet store them.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -k "TestNewPersonaFields or TestGeneratePersonaExtendedRequest or TestSmartFallbackNewFields" -x -q 2>&1 | head -40</automated>
  </verify>
  <done>New test classes appended; tests run and FAIL (RED) because implementation does not yet exist; existing passing tests are unaffected</done>
</task>

<task type="tdd">
  <name>Task 2: Implement backend persona schema extension (GREEN phase)</name>
  <files>backend/routes/onboarding.py</files>
  <read_first>
    - backend/routes/onboarding.py (read entire file — understand GeneratePersonaRequest, PERSONA_PROMPT, generate_persona function body, _generate_smart_persona function)
    - .planning/phases/27-onboarding-reimagination/27-RESEARCH.md (Pattern 4 code examples for GeneratePersonaRequest, persona_doc, PERSONA_PROMPT extension, _generate_smart_persona extension)
    - backend/tests/test_onboarding_core.py (read the new test classes added in Task 1 to understand exact assertions to satisfy)
  </read_first>
  <behavior>
    Four changes to onboarding.py:

    1. GeneratePersonaRequest — add three new Optional fields:
       - voice_sample_url: Optional[str] = None
       - visual_preference: Optional[str] = None
       - writing_samples: Optional[List[str]] = None

    2. PERSONA_PROMPT JSON schema — add two new fields to the return schema comment:
       - "personality_traits": ["trait1", "trait2", "trait3"]  (array of 3-5 personality trait strings)
       - "voice_style": "Description of inferred voice style from writing patterns" (string)

    3. generate_persona() body — add four new keys to persona_doc dict:
       - "voice_style": persona_card.get("voice_style", "")
       - "visual_preferences": data.visual_preference or "minimal"
       - "writing_samples": data.writing_samples or []
       - "personality_traits": persona_card.get("personality_traits", [])

    4. _generate_smart_persona() — add two new keys to the return dict:
       - "personality_traits": ["Analytical", "Strategic", "Authentic"]
       - "voice_style": f"Professional {style_words} voice with structured insights"
       where style_words comes from get_ans(2) which already exists in the function
  </behavior>
  <action>
    Edit backend/routes/onboarding.py with exactly these changes:

    Change 1 — GeneratePersonaRequest (after line 103 `posts_analysis: Optional[str] = None`):
    ```python
    voice_sample_url: Optional[str] = None        # R2 URL or None if not recorded/uploaded
    visual_preference: Optional[str] = None         # palette key: bold|minimal|corporate|creative|warm|dark
    writing_samples: Optional[List[str]] = None     # raw post texts used for style analysis
    ```

    Change 2 — PERSONA_PROMPT (add to JSON schema block, after "writing_style_notes" line):
    ```
      "personality_traits": ["trait1", "trait2", "trait3"],
      "voice_style": "Description of inferred voice style from writing patterns"
    ```

    Change 3 — persona_doc dict in generate_persona() (add after "onboarding_answers" line):
    ```python
    "voice_style": persona_card.get("voice_style", ""),
    "visual_preferences": data.visual_preference or "minimal",
    "writing_samples": data.writing_samples or [],
    "personality_traits": persona_card.get("personality_traits", []),
    ```

    Change 4 — _generate_smart_persona return dict (add two new keys at end of return dict):
    ```python
    "personality_traits": ["Analytical", "Strategic", "Authentic"],
    "voice_style": f"Professional {style_words} voice with structured insights",
    ```

    After all edits, run full test suite to verify GREEN:
    `cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -x -q 2>&1 | tail -20`
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>ALL tests in test_onboarding_core.py pass (GREEN); no regressions; new fields present in GeneratePersonaRequest and persona_doc; ONBD-08 model name tests still pass</done>
</task>

</tasks>

<verification>
Full test class verification:
```
cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -v 2>&1 | tail -40
```

Field presence verification:
```
grep -n "voice_sample_url\|visual_preference\|writing_samples\|personality_traits\|voice_style" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py
```
Expected: at least 8 matches covering GeneratePersonaRequest fields, persona_doc keys, PERSONA_PROMPT schema, and _generate_smart_persona return.

ONBD-08 model name:
```
grep -c "claude-sonnet-4-20250514" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py
```
Expected: 2 (analyze_posts and generate_persona functions).
</verification>

<success_criteria>
- `pytest tests/test_onboarding_core.py -x -q` exits 0 with all tests passing
- GeneratePersonaRequest has 5 fields: answers, posts_analysis, voice_sample_url, visual_preference, writing_samples
- persona_doc written to DB includes voice_style, visual_preferences, writing_samples, personality_traits
- _generate_smart_persona returns personality_traits (list) and voice_style (str)
- PERSONA_PROMPT JSON schema includes personality_traits and voice_style fields
- No wrong model name "claude-4-sonnet-20250514" in onboarding.py
</success_criteria>

<output>
After completion, create `.planning/phases/27-onboarding-reimagination/27-01-SUMMARY.md`
</output>
