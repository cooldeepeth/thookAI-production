---
phase: 27-onboarding-reimagination
plan: 05
type: execute
wave: 4
depends_on:
  - 27-01
  - 27-04
files_modified:
  - backend/tests/test_onboarding_core.py
  - backend/routes/onboarding.py
autonomous: true
requirements:
  - ONBD-01
  - ONBD-05
  - ONBD-06
  - ONBD-07
  - ONBD-08
must_haves:
  truths:
    - "Full pytest suite passes with zero failures"
    - "test_onboarding_core.py::TestNewPersonaFields all pass"
    - "test_onboarding_core.py::TestModelCorrectness all pass"
    - "PERSONA_PROMPT JSON schema includes personality_traits and voice_style fields"
    - "generate_persona returns source field with value llm or smart_fallback"
    - "onboarding.py contains no wrong model name claude-4-sonnet-20250514"
  artifacts:
    - path: "backend/routes/onboarding.py"
      provides: "Production-ready generate_persona with all schema fields and extended prompt"
      contains: "personality_traits"
    - path: "backend/tests/test_onboarding_core.py"
      provides: "Complete test coverage of all new fields and extended request model"
      contains: "TestNewPersonaFields"
  key_links:
    - from: "backend/routes/onboarding.py"
      to: "backend/tests/test_onboarding_core.py"
      via: "pytest full test run"
      pattern: "PASSED.*test_onboarding_core"
---

<objective>
Final audit and hardening pass for the backend onboarding route. Verify complete test coverage, validate all persona fields are correct, confirm no regression in existing tests, and produce a clean test run as the phase gate.

Purpose: ONBD-05/ONBD-06 require non-null fields in persona. ONBD-08 requires correct model name. This plan ensures the backend is verified end-to-end before the phase is marked complete. Also audits edge cases: empty answers, missing optional fields, LLM timeout fallback.

Output: Green pytest run; any audit findings fixed; onboarding.py hardened with no regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-onboarding-reimagination/27-RESEARCH.md
@.planning/phases/27-onboarding-reimagination/27-01-SUMMARY.md
@.planning/phases/27-onboarding-reimagination/27-04-SUMMARY.md

<interfaces>
<!-- Expected final GeneratePersonaRequest shape (after Plan 01) -->
class GeneratePersonaRequest(BaseModel):
    answers: List[Dict[str, Any]] = Field(min_length=1)
    posts_analysis: Optional[str] = None
    voice_sample_url: Optional[str] = None
    visual_preference: Optional[str] = None
    writing_samples: Optional[List[str]] = None

<!-- Expected final persona_doc top-level keys (after Plan 01) -->
# user_id, card, voice_fingerprint, content_identity, performance_intelligence,
# learning_signals, uom, onboarding_answers, created_at, updated_at,
# voice_style, visual_preferences, writing_samples, personality_traits

<!-- Expected PERSONA_PROMPT additions (after Plan 01) -->
# JSON schema includes: personality_traits (array), voice_style (string)

<!-- Expected _generate_smart_persona additions (after Plan 01) -->
# Returns: personality_traits (list of 3 strings), voice_style (non-empty string)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit onboarding.py for correctness and run full test suite</name>
  <files>backend/routes/onboarding.py, backend/tests/test_onboarding_core.py</files>
  <read_first>
    - backend/routes/onboarding.py (read entire file — check GeneratePersonaRequest fields, PERSONA_PROMPT JSON schema, persona_doc construction, _generate_smart_persona return dict, model name occurrences)
    - backend/tests/test_onboarding_core.py (read entire file — verify all three new test classes from Plan 01 are present: TestGeneratePersonaExtendedRequest, TestNewPersonaFields, TestSmartFallbackNewFields)
    - .planning/phases/27-onboarding-reimagination/27-RESEARCH.md (Pitfall 3 — LLM returning null for new fields; Pitfall 6 — model name verification)
  </read_first>
  <action>
    Step 1 — Audit onboarding.py with grep checks (run all, fix any that fail):

    Check 1 — Correct model name only:
    ```bash
    grep -c "claude-sonnet-4-20250514" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py
    ```
    Expected: 2. If different, locate any other LlmChat.with_model() calls and fix.

    Check 2 — Wrong model name absent:
    ```bash
    grep "claude-4-sonnet-20250514" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py
    ```
    Expected: no output (empty). If found, replace all occurrences with "claude-sonnet-4-20250514".

    Check 3 — New GeneratePersonaRequest fields present:
    ```bash
    grep -c "voice_sample_url\|visual_preference\|writing_samples" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py
    ```
    Expected: at least 6 (3 in class definition + 3 in persona_doc). If fewer, re-apply Plan 01 changes.

    Check 4 — persona_doc has all four new keys:
    ```bash
    grep -n "personality_traits\|voice_style\|visual_preferences\|writing_samples" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py
    ```
    Expected: at least 8 lines (4 in persona_doc + 4 in _generate_smart_persona + some in PERSONA_PROMPT).

    Check 5 — PERSONA_PROMPT has personality_traits in JSON schema:
    ```bash
    grep "personality_traits" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py
    ```
    Expected: at least 3 occurrences (PERSONA_PROMPT schema, persona_doc, _generate_smart_persona).

    Step 2 — Run full test suite:
    ```bash
    cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -v --tb=short 2>&1
    ```

    Step 3 — Fix any failures:
    If audit checks fail or tests fail, make the specific targeted fixes described in the audit. Common issues:
    - If TestGeneratePersonaExtendedRequest fails: FakeRequest in tests may not match updated GeneratePersonaRequest — update the test helper to use the real GeneratePersonaRequest instead of a FakeRequest
    - If TestNewPersonaFields.test_persona_doc_has_visual_preferences fails with "minimal" not matching: verify `data.visual_preference or "minimal"` is in persona_doc construction (not `data.visual_preference or None`)
    - If TestSmartFallbackNewFields fails: verify _generate_smart_persona returns `"personality_traits"` and `"voice_style"` keys

    Step 4 — Run full backend suite as final gate:
    ```bash
    cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/ -x -q 2>&1 | tail -30
    ```
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
    - pytest tests/test_onboarding_core.py exits 0 with all tests PASSED
    - No "claude-4-sonnet-20250514" (wrong model) in onboarding.py
    - Exactly 2 occurrences of "claude-sonnet-4-20250514" in onboarding.py
    - persona_doc contains voice_style, visual_preferences, writing_samples, personality_traits keys
    - PERSONA_PROMPT includes personality_traits and voice_style in JSON schema
    - _generate_smart_persona returns personality_traits and voice_style
  </done>
</task>

<task type="auto">
  <name>Task 2: Add edge case tests for new fields and run complete suite</name>
  <files>backend/tests/test_onboarding_core.py</files>
  <read_first>
    - backend/tests/test_onboarding_core.py (read entire file after Plan 01 — specifically look at TestNewPersonaFields and TestGeneratePersonaExtendedRequest to identify any missing edge cases)
    - .planning/phases/27-onboarding-reimagination/27-RESEARCH.md (Pitfall 3 — LLM returning null for new fields; Common Pitfalls; Validation Architecture — Wave 0 gaps list)
    - .planning/phases/27-onboarding-reimagination/27-VALIDATION.md (Per-Task Verification Map — ONBD-05 and ONBD-06 test requirements)
  </read_first>
  <action>
    Audit the existing test classes for missing coverage. Add any missing tests:

    Missing tests to add (only if not already present from Plan 01):

    In TestNewPersonaFields (add if missing):
    - `test_persona_doc_personality_traits_is_list`: assert isinstance(doc["personality_traits"], list)
    - `test_persona_doc_voice_style_is_string`: assert isinstance(doc["voice_style"], str)
    - `test_persona_doc_has_voice_style_from_llm_output`: Pass MOCK_PERSONA_CARD_EXTENDED (with personality_traits and voice_style in LLM response) — verify doc["voice_style"] equals the LLM-returned voice_style value
    - `test_persona_doc_personality_traits_from_llm_output`: Verify doc["personality_traits"] equals MOCK_PERSONA_CARD_EXTENDED["personality_traits"]

    In TestGeneratePersonaExtendedRequest (add if missing):
    - `test_generate_persona_writing_samples_stored_in_doc`: Pass writing_samples=["post1", "post2"] — verify doc["writing_samples"] == ["post1", "post2"]
    - `test_generate_persona_visual_preference_stored_in_doc`: Pass visual_preference="creative" — verify doc["visual_preferences"] == "creative"

    After adding any missing tests, run:
    ```bash
    cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -v --tb=short 2>&1 | tail -50
    ```

    Then run the full backend suite as final gate:
    ```bash
    cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/ -x -q --timeout=60 2>&1 | tail -30
    ```

    If full suite fails on non-onboarding tests (pre-existing failures), document them but do not fix them in this plan — this plan scope is onboarding only.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All tests in TestNewPersonaFields pass
    - All tests in TestGeneratePersonaExtendedRequest pass
    - All tests in TestSmartFallbackNewFields pass
    - All pre-existing TestOnboardingQuestions, TestModelCorrectness, TestPersonaGeneration, TestSmartFallback tests still pass
    - Total passing test count >= 45 (30 existing + 15+ new)
    - Full suite command exits cleanly or documents pre-existing failures that are not in scope
  </done>
</task>

</tasks>

<verification>
Final model name audit:
```
grep -n "claude-4-sonnet-20250514" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py && echo "BUG FOUND" || echo "CLEAN"
```
Expected: CLEAN (no wrong model name).

Correct model name count:
```
grep -c "claude-sonnet-4-20250514" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py
```
Expected: 2.

New field presence in persona_doc:
```
grep -A2 "personality_traits\|voice_style\|visual_preferences\|writing_samples" /Users/kuldeepsinhparmar/thookAI-production/backend/routes/onboarding.py | grep -v "^--"
```
Expected: 8+ lines covering all four new fields in GeneratePersonaRequest, persona_doc, and _generate_smart_persona.

Full test pass:
```
cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_onboarding_core.py --co -q 2>&1 | grep "test session"
```
Expected: Shows test count >= 45.
</verification>

<success_criteria>
- pytest tests/test_onboarding_core.py exits 0 with all tests passing
- No wrong model name "claude-4-sonnet-20250514" in onboarding.py
- GeneratePersonaRequest has 5 fields total
- persona_doc has 14 top-level keys (existing 10 + 4 new)
- _generate_smart_persona returns personality_traits (list) and voice_style (str)
- PERSONA_PROMPT JSON schema comment includes personality_traits and voice_style
- All ONBD-05, ONBD-06, ONBD-08 requirements traceable to passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/27-onboarding-reimagination/27-05-SUMMARY.md`
</output>
