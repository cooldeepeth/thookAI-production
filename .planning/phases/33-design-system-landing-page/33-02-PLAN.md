---
phase: 33-design-system-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Dashboard/Analytics.jsx
  - frontend/src/pages/Dashboard/ContentLibrary.jsx
  - frontend/src/pages/Dashboard/ContentCalendar.jsx
  - frontend/src/pages/Dashboard/Campaigns.jsx
  - frontend/src/pages/Dashboard/Connections.jsx
  - frontend/src/pages/Dashboard/StrategyDashboard.jsx
  - frontend/src/pages/Dashboard/Admin.jsx
  - frontend/src/pages/Dashboard/AdminUsers.jsx
  - frontend/src/pages/Dashboard/DailyBrief.jsx
  - frontend/src/pages/Dashboard/PersonaEngine.jsx
  - frontend/src/pages/Dashboard/DashboardHome.jsx
  - frontend/src/pages/Dashboard/Settings.jsx
  - frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx
  - frontend/src/pages/Onboarding/PhaseTwo.jsx
  - frontend/src/pages/Onboarding/PhaseThree.jsx
autonomous: true
requirements:
  - DSGN-01

must_haves:
  truths:
    - "Status badges that mean 'published', 'active', 'connected', or 'approved' use text-lime and bg-lime/10 (not text-green-400 or bg-green-500)"
    - "No text-emerald-400 or bg-emerald-500 classes exist in dashboard or onboarding pages"
    - "Settings.jsx TIER_GRADIENTS uses to-violet/20 for studio and custom tiers instead of to-green-500/20"
    - "PersonaEngine.jsx uses bg-surface token instead of raw #0A0A0A hex"
  artifacts:
    - path: "frontend/src/pages/Dashboard/Analytics.jsx"
      provides: "Analytics page with lime tokens for success/positive states"
      contains: "text-lime"
    - path: "frontend/src/pages/Dashboard/Settings.jsx"
      provides: "Settings with corrected TIER_GRADIENTS (no green-500)"
      contains: "to-violet/20"
    - path: "frontend/src/pages/Dashboard/PersonaEngine.jsx"
      provides: "PersonaEngine with bg-surface token instead of raw hex"
      contains: "bg-surface"
  key_links:
    - from: "frontend/src/pages/Dashboard/Analytics.jsx"
      to: "frontend/tailwind.config.js"
      via: "text-lime token class"
      pattern: "text-lime"
    - from: "frontend/src/pages/Dashboard/Settings.jsx"
      to: "frontend/tailwind.config.js"
      via: "gradient token classes"
      pattern: "to-violet/20"
---

<objective>
Migrate green/emerald token violations across 15 dashboard and onboarding files to the
correct ThookAI lime-based design tokens. This is the largest single sweep in Phase 33 —
covering every authenticated page a user sees after login.

Purpose: After login, users spend nearly all their time in Dashboard. Inconsistent
green-400/emerald-400 "success" states undermine the lime/dark brand identity that the
design system establishes. Fixing these makes the authenticated experience visually coherent.

Output: 15 files with all actionable green/emerald token violations replaced. Platform-brand
colors in Shells/* and data arrays (LinkedIn blue, X blue, IG pink) are explicitly exempt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/rules/design-system.md

<interfaces>
<!-- Token substitution rules for this plan. Apply these mechanically — no judgment calls. -->

WRONG → CORRECT replacements:
  text-green-400    →  text-lime          (positive/active/published/connected states)
  text-green-500    →  text-lime
  bg-green-500/10   →  bg-lime/10
  bg-green-500/15   →  bg-lime/10
  bg-green-500/20   →  bg-lime/10
  text-emerald-400  →  text-lime
  text-emerald-500  →  text-lime
  bg-emerald-500/10 →  bg-lime/10
  bg-emerald-500/15 →  bg-lime/10
  bg-emerald-500/20 →  bg-lime/10
  color="text-emerald-400"  →  "text-lime"    (if used as a prop string)
  to-green-500/20   →  to-violet/20       (in TIER_GRADIENTS for studio/custom — violet is the premium accent)
  backgroundColor: "#0A0A0A"  →  use className="bg-surface" instead (PersonaEngine.jsx)

EXEMPT (do NOT change these green usages):
  Platform shell components (XShell.jsx, InstagramShell.jsx, LinkedInShell.jsx)
  #00BA7C  — X/Twitter retweet green (platform brand)
  Data arrays that define platform colors: { color: "#0A66C2" } etc.
  DashboardHome.jsx platform-color data arrays (VERIFIED exempt per RESEARCH section 1b)
  DailyBrief.jsx — platform hex in data arrays (2 of 3 violations exempt)
  VisualPaletteStep.jsx — color picker intentional hex values (explicitly skipped this plan)
  AgencyWorkspace/index.jsx — check context before changing (may be platform brand)

IMPORTANT: After each file edit, the executor MUST verify the change did not introduce
regressions by checking the file still renders (no unclosed JSX, no missing imports).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core dashboard pages — status badge token migration</name>
  <files>
    frontend/src/pages/Dashboard/Analytics.jsx,
    frontend/src/pages/Dashboard/ContentLibrary.jsx,
    frontend/src/pages/Dashboard/ContentCalendar.jsx,
    frontend/src/pages/Dashboard/Campaigns.jsx,
    frontend/src/pages/Dashboard/Connections.jsx,
    frontend/src/pages/Dashboard/StrategyDashboard.jsx,
    frontend/src/pages/Dashboard/DashboardHome.jsx,
    frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx
  </files>
  <action>
For each of the 8 files below, apply the mechanical token substitution from the interfaces block.
Process file by file. For each file, do a targeted search-and-replace on the EXACT violations
documented in RESEARCH.md section 1b. Do not change lines that aren't in scope.

ANALYTICS.jsx (lines 22, 29, 285, 355):
- `text-green-400` → `text-lime`
- `bg-green-500/10` → `bg-lime/10`

CONTENT LIBRARY.jsx (lines 28, 437):
- `bg-green-500/20 text-green-400` → `bg-lime/10 text-lime`

CONTENT CALENDAR.jsx (line 37):
- `bg-green-500/10 text-green-400` → `bg-lime/10 text-lime`

CAMPAIGNS.jsx (lines 260, 303):
- `bg-emerald-500/15 text-emerald-400` → `bg-lime/10 text-lime`

CONNECTIONS.jsx (line 238):
- `bg-green-500/10 text-green-400` → `bg-lime/10 text-lime`

STRATEGY DASHBOARD.jsx (line 174):
- `text-green-400` → `text-lime`

DASHBOARD HOME.jsx (line 32):
- `text-green-500` for "approved" status → `text-lime`
- IMPORTANT: Lines with platform hex data arrays (LinkedIn #0A66C2, X #1D9BF0, IG #E1306C)
  in DashboardHome — do NOT touch those. Only fix line 32 (`text-green-500`).

CONTENT OUTPUT.jsx (line 939):
- `text-green-400` on check icon → `text-lime`

After all 8 files, run:
```bash
grep -rn "text-green-[45]\|bg-green-[45]\|text-emerald-[45]\|bg-emerald-[45]" \
  frontend/src/pages/Dashboard/Analytics.jsx \
  frontend/src/pages/Dashboard/ContentLibrary.jsx \
  frontend/src/pages/Dashboard/ContentCalendar.jsx \
  frontend/src/pages/Dashboard/Campaigns.jsx \
  frontend/src/pages/Dashboard/Connections.jsx \
  frontend/src/pages/Dashboard/StrategyDashboard.jsx \
  frontend/src/pages/Dashboard/DashboardHome.jsx \
  frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx
```
Expected: zero output.
  </action>
  <verify>
    <automated>grep -rn "text-green-[45]\|bg-green-[45]\|text-emerald-[45]\|bg-emerald-[45]" frontend/src/pages/Dashboard/Analytics.jsx frontend/src/pages/Dashboard/ContentLibrary.jsx frontend/src/pages/Dashboard/ContentCalendar.jsx frontend/src/pages/Dashboard/Campaigns.jsx frontend/src/pages/Dashboard/Connections.jsx frontend/src/pages/Dashboard/StrategyDashboard.jsx frontend/src/pages/Dashboard/DashboardHome.jsx frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx; echo "Exit $?"</automated>
  </verify>
  <done>Zero grep matches for green-4/green-5/emerald-4/emerald-5 variants across all 8 dashboard files. Each file contains text-lime where the status colors previously used green/emerald.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining dashboard pages + Settings + Onboarding token migration</name>
  <files>
    frontend/src/pages/Dashboard/Admin.jsx,
    frontend/src/pages/Dashboard/AdminUsers.jsx,
    frontend/src/pages/Dashboard/DailyBrief.jsx,
    frontend/src/pages/Dashboard/PersonaEngine.jsx,
    frontend/src/pages/Dashboard/Settings.jsx,
    frontend/src/pages/Onboarding/PhaseTwo.jsx,
    frontend/src/pages/Onboarding/PhaseThree.jsx
  </files>
  <action>
Apply token migration to the remaining 7 files.

ADMIN.jsx (line 140):
- `color="text-emerald-400"` as a prop string → change to `"text-lime"` (same prop, new value)
- If `color` is passed to a component that spreads it as a className, the replacement is correct.

ADMIN USERS.jsx (lines 295, 328):
- `bg-green-500/15 text-green-400` for active badge → `bg-lime/10 text-lime`

DAILY BRIEF.jsx (line 17):
- Only fix: `text-green-400` for "energized" state → `text-lime`
- EXEMPT: Do NOT change any lines containing `#0A66C2`, `#1D9BF0`, `#E1306C` — those are
  platform brand hex values in a data array. Inspect the file to confirm which line is which
  before editing.

PERSONA ENGINE.jsx (line 167):
- `backgroundColor: "#0A0A0A"` inline style → remove the inline style and add `className="bg-surface"` instead.
- The element likely has an existing `className` prop — append `bg-surface` to it and remove the `style` prop entirely (or if the style prop has multiple keys, only remove the backgroundColor key).

SETTINGS.jsx (lines 39, 41 — TIER_GRADIENTS const):
- `to-green-500/20` for the `custom` tier gradient → `to-violet/20`
- `to-green-500/20` for the `studio` tier gradient → `to-violet/20`
- The TIER_GRADIENTS object is a const at the top of the file. Find the two entries with `to-green-500/20` and replace both.
- The `custom` tier is the primary paid plan — violet is the correct premium accent.
- Verify: the `custom` tier entry should become: `"from-lime/20 to-violet/20"`
- Verify: the `studio` tier entry should become: `"from-lime/20 to-violet/20"`

ONBOARDING PHASE TWO.jsx:
- Run grep to identify the specific violations, then replace any `text-green-400`/`bg-green-500` 
  patterns with lime tokens. Do not change intentional color palette hex values used in the 
  visual identity picker steps.

ONBOARDING PHASE THREE.jsx:
- Same approach as PhaseTwo — fix green status colors, preserve intentional palette values.

After all 7 files, run final sweep to confirm no violations remain:
```bash
grep -rn "text-green-[45]\|bg-green-[45]\|text-emerald-[45]\|bg-emerald-[45]\|to-green-500\|backgroundColor.*#0A0A0A" \
  frontend/src/pages/Dashboard/Admin.jsx \
  frontend/src/pages/Dashboard/AdminUsers.jsx \
  frontend/src/pages/Dashboard/DailyBrief.jsx \
  frontend/src/pages/Dashboard/PersonaEngine.jsx \
  frontend/src/pages/Dashboard/Settings.jsx \
  frontend/src/pages/Onboarding/PhaseTwo.jsx \
  frontend/src/pages/Onboarding/PhaseThree.jsx
```
Expected: zero output (ignoring exempt platform-brand lines).
  </action>
  <verify>
    <automated>grep -rn "text-green-[45]\|bg-green-[45]\|to-green-500\|bg-emerald-[45]\|text-emerald-[45]" frontend/src/pages/Dashboard/Admin.jsx frontend/src/pages/Dashboard/AdminUsers.jsx frontend/src/pages/Dashboard/DailyBrief.jsx frontend/src/pages/Dashboard/PersonaEngine.jsx frontend/src/pages/Dashboard/Settings.jsx frontend/src/pages/Onboarding/PhaseTwo.jsx frontend/src/pages/Onboarding/PhaseThree.jsx; echo "Exit $?"</automated>
  </verify>
  <done>Zero grep matches for green-4/5/emerald-4/5/to-green-500 in the 7 files. Settings.jsx TIER_GRADIENTS for custom and studio now contains `to-violet/20`. PersonaEngine.jsx uses `bg-surface` instead of the inline #0A0A0A hex.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tailwind JIT | Token class names must exactly match tailwind.config.js; wrong names silently produce nothing |
| Platform-brand exemption | LinkedIn/X/IG hex in data arrays must not be altered — they represent external brand identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | Tampering | Status badge colors | mitigate | Mechanical grep-verify after each file; automated check in verify step |
| T-33-02-02 | Tampering | Platform brand hex arrays | accept | EXEMPT block documents intentional hex; executor instructed explicitly in Task 1 and 2 |
| T-33-02-03 | Information Disclosure | Accidental platform color removal | mitigate | DailyBrief and DashboardHome instructions explicitly guard the data arrays |
</threat_model>

<verification>
1. Full dashboard sweep to confirm no actionable green/emerald violations remain:
   ```bash
   grep -rn "text-green-[45]\|bg-green-[45]\|text-emerald-[45]\|bg-emerald-[45]\|to-green-500" \
     frontend/src/pages/Dashboard/ \
     frontend/src/pages/Onboarding/ \
     --include="*.jsx" \
     | grep -v "Shells/"
   ```
   Expected: zero output.

2. Confirm lime tokens present in key files:
   ```bash
   grep -c "text-lime" frontend/src/pages/Dashboard/Analytics.jsx \
                        frontend/src/pages/Dashboard/Connections.jsx
   ```
   Expected: 1+ in each.

3. Settings TIER_GRADIENTS verification:
   ```bash
   grep "custom\|studio" frontend/src/pages/Dashboard/Settings.jsx | grep "gradient\|Gradient\|to-"
   ```
   Expected: both entries contain `to-violet/20`.
</verification>

<success_criteria>
- Full grep sweep of `frontend/src/pages/Dashboard/` (excluding Shells/) returns zero matches for `text-green-[45]`, `bg-green-[45]`, `text-emerald-[45]`, `bg-emerald-[45]`
- Settings.jsx TIER_GRADIENTS custom and studio entries contain `to-violet/20`
- PersonaEngine.jsx contains `bg-surface` and no `backgroundColor: "#0A0A0A"` inline style
</success_criteria>

<output>
After completion, create `.planning/phases/33-design-system-landing-page/33-02-SUMMARY.md`
</output>
