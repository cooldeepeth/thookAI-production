---
phase: 33-design-system-landing-page
plan: 04
type: execute
wave: 3
depends_on:
  - 33-03
files_modified:
  - frontend/src/pages/LandingPage.jsx
  - frontend/src/pages/Landing/Navbar.jsx
  - frontend/src/pages/Landing/Hero.jsx
  - frontend/src/pages/Landing/Features.jsx
  - frontend/src/pages/Landing/HowItWorks.jsx
  - frontend/src/pages/Landing/PricingSection.jsx
  - frontend/src/pages/Landing/SocialProof.jsx
  - frontend/src/pages/Landing/DiscoverBanner.jsx
  - frontend/src/pages/Landing/AgentCouncil.jsx
  - frontend/src/pages/Landing/Footer.jsx
autonomous: true
requirements:
  - DSGN-02
  - DSGN-03
  - DSGN-04

must_haves:
  truths:
    - "LandingPage.jsx is under 100 lines and is a pure composition of section components"
    - "A HowItWorks section with 3 numbered steps (Build Your Persona, Generate Content, Publish & Learn) is visible on the landing page"
    - "A SocialProof section with at least 4 metric cards (15+ AI Agents, 3 Platforms, 200 Free Credits, < 5 min setup) is visible on the landing page"
    - "The Pricing section uses the PlanBuilder component with mode='landing' (interactive sliders, not static tier cards)"
    - "At 375px width, the Navbar shows a hamburger icon (Menu) and no desktop links. Clicking it opens a shadcn Sheet drawer with the nav links"
    - "At 375px width, no section causes horizontal scroll (overflow-x hidden on root)"
    - "Footer shows dynamic year via new Date().getFullYear() and links to /privacy and /terms"
  artifacts:
    - path: "frontend/src/pages/LandingPage.jsx"
      provides: "Root landing page — imports and composes 9 section components"
      min_lines: 20
      contains: "HowItWorks"
    - path: "frontend/src/pages/Landing/Navbar.jsx"
      provides: "Responsive navbar with mobile Sheet drawer"
      contains: "Sheet"
    - path: "frontend/src/pages/Landing/HowItWorks.jsx"
      provides: "3-step how-it-works section"
      contains: "Build Your Persona"
    - path: "frontend/src/pages/Landing/SocialProof.jsx"
      provides: "Metrics row social proof section"
      contains: "AI Agents"
    - path: "frontend/src/pages/Landing/PricingSection.jsx"
      provides: "Pricing section using shared PlanBuilder component"
      contains: "PlanBuilder"
    - path: "frontend/src/pages/Landing/Footer.jsx"
      provides: "Footer with dynamic year and legal links"
      contains: "getFullYear"
  key_links:
    - from: "frontend/src/pages/LandingPage.jsx"
      to: "frontend/src/pages/Landing/"
      via: "import statements for all 9 section components"
      pattern: "import.*Landing/"
    - from: "frontend/src/pages/Landing/PricingSection.jsx"
      to: "frontend/src/components/PlanBuilder.jsx"
      via: "import { PlanBuilder } from '@/components/PlanBuilder'"
      pattern: "PlanBuilder"
    - from: "frontend/src/pages/Landing/Navbar.jsx"
      to: "frontend/src/components/ui/sheet"
      via: "Sheet, SheetContent, SheetTrigger imports"
      pattern: "Sheet"
---

<objective>
Split the monolithic LandingPage.jsx into section components under `pages/Landing/`,
add the two missing sections (HowItWorks and SocialProof), replace static pricing with
the interactive PlanBuilder, and upgrade the Navbar with a mobile Sheet drawer.

Purpose: The current 694-line LandingPage.jsx would grow to ~1,100 lines after all
additions — 38% over the 800-line CLAUDE.md limit. Splitting into section components
keeps each file focused, enables per-section testing, and satisfies DSGN-02/03/04.

Output: 10 files total — a slim LandingPage.jsx root plus 9 focused section components.
The landing page gains HowItWorks, SocialProof, interactive pricing, and a fully
functional mobile navigation drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/rules/design-system.md
@.planning/phases/33-design-system-landing-page/33-03-SUMMARY.md
@frontend/src/pages/LandingPage.jsx

<interfaces>
<!-- Section components already defined as inner functions in LandingPage.jsx.
     Extract them to files, do not rewrite them from scratch unless documented below. -->

EXISTING INNER FUNCTIONS to extract (move, not rewrite):
  Navbar()        → frontend/src/pages/Landing/Navbar.jsx    (UPGRADE: add mobile Sheet)
  Hero()          → frontend/src/pages/Landing/Hero.jsx      (preserve as-is)
  Features()      → frontend/src/pages/Landing/Features.jsx  (preserve as-is)
  DiscoverBanner  → frontend/src/pages/Landing/DiscoverBanner.jsx (preserve as-is)
  AgentCouncil()  → frontend/src/pages/Landing/AgentCouncil.jsx (update model names — see below)

NEW sections to create:
  HowItWorks      → frontend/src/pages/Landing/HowItWorks.jsx
  SocialProof     → frontend/src/pages/Landing/SocialProof.jsx
  PricingSection  → frontend/src/pages/Landing/PricingSection.jsx (replaces static pricing block)
  Footer          → frontend/src/pages/Landing/Footer.jsx (extract and update year)

KEY: Each section file exports a single named component. No default exports.
  e.g. export function HowItWorks() { ... }

PITFALL: useNavigate() requires Router context. Always wrap in MemoryRouter in tests.
  Sections that use useNavigate: Navbar, Hero, DiscoverBanner — these need Router in tests.

SHEET IMPORT for Navbar (already installed):
  import { Sheet, SheetContent, SheetTrigger } from "@/components/ui/sheet";
  import { Menu, X } from "lucide-react";  // X is close icon

PLAN BUILDER import for PricingSection:
  import { PlanBuilder } from "@/components/PlanBuilder";

ANIMATION PATTERN (use whileInView for all new sections):
  import { motion } from "framer-motion";
  <motion.div
    initial={{ opacity: 0, y: 20 }}
    whileInView={{ opacity: 1, y: 0 }}
    viewport={{ once: true }}
    transition={{ duration: 0.5, delay: i * 0.15 }}
  >

TOKEN RULES for all new JSX (no exceptions):
  Primary actions: btn-primary class
  Card surfaces: card-thook class
  Headings: font-display font-bold
  Numbers/stats: font-mono text-lime
  Body text: text-zinc-400
  NO raw hex values — use token classes only

AGENT COUNCIL model names correction (in AgentCouncil.jsx):
  Replace "GPT-4o" → "claude-sonnet-4-20250514"
  Replace "o1-mini" → "claude-haiku-4-5"
  Replace "Claude 3.5" → "Claude Sonnet 4" (or remove model name references if cleaner)
  The agents are ThookAI's own agents — they use Anthropic models internally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract existing sections + create Landing/ directory structure</name>
  <files>
    frontend/src/pages/Landing/Hero.jsx,
    frontend/src/pages/Landing/Features.jsx,
    frontend/src/pages/Landing/DiscoverBanner.jsx,
    frontend/src/pages/Landing/AgentCouncil.jsx,
    frontend/src/pages/Landing/Footer.jsx
  </files>
  <action>
Create the `frontend/src/pages/Landing/` directory and extract existing inner functions
from LandingPage.jsx into individual files. Do NOT modify LandingPage.jsx yet (that
happens in Task 3 after all components are ready).

For each component:
1. Copy the function body from LandingPage.jsx exactly
2. Add the required imports at the top of the new file
3. Change the function declaration to a named export:
   `export function Hero() { ... }` (not `export default`)
4. Do not modify the JSX/logic — this is extraction, not rewriting

HERO.JSX:
- Copy the `Hero()` inner function from LandingPage.jsx
- Needs imports: useState, useEffect, useNavigate (react-router-dom), motion (framer-motion),
  Zap, ArrowRight, ChevronRight, Sparkles (lucide-react)
- No changes to JSX — preserve exactly

FEATURES.JSX:
- Copy the `Features()` inner function (bento grid section)
- Needs imports: motion (framer-motion), plus any icons used
- Preserve exactly

DISCOVER BANNER.JSX:
- Copy the DiscoverBanner inner function
- Needs imports: useNavigate, motion, Sparkles, ArrowRight
- Preserve exactly

AGENT COUNCIL.JSX:
- Copy the AgentCouncil inner function
- Update model name strings only (per interfaces block):
  Find "GPT-4o" → replace with "Claude Sonnet 4"
  Find "o1-mini" → replace with "Claude Haiku"
  Find "Claude 3.5" → replace with "Claude Sonnet 4"
- This is a content accuracy fix, not a visual change

FOOTER.JSX:
- Copy the Footer inner function
- Find the static year string `© 2025` → replace with `© {new Date().getFullYear()}`
- Ensure links to /privacy and /terms are present (they should be in existing footer)
- Add `data-testid="landing-footer"` to the footer root element

Verify: All 5 files created with named exports.
```bash
grep "export function" \
  frontend/src/pages/Landing/Hero.jsx \
  frontend/src/pages/Landing/Features.jsx \
  frontend/src/pages/Landing/DiscoverBanner.jsx \
  frontend/src/pages/Landing/AgentCouncil.jsx \
  frontend/src/pages/Landing/Footer.jsx
```
Expected: 5 lines (one per file).
  </action>
  <verify>
    <automated>grep -l "export function" frontend/src/pages/Landing/Hero.jsx frontend/src/pages/Landing/Features.jsx frontend/src/pages/Landing/DiscoverBanner.jsx frontend/src/pages/Landing/AgentCouncil.jsx frontend/src/pages/Landing/Footer.jsx | wc -l; grep "getFullYear" frontend/src/pages/Landing/Footer.jsx</automated>
  </verify>
  <done>5 files exist in frontend/src/pages/Landing/, each with a named export function. Footer.jsx contains `getFullYear()`.</done>
</task>

<task type="auto">
  <name>Task 2: Create Navbar (with mobile Sheet), HowItWorks, SocialProof, PricingSection</name>
  <files>
    frontend/src/pages/Landing/Navbar.jsx,
    frontend/src/pages/Landing/HowItWorks.jsx,
    frontend/src/pages/Landing/SocialProof.jsx,
    frontend/src/pages/Landing/PricingSection.jsx
  </files>
  <action>
Create the 4 new/upgraded section components.

***
NAVBAR.JSX — Extract + upgrade with mobile Sheet drawer:

Copy the existing Navbar() function from LandingPage.jsx. Then add mobile menu:

```jsx
import { useState, useEffect } from "react";
import { useNavigate } from "react-router-dom";
import { Zap, Sparkles, Menu } from "lucide-react";
import { Sheet, SheetContent } from "@/components/ui/sheet";

export function Navbar() {
  const navigate = useNavigate();
  const [scrolled, setScrolled] = useState(false);
  const [mobileOpen, setMobileOpen] = useState(false);

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 20);
    window.addEventListener("scroll", onScroll);
    return () => window.removeEventListener("scroll", onScroll);
  }, []);

  const links = [
    { href: "#features", label: "Product" },
    { href: "#agents", label: "Agents" },
    { href: "#pricing", label: "Pricing" },
  ];

  return (
    <nav
      className={`fixed top-0 left-0 right-0 z-50 flex items-center justify-between px-6 md:px-12 h-16 transition-all ${
        scrolled ? "bg-[#050505]/90 backdrop-blur-md border-b border-white/5" : ""
      }`}
      data-testid="landing-navbar"
    >
      {/* Logo */}
      <div className="flex items-center gap-2">
        <div className="w-8 h-8 bg-lime rounded-lg flex items-center justify-center">
          <Zap size={16} className="text-black" fill="black" />
        </div>
        <span className="font-display font-bold text-lg text-white">Thook</span>
        <span className="text-[10px] font-mono text-lime bg-lime/10 px-1.5 py-0.5 rounded-md">AI</span>
      </div>

      {/* Desktop nav links — hidden on mobile */}
      <div className="hidden md:flex items-center gap-8 text-sm text-zinc-400">
        {links.map((l) => (
          <a key={l.href} href={l.href} className="hover:text-white transition-colors">{l.label}</a>
        ))}
        <button
          onClick={() => navigate("/discover")}
          className="text-lime hover:text-lime/80 transition-colors flex items-center gap-1.5 font-medium"
        >
          <Sparkles size={14} />
          Discover Your Voice
        </button>
      </div>

      {/* Right side: desktop sign in + CTA, mobile hamburger */}
      <div className="flex items-center gap-3">
        <button
          onClick={() => navigate("/auth")}
          data-testid="nav-signin-btn"
          className="text-sm text-zinc-400 hover:text-white transition-colors hidden md:block"
        >
          Sign in
        </button>
        <button
          onClick={() => navigate("/auth")}
          data-testid="nav-cta-btn"
          className="btn-primary text-sm py-2 px-5"
        >
          Get started
        </button>
        {/* Mobile hamburger — visible only on mobile */}
        <button
          className="md:hidden text-zinc-400 hover:text-white focus-ring rounded p-1"
          aria-label="Open navigation menu"
          data-testid="mobile-menu-btn"
          onClick={() => setMobileOpen(true)}
        >
          <Menu size={20} />
        </button>
      </div>

      {/* Mobile sheet drawer */}
      <Sheet open={mobileOpen} onOpenChange={setMobileOpen}>
        <SheetContent side="right" className="bg-surface w-[260px] border-l border-white/5">
          <div className="flex flex-col gap-6 pt-8">
            {links.map((l) => (
              <a
                key={l.href}
                href={l.href}
                className="text-zinc-400 hover:text-white transition-colors text-base"
                onClick={() => setMobileOpen(false)}
              >
                {l.label}
              </a>
            ))}
            <button
              onClick={() => { setMobileOpen(false); navigate("/discover"); }}
              className="text-lime text-left font-medium"
            >
              Discover Your Voice
            </button>
            <div className="border-t border-white/5 pt-4 flex flex-col gap-3">
              <button
                onClick={() => { setMobileOpen(false); navigate("/auth"); }}
                className="text-zinc-400 hover:text-white text-sm text-left"
              >
                Sign in
              </button>
              <button
                onClick={() => { setMobileOpen(false); navigate("/auth"); }}
                className="btn-primary text-sm py-2 px-5 w-full"
              >
                Get started
              </button>
            </div>
          </div>
        </SheetContent>
      </Sheet>
    </nav>
  );
}
```

***
HOW IT WORKS.JSX — new section, 3 numbered steps:

```jsx
import { motion } from "framer-motion";
import { Brain, Wand2, TrendingUp } from "lucide-react";

const STEPS = [
  {
    number: 1,
    icon: Brain,
    title: "Build Your Persona",
    description: "Answer 7 questions about your voice, goals, and audience. Our AI builds a deep persona fingerprint that captures exactly how you think and write.",
  },
  {
    number: 2,
    icon: Wand2,
    title: "Generate Content",
    description: "Describe what you want to say. 15 specialist AI agents craft platform-native posts for LinkedIn, X, and Instagram in your authentic voice.",
  },
  {
    number: 3,
    icon: TrendingUp,
    title: "Publish & Learn",
    description: "Schedule, publish, and watch ThookAI improve from your edits and real engagement data — getting sharper with every post.",
  },
];

export function HowItWorks() {
  return (
    <section id="how-it-works" className="py-24 px-6 max-w-7xl mx-auto" data-testid="how-it-works-section">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ duration: 0.5 }}
        className="text-center mb-16"
      >
        <h2 className="font-display font-bold text-4xl text-white mb-4">
          From zero to published in 5 minutes
        </h2>
        <p className="text-zinc-400 text-lg max-w-2xl mx-auto">
          Three simple steps to go from idea to published post — no content expertise required.
        </p>
      </motion.div>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
        {STEPS.map((step, i) => {
          const Icon = step.icon;
          return (
            <motion.div
              key={step.number}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: i * 0.15 }}
              className="card-thook p-6 text-center"
              data-testid={`how-it-works-step-${step.number}`}
            >
              <div className="w-10 h-10 rounded-full bg-lime text-black font-display font-bold text-lg flex items-center justify-center mx-auto mb-4">
                {step.number}
              </div>
              <Icon size={28} className="text-lime mx-auto mb-3" />
              <h3 className="font-display font-semibold text-white text-lg mb-2">{step.title}</h3>
              <p className="text-zinc-400 text-sm leading-relaxed">{step.description}</p>
            </motion.div>
          );
        })}
      </div>
    </section>
  );
}
```

***
SOCIAL PROOF.JSX — metrics row (factual, no fake testimonials):

```jsx
import { motion } from "framer-motion";

const METRICS = [
  { value: "15+", label: "Specialist AI Agents" },
  { value: "3", label: "Social Platforms" },
  { value: "200", label: "Free Credits to Start" },
  { value: "< 5 min", label: "To Your First Post" },
];

export function SocialProof() {
  return (
    <section className="py-16 px-6 max-w-7xl mx-auto" data-testid="social-proof-section">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ duration: 0.5 }}
        className="text-center mb-10"
      >
        <p className="text-zinc-500 text-sm uppercase tracking-widest font-mono">By the numbers</p>
      </motion.div>
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
        {METRICS.map((m, i) => (
          <motion.div
            key={m.label}
            initial={{ opacity: 0, y: 16 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.4, delay: i * 0.1 }}
            className="card-thook p-6 text-center"
          >
            <p className="font-mono text-4xl text-lime mb-2">{m.value}</p>
            <p className="text-sm text-zinc-400">{m.label}</p>
          </motion.div>
        ))}
      </div>
    </section>
  );
}
```

***
PRICING SECTION.JSX — uses shared PlanBuilder component:

```jsx
import { motion } from "framer-motion";
import { PlanBuilder } from "@/components/PlanBuilder";

export function PricingSection() {
  return (
    <section id="pricing" className="py-24 px-6 max-w-7xl mx-auto" data-testid="pricing-section">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ duration: 0.5 }}
        className="text-center mb-16"
      >
        <h2 className="font-display font-bold text-4xl text-white mb-4">
          Pay for what you actually use
        </h2>
        <p className="text-zinc-400 text-lg max-w-2xl mx-auto">
          No rigid tiers. Slide the controls to match your content needs — see the exact price before you commit.
        </p>
      </motion.div>
      <div className="max-w-2xl mx-auto">
        <PlanBuilder mode="landing" />
      </div>
    </section>
  );
}
```

After creating all 4 files, verify named exports exist:
```bash
grep "export function" \
  frontend/src/pages/Landing/Navbar.jsx \
  frontend/src/pages/Landing/HowItWorks.jsx \
  frontend/src/pages/Landing/SocialProof.jsx \
  frontend/src/pages/Landing/PricingSection.jsx
```
Expected: 4 lines.
  </action>
  <verify>
    <automated>grep -l "export function" frontend/src/pages/Landing/Navbar.jsx frontend/src/pages/Landing/HowItWorks.jsx frontend/src/pages/Landing/SocialProof.jsx frontend/src/pages/Landing/PricingSection.jsx | wc -l; grep "Sheet" frontend/src/pages/Landing/Navbar.jsx; grep "Build Your Persona" frontend/src/pages/Landing/HowItWorks.jsx; grep "PlanBuilder" frontend/src/pages/Landing/PricingSection.jsx</automated>
  </verify>
  <done>
    - All 4 files have named exports
    - Navbar.jsx imports and uses Sheet
    - HowItWorks.jsx contains "Build Your Persona" step text
    - PricingSection.jsx contains PlanBuilder component usage
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite LandingPage.jsx as thin composition root</name>
  <files>frontend/src/pages/LandingPage.jsx</files>
  <action>
Replace the current monolithic LandingPage.jsx with a slim composition root that imports
and renders all section components in order. The existing inner functions have all been
moved to Landing/ directory in Tasks 1 and 2.

New LandingPage.jsx should look like:

```jsx
import { Navbar } from "@/pages/Landing/Navbar";
import { Hero } from "@/pages/Landing/Hero";
import { Features } from "@/pages/Landing/Features";
import { HowItWorks } from "@/pages/Landing/HowItWorks";
import { DiscoverBanner } from "@/pages/Landing/DiscoverBanner";
import { SocialProof } from "@/pages/Landing/SocialProof";
import { AgentCouncil } from "@/pages/Landing/AgentCouncil";
import { PricingSection } from "@/pages/Landing/PricingSection";
import { Footer } from "@/pages/Landing/Footer";

export default function LandingPage() {
  return (
    <div className="bg-[#050505] min-h-screen overflow-x-hidden" data-testid="landing-page">
      <Navbar />
      <Hero />
      <Features />
      <HowItWorks />
      <DiscoverBanner />
      <SocialProof />
      <AgentCouncil />
      <PricingSection />
      <Footer />
    </div>
  );
}
```

CRITICAL: `overflow-x-hidden` on the root div prevents horizontal scroll at 375px.
This is required for DSGN-04.

Section order:
1. Navbar (fixed, always visible)
2. Hero (above fold)
3. Features (bento grid)
4. HowItWorks (3 steps) ← NEW
5. DiscoverBanner (CTA banner)
6. SocialProof (metrics) ← NEW
7. AgentCouncil (agent grid)
8. PricingSection (interactive plan builder) ← REPLACED
9. Footer

NOTE: The path alias `@/pages/` maps to `src/pages/` — use `@/pages/Landing/...` for
the section imports. This follows the project's CRACO webpack alias pattern.
If `@/pages/` is not in the alias config (the alias is `@/` → `src/`), use:
`import { Navbar } from "./Landing/Navbar"` (relative import from LandingPage.jsx).
Verify by checking frontend/craco.config.js or frontend/jsconfig.json for the alias config.
If only `@/` → `src/` is configured, use relative imports to keep it consistent.

After writing, verify the line count:
```bash
wc -l frontend/src/pages/LandingPage.jsx
```
Expected: fewer than 40 lines.

Also verify all 9 section component names appear in the file:
```bash
grep -c "import" frontend/src/pages/LandingPage.jsx
```
Expected: 9 (one import per section).
  </action>
  <verify>
    <automated>wc -l frontend/src/pages/LandingPage.jsx; grep "HowItWorks\|SocialProof\|PricingSection" frontend/src/pages/LandingPage.jsx; grep "overflow-x-hidden" frontend/src/pages/LandingPage.jsx</automated>
  </verify>
  <done>
    - LandingPage.jsx is under 40 lines
    - File contains HowItWorks, SocialProof, and PricingSection imports
    - Root div has `overflow-x-hidden` class
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile Sheet nav | Sheet focus-traps and locks scroll — ARIA managed by Radix primitives |
| PlanBuilder (landing mode) | Sliders produce bounded numeric values only; CTA navigates to /auth, makes no state changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Denial of Service | Framer Motion animations | accept | All sections use whileInView + viewport once:true — animations fire at most once per section; no infinite loops |
| T-33-04-02 | Tampering | useNavigate in extracted components | mitigate | Each component that uses useNavigate is rendered inside BrowserRouter (App.js wraps all routes); test wrapper uses MemoryRouter per Pitfall 6 in RESEARCH.md |
| T-33-04-03 | Elevation of Privilege | PricingSection calling checkout API | mitigate | PricingSection uses mode="landing" — PlanBuilder landing mode only calls navigate("/auth"); checkout endpoint not called |
</threat_model>

<verification>
1. HowItWorks section exists with 3 numbered steps:
   ```bash
   grep -c "data-testid=\"how-it-works-step-" frontend/src/pages/Landing/HowItWorks.jsx
   ```
   Expected: 3.

2. SocialProof section has metric cards:
   ```bash
   grep "AI Agents\|Free Credits\|Social Platforms" frontend/src/pages/Landing/SocialProof.jsx
   ```
   Expected: 3+ matches.

3. Navbar has mobile sheet:
   ```bash
   grep "Sheet\|mobile-menu-btn" frontend/src/pages/Landing/Navbar.jsx
   ```
   Expected: both present.

4. LandingPage.jsx line count:
   ```bash
   wc -l frontend/src/pages/LandingPage.jsx
   ```
   Expected: under 40 lines.

5. No hardcoded hex in new section files (excluding acceptable #050505 page bg):
   ```bash
   grep -rn "#[0-9A-Fa-f]\{6\}" frontend/src/pages/Landing/ | grep -v "#050505"
   ```
   Expected: zero output.

6. Footer has dynamic year:
   ```bash
   grep "getFullYear" frontend/src/pages/Landing/Footer.jsx
   ```

7. PricingSection uses PlanBuilder:
   ```bash
   grep "PlanBuilder" frontend/src/pages/Landing/PricingSection.jsx
   ```

8. Build check (no import errors):
   ```bash
   cd frontend && npm run build 2>&1 | grep -E "ERROR|error" | head -20
   ```
   Expected: no errors referencing Landing/ files.
</verification>

<success_criteria>
- LandingPage.jsx is under 40 lines and contains 9 component imports
- `frontend/src/pages/Landing/` directory contains 9 files, each with a named export
- `grep "data-testid=\"how-it-works-step-" frontend/src/pages/Landing/HowItWorks.jsx | wc -l` returns 3
- `grep "PlanBuilder" frontend/src/pages/Landing/PricingSection.jsx` returns 1 match
- `grep "Sheet" frontend/src/pages/Landing/Navbar.jsx` returns 1+ matches
- `grep "getFullYear" frontend/src/pages/Landing/Footer.jsx` returns 1 match
- `npm run build` completes without errors on the landing page files
</success_criteria>

<output>
After completion, create `.planning/phases/33-design-system-landing-page/33-04-SUMMARY.md`
</output>
