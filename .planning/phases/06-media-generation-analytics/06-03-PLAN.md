---
phase: 06-media-generation-analytics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_analytics_social.py
  - backend/services/social_analytics.py
  - backend/agents/analyst.py
  - backend/services/persona_refinement.py
autonomous: true
requirements:
  - ANAL-01
  - ANAL-02
  - ANAL-03
  - ANAL-04

must_haves:
  truths:
    - "Social analytics service fetches real metrics from LinkedIn/X/Instagram APIs when tokens are available"
    - "Performance intelligence aggregates real engagement data into persona_engines per platform"
    - "Optimal posting times are calculated from published post performance data grouped by day/hour"
    - "Analytics dashboard prefers real performance_data.latest over simulated metrics"
    - "Persona evolution timeline tracks changes over time"
    - "Celery tasks poll_post_metrics_24h and poll_post_metrics_7d call update_post_performance"
  artifacts:
    - path: "backend/tests/test_analytics_social.py"
      provides: "Tests for social analytics, performance intelligence, optimal times, persona evolution"
      min_lines: 200
    - path: "backend/services/social_analytics.py"
      provides: "Platform-specific metric fetchers and unified update function"
      contains: "update_post_performance"
    - path: "backend/agents/analyst.py"
      provides: "Analytics overview preferring real data over simulated"
      contains: "performance_data"
    - path: "backend/services/persona_refinement.py"
      provides: "Optimal posting times and performance intelligence calculation"
      contains: "calculate_optimal_posting_times"
  key_links:
    - from: "backend/tasks/content_tasks.py"
      to: "backend/services/social_analytics.py"
      via: "poll_post_metrics_24h/7d tasks"
      pattern: "update_post_performance"
    - from: "backend/services/social_analytics.py"
      to: "backend/services/persona_refinement.py"
      via: "_aggregate_performance_intelligence writes to persona_engines"
      pattern: "performance_intelligence"
    - from: "backend/agents/analyst.py"
      to: "database"
      via: "reads performance_data.latest from content_jobs"
      pattern: "performance_data.*latest"
    - from: "backend/services/persona_refinement.py"
      to: "database"
      via: "optimal_posting_times written to persona_engines"
      pattern: "optimal_posting_times"
---

<objective>
Verify social analytics polling, performance intelligence aggregation, optimal posting times calculation, and real-data analytics display all work correctly.

Purpose: Confirm ANAL-01 through ANAL-04 requirements. The social_analytics.py service, persona_refinement.py calculations, and analyst.py real-data preference all exist — we need tests proving the complete data flow works.
Output: Test file proving analytics and intelligence paths work end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/services/social_analytics.py
@backend/agents/analyst.py
@backend/services/persona_refinement.py
@backend/routes/analytics.py
@backend/tasks/content_tasks.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test social analytics fetchers and update_post_performance flow</name>
  <files>backend/tests/test_analytics_social.py, backend/services/social_analytics.py</files>
  <read_first>
    - backend/services/social_analytics.py (all 3 platform fetchers + update_post_performance + _aggregate_performance_intelligence)
    - backend/tasks/content_tasks.py lines 616-668 (poll_post_metrics_24h and poll_post_metrics_7d tasks)
    - backend/routes/platforms.py (get_platform_token function)
    - backend/tests/conftest.py (existing test patterns)
  </read_first>
  <behavior>
    - Test 1: fetch_linkedin_post_metrics with mocked 200 response returns likes, comments, shares, impressions, engagement_rate, platform="linkedin"
    - Test 2: fetch_linkedin_post_metrics with 401 response returns {"error": "token_expired"}
    - Test 3: fetch_linkedin_post_metrics with 429 response returns {"error": "rate_limited", "retry_after": int}
    - Test 4: fetch_x_post_metrics with mocked 200 response returns likes, retweets, replies, bookmarks, impressions, engagement_rate, platform="x"
    - Test 5: fetch_x_post_metrics with 401 returns {"error": "token_expired"}
    - Test 6: fetch_instagram_post_metrics with mocked 200 response returns likes, comments, shares, saved, impressions, engagement_rate, platform="instagram"
    - Test 7: fetch_instagram_post_metrics with OAuthException returns {"error": "token_expired"}
    - Test 8: update_post_performance with LinkedIn job persists metrics to content_jobs.performance_data.latest and performance_metrics (backward compat)
    - Test 9: update_post_performance calls _aggregate_performance_intelligence which updates persona_engines.performance_intelligence
    - Test 10: update_post_performance with fetch error stores last_error in performance_data and returns False
    - Test 11: update_post_performance with missing publish_results returns False
    - Test 12: _aggregate_performance_intelligence updates running averages (posts_tracked increments, avg_engagement_rate recalculated)
  </behavior>
  <action>
    Create backend/tests/test_analytics_social.py with tests for ANAL-01.

    Test setup for platform fetchers:
    - Use pytest-asyncio with @pytest.mark.asyncio
    - Mock httpx.AsyncClient using unittest.mock.patch and AsyncMock
    - For LinkedIn test: mock response to /v2/socialActions/{urn} returning JSON:
      {"likesSummary": {"totalLikes": 42}, "commentsSummary": {"totalFirstLevelComments": 8}, "sharesSummary": {"totalShares": 3}}
    - For X test: mock response to /2/tweets/{id} returning JSON:
      {"data": {"public_metrics": {"like_count": 100, "retweet_count": 25, "reply_count": 10, "bookmark_count": 5, "quote_count": 3}, "non_public_metrics": {"impression_count": 5000}}}
    - For Instagram test: mock response to /{media_id}/insights returning JSON:
      {"data": [{"name": "impressions", "values": [{"value": 3000}]}, {"name": "likes", "values": [{"value": 150}]}, {"name": "comments", "values": [{"value": 20}]}, {"name": "shares", "values": [{"value": 10}]}, {"name": "saved", "values": [{"value": 30}]}]}

    Test setup for update_post_performance (Tests 8-12):
    - Mock db.content_jobs.find_one to return a job doc with:
      {"job_id": "j1", "user_id": "u1", "platform": "linkedin", "publish_results": {"linkedin": {"post_id": "urn:li:share:123"}}}
    - Mock routes.platforms.get_platform_token to return "test_access_token"
    - Mock fetch_linkedin_post_metrics to return valid metrics dict
    - Mock db.content_jobs.update_one as AsyncMock
    - Mock db.persona_engines.find_one to return {"user_id": "u1", "performance_intelligence": {}}
    - Mock db.persona_engines.update_one as AsyncMock
    - Call update_post_performance("j1", "u1", "linkedin")
    - Verify db.content_jobs.update_one was called with $set containing performance_data.latest
    - Verify db.persona_engines.update_one was called with $set containing performance_intelligence.linkedin

    For _aggregate_performance_intelligence (Test 12):
    - Start with persona having performance_intelligence.linkedin.posts_tracked=5, avg_engagement_rate=0.04
    - Call with new metrics having engagement_rate=0.06
    - Verify posts_tracked becomes 6
    - Verify avg_engagement_rate is recalculated using running average formula: old_avg + (new_rate - old_avg) / new_count
    - Expected: 0.04 + (0.06 - 0.04) / 6 = 0.0433 (rounded to 4 decimals)
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_analytics_social.py -x -v -k "test_fetch or test_update or test_aggregate" 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_analytics_social.py exists
    - grep -c "async def test_" backend/tests/test_analytics_social.py returns >= 12
    - grep "update_post_performance" backend/services/social_analytics.py returns the function definition
    - grep "_aggregate_performance_intelligence" backend/services/social_analytics.py returns the function definition
    - grep "performance_data" backend/services/social_analytics.py returns matches showing it writes performance_data.latest
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>Social analytics polling verified: all 3 platform fetchers work, update_post_performance persists real data, aggregation updates persona intelligence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test optimal posting times, real-data analytics display, and persona evolution</name>
  <files>backend/tests/test_analytics_social.py, backend/services/persona_refinement.py, backend/agents/analyst.py</files>
  <read_first>
    - backend/services/persona_refinement.py (calculate_optimal_posting_times, calculate_performance_intelligence, get_persona_evolution_timeline functions)
    - backend/agents/analyst.py (get_analytics_overview, get_content_analytics — real data preference over simulated)
    - backend/routes/analytics.py (optimal-times endpoint, persona/evolution endpoint)
    - backend/tests/test_analytics_social.py (tests from Task 1)
  </read_first>
  <behavior>
    - Test 1: calculate_optimal_posting_times with 15+ published posts having real performance_data returns dict with platform keys, each containing day_of_week and hour recommendations
    - Test 2: calculate_optimal_posting_times with < 10 posts returns empty dict (threshold not met)
    - Test 3: calculate_optimal_posting_times stores result in persona_engines.optimal_posting_times and optimal_times_calculated_at
    - Test 4: get_content_analytics with job having performance_data.latest returns is_estimated=False, metrics from real data
    - Test 5: get_content_analytics with job having NO performance_data returns is_estimated=True, simulated metrics
    - Test 6: get_analytics_overview counts real_data_posts vs estimated_posts correctly
    - Test 7: get_analytics_overview with mix of real and simulated data still returns valid aggregation
    - Test 8: get_persona_evolution_timeline returns evolution history with timestamps and field changes
    - Test 9: apply_persona_refinements creates an evolution snapshot in persona history
    - Test 10: GET /api/analytics/optimal-times returns optimal_times dict or message when empty
  </behavior>
  <action>
    Add tests to backend/tests/test_analytics_social.py for ANAL-02, ANAL-03, ANAL-04.

    For calculate_optimal_posting_times (ANAL-02, Tests 1-3):
    - Mock db.content_jobs.find to return 15 published jobs with real performance_data:
      Each job has: platform="linkedin", published_at=datetime varying across days/hours, performance_data={"latest": {"engagement_rate": float, "impressions": int}}
    - Distribute jobs across different days and hours so the function can identify patterns
    - Call calculate_optimal_posting_times("u1")
    - Verify return dict has "linkedin" key with day and hour recommendations
    - Mock db.persona_engines.update_one and verify it's called with $set containing optimal_posting_times

    For real-data analytics (ANAL-03, Tests 4-7):
    - Test get_content_analytics:
      Mock db.content_jobs.find_one returning job with performance_data.latest = {"impressions": 5000, "likes": 200, "comments": 30, "engagement_rate": 0.046}
      Assert result has is_estimated=False, metrics matching the real data
    - Test get_content_analytics without performance_data:
      Mock job without performance_data field
      Assert result has is_estimated=True
    - Test get_analytics_overview:
      Mock cursor returning 5 jobs: 3 with real performance_data.latest, 2 without
      Assert summary.real_data_posts == 3 and summary.estimated_posts == 2
      Assert is_estimated == True (because some posts used simulated)

    For persona evolution (ANAL-04, Tests 8-9):
    - Mock db.persona_engines.find_one returning a persona with evolution_history array containing 3 snapshots with timestamps
    - Call get_persona_evolution_timeline("u1")
    - Assert result.success == True and result contains evolution data
    - Test apply_persona_refinements: mock update_one, verify it stores the refinement with timestamp

    For optimal-times endpoint (Test 10):
    - Use httpx.AsyncClient with FastAPI app
    - Mock get_current_user, mock db.persona_engines.find_one
    - GET /api/analytics/optimal-times
    - Assert 200 response with optimal_times key

    Key verification for ANAL-03 (analyst.py real-data preference):
    - grep "performance_data" backend/agents/analyst.py must show it reads performance_data.latest
    - grep "is_estimated" backend/agents/analyst.py must show it tracks real vs simulated
    - The analyst already has this code (lines 89-96 in get_content_analytics, lines 262-271 in get_analytics_overview)
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_analytics_social.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend/tests/test_analytics_social.py returns >= 20 (12 from Task 1 + 10 from Task 2, minus overlap)
    - grep "calculate_optimal_posting_times" backend/services/persona_refinement.py returns the function definition
    - grep "performance_data.*latest" backend/agents/analyst.py returns matches showing real-data preference
    - grep "is_estimated" backend/agents/analyst.py returns matches
    - grep "real_data_count" backend/agents/analyst.py returns a match
    - grep "evolution" backend/services/persona_refinement.py returns matches for evolution tracking
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>Analytics verified: optimal posting times calculated from real data, dashboard prefers real metrics over simulated, persona evolution tracked over time.</done>
</task>

</tasks>

<verification>
All analytics and social tests pass:
```bash
cd backend && python -m pytest tests/test_analytics_social.py -x -v
```
Full test suite still passes (no regressions):
```bash
cd backend && python -m pytest --timeout=120 -x
```
</verification>

<success_criteria>
- ANAL-01: Tests prove social analytics service polls all 3 platforms and persists data via update_post_performance
- ANAL-02: Tests prove calculate_optimal_posting_times produces real recommendations from engagement data
- ANAL-03: Tests prove get_content_analytics and get_analytics_overview prefer real performance_data.latest over simulated metrics
- ANAL-04: Tests prove persona evolution timeline is tracked and refinements are persisted
- All tests pass, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-media-generation-analytics/06-03-SUMMARY.md`
</output>
