---
phase: 28
slug: content-generation-multi-format
status: draft
shadcn_initialized: true
preset: new-york
created: 2026-04-12
---

# Phase 28 — UI Design Contract: Content Generation Multi-Format

> Visual and interaction contract for extending ContentStudio with 9 platform-specific formats,
> real-time pipeline progress, platform-native output shells, and edit/approve/schedule actions.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
> Source of truth for executor and auditor — do not deviate without updating this file.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | new-york (confirmed via `frontend/components.json`) |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide React 0.507.0 |
| Font — Display | Clash Display 600/700 (`font-display`) |
| Font — Body | Plus Jakarta Sans 400–700 (default body) |
| Font — Mono | JetBrains Mono 400/500 (`font-mono`) |
| Animation | Framer Motion 12.38.0 |
| JSX | Yes (no TypeScript in frontend) |

Source: `frontend/components.json`, `frontend/tailwind.config.js`, `.claude/rules/design-system.md`, Phase 27 UI-SPEC

---

## Spacing Scale

Identical to Phase 27 — inherited from project-wide spacing contract. No exceptions for this phase.

| Token | Value | Tailwind | Usage |
|-------|-------|----------|-------|
| xs | 4px | `p-1` / `gap-1` | Icon gaps, badge padding |
| sm | 8px | `p-2` / `gap-2` | Inline element spacing |
| md | 16px | `p-4` / `gap-4` | Card padding, default element spacing |
| lg | 24px | `p-6` / `gap-6` | Section padding |
| xl | 32px | `p-8` / `gap-8` | Page-level padding |
| 2xl | 48px | `py-12` | Major vertical breaks |
| 3xl | 64px | `py-16` | Reserved — not used in ContentStudio |

Exceptions:
- Format picker buttons: `py-2 px-3` (existing pattern from `InputPanel.jsx`) — no change.
- Platform shell max-width: LinkedIn `max-w-[555px]`, X `max-w-[598px]`, Instagram `max-w-[470px]` — native platform dimensions, not on scale. Do not alter these values.
- AgentPipeline agent cards: `p-4` (16px) — existing, unchanged.
- Story slide indicator badge: `px-2 py-1` (4px vertical) for slide-number label inside `InstagramShell` — uses `py-1` (4px) which is on the approved spacing scale.
- Hashtag chips inside `InstagramShell`: contract specifies `py-1` (4px). Any live code using `py-0.5` (2px) must be updated to `py-1` during Phase 28 execution.

Source: Phase 27 UI-SPEC, `InputPanel.jsx`, `LinkedInShell.jsx`, `XShell.jsx`, `InstagramShell.jsx`

---

## Typography

4 sizes, 2 weights. Identical to Phase 27 — inherited.

| Role | Size | Weight | Line Height | Class / Notes |
|------|------|--------|-------------|---------------|
| Heading | 28px (`text-3xl`) | 700 | 1.2 | `font-display font-bold` — ContentStudio panel headings only (not used in shells) |
| Body | 16px (`text-base`) | 400 | 1.5 | Platform shell content rendering (LinkedIn `text-sm text-gray-800`, X `text-[15px] text-white`) — shells use platform-native values, see Shell Typography below |
| Label | 14px (`text-sm`) | 400 | 1.4 | Button labels, format type names, pipeline stage names |
| Micro / mono | 12px (`text-xs`) | 400 | 1.3 | Character counters, agent model tags, section headers (`PLATFORM`, `FORMAT` labels), credit counts |

Weight declarations:
- 400 (`font-normal`) — body text, labels, micro/mono roles
- 700 (`font-bold`) — display headings and AgentPipeline agent name labels

Shell-internal typography (existing, do not change):
- `LinkedInShell` body text: `text-sm text-gray-800` (14px) — platform-native weight rendering
- `XShell` tweet text: `text-[15px] text-white` — platform-native
- `InstagramShell` caption text: `text-sm text-gray-900` (14px) — platform-native

Rules:
- AgentPipeline agent name: `text-sm font-bold` — override existing `font-semibold` (600) → `font-bold` (700). This is a deliberate visual update in Phase 28: the previous `font-semibold` is replaced to keep the type scale at exactly 2 weights (400 regular, 700 bold). Executors must update `AgentPipeline.jsx` accordingly.
- Format picker section label: `text-xs text-zinc-500 uppercase tracking-wider font-mono` — existing `InputPanel.jsx` pattern.
- Story slide label inside InstagramShell: `text-xs font-mono text-zinc-400` — micro/mono role at 12px.
- All `font-mono` usage: character counters, model tags, credit badge, section sub-labels.

Source: Phase 27 UI-SPEC, `InputPanel.jsx`, `AgentPipeline.jsx`, `LinkedInShell.jsx`, `XShell.jsx`, `InstagramShell.jsx`

---

## Color

| Role | Value | Tailwind / CSS |
|------|-------|----------------|
| Dominant (60%) | `#050505` | `bg-[#050505]` — page/studio background |
| Secondary (30%) | `#0F0F10` + `#18181B` | `card-thook` / `bg-surface-2` — all ThookAI-side cards, panels, input areas |
| Accent lime (10%) | `#D4FF00` | See reserved list below |
| Accent violet | `#7000FF` | Video toggle, video-related UI, Studio+ gate elements |
| Destructive | `#EF4444` | Error states, pipeline error, character-limit-exceeded |
| Border subtle | `#27272A` | `border-white/5` — card borders, panel dividers |
| Border active | `rgba(212,255,0,0.2)` | Active agent card border (`border-lime/20`) |

Accent lime (`#D4FF00`) reserved for:
1. Primary CTA button background (`btn-primary`) — "Generate with AI", "Approve Content", "Schedule"
2. AgentPipeline: running-agent indicator dot (`w-2 h-2 bg-lime rounded-full animate-pulse`)
3. AgentPipeline: running-agent card left-border glow (`border-lime/20 bg-lime/3`)
4. AgentPipeline: done-agent status icon background (`bg-lime/15`)
5. AgentPipeline: done-agent checkmark (`text-lime`) and model tag when running (`bg-lime/10 text-lime`)
6. Format picker: active platform and format button background/border (`border-white/20 bg-white/5`) — note: NOT lime on platform tabs; lime only used for status/CTA
7. QCBadge: good-score bar fill (`bg-lime`) and score text (`text-lime`)
8. Credit badge next to Generate button (`text-lime font-mono bg-lime/10`)
9. Scout Research "sources found" badge (`bg-lime/10 text-lime`)
10. `LinkedInShell` and `XShell` Post/Post Thread button: `bg-[#D4FF00]` — existing pattern, do not change to Tailwind class inside shells (shells use inline hex for platform-native aesthetic)

Accent violet (`#7000FF`) reserved for:
- Video toggle: active state (`bg-violet`)
- Video style selection: active button (`border-violet/40 bg-violet/10 text-violet`)
- Video generation "Studio+" tier gate badge and lock icon
- `VideoStatusBadge`: "generating" state text/bg (`text-violet bg-violet/10`)
- Video link in completed VideoStatusBadge (`text-violet hover:underline`)

Destructive reserved for:
- AgentPipeline error state container (`bg-red-500/10 border-red-500/20`) and error text (`text-red-400`)
- Character limit exceeded: LinkedIn `text-red-500` at 3000 chars, X `text-red-500` per tweet, Instagram `text-red-500` at 2200 chars
- InputPanel error paragraph (`text-red-400`)
- QCBadge needs-work fill (`bg-red-400`)

Platform-brand colors (shells only — do not use outside shell components):
- LinkedIn: `#0A66C2` — avatar gradient, header tint, hashtag/mention color
- X: `#1D9BF0` — avatar gradient, char-count arc stroke, verified badge
- Instagram: gradient `#FCAF45` → `#E1306C` → `#5B51D8` — avatar ring only

Source: `InputPanel.jsx`, `AgentPipeline.jsx`, `ContentOutput.jsx`, `LinkedInShell.jsx`, `XShell.jsx`, `InstagramShell.jsx`, `tailwind.config.js`, `.claude/rules/design-system.md`

---

## Format Picker: UI Contract

This is the primary new surface for this phase. The `PLATFORMS` constant in `InputPanel.jsx` expands from 6 format types to 9.

### Platform Tabs (unchanged visual contract)

3 tabs — LinkedIn, X, Instagram — rendered as `flex gap-2`. Each tab:
- Width: `flex-1`
- Layout: `flex flex-col items-center gap-1 py-2 rounded-xl border`
- Inactive: `border-white/5 hover:border-white/10` with platform icon at `text-zinc-600` (18px)
- Active: `border-white/20 bg-white/5` with platform icon in its brand color
- Label: `text-xs` — white when active, `text-zinc-600` when inactive

### Format Type Buttons (expanded to 3 for LinkedIn and Instagram)

LinkedIn now has 3 format buttons: Post, Article, Carousel.
Instagram now has 3 format buttons: Feed, Reel, Story.
X remains 2: Tweet, Thread.

Layout rule for 3-button rows:
- Container: `flex gap-2 flex-wrap` (add `flex-wrap` for narrow screens)
- Each button: `flex-1 py-1 rounded-lg text-xs transition-colors border` — reduce from `py-2 text-xs` to `py-1 text-xs` when 3 buttons are present to prevent overflow at 375px
- Inactive: `border-white/5 text-zinc-500 hover:text-zinc-300`
- Active: `border-white/20 bg-white/8 text-white`
- `data-testid`: `content-type-{id}` (existing pattern — extend for new types)

New format button `data-testid` values:
| Format | data-testid |
|--------|-------------|
| LinkedIn Article | `content-type-article` |
| Instagram Story | `content-type-story_sequence` |

Existing format buttons keep their current `data-testid` values unchanged.

### Composer Header (unchanged visual)

The composer header below the format picker shows: `{PlatformIcon} {Platform} {FormatLabel}`. The `FormatLabel` must display the human-readable label (Post, Article, Carousel, Tweet, Thread, Feed, Reel, Story) not the backend key.

Format label display map:
| Backend key | Display label |
|-------------|---------------|
| `post` | Post |
| `article` | Article |
| `carousel_caption` | Carousel |
| `tweet` | Tweet |
| `thread` | Thread |
| `feed_caption` | Feed |
| `reel_caption` | Reel |
| `story_sequence` | Story |

Source: `InputPanel.jsx` (existing pattern), `28-RESEARCH.md` (format matrix)

---

## AgentPipeline: Visual Contract (verified, no changes needed)

The `AgentPipeline.jsx` component implements all required progress states. Visual contract is locked — do not modify styles except for the agent name typography update noted in the Typography section.

### States per agent card

| State | Card class | Icon | Icon bg | Text color |
|-------|-----------|------|---------|-----------|
| `waiting` | `opacity-40` | `Clock size={14} text-zinc-600` | `bg-white/5` | `text-zinc-600` |
| `running` | `border-lime/20 bg-lime/3` | `Loader2 size={14} text-white animate-spin` | `bg-white/10` | `text-white` |
| `done` | `border-white/10` | `Check size={14} text-lime` | `bg-lime/15` | `text-white` |
| `error` | — | `! text-red-400 text-xs` | — | — |

### Agent label mapping

| Agent key | Display Name | Model badge |
|-----------|-------------|-------------|
| `commander` | Commander | GPT-4o |
| `scout` | Scout | Perplexity |
| `thinker` | Thinker | o4-mini |
| `writer` | Writer | Claude |
| `qc` | QC | GPT-4.1-mini |

Running description and done description are already correct in `AgentPipeline.jsx` — do not change.

### Header

- Running indicator: `w-2 h-2 bg-lime rounded-full animate-pulse` with `text-xs font-mono text-lime uppercase tracking-wider` — "Agents at work"
- Platform label: `font-display font-bold text-white text-lg` — "Creating your {platform} content"
- Input preview: `text-zinc-500 text-sm` truncated at 60 chars — existing, unchanged

### Error block

Container: `mt-4 p-3 bg-red-500/10 border border-red-500/20 rounded-xl`
Error text: `text-red-400 text-sm`
Fallback copy: "Something went wrong. Please try again."

Source: `AgentPipeline.jsx` (confirmed, fully implemented — no UI changes needed for CONT-12)

---

## Platform Shell: Visual Contract

### LinkedInShell (CONT-01, CONT-02, CONT-03)

- Shell used for: LinkedIn Post, LinkedIn Article, LinkedIn Carousel — all three share this shell (v3.0 scope)
- Container: `bg-white rounded-lg shadow-lg overflow-hidden max-w-[555px] mx-auto`
- Character limits: Post/Article 3,000 chars (hard), warning at 2,700. Carousel 1,500 chars — **no dedicated carousel shell for v3.0**: Writer FORMAT_RULES instruct the LLM to stay within 1,500 chars for carousel; the LinkedInShell enforces 3,000 (upper bound). This is acceptable.
- Counter colors: normal `text-gray-400`, warning `text-yellow-600`, limit `text-red-500 font-bold`
- Hashtag/mention color: `text-[#0A66C2]`
- Edit mode: `<textarea>` inside the content area with `bg-transparent outline-none resize-none`
- Read-only mode: `whitespace-pre-wrap` div rendering
- No visual distinction between article and post shells for v3.0. Article content will have a bold first line (as produced by the Writer) rendering naturally in the existing shell.

### XShell (CONT-04, CONT-05)

- Shell used for: X Tweet (1 card), X Thread (multiple cards)
- Container per tweet: `bg-black border border-[#2F3336] rounded-2xl overflow-hidden max-w-[598px] mx-auto space-y-3`
- Tweet body: `text-[15px] text-white leading-relaxed whitespace-pre-wrap`
- Char counter: SVG circle arc — blue (`#1D9BF0`) normal, yellow (`#FFD400`) at >80%, red (`#F4212E`) at limit. Remaining count shown inside circle when over.
- Thread numbering: `{n}/{total}` in `text-[#1D9BF0] text-xs` — top-right of header row
- "Add another tweet" button: `border-dashed border-[#2F3336] text-[#1D9BF0]` — only visible in edit mode
- Thread parse pattern: `1/ ... 2/ ...` — already implemented in `XShell.jsx`

### InstagramShell (CONT-06, CONT-07, CONT-08)

- Shell used for: Instagram Feed, Reel, Story — all three share this shell (v3.0 scope)
- Container: `bg-white rounded-lg shadow-lg overflow-hidden max-w-[470px] mx-auto`
- Max chars: 2,200 — hard limit enforced in `handleChange`
- Hashtag count: 10–15 recommended range. Counter: green when in range, yellow when below minimum, no color when over maximum.
- Hashtag chip: `text-xs bg-gray-100 text-[#00376B] px-2 py-1 rounded-full` — use `py-1` (4px, on scale). Update any live code using `py-0.5` to `py-1`.
- Hashtag suggestion buttons: `text-xs bg-[#E1306C]/10 text-[#E1306C] px-2 py-1 rounded-full`

#### Instagram Story Sequence: Slide Display Contract

When `content_type === "story_sequence"`, `InstagramShell` must detect story format and render slides individually. This is the primary new rendering behavior for InstagramShell.

Detection: `content.includes("Slide 1:")` — if true, content is a story sequence.

Parsing:
```javascript
const storySlides = content.split(/Slide \d+:\s*/i).filter(Boolean);
```

Story slide display (within existing shell, below the image placeholder):
- Each slide rendered as a separate block: `rounded-xl border border-white/10 bg-gray-50 p-3 mb-2`
- Slide number badge: `px-2 py-1 text-xs font-mono text-zinc-400 mb-1` — "Slide {n} of {total}". Uses `py-1` (4px) — on the approved spacing scale.
- Slide content: `text-sm text-gray-900 leading-relaxed whitespace-pre-wrap`
- Edit mode for stories: single `<textarea>` over the full content (do not split into per-slide inputs — v3.0 simplification)
- If NOT story format: render existing caption single-block layout unchanged

The image placeholder retains its existing styling — square aspect ratio, gray gradient, placeholder icon. For story, a note "Vertical story format (9:16)" in `text-xs text-gray-400` replaces the subtitle text "Your visual content here".

Source: `InstagramShell.jsx`, `28-RESEARCH.md` Pitfall 3 and Story Display pattern

---

## Edit / Approve / Schedule: Interaction Contract

These actions are implemented in `ContentOutput.jsx`. Visual contract is locked — verify behavior only, no new UI needed.

### Edit

- Trigger: Edit button (`Edit2` icon, `text-zinc-400 hover:text-white`) in ContentOutput action bar — `aria-label="Edit content"` (icon-only button, required for accessibility)
- State change: `isEditing` flips to `true` — shell switches textarea view
- Save: clicking shell's "Post"/"Post Thread"/"Share" button or a separate "Save edits" ghost button calls `onContentChange`
- Cancel: `X` icon button — reverts `text` state to original `content` prop — `aria-label="Cancel edit"` (icon-only button, required for accessibility)
- Visual: No new elements. Existing action bar pattern unchanged.

### Approve

- Trigger: "Approve Content" button — `btn-primary` — in the action bar row below the platform shell
- Copy: "Approve Content" (verb + noun — explicit action target)
- Loading state: spinner replaces Zap icon, text becomes "Approving..."
- Success: job `status` updates to `"approved"`. Button transitions to disabled state showing `Check size={14} text-lime` with "Approved" label in `text-lime font-mono text-xs`
- API call: `PATCH /api/content/job/{job_id}/status` with `{status: "approved", edited_content: ...}`
- Error: `text-red-400 text-xs` below the button row — "Approval failed. Try again."

### Schedule

- Trigger: "Schedule Post" button — `btn-ghost` — in the action bar. Only available after `status === "approved"`.
- Disabled state (before approve): `opacity-40 cursor-not-allowed` with tooltip on hover — "Approve first to enable scheduling"
- Date/time picker: existing `PublishPanel` component with native `<input type="datetime-local">` — no new picker needed
- Copy on submit: "Schedule Post"
- Success toast: `toast({ title: "Scheduled", description: "Your post is queued for {datetime}" })`
- Error: inline below PublishPanel — "Scheduling failed. Please try again."
- API: `POST /api/content/schedule` (or `/api/dashboard/schedule/create` — verify in plan phase; minimal endpoint saves `scheduled_at` and sets `status: "scheduled"`)

Source: `ContentOutput.jsx` (existing approve/edit flow), `28-RESEARCH.md` CONT-11

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Generate CTA | "Generate with AI" |
| Generate loading | "Agents at work..." |
| Generate hint | "~45–60 seconds · 5 agents · your voice" |
| Credit badge (no video) | "10 credits" |
| Credit badge (with video) | "60 credits" |
| Approve CTA | "Approve Content" |
| Approve loading | "Approving..." |
| Approve success | "Approved" |
| Schedule CTA | "Schedule Post" |
| Schedule disabled tooltip | "Approve first to enable scheduling" |
| Schedule success toast title | "Scheduled" |
| Schedule success toast body | "Your post is queued for {formatted datetime}" |
| Pipeline header | "Agents at work" |
| Pipeline subheading | "Creating your {platform} content" |
| Pipeline waiting | "Waiting..." |
| Pipeline error fallback | "Something went wrong. Please try again." |
| Story slide badge | "Slide {n} of {total}" |
| Story image note | "Vertical story format (9:16)" |
| Instagram story display note (if no slides detected) | No special copy — renders as normal caption |
| InputPanel error | "{API error message}" (passed through from API response) |
| Empty output state | "Generate your first piece of content by filling in the topic above." |
| Output heading | "Your {Format} is ready" (e.g. "Your Tweet is ready", "Your Article is ready") — shown in ContentOutput header after pipeline completes |
| Approve error | "Approval failed. Try again." |
| Schedule error | "Scheduling failed. Please try again." |
| Video upgrade tooltip | "Upgrade to Studio or Agency to generate videos with your content" |

Destructive actions: None in this phase. Content generation, approval, and scheduling are all reversible (job status can be updated). No confirmation modal required.

Source: `InputPanel.jsx`, `AgentPipeline.jsx`, `ContentOutput.jsx`, REQUIREMENTS.md CONT-01–12

---

## Empty State Contract

ContentStudio renders two panels side by side: InputPanel (left) and ContentOutput or AgentPipeline (right).

**Right panel empty state** (no job active, no generation running):

The right panel shows a centered empty state:
- Icon: `Sparkles size={32} className="text-zinc-700"` centered
- Heading: `text-zinc-600 text-sm mt-3` — "Your content will appear here"
- Body: `text-zinc-700 text-xs mt-1` — "Generate your first piece of content by filling in the topic above."

This state already exists in `ContentOutput.jsx` — no new UI needed, confirm copy matches above.

**Output ready state** (after pipeline completes, job reaches `reviewing`):

Header row above the platform shell:
- `font-display font-bold text-white` — "Your {Format} is ready" — format label from display map above
- Format badge: `text-xs font-mono bg-white/5 text-zinc-400 rounded-full px-2 py-1` — e.g. "LinkedIn · Post" or "X · Thread". Uses `py-1` (4px) — on the approved spacing scale.

Source: `ContentOutput.jsx` (existing empty state pattern — verify copy matches contract)

---

## Animation Contract

All existing animation patterns from Phase 27 apply unchanged:

| Context | Motion |
|---------|--------|
| Platform shell entry | `initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }}` — existing in all three shells |
| Agent card entry | `delay: i * 0.08` stagger on `AgentPipeline` cards — existing |
| Story slide entry | `initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: slideIndex * 0.05 }}` — new, follows established stagger pattern |
| Format tab switch | CSS `transition-all` only (no Framer Motion) — existing `InputPanel.jsx` |
| Approve success | CSS `transition-colors` on button — no Framer Motion needed |
| QCBadge progress bar | `motion.div` width animation `duration: 0.8 ease: easeOut` — existing |

Source: `AgentPipeline.jsx`, `LinkedInShell.jsx`, Phase 27 UI-SPEC animation contract

---

## Responsive Contract

| Breakpoint | ContentStudio Layout |
|------------|---------------------|
| Mobile (<768px) | Single column: InputPanel full width, ContentOutput below. Format buttons: `flex-wrap` with `py-1 text-xs` for 3-button rows. Platform shells: full width, `max-w-full` override. |
| Tablet (768px+) | Two-column split: InputPanel left, ContentOutput right. Shell max-widths restored. |
| Desktop (1024px+) | Same as tablet. `max-w-[555px]` etc. honored for shells. |

Format picker 3-button rule at mobile (375px):
- LinkedIn formats: 3 buttons with `text-xs py-1 flex-1` — 3 equal-width buttons fit without overflow.
- Instagram formats: same rule.
- X formats: `text-xs py-2` (2 buttons — no change needed).

Source: `InputPanel.jsx` (existing flex layout), `28-RESEARCH.md` Pitfall 4

---

## Accessibility Contract

- All new format buttons follow existing `button` element pattern — keyboard focusable by default.
- Format buttons: visible text labels are sufficient accessible names. No `aria-label` needed unless icon-only.
- Story slide blocks: `role="list"` on slide container, `role="listitem"` on each slide div.
- Approve button: `aria-disabled="true"` when `isRunning` — existing disabled pattern.
- Schedule button: `aria-disabled="true"` when not yet approved; `title="Approve first to enable scheduling"` for tooltip text.
- ContentOutput action bar — icon-only buttons require explicit `aria-label`:
  - Edit2 button: `aria-label="Edit content"` — required because no visible label text accompanies the icon.
  - X (cancel edit) button: `aria-label="Cancel edit"` — required because no visible label text accompanies the icon.
- AgentPipeline: existing `data-testid` attributes satisfy test-accessibility needs. No new `aria-live` regions needed — pipeline polling is not announced to screen readers (improvement deferred).
- Color contrast: all new elements use lime on dark backgrounds (contrast ~14:1) or zinc-400 on `#050505` (contrast ~6:1, exceeds WCAG AA).

---

## data-testid Contract

Every new interactive element must include a `data-testid` attribute.

| Element | data-testid |
|---------|-------------|
| LinkedIn Article format button | `content-type-article` |
| Instagram Story format button | `content-type-story_sequence` |
| Story slide container | `story-slides-container` |
| Each story slide block | `story-slide-{n}` (e.g. `story-slide-0`) |
| Approve button | `approve-content-btn` |
| Schedule button | `schedule-content-btn` |
| Schedule date input | `schedule-datetime-input` |
| Schedule submit button | `schedule-submit-btn` |
| Output ready header | `content-output-header` |
| Output format badge | `content-format-badge` |
| Pipeline error block | `pipeline-error` (already `data-testid` on outer div — verify) |

Existing `data-testid` values to preserve:
- `input-panel`, `platform-tab-{id}`, `content-type-{id}`, `content-input-textarea`
- `generate-content-btn`, `video-toggle`, `video-style-{id}`, `studio-error`
- `agent-pipeline`, `agent-{key}` (e.g. `agent-commander`)
- `linkedin-shell`, `x-shell`, `instagram-shell`

---

## Character Limit Contract

| Format | Shell | Hard Limit | Warning Threshold | Counter Style |
|--------|-------|-----------|------------------|---------------|
| LinkedIn Post | LinkedInShell | 3,000 | 2,700 | `text-xs font-mono` — gray → yellow → red |
| LinkedIn Article | LinkedInShell | 3,000 | — | Same as post shell (adequate for v3.0) |
| LinkedIn Carousel | LinkedInShell | 3,000 (shell) / 1,500 (Writer) | — | Shell enforces 3,000; Writer FORMAT_RULES instruct 1,500 |
| X Tweet | XShell | 280/tweet | 224 (80%) | SVG arc — blue → yellow → red |
| X Thread | XShell | 280/tweet | 224 (80%) | Same per-tweet arc |
| Instagram Feed | InstagramShell | 2,200 | — | `text-xs` — gray → red |
| Instagram Reel | InstagramShell | 2,200 | — | Same as feed |
| Instagram Story | InstagramShell | 2,200 (full content) | — | Same counter; slide splits are display-only, not independently counted |

Source: `28-RESEARCH.md` Character Limit Enforcement table, shell code review

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Card (already installed) | Not required |
| No third-party registries | — | Not applicable |

No new shadcn components are added in this phase. The story slide display uses only existing Tailwind classes and Framer Motion — no new library or component registry block required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
