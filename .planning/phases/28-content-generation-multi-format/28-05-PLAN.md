---
phase: 28-content-generation-multi-format
plan: 05
type: execute
wave: 3
depends_on:
  - 28-02
  - 28-03
  - 28-04
files_modified:
  - backend/tests/test_content_phase28.py
  - .planning/phases/28-content-generation-multi-format/28-VALIDATION.md
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08
  - CONT-09
  - CONT-10
  - CONT-11
  - CONT-12

must_haves:
  truths:
    - "Full backend test suite passes (pytest) including existing + phase 28 tests"
    - "Full frontend test suite passes (npm test) including existing + phase 28 tests"
    - "No regressions introduced in test_content_sprint3.py"
    - "VALIDATION.md updated with final per-task status (all green)"
    - "All 12 CONT requirement IDs have at least one PASS in the test suites"
  artifacts:
    - path: "backend/tests/test_content_phase28.py"
      provides: "Final passing test file — all tests green"
      min_lines: 120
    - path: ".planning/phases/28-content-generation-multi-format/28-VALIDATION.md"
      provides: "Updated validation status — nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "backend/tests/test_content_phase28.py"
      to: "all CONT requirements"
      via: "pytest PASS"
      pattern: "PASSED"
---

<objective>
Integration verification: run the full backend and frontend test suites after all implementation plans (02, 03, 04) complete, fix any remaining failures, and update VALIDATION.md to reflect the final green state. This is the phase gate.

Purpose: Every implementation plan in this phase could have independently introduced regressions. This plan runs all suites together and fixes any failures before the phase is signed off.

Output: All suites green, VALIDATION.md updated with `nyquist_compliant: true` and all task rows marked green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-content-generation-multi-format/28-VALIDATION.md
@.planning/phases/28-content-generation-multi-format/28-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend suite and fix any remaining failures</name>
  <files>backend/tests/test_content_phase28.py</files>
  <read_first>
    - .planning/phases/28-content-generation-multi-format/28-RESEARCH.md (Validation Architecture section — quick run commands)
    - backend/tests/test_content_phase28.py (current state — read before running)
  </read_first>
  <action>
    Run the phase-specific backend tests first to get a clear picture:
    ```
    cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_content_phase28.py -v 2>&1
    ```

    If any tests FAIL:
    1. Read the failure message carefully.
    2. Determine if the fix is in the test (wrong assertion) or in the implementation (wrong code).
    3. Per TDD discipline: fix implementation, not tests — UNLESS the test assertion is factually wrong (e.g., wrong expected key name based on the actual code produced).
    4. Apply the fix to the appropriate file (writer.py, commander.py, content.py, or ContentOutput.jsx).
    5. Re-run until green.

    Then run the full backend suite to detect regressions:
    ```
    cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest -v --tb=short 2>&1 | tail -60
    ```

    Common regression points to watch for:
    - `test_content_sprint3.py`: May fail if PLATFORM_CONTENT_TYPES changed incorrectly (story_sequence added but existing types removed by mistake).
    - Any test that imports from `agents.writer` and expects `PLATFORM_RULES` — should now expect `FORMAT_RULES`.
    - Commander tests if any exist — WORD_COUNT_DEFAULTS addition should not break existing behavior.

    Fix regressions before proceeding to frontend.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_content_phase28.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `pytest tests/test_content_phase28.py -v` shows all tests PASSED (no FAILED, no ERROR).
    - `pytest -v --tb=short 2>&1 | grep -E "FAILED|ERROR"` returns nothing.
    - At minimum: `pytest tests/test_content_sprint3.py tests/test_content_phase28.py -v` — both suites all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full frontend suite and fix any remaining failures</name>
  <files>frontend/src/__tests__/pages/ContentStudio.test.jsx</files>
  <read_first>
    - frontend/src/__tests__/pages/ContentStudio.test.jsx (current state)
    - .planning/phases/28-content-generation-multi-format/28-VALIDATION.md (CONT-10 and CONT-12 frontend test rows)
  </read_first>
  <action>
    Run the ContentStudio-specific frontend tests first:
    ```
    cd /Users/kuldeepsinhparmar/thookAI-production/frontend && npx react-scripts test --watchAll=false --testPathPattern=ContentStudio 2>&1
    ```

    If any test FAILS:
    1. Read the failure output.
    2. If a data-testid is missing from a component, add it.
    3. If a component change broke an existing test assertion (e.g., format button count changed), update the test to match the new correct count — document the change.
    4. Re-run until green.

    Then run the full frontend suite:
    ```
    cd /Users/kuldeepsinhparmar/thookAI-production/frontend && npx react-scripts test --watchAll=false 2>&1 | tail -50
    ```

    Fix any failures in tests unrelated to Phase 28 that may have been introduced by the InputPanel or InstagramShell changes.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/frontend && npx react-scripts test --watchAll=false --testPathPattern=ContentStudio 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ContentStudio test suite: all PASS.
    - Full frontend suite: all PASS (no new failures).
    - New tests (nine_format_types_total_present, instagram_story_format_button_present, linkedin_article_format_button_present) all PASS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end UI verification of all 9 formats</name>
  <files>frontend/src/pages/Dashboard/ContentStudio/index.jsx</files>
  <action>
    No automated action. Human verifies the complete implementation end-to-end in the browser.
    All automated tasks (1 and 2) must be green before this checkpoint runs.
  </action>
  <what-built>
    All 9 content format changes are implemented:
    - backend/agents/writer.py: FORMAT_RULES dict with 8 content_type keys
    - backend/agents/commander.py: WORD_COUNT_DEFAULTS with per-format floors
    - backend/routes/content.py: story_sequence added to instagram allowlist
    - frontend InputPanel.jsx: 9 format buttons (LinkedIn+3, X+2, Instagram+3)
    - frontend InstagramShell.jsx: story slide detection and render
    - frontend ContentOutput.jsx: schedule API fixed to JSON body, data-testids added
    - All backend and frontend tests passing
  </what-built>
  <how-to-verify>
    1. Start the backend: `cd backend && uvicorn server:app --reload`
    2. Start the frontend: `cd frontend && npm start`
    3. Go to http://localhost:3000/dashboard/studio
    4. Verify LinkedIn platform shows 3 format buttons: Post, Article, Carousel
    5. Click "Article" and verify it shows in the composer header as "LinkedIn Article"
    6. Click Instagram platform and verify 3 buttons: Feed, Reel, Story
    7. Click "Story" and verify it shows as "Instagram Story" in composer header
    8. Type a topic (e.g., "My journey from engineer to founder") and click "Generate with AI"
    9. Watch the AgentPipeline and verify all 5 agents progress through running to done states
    10. Once complete, verify content appears in the platform shell
    11. If testing Instagram Story: verify the output renders as individual slide blocks if Writer produced "Slide 1:" format
    12. Click "Approve Content" and verify button shows "Approving..." then "Approved" with lime text
    13. Verify "Schedule Post" button becomes active after approval
    14. Click "Schedule Post", pick a date/time, click Schedule, verify success toast "Scheduled"
  </how-to-verify>
  <verify>
    <automated>MISSING -- human checkpoint, no automated command</automated>
  </verify>
  <done>Human confirms all 14 verification steps pass with no errors reported.</done>
  <resume-signal>Type "verified" if all steps pass, or describe any issue found (e.g., "story slides not splitting", "schedule 500 error")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update VALIDATION.md to final green state</name>
  <files>.planning/phases/28-content-generation-multi-format/28-VALIDATION.md</files>
  <read_first>
    - .planning/phases/28-content-generation-multi-format/28-VALIDATION.md (full file — read current state)
  </read_first>
  <action>
    Update VALIDATION.md:

    1. Set `nyquist_compliant: true` in frontmatter.
    2. Set `wave_0_complete: true` in frontmatter.
    3. Update the Per-Task Verification Map table — fill in Plan, Wave, and Status columns for all 12 rows:
       | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
       Each CONT-01 to CONT-09 → Plan 02, Wave 1, green
       CONT-10 → Plan 03, Wave 2, green
       CONT-11 → Plan 04, Wave 2, green
       CONT-12 → Plan 04, Wave 2, green

    4. Update Wave 0 Requirements checklist — all 4 items checked.
    5. Add a "Phase Completion" section at the bottom:
       ```
       ## Phase Completion

       **Status:** Complete
       **Date:** 2026-04-12
       **Backend tests:** pytest tests/test_content_phase28.py — all PASS
       **Frontend tests:** npx react-scripts test ContentStudio — all PASS
       **Human verification:** Approved (checkpoint in Plan 05)
       ```

    6. Update the Validation Sign-Off checklist — all 6 items checked.
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" /Users/kuldeepsinhparmar/thookAI-production/.planning/phases/28-content-generation-multi-format/28-VALIDATION.md</automated>
  </verify>
  <done>
    - VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`.
    - All 12 CONT requirement rows have status green.
    - All 6 Sign-Off checkboxes are checked.
    - Phase Completion section exists with current date.
  </done>
</task>

</tasks>

<verification>
After all tasks complete (including human checkpoint):
- `cd backend && pytest tests/test_content_phase28.py -v` — all PASS
- `cd backend && pytest -q 2>&1 | tail -5` — shows no failures
- `cd frontend && npx react-scripts test --watchAll=false 2>&1 | tail -10` — all PASS
- `grep "nyquist_compliant: true" .planning/phases/28-content-generation-multi-format/28-VALIDATION.md` — found
</verification>

<success_criteria>
Full backend and frontend test suites pass with zero failures. Human has verified the UI flow end-to-end. VALIDATION.md is updated to nyquist_compliant: true. All 12 CONT requirements are covered by passing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/28-content-generation-multi-format/28-05-SUMMARY.md`
</output>
