---
phase: 28-content-generation-multi-format
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_content_phase28.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08
  - CONT-09
  - CONT-11
  - CONT-12

must_haves:
  truths:
    - "All 12 CONT requirements have at least one failing test before implementation begins (RED state)"
    - "Test file runs with pytest without import errors"
    - "FORMAT_RULES dict structure is asserted as having exactly 9 keys"
    - "Each format test specifies the verifiable output characteristic (char limit, hashtag count, slide markers)"
  artifacts:
    - path: "backend/tests/test_content_phase28.py"
      provides: "Phase 28 backend test contract (12 tests, all RED initially)"
      min_lines: 120
  key_links:
    - from: "backend/tests/test_content_phase28.py"
      to: "backend/agents/writer.py"
      via: "import writer; assert FORMAT_RULES dict structure"
      pattern: "from agents.writer import FORMAT_RULES"
    - from: "backend/tests/test_content_phase28.py"
      to: "backend/routes/content.py"
      via: "PLATFORM_CONTENT_TYPES includes story_sequence"
      pattern: "from routes.content import PLATFORM_CONTENT_TYPES"
---

<objective>
Create the test scaffold for Phase 28 — all backend tests are written first in RED state before any implementation. This is the Nyquist-compliant Wave 0 that gives implementation plans (02, 04) a concrete, checkable contract.

Purpose: Every implementation task in this phase has a failing test it must turn green. No implementation is accepted without a passing automated test.

Output: `backend/tests/test_content_phase28.py` with 12+ tests covering CONT-01 through CONT-12. All tests fail on the unmodified codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-content-generation-multi-format/28-RESEARCH.md
@.planning/phases/28-content-generation-multi-format/28-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write all Phase 28 backend tests (RED state)</name>
  <files>backend/tests/test_content_phase28.py</files>
  <read_first>
    - backend/agents/writer.py (lines 1-15 — PLATFORM_RULES dict to know what you're replacing)
    - backend/routes/content.py (lines 26-30 — PLATFORM_CONTENT_TYPES to know what's missing)
    - backend/agents/commander.py (lines 50-58 — COMMANDER_PROMPT to see estimated_word_count field)
    - backend/agents/pipeline.py (grep for "current_agent" update calls)
    - backend/tests/test_content_sprint3.py (check test patterns and fixtures used — mirror style)
    - .planning/phases/28-content-generation-multi-format/28-RESEARCH.md (full file — test map section)
    - .planning/phases/28-content-generation-multi-format/28-VALIDATION.md (full file — per-task map)
  </read_first>
  <behavior>
    The following tests MUST exist and MUST fail on the unmodified codebase:

    test_format_rules_dispatch:
      - `from agents.writer import FORMAT_RULES` succeeds (after Plan 02 adds it) — but on unmodified code, `FORMAT_RULES` does not exist, so this import fails. Write with try/except to produce a clear failure message, or use `pytest.importorskip` pattern.
      - Assert `FORMAT_RULES` is a dict with exactly 9 keys: "post", "article", "carousel_caption", "tweet", "thread", "feed_caption", "reel_caption", "story_sequence", and one more that covers all 9.
      - Wait — the 9 keys ARE exactly those 8 listed above. Count: post, article, carousel_caption, tweet, thread, feed_caption, reel_caption, story_sequence = 8 keys. Assert len(FORMAT_RULES) == 8.
      - Assert each key maps to a non-empty string.

    test_format_rules_content_type_lookup:
      - Import `FORMAT_RULES` and verify `FORMAT_RULES["tweet"]` contains "280" (char limit is in the string).
      - Verify `FORMAT_RULES["article"]` contains "600" or "words" (min word count reference).
      - Verify `FORMAT_RULES["story_sequence"]` contains "Slide" (slide format marker).
      - Verify `FORMAT_RULES["thread"]` contains "1/" (numbering format).

    test_word_count_defaults_exist:
      - `from agents.commander import WORD_COUNT_DEFAULTS` — dict with at least 8 content_type keys.
      - Assert WORD_COUNT_DEFAULTS["article"] >= 600 (articles need more words than posts).
      - Assert WORD_COUNT_DEFAULTS["tweet"] <= 60 (tweets are short — ~280 chars = ~40-50 words).
      - Assert WORD_COUNT_DEFAULTS["story_sequence"] <= 120 (3-5 slides, 15-25 words each).

    test_story_sequence_in_allowlist:
      - `from routes.content import PLATFORM_CONTENT_TYPES`
      - Assert "story_sequence" in PLATFORM_CONTENT_TYPES["instagram"].
      - Assert len(PLATFORM_CONTENT_TYPES["instagram"]) == 3.

    test_linkedin_post_format:
      - Mock FORMAT_RULES["post"] string and assert it contains "3,000" or "3000" (char limit) and "hashtags" (hashtag guidance).

    test_linkedin_article_format:
      - Assert FORMAT_RULES["article"] contains "600" and "header" or "headline" (article structure markers).

    test_linkedin_carousel_format:
      - Assert FORMAT_RULES["carousel_caption"] contains "1,500" or "1500" (carousel char limit) and ("Swipe" or "carousel" or "slides").

    test_x_tweet_under_280:
      - Assert FORMAT_RULES["tweet"] contains "280" and "HARD LIMIT" (case insensitive match on the word "hard" and "limit" or "HARD LIMIT" substring).

    test_x_thread_numbered:
      - Assert FORMAT_RULES["thread"] contains "1/" (numbered format marker).
      - Assert FORMAT_RULES["thread"] contains "280" (per-tweet limit).

    test_instagram_feed_hashtags:
      - Assert FORMAT_RULES["feed_caption"] contains "10" and "hashtag" (hashtag count guidance).

    test_instagram_reel_format:
      - Assert FORMAT_RULES["reel_caption"] contains ("script" or "talking point" or "Reel") — reel-specific content type.

    test_instagram_story_sequence:
      - Assert FORMAT_RULES["story_sequence"] contains "Slide 1:" (slide format marker).
      - Assert FORMAT_RULES["story_sequence"] contains "3" or "5" (slide count reference).

    test_approve_and_schedule:
      - This is an integration test. Use pytest-asyncio + httpx AsyncClient (pattern from test_content_sprint3.py).
      - POST /api/content/create with a mocked or test user → get job_id.
      - PATCH /api/content/job/{job_id}/status with {"status": "approved"} → assert response 200 and job["status"] == "approved".
      - If approve test is too complex to wire without a live DB, write a unit test asserting that `ContentStatusUpdate` Pydantic model accepts "approved" and rejects "invalid_status".

    test_pipeline_stage_progression:
      - Import `run_agent_pipeline` from `agents.pipeline`.
      - Assert the function signature accepts `job_id`, `platform`, `content_type`, `raw_input`, `user_id` parameters.
      - Write a unit-level test that patches `db.content_jobs.update_one` and verifies it is called with `{"current_agent": "commander"}` at some point during a mocked pipeline run. Use `unittest.mock.patch` for all LLM calls.

    Test structure rules:
    - File: `backend/tests/test_content_phase28.py`
    - Module docstring: "Phase 28 — Content Generation Multi-Format tests"
    - All tests use `pytest.mark.unit` for dict/structure tests, `pytest.mark.integration` for API tests
    - Import guard: wrap imports that will fail on unmodified code in try/except; use `pytest.skip` with reason "FORMAT_RULES not yet implemented — will pass after Plan 02"
    - Run the file immediately after writing: `cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_content_phase28.py -v 2>&1 | head -60`
    - Expected result: mostly FAILED or SKIPPED (not ERROR). If any test errors with an unexpected import error, fix the import guard.
  </behavior>
  <action>
    Create `backend/tests/test_content_phase28.py`. Follow patterns from `backend/tests/test_content_sprint3.py` for imports, fixtures, and async client setup.

    File header:
    ```python
    """Phase 28 — Content Generation Multi-Format tests.

    Tests cover CONT-01 through CONT-12. Written in RED state before implementation.
    Tests will pass after Plan 02 (backend changes) and Plan 04 (CONT-11/12 fixes).

    Run: cd backend && pytest tests/test_content_phase28.py -v
    """
    import pytest
    try:
        from agents.writer import FORMAT_RULES
        HAS_FORMAT_RULES = True
    except ImportError:
        FORMAT_RULES = {}
        HAS_FORMAT_RULES = False

    try:
        from agents.commander import WORD_COUNT_DEFAULTS
        HAS_WORD_COUNT_DEFAULTS = True
    except ImportError:
        WORD_COUNT_DEFAULTS = {}
        HAS_WORD_COUNT_DEFAULTS = False

    from routes.content import PLATFORM_CONTENT_TYPES
    from models.content import ContentStatusUpdate  # adjust if Pydantic model location differs
    ```

    Adjust import path for `ContentStatusUpdate` — it lives in `routes/content.py` as a class, not a separate models file. Import it as:
    ```python
    from routes.content import ContentStatusUpdate
    ```

    Write all 13+ test functions as specified in `<behavior>`. Each function must have a clear docstring mapping to its requirement ID (e.g., `# CONT-09`).

    After writing, run immediately:
    ```
    cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_content_phase28.py -v 2>&1 | head -80
    ```

    If any test throws an unhandled ImportError (not a pytest failure), add an import guard for that specific import. The goal is all tests reach FAIL state, not ERROR state.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && pytest tests/test_content_phase28.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `backend/tests/test_content_phase28.py` exists with 13+ test functions.
    - Running `pytest tests/test_content_phase28.py -v` produces output with no ERRORs (only FAILED or SKIPPED).
    - At least 8 tests are in FAILED state (not just skipped), proving they are runnable contracts.
    - Every test function has a comment referencing its requirement ID (# CONT-XX).
  </done>
</task>

</tasks>

<verification>
After test file creation:
- `cd backend && pytest tests/test_content_phase28.py -v` — must run without SyntaxError or unhandled ImportError
- Count of FAILED tests must be >= 8
- Count of ERROR tests must be 0
</verification>

<success_criteria>
All 13+ Phase 28 tests exist in RED state. Zero import errors. The test file is the executable contract for implementation plans 02 and 04.
</success_criteria>

<output>
After completion, create `.planning/phases/28-content-generation-multi-format/28-01-SUMMARY.md`
</output>
