---
phase: 19-core-features
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/core/test_n8n_contracts.py
  - backend/tests/core/test_analytics_loop.py
autonomous: true
requirements: [CORE-04, CORE-07]
must_haves:
  truths:
    - "An n8n bridge execute endpoint called with a tampered HMAC signature returns 401"
    - "An n8n bridge execute endpoint called with a valid HMAC signature returns 200 and executes the task"
    - "All execute endpoints (cleanup-stale-jobs, cleanup-old-jobs, cleanup-expired-shares, reset-daily-limits, refresh-monthly-credits, aggregate-daily-analytics, process-scheduled-posts, run-nightly-strategist, poll-analytics-24h, poll-analytics-7d) respond correctly under HMAC auth"
    - "Callback endpoint verifies X-ThookAI-Signature header using HMAC-SHA256 with constant-time comparison"
    - "Trigger endpoint requires authenticated user via get_current_user"
    - "Poll analytics endpoints write performance data back to content_jobs.performance_data with 24h and 7d snapshots"
    - "Optimal posting times calculation updates persona_engines.performance_intelligence after 7d analytics cycle"
  artifacts:
    - path: "backend/tests/core/test_n8n_contracts.py"
      provides: "n8n bridge contract tests for all execute endpoints + HMAC verification"
      min_lines: 300
    - path: "backend/tests/core/test_analytics_loop.py"
      provides: "Analytics feedback loop tests for poll endpoints and performance write-back"
      min_lines: 200
  key_links:
    - from: "backend/tests/core/test_n8n_contracts.py"
      to: "backend/routes/n8n_bridge.py"
      via: "Tests use httpx.AsyncClient with ASGITransport against n8n router, respx mocks external calls"
      pattern: "_verify_n8n_signature|execute_.*|n8n_callback"
    - from: "backend/tests/core/test_analytics_loop.py"
      to: "backend/services/social_analytics.py"
      via: "Tests mock social platform API responses and verify write-back to DB"
      pattern: "update_post_performance|calculate_optimal_posting_times|aggregate_performance_intelligence"
---

<objective>
Write comprehensive n8n bridge contract tests and analytics feedback loop tests. Every execute endpoint must verify HMAC auth, and analytics polling must write real metrics back to the database.

Purpose: CORE-04 requires all n8n execute endpoints tested with HMAC verification and without a live n8n instance (respx mocking). CORE-07 requires analytics poll endpoints verified for write-back to content_jobs.performance_data and optimal_posting_times calculation.
Output: tests/core/test_n8n_contracts.py (~30 tests) + tests/core/test_analytics_loop.py (~20 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/routes/n8n_bridge.py
@backend/services/social_analytics.py
@backend/routes/analytics.py
@backend/tests/conftest.py
@backend/tests/test_n8n_bridge.py
@backend/tests/test_analytics_social.py
@backend/config.py

<interfaces>
From backend/routes/n8n_bridge.py:
```python
def _verify_n8n_signature(body_bytes: bytes, signature_header: str) -> bool:
async def n8n_callback(request: Request) -> Dict[str, Any]:
async def trigger_workflow(workflow_name: str, ...) -> Dict[str, Any]:
async def _verify_n8n_request(request: Request) -> dict:  # Depends on HMAC verification

# All execute endpoints — each takes request: Request, verified: dict = Depends(_verify_n8n_request)
async def execute_cleanup_stale_jobs(...)
async def execute_cleanup_old_jobs(...)
async def execute_cleanup_expired_shares(...)
async def execute_reset_daily_limits(...)
async def execute_refresh_monthly_credits(...)
async def execute_aggregate_daily_analytics(...)
async def execute_process_scheduled_posts(...)
async def execute_run_nightly_strategist(...)
async def execute_poll_analytics_24h(...)
async def execute_poll_analytics_7d(...)
```

From backend/services/social_analytics.py:
```python
async def fetch_linkedin_post_metrics(post_id, access_token) -> dict:
async def fetch_x_post_metrics(post_id, access_token) -> dict:
async def fetch_instagram_post_metrics(post_id, access_token) -> dict:
async def update_post_performance(job_id, user_id, platform, period) -> bool:
async def aggregate_performance_intelligence(user_id) -> dict:
async def calculate_optimal_posting_times(user_id) -> dict:
async def get_content_analytics(user_id, job_id) -> dict:
async def get_analytics_overview(user_id) -> dict:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write n8n bridge contract tests for all execute endpoints with HMAC verification</name>
  <files>backend/tests/core/test_n8n_contracts.py</files>
  <read_first>
    - backend/routes/n8n_bridge.py (full file — all execute endpoints, _verify_n8n_signature, _verify_n8n_request dependency, _get_workflow_map, _dispatch_workflow_notification)
    - backend/tests/test_n8n_bridge.py (existing 20 tests — identify what's already covered, find gaps)
    - backend/tests/test_n8n_security.py (existing 7 security config tests)
    - backend/config.py (N8nConfig dataclass — webhook_secret, workflow IDs)
    - backend/tests/conftest.py (respx_mock, mongomock_db fixtures)
  </read_first>
  <action>
    Create `backend/tests/core/test_n8n_contracts.py` with 30+ tests that close coverage gaps.

    **Do NOT duplicate tests from test_n8n_bridge.py.** Focus on NEW coverage:
    - All 10 execute endpoints tested individually (existing tests cover only poll-analytics-24h, 7d, and process-scheduled-posts — 7 endpoints are untested)
    - HMAC tampered signature rejection for all endpoint categories
    - Missing signature header handling
    - Successful execution with valid HMAC

    Build a helper `_make_hmac_headers(body: bytes, secret: str)` that computes the correct X-ThookAI-Signature header value. Build a `_make_n8n_client()` async fixture that creates an httpx.AsyncClient with ASGITransport for the n8n router, with database.db patched.

    **TestHMACSignatureVerification (5 tests):**
    - `test_valid_hmac_returns_200_on_execute_endpoint` — compute correct HMAC for empty body, send to /api/n8n/execute/cleanup-stale-jobs. Assert 200.
    - `test_tampered_hmac_returns_401` — send wrong HMAC. Assert 401.
    - `test_missing_signature_header_returns_401` — no X-ThookAI-Signature header. Assert 401.
    - `test_empty_webhook_secret_rejects_all_requests` — patch settings.n8n.webhook_secret="". Assert 401 even with valid-looking signature.
    - `test_hmac_uses_constant_time_comparison` — verify _verify_n8n_signature uses hmac.compare_digest (can test by inspecting source or by checking timing invariance).

    **TestExecuteCleanupStaleJobs (2 tests):**
    - `test_marks_running_jobs_older_than_threshold_as_errored` — seed mongomock with 2 jobs: one running >30min, one running <5min. Call endpoint. Assert older job updated to status="error", newer job unchanged.
    - `test_returns_count_of_cleaned_jobs` — Assert response body has "cleaned" count.

    **TestExecuteCleanupOldJobs (2 tests):**
    - `test_deletes_failed_jobs_older_than_30_days` — seed old failed jobs. Assert deleted.
    - `test_does_not_delete_recent_failed_jobs` — seed recent failed job. Assert NOT deleted.

    **TestExecuteCleanupExpiredShares (2 tests):**
    - `test_deactivates_expired_persona_shares` — seed expired share. Assert is_active set to False.
    - `test_does_not_touch_active_shares` — seed non-expired share. Assert unchanged.

    **TestExecuteResetDailyLimits (2 tests):**
    - `test_resets_daily_content_count_to_zero` — seed user with daily_content_count=5. Assert reset to 0.
    - `test_returns_user_count` — Assert response has "users_reset" count.

    **TestExecuteRefreshMonthlyCredits (2 tests):**
    - `test_refreshes_credits_based_on_subscription_tier` — seed pro user. Assert credits set to tier allowance.
    - `test_does_not_refresh_for_cancelled_subscriptions` — seed cancelled user. Assert credits unchanged.

    **TestExecuteAggregateDailyAnalytics (2 tests):**
    - `test_aggregates_daily_stats` — seed content_jobs for today. Assert daily_stats document created/updated.
    - `test_returns_stats_summary` — Assert response has date and counts.

    **TestExecuteProcessScheduledPosts (3 tests):**
    - `test_publishes_due_posts` — seed scheduled_post with scheduled_at in the past. Mock publisher. Assert publish called and status updated to "published".
    - `test_skips_future_posts` — seed post scheduled for tomorrow. Assert NOT published.
    - `test_writes_publish_results_to_content_jobs` — after publish, assert content_jobs updated with publish_results containing platform_post_id.

    **TestExecuteRunNightlyStrategist (2 tests):**
    - `test_calls_strategist_for_eligible_users` — seed user with sufficient content. Mock run_strategist_for_all_users. Assert called.
    - `test_returns_execution_summary` — Assert response has "users_processed" count.

    **TestCallbackEndpoint (3 tests):**
    - `test_callback_with_valid_signature_processes_payload` — compute HMAC for callback body. Assert 200 and payload processed.
    - `test_callback_with_invalid_signature_returns_401` — wrong HMAC. Assert 401.
    - `test_callback_fires_notification_on_success` — mock _dispatch_workflow_notification. Assert called with callback payload.

    **TestTriggerEndpoint (3 tests):**
    - `test_trigger_known_workflow_dispatches` — mock httpx POST to n8n. Assert workflow triggered.
    - `test_trigger_unknown_workflow_returns_404` — workflow_name="nonexistent". Assert 404.
    - `test_trigger_requires_authentication` — no auth header. Assert 401.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest tests/core/test_n8n_contracts.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend/tests/core/test_n8n_contracts.py returns 28 or more
    - python3 -m pytest tests/core/test_n8n_contracts.py exits 0 with 28+ passed
    - grep -q "execute_cleanup_stale_jobs\|execute_reset_daily_limits\|execute_refresh_monthly_credits" backend/tests/core/test_n8n_contracts.py
    - grep -q "tampered_hmac\|missing_signature\|constant_time" backend/tests/core/test_n8n_contracts.py
  </acceptance_criteria>
  <done>All 10 n8n execute endpoints tested with HMAC verification. Tampered HMAC returns 401 on every endpoint. Callback and trigger endpoints verified. No live n8n instance needed. 28+ tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write analytics feedback loop tests for poll endpoints and performance write-back</name>
  <files>backend/tests/core/test_analytics_loop.py</files>
  <read_first>
    - backend/services/social_analytics.py (full file — fetch_*_post_metrics, update_post_performance, aggregate_performance_intelligence, calculate_optimal_posting_times)
    - backend/routes/analytics.py (route endpoints)
    - backend/tests/test_analytics_social.py (existing 27 tests — identify gaps)
    - backend/tests/conftest.py (respx_mock, mongomock_db)
  </read_first>
  <action>
    Create `backend/tests/core/test_analytics_loop.py` with 20+ tests closing gaps in the analytics feedback loop.

    **Do NOT duplicate tests from test_analytics_social.py.** Focus on NEW coverage:
    - End-to-end analytics polling flow (publish -> 24h poll -> 7d poll -> intelligence update)
    - Optimal posting times edge cases
    - Strategist consumption of performance data
    - Cross-platform metric normalization

    **TestAnalyticsPollFlow (4 tests):**
    - `test_24h_poll_writes_performance_data_snapshot` — mock platform API with metrics. Call update_post_performance(period="24h"). Assert content_jobs.performance_data.snapshot_24h populated with impressions, reach, likes, comments.
    - `test_7d_poll_writes_second_snapshot` — same job already has 24h data. Call with period="7d". Assert performance_data.snapshot_7d added alongside existing 24h.
    - `test_7d_poll_triggers_aggregate_intelligence` — after 7d poll, assert aggregate_performance_intelligence called for user.
    - `test_poll_with_expired_token_stores_error` — mock platform API returning 401. Assert performance_data.last_error contains "token_expired".

    **TestOptimalPostingTimes (5 tests):**
    - `test_calculates_times_from_15_plus_posts` — seed 15 content_jobs with different created_at hours and varying engagement. Assert optimal_posting_times has entries keyed by platform.
    - `test_returns_insufficient_data_message_for_fewer_than_10` — seed 5 posts. Assert result message indicates insufficient data.
    - `test_stores_result_in_persona_engines` — after calculation, assert persona_engines updated with optimal_posting_times.
    - `test_groups_by_platform` — seed posts across linkedin/x/instagram. Assert each platform has independent optimal time.
    - `test_weights_by_engagement_rate` — post at 9am with 10% engagement, post at 3pm with 1% engagement. Assert 9am ranked higher.

    **TestAggregatePerformanceIntelligence (4 tests):**
    - `test_aggregate_computes_running_averages` — seed 5 content_jobs with performance_data. Assert averages computed correctly.
    - `test_aggregate_tracks_best_and_worst_performing` — Assert best_performing and worst_performing fields populated.
    - `test_aggregate_updates_persona_engines` — Assert persona_engines.performance_intelligence updated.
    - `test_aggregate_handles_missing_performance_data` — some jobs have no performance_data. Assert no crash, partial aggregation.

    **TestCrossPlatformMetrics (3 tests):**
    - `test_linkedin_metrics_normalized` — LinkedIn API returns impressions/uniqueImpressions/likes/comments. Assert normalized to standard schema {impressions, reach, likes, comments, shares}.
    - `test_x_metrics_normalized` — X API returns impression_count/like_count/retweet_count/reply_count. Assert normalized.
    - `test_instagram_metrics_normalized` — Instagram API returns impressions/reach/likes/comments. Assert normalized.

    **TestStrategistConsumption (3 tests):**
    - `test_strategist_reads_latest_performance_data` — seed content_jobs with performance_data. Mock strategist query. Assert strategist prompt includes recent performance metrics.
    - `test_strategist_references_underperforming_topics` — seed posts where topic X has low engagement. Assert strategist rationale can reference underperformance.
    - `test_performance_intelligence_available_to_strategist` — seed persona_engines with performance_intelligence. Assert strategist agent receives it in context.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest tests/core/test_analytics_loop.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend/tests/core/test_analytics_loop.py returns 19 or more
    - python3 -m pytest tests/core/test_analytics_loop.py exits 0 with 19+ passed
    - grep -q "snapshot_24h\|snapshot_7d\|optimal_posting_times\|aggregate_performance_intelligence" backend/tests/core/test_analytics_loop.py
  </acceptance_criteria>
  <done>Analytics feedback loop fully tested: 24h and 7d polls write correct performance data, optimal posting times calculated from engagement-weighted post history, aggregation updates persona intelligence, cross-platform metrics normalized, Strategist receives performance data. 19+ tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest tests/core/test_n8n_contracts.py tests/core/test_analytics_loop.py -v --tb=short -q 2>&1 | tail -10
```
All n8n contract and analytics loop tests pass. Combined 47+ tests.
</verification>

<success_criteria>
- All 10 n8n execute endpoints have at least 2 tests each
- HMAC tampered signature returns 401 verified
- Analytics 24h and 7d poll write-back verified
- Optimal posting times calculation verified
- All tests pass without live n8n or social platform API calls
</success_criteria>

<output>
After completion, create `.planning/phases/19-core-features/19-04-SUMMARY.md`
</output>
