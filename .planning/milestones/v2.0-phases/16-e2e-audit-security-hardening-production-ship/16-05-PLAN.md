---
phase: 16-e2e-audit-security-hardening-production-ship
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_stripe_e2e.py
  - backend/tests/test_oauth_flows.py
autonomous: true
requirements:
  - E2E-06
  - E2E-07
must_haves:
  truths:
    - "Stripe checkout session creation works for all plan types (pro, studio, agency) and credit packs (100, 500, 1000)"
    - "Stripe webhook signature verification accepts valid signatures and rejects invalid ones"
    - "OAuth flows for LinkedIn, X/Twitter, Instagram, and Google all have correct redirect URL construction and token exchange logic"
    - "Missing or invalid Stripe Price IDs produce explicit startup errors, not runtime crashes"
  artifacts:
    - path: "backend/tests/test_stripe_e2e.py"
      provides: "Comprehensive Stripe billing flow tests across all plan types"
      min_lines: 120
    - path: "backend/tests/test_oauth_flows.py"
      provides: "OAuth flow tests for all 4 platforms (LinkedIn, X, Instagram, Google)"
      min_lines: 100
  key_links:
    - from: "test_stripe_e2e.py"
      to: "backend/services/stripe_service.py, backend/routes/billing.py"
      via: "Checkout creation, webhook handling, subscription updates"
      pattern: "create_checkout_session|webhook"
    - from: "test_oauth_flows.py"
      to: "backend/routes/platforms.py, backend/routes/auth_google.py"
      via: "OAuth redirect URL construction, token exchange, token storage"
      pattern: "oauth|callback|authorize"
---

<objective>
Verify Stripe billing flows and OAuth platform connections work correctly.

Purpose: E2E-06 validates all Stripe checkout, webhook, and subscription flows. E2E-07 validates OAuth for LinkedIn, X, Instagram, and Google.
Output: Two comprehensive test files covering all billing and OAuth paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/services/stripe_service.py
@backend/routes/billing.py
@backend/routes/platforms.py
@backend/routes/auth_google.py
@backend/tests/test_stripe_billing.py
@backend/tests/test_platform_oauth.py

<interfaces>
From backend/services/stripe_service.py:
```python
# create_checkout_session(user_id, price_id, success_url, cancel_url) -> session
# handle_webhook_event(payload, sig_header) -> dict
# Checks for missing STRIPE_PRICE_* env vars at startup
# Price IDs: price_pro_monthly, price_pro_annual, price_studio_monthly,
#            price_studio_annual, price_agency_monthly, price_agency_annual,
#            price_credits_100, price_credits_500, price_credits_1000
```

From backend/routes/platforms.py:
```python
# GET /api/platforms/linkedin/auth -> redirect to LinkedIn OAuth
# GET /api/platforms/linkedin/callback -> exchange code for token
# GET /api/platforms/twitter/auth -> redirect to Twitter OAuth with PKCE
# GET /api/platforms/twitter/callback -> exchange code for token
# GET /api/platforms/instagram/auth -> redirect to Meta OAuth
# GET /api/platforms/instagram/callback -> exchange code for token
# GET /api/platforms/connections -> list connected platforms
# DELETE /api/platforms/{platform}/disconnect -> remove stored tokens
```

From backend/routes/auth_google.py:
```python
# GET /api/auth/google -> redirect to Google OAuth
# GET /api/auth/google/callback -> exchange code for user info + JWT
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stripe billing comprehensive verification</name>
  <files>backend/tests/test_stripe_e2e.py</files>
  <behavior>
    - Test 1 (checkout_pro_monthly): create_checkout_session with pro monthly price_id returns valid session URL
    - Test 2 (checkout_studio_annual): create_checkout_session with studio annual price_id returns valid session URL
    - Test 3 (checkout_agency_monthly): create_checkout_session with agency monthly price_id succeeds
    - Test 4 (checkout_credits_100): create_checkout_session for 100 credit pack succeeds
    - Test 5 (checkout_credits_500): create_checkout_session for 500 credit pack succeeds
    - Test 6 (checkout_credits_1000): create_checkout_session for 1000 credit pack succeeds
    - Test 7 (webhook_valid_signature): handle_webhook_event with correct HMAC accepts the event
    - Test 8 (webhook_invalid_signature): handle_webhook_event with wrong signature raises error
    - Test 9 (webhook_subscription_created): checkout.session.completed event updates user subscription_tier
    - Test 10 (webhook_subscription_cancelled): customer.subscription.deleted event downgrades to free tier
    - Test 11 (missing_price_id_error): Blank price ID raises explicit error at checkout time
    - Test 12 (billing_routes_auth_required): POST /api/billing/* endpoints return 401 without auth
  </behavior>
  <read_first>
    - backend/services/stripe_service.py (full file — checkout, webhook, subscription logic)
    - backend/routes/billing.py (route handlers)
    - backend/config.py (StripeConfig fields)
    - backend/tests/test_stripe_billing.py (existing patterns)
  </read_first>
  <action>
    Create `backend/tests/test_stripe_e2e.py` with class `TestStripeBillingE2E`.

    Follow existing patterns from `test_stripe_billing.py`. All Stripe API calls mocked — no real Stripe.

    Setup:
    - `patch("services.stripe_service.stripe")` to mock the stripe Python SDK
    - Mock `stripe.checkout.Session.create` to return a mock session with `id`, `url`
    - Mock `stripe.Webhook.construct_event` for webhook tests
    - Use `dependency_overrides[get_current_user]` for authenticated routes
    - Mock settings.stripe with all price IDs set to "price_test_xxx" values

    Tests:

    1-6. Checkout tests for each plan/credit type:
       - For each of [pro_monthly, pro_annual, studio_monthly, studio_annual, agency_monthly, agency_annual, credits_100, credits_500, credits_1000]:
       - Call the checkout creation route/service with the corresponding price_id
       - Assert stripe.checkout.Session.create was called with the correct `line_items` containing that price_id
       - Assert response includes a checkout URL
       - Group as parameterized test: `@pytest.mark.parametrize("tier,price_field", [("pro", "price_pro_monthly"), ...])`

    7. `test_webhook_valid_signature_accepted`:
       - Mock `stripe.Webhook.construct_event` to return a valid event dict
       - POST /api/billing/webhook with valid payload and Stripe-Signature header
       - Assert 200 response

    8. `test_webhook_invalid_signature_rejected`:
       - Mock `stripe.Webhook.construct_event` to raise `stripe.error.SignatureVerificationError`
       - POST /api/billing/webhook with bad signature
       - Assert 400 response

    9. `test_webhook_checkout_completed_updates_subscription`:
       - Mock webhook event type "checkout.session.completed" with metadata containing user_id and tier
       - Mock db.users.update_one
       - POST webhook endpoint
       - Assert db.users.update_one called with `{"$set": {"subscription_tier": expected_tier}}`

    10. `test_webhook_subscription_deleted_downgrades`:
        - Mock webhook event type "customer.subscription.deleted"
        - Mock db.users.update_one
        - POST webhook endpoint
        - Assert user downgraded to "free" tier

    11. `test_missing_price_id_returns_error`:
        - Mock settings.stripe with one price_id as empty string
        - Attempt checkout with that tier
        - Assert explicit error message (not a cryptic Stripe InvalidRequest)

    12. `test_billing_routes_require_auth`:
        - Create TestClient WITHOUT auth override
        - POST /api/billing/checkout
        - Assert 401 or 403
  </action>
  <acceptance_criteria>
    - File exists: backend/tests/test_stripe_e2e.py
    - Contains parameterized test covering all 9 price types (6 subscriptions + 3 credit packs)
    - Contains webhook signature validation tests (valid + invalid)
    - Contains subscription lifecycle tests (create + cancel)
    - All Stripe calls mocked — no real API calls
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_stripe_e2e.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All Stripe E2E tests pass. Checkout works for all plan types and credit packs, webhook signatures verified, subscription lifecycle tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OAuth flow verification for all platforms</name>
  <files>backend/tests/test_oauth_flows.py</files>
  <behavior>
    - Test 1 (linkedin_auth_redirect): GET /api/platforms/linkedin/auth returns redirect to LinkedIn OAuth URL with correct client_id and scope
    - Test 2 (linkedin_callback_exchanges_token): GET /api/platforms/linkedin/callback?code=xxx exchanges code for access token and stores encrypted token
    - Test 3 (twitter_auth_redirect_with_pkce): GET /api/platforms/twitter/auth returns redirect with PKCE code_challenge
    - Test 4 (twitter_callback_exchanges_token): GET /api/platforms/twitter/callback?code=xxx&state=xxx exchanges code for token
    - Test 5 (instagram_auth_redirect): GET /api/platforms/instagram/auth returns redirect to Meta OAuth URL
    - Test 6 (instagram_callback_exchanges_token): GET /api/platforms/instagram/callback?code=xxx exchanges code for token
    - Test 7 (google_auth_redirect): GET /api/auth/google returns redirect to Google OAuth URL
    - Test 8 (google_callback_creates_user_and_jwt): GET /api/auth/google/callback?code=xxx creates/finds user and returns JWT
    - Test 9 (disconnect_removes_tokens): DELETE /api/platforms/linkedin/disconnect removes stored platform_tokens
    - Test 10 (connections_list): GET /api/platforms/connections returns list of connected platforms
  </behavior>
  <read_first>
    - backend/routes/platforms.py (LinkedIn, Twitter, Instagram OAuth flows)
    - backend/routes/auth_google.py (Google OAuth flow)
    - backend/tests/test_platform_oauth.py (existing OAuth test patterns)
    - backend/config.py (PlatformOAuthConfig, GoogleConfig)
  </read_first>
  <action>
    Create `backend/tests/test_oauth_flows.py` with two test classes: `TestPlatformOAuthFlows` and `TestGoogleOAuthFlow`.

    Setup:
    - Mock `httpx.AsyncClient` for all HTTP calls to OAuth providers
    - Mock `settings.platforms` with test client_id/secret values
    - Mock `settings.google` with test client_id/secret
    - Mock db.platform_tokens for token storage/retrieval
    - Use `dependency_overrides[get_current_user]` for authenticated endpoints

    TestPlatformOAuthFlows:

    1. `test_linkedin_auth_redirect_url`:
       - GET /api/platforms/linkedin/auth
       - Assert 302 redirect
       - Parse Location header URL
       - Assert contains `client_id`, `response_type=code`, `scope` includes `w_member_social` or `openid`
       - Assert redirect_uri points back to /api/platforms/linkedin/callback

    2. `test_linkedin_callback_stores_encrypted_token`:
       - Mock httpx POST to LinkedIn token endpoint returning `{"access_token": "test_token", "expires_in": 5184000}`
       - GET /api/platforms/linkedin/callback?code=test_code&state=valid_state
       - Assert db.platform_tokens.update_one called with encrypted access_token (not plaintext)
       - Assert response redirects to frontend success page

    3. `test_twitter_auth_redirect_has_pkce`:
       - GET /api/platforms/twitter/auth
       - Assert 302 redirect
       - Parse Location header
       - Assert contains `code_challenge` and `code_challenge_method=S256`

    4. `test_twitter_callback_exchanges_code`:
       - Mock httpx POST to Twitter token endpoint
       - GET /api/platforms/twitter/callback?code=test_code&state=valid_state
       - Assert token exchange request includes `code_verifier`
       - Assert db.platform_tokens.update_one called

    5. `test_instagram_auth_redirect`:
       - GET /api/platforms/instagram/auth
       - Assert 302 redirect to Meta OAuth URL (graph.facebook.com or facebook.com/v*)
       - Assert scope includes `instagram_basic` or similar

    6. `test_instagram_callback_exchanges_token`:
       - Mock httpx POST/GET for Instagram token exchange
       - GET /api/platforms/instagram/callback?code=test_code
       - Assert token stored in db.platform_tokens

    7. `test_disconnect_removes_platform_tokens`:
       - Mock db.platform_tokens.delete_one
       - DELETE /api/platforms/linkedin/disconnect
       - Assert db.platform_tokens.delete_one called with user_id and platform="linkedin"

    8. `test_connections_list_returns_connected_platforms`:
       - Mock db.platform_tokens.find returning [{"platform": "linkedin"}, {"platform": "twitter"}]
       - GET /api/platforms/connections
       - Assert response contains both platforms

    TestGoogleOAuthFlow:

    9. `test_google_auth_redirects_to_google`:
       - GET /api/auth/google
       - Assert 302 redirect to accounts.google.com
       - Assert scope includes `email` and `profile`

    10. `test_google_callback_creates_or_finds_user`:
        - Mock httpx POST to Google token endpoint
        - Mock httpx GET to Google userinfo endpoint returning {email, name, sub}
        - Mock db.users.find_one (returns None = new user) and db.users.insert_one
        - GET /api/auth/google/callback?code=test_code
        - Assert user created in db.users
        - Assert response contains JWT token or redirects with token

    All external HTTP calls (to LinkedIn, Twitter, Meta, Google APIs) are mocked via httpx. No real OAuth calls.
  </action>
  <acceptance_criteria>
    - File exists: backend/tests/test_oauth_flows.py
    - Contains tests for all 4 platforms: LinkedIn, X/Twitter, Instagram, Google
    - Tests verify redirect URL construction, token exchange, and token storage
    - Twitter OAuth tests verify PKCE (code_challenge present)
    - All external API calls mocked — no real OAuth
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_oauth_flows.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All OAuth flow tests pass. LinkedIn, X/Twitter (with PKCE), Instagram, and Google OAuth flows verified. Token storage and disconnect tested.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_stripe_e2e.py tests/test_oauth_flows.py -x -v
```
</verification>

<success_criteria>
- E2E-06: Stripe billing works for all plan types (pro/studio/agency monthly+annual, credits 100/500/1000), webhook signatures verified, subscription lifecycle tested
- E2E-07: OAuth flows verified for LinkedIn, X/Twitter (PKCE), Instagram, Google — redirect URLs correct, token exchange works, tokens stored encrypted
</success_criteria>

<output>
After completion, create `.planning/phases/16-e2e-audit-security-hardening-production-ship/16-05-SUMMARY.md`
</output>
