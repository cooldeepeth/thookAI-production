---
phase: 16-e2e-audit-security-hardening-production-ship
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_lightrag_isolation.py
  - backend/tests/test_sse_scoping.py
autonomous: true
requirements:
  - E2E-03
  - E2E-04
must_haves:
  truths:
    - "Authenticated as User A, a query to LightRAG with User B's user_id returns zero results — per-user graph isolation is enforced"
    - "SSE notification stream for User A never delivers events belonging to User B — user_id scoping verified"
    - "LightRAG insert tags documents with CREATOR:{user_id} and query filters by user_id metadata"
    - "Notification service creates notifications with user_id and SSE generator filters by user_id"
  artifacts:
    - path: "backend/tests/test_lightrag_isolation.py"
      provides: "Tests verifying per-user LightRAG graph namespace isolation"
      min_lines: 80
    - path: "backend/tests/test_sse_scoping.py"
      provides: "Tests verifying SSE notification user-id scoping"
      min_lines: 60
  key_links:
    - from: "test_lightrag_isolation.py"
      to: "backend/services/lightrag_service.py"
      via: "Verifies doc_filter_func lambda contains user_id check"
      pattern: "doc_filter_func.*user_id"
    - from: "test_sse_scoping.py"
      to: "backend/routes/notifications.py"
      via: "Verifies SSE generator queries by user_id"
      pattern: "user_id.*notifications"
---

<objective>
Verify per-user data isolation in LightRAG knowledge graph and SSE notification streams.

Purpose: E2E-03 confirms no cross-user data leakage in LightRAG queries. E2E-04 confirms SSE events are scoped to the authenticated user.
Output: Two test files proving isolation at the service and route layers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/services/lightrag_service.py
@backend/routes/notifications.py
@backend/services/notification_service.py
@backend/tests/test_lightrag_service.py

<interfaces>
From backend/services/lightrag_service.py:
```python
async def insert_content(user_id: str, content: str, metadata: dict) -> bool:
    # Tags with [CREATOR:{user_id}] in metadata header
    # Sets metadata.user_id in request body

async def query_knowledge_graph(user_id: str, topic: str, mode: str = "hybrid") -> str:
    # Uses doc_filter_func lambda: meta.get('user_id') == '{user_id}'
    # Also includes user_id in natural language query
```

From backend/routes/notifications.py:
```python
async def _sse_event_generator(user_id: str, request: Request):
    # Queries db.notifications with user_id filter
    # Only yields notifications belonging to this user_id

@router.get("/stream")
async def notification_stream(current_user: dict = Depends(get_current_user)):
    user_id = current_user["user_id"]
    # Passes authenticated user_id to SSE generator
```

From backend/services/notification_service.py:
```python
async def create_notification(user_id: str, type: str, title: str, body: str, metadata=None):
    # Creates notification doc with user_id field
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LightRAG per-user graph isolation verification</name>
  <files>backend/tests/test_lightrag_isolation.py</files>
  <behavior>
    - Test 1 (insert_tags_user_id): insert_content("user_A", ...) sends HTTP request body containing metadata.user_id="user_A" and text header containing "[CREATOR:user_A]"
    - Test 2 (query_filters_by_user_id): query_knowledge_graph("user_A", "topic") sends doc_filter_func containing "user_A" — not "user_B" or a wildcard
    - Test 3 (cross_user_query_blocked): Calling query_knowledge_graph("user_B", "topic") sends doc_filter_func containing "user_B" — proving User B cannot access User A's documents
    - Test 4 (insert_different_users_different_doc_ids): insert_content for user_A and user_B produce doc_ids prefixed with respective user_ids
    - Test 5 (query_natural_language_scoped): The natural language query string sent to LightRAG includes "For creator {user_id}" as a secondary isolation signal
  </behavior>
  <read_first>
    - backend/services/lightrag_service.py (insert_content, query_knowledge_graph)
    - backend/tests/test_lightrag_service.py (existing test patterns)
    - backend/config.py (LightRAGConfig)
  </read_first>
  <action>
    Create `backend/tests/test_lightrag_isolation.py` with class `TestLightRAGPerUserIsolation`.

    Test approach: Mock `httpx.AsyncClient` to capture the exact HTTP request body sent to LightRAG service. Verify that user_id is embedded in:
    1. The metadata header tag `[CREATOR:{user_id}]`
    2. The `metadata.user_id` field in the request JSON
    3. The `doc_filter_func` lambda string in query params
    4. The natural language query text

    Setup:
    - Patch `settings.lightrag.is_configured` to return True
    - Patch `settings.lightrag.base_url` to "http://fake-lightrag:9621"
    - Use `unittest.mock.patch("services.lightrag_service.httpx.AsyncClient")` to capture requests
    - The mocked client's `post` method should return a mock Response with status 200 and appropriate JSON body

    Tests:

    1. `test_insert_content_tags_creator_user_id`:
       - Call `await insert_content("user_alpha", "Test content about AI", {"job_id": "j1", "platform": "linkedin"})`
       - Capture the `post` call args
       - Assert request JSON `text` field starts with `[CREATOR:user_alpha]`
       - Assert request JSON `metadata.user_id` == "user_alpha"
       - Assert doc_id starts with "user_alpha_"

    2. `test_query_filters_by_exact_user_id`:
       - Call `await query_knowledge_graph("user_alpha", "AI trends")`
       - Capture the `post` call args
       - Assert `param.doc_filter_func` contains "user_alpha"
       - Assert the string `meta.get('user_id') == 'user_alpha'` appears in the filter lambda

    3. `test_different_users_get_different_filters`:
       - Call query for "user_alpha" and "user_beta" separately
       - Assert "user_alpha" filter does NOT contain "user_beta"
       - Assert "user_beta" filter does NOT contain "user_alpha"

    4. `test_insert_doc_ids_prefixed_per_user`:
       - Insert for "user_alpha" with job_id "j1" -> doc_id should be "user_alpha_j1"
       - Insert for "user_beta" with job_id "j1" -> doc_id should be "user_beta_j1"

    5. `test_query_natural_language_includes_creator`:
       - Call query for "user_gamma"
       - Assert the query text sent to LightRAG starts with "For creator user_gamma:"
  </action>
  <acceptance_criteria>
    - File exists: backend/tests/test_lightrag_isolation.py
    - Contains at least 5 test methods
    - Tests verify user_id in insert metadata, query filter, doc_id prefix, and natural language query
    - No cross-user data leakage possible based on code inspection
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_lightrag_isolation.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>All LightRAG isolation tests pass. Per-user graph namespace enforcement verified: insert tags with user_id, query filters by user_id, no cross-user leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSE notification user-id scoping verification</name>
  <files>backend/tests/test_sse_scoping.py</files>
  <behavior>
    - Test 1 (sse_filters_by_user_id): SSE generator for user_A only yields notifications where user_id == user_A
    - Test 2 (cross_user_notification_not_leaked): Notification created for user_B is NOT delivered to user_A's SSE stream
    - Test 3 (sse_requires_auth): GET /api/notifications/stream without auth token returns 401
    - Test 4 (notification_created_with_user_id): create_notification always sets user_id field on the document
    - Test 5 (sse_content_type): GET /api/notifications/stream returns Content-Type: text/event-stream
  </behavior>
  <read_first>
    - backend/routes/notifications.py (_sse_event_generator, notification_stream)
    - backend/services/notification_service.py (create_notification)
    - backend/auth_utils.py (get_current_user)
  </read_first>
  <action>
    Create `backend/tests/test_sse_scoping.py` with class `TestSSEUserScoping`.

    Test approach: Use FastAPI TestClient with mocked database. Verify SSE stream only returns notifications matching the authenticated user's ID.

    Setup per test:
    - Create a minimal FastAPI app with notifications router mounted
    - Override `get_current_user` dependency to return a specific user
    - Mock `database.db.notifications` collection

    Tests:

    1. `test_sse_stream_filters_by_authenticated_user`:
       - Set current_user to user_id="user_A"
       - Mock db.notifications.find to return a cursor with 2 notifications: one for user_A, one for user_B
       - Actually, verify at the code level: read the _sse_event_generator function and confirm the MongoDB query includes `{"user_id": user_id}` filter
       - Alternative implementation: Use AST or source inspection to verify the query dict passed to db.notifications.find contains user_id

    2. `test_notification_service_always_sets_user_id`:
       - Mock db.notifications.insert_one
       - Call `await create_notification("user_A", "test", "Test", "Body")`
       - Assert the document passed to insert_one has `user_id: "user_A"`
       - Call `await create_notification("user_B", "test", "Test", "Body")`
       - Assert the document passed to insert_one has `user_id: "user_B"`

    3. `test_sse_endpoint_requires_authentication`:
       - Create TestClient WITHOUT dependency override for get_current_user
       - GET /api/notifications/stream
       - Assert response status is 401 or 403

    4. `test_sse_returns_event_stream_content_type`:
       - Create TestClient WITH dependency override for get_current_user
       - Mock db.notifications.find to return empty cursor (use a simple async generator)
       - GET /api/notifications/stream
       - Assert response headers Content-Type contains "text/event-stream"

    5. `test_sse_generator_query_includes_user_id_filter`:
       - Use `inspect.getsource(_sse_event_generator)` to get the function source
       - Assert the source contains a MongoDB query with `"user_id"` key
       - This is a static analysis test confirming the isolation logic is present in code

    Pattern: Follow existing test patterns from test_strategy_routes.py for FastAPI TestClient with dependency overrides.
  </action>
  <acceptance_criteria>
    - File exists: backend/tests/test_sse_scoping.py
    - Contains at least 5 test methods
    - Tests verify user_id filtering in SSE generator, notification service, and auth requirement
    - SSE endpoint returns text/event-stream content type
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_sse_scoping.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>All SSE scoping tests pass. User-id isolation verified: SSE stream filters by authenticated user, notifications tagged with user_id, auth required.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_lightrag_isolation.py tests/test_sse_scoping.py -x -v
```
</verification>

<success_criteria>
- E2E-03: LightRAG per-user graph isolation verified — insert tags user_id, query filters user_id, cross-user access returns zero results
- E2E-04: SSE notification user-id scoping verified — stream filters by authenticated user, no cross-user leakage
</success_criteria>

<output>
After completion, create `.planning/phases/16-e2e-audit-security-hardening-production-ship/16-03-SUMMARY.md`
</output>
