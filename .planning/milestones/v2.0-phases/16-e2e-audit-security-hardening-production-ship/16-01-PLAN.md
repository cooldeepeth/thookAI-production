---
phase: 16-e2e-audit-security-hardening-production-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_e2e_critical_path.py
  - backend/tests/test_dead_links.py
autonomous: true
requirements:
  - E2E-01
  - E2E-10
must_haves:
  truths:
    - "The full critical path (signup -> onboard -> generate -> schedule -> publish -> analytics -> strategy recommend -> one-click approve -> publish again) is verified in an automated test"
    - "All media URLs returned by API endpoints are validated for reachability — no dead R2 links or stale CDN references"
    - "All internal API endpoint paths referenced in frontend code resolve to real FastAPI routes"
  artifacts:
    - path: "backend/tests/test_e2e_critical_path.py"
      provides: "E2E smoke test covering full user journey through all v2.0 features"
      min_lines: 200
    - path: "backend/tests/test_dead_links.py"
      provides: "Dead link detection across media URLs and API endpoint references"
      min_lines: 80
  key_links:
    - from: "test_e2e_critical_path.py"
      to: "routes/auth.py -> routes/onboarding.py -> routes/content.py -> routes/strategy.py"
      via: "Sequential TestClient calls simulating full user journey"
      pattern: "client\\.(post|get).*\\/api\\/"
    - from: "test_dead_links.py"
      to: "routes/*.py, frontend/src/**/*.js"
      via: "AST/string scan for route paths and URL patterns"
      pattern: "\\/api\\/[a-z]"
---

<objective>
Verify the full critical path end-to-end and detect dead links across the platform.

Purpose: E2E-01 confirms that every user-facing workflow (signup through re-publish) completes without errors in an automated smoke test. E2E-10 confirms no dead media URLs or broken API endpoint references exist.
Output: Two test files that can run in CI without external services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/routes/auth.py
@backend/routes/onboarding.py
@backend/routes/content.py
@backend/routes/strategy.py
@backend/routes/n8n_bridge.py
@backend/routes/notifications.py
@backend/routes/billing.py
@backend/routes/platforms.py
@backend/routes/media.py
@backend/routes/analytics.py
@backend/routes/obsidian.py
@backend/server.py
@backend/tests/test_e2e_ship.py

<interfaces>
<!-- Existing test patterns from test_e2e_ship.py -->
From backend/tests/test_e2e_ship.py:
```python
FAKE_USER = {
    "user_id": "user_test_e2e_001",
    "email": "e2e@thookai.io",
    "subscription_tier": "pro",
    "credits": 200,
    "onboarding_completed": True,
}
# Pattern: dependency_overrides[get_current_user] for auth bypass
# Pattern: patch("routes.<module>.db") for database mocking
# Pattern: AsyncMock for async functions
```

From backend/server.py (registered routes):
```python
# All routers are included via app.include_router() in server.py
# Auth: /api/auth
# Onboarding: /api/onboarding
# Content: /api/content
# Strategy: /api/strategy
# N8n Bridge: /api/n8n
# Notifications: /api/notifications
# Billing: /api/billing
# Platforms: /api/platforms
# Media: /api/media
# Analytics: /api/analytics
# Obsidian: /api/obsidian
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E critical path smoke test</name>
  <files>backend/tests/test_e2e_critical_path.py</files>
  <behavior>
    - Test 1 (signup_to_onboard): POST /api/auth/register returns 200 with user_id -> POST /api/onboarding/start returns 200 -> POST /api/onboarding/answer (7 answers) returns 200 -> POST /api/onboarding/generate-persona returns 200 with persona card
    - Test 2 (generate_content): POST /api/content/generate with {platform:"linkedin", content_type:"post", raw_input:"test"} returns 200 with job_id and status "processing" or "reviewing"
    - Test 3 (schedule_and_publish): POST /api/content/{job_id}/schedule returns 200 -> verify content_jobs doc has status "scheduled" -> simulate n8n callback marking published
    - Test 4 (analytics_flow): After publish, verify analytics_24h_due_at and analytics_7d_due_at are set on content_jobs doc -> simulate analytics poll endpoint returns metrics -> verify performance_data written
    - Test 5 (strategy_recommend_approve_republish): Verify strategy_recommendations doc with status "pending_approval" exists -> POST /api/strategy/{id}/approve returns 200 with generate_payload -> POST /api/content/generate with that payload returns 200 (second generation cycle)
    - Test 6 (notification_stream): GET /api/notifications/stream returns 200 with text/event-stream content type
    - Test 7 (full_journey_sequential): Chain tests 1-5 in sequence — a single user goes from signup through second publish
  </behavior>
  <read_first>
    - backend/tests/test_e2e_ship.py (existing E2E patterns)
    - backend/routes/auth.py (register endpoint)
    - backend/routes/onboarding.py (start, answer, generate-persona endpoints)
    - backend/routes/content.py (generate, schedule endpoints)
    - backend/routes/strategy.py (approve endpoint)
    - backend/agents/pipeline.py (pipeline flow)
  </read_first>
  <action>
    Create `backend/tests/test_e2e_critical_path.py` with a `TestE2ECriticalPath` class.

    Follow existing test patterns from `test_e2e_ship.py`:
    - Use `from fastapi.testclient import TestClient` (sync client for sequential flow)
    - Use `dependency_overrides[get_current_user]` to bypass JWT auth
    - Use `patch("routes.<module>.db")` to mock MongoDB operations
    - Use `AsyncMock` for all async database methods (`find_one`, `insert_one`, `update_one`, `find`)
    - Use `patch("agents.pipeline.run_pipeline")` to mock the content pipeline (returns a job dict with status "reviewing")
    - Use `patch("agents.strategist.run_strategist_for_user")` to mock strategist

    Test structure:
    1. `test_signup_creates_user`: Mock db.users.find_one (returns None for no existing user), db.users.insert_one. POST /api/auth/register with {email, password, name}. Assert 200 + user_id in response.
    2. `test_onboarding_generates_persona`: Mock db.persona_engines.find_one (returns None), db.persona_engines.insert_one, mock LLM client. POST /api/onboarding/start, then 7x POST /api/onboarding/answer, then POST /api/onboarding/generate-persona. Assert 200 + persona card fields present.
    3. `test_content_generation_starts`: Mock db.content_jobs.insert_one, mock pipeline.run_pipeline as AsyncMock. POST /api/content/generate with {platform:"linkedin", content_type:"post", raw_input:"AI trends"}. Assert 200 + job_id returned.
    4. `test_schedule_then_publish`: Mock db.content_jobs.find_one (returns job with status "approved"), db.scheduled_posts.insert_one. POST /api/content/{job_id}/schedule with {scheduled_at: future_datetime}. Assert 200. Then simulate n8n publish callback: mock db.content_jobs.find_one_and_update. POST /api/n8n/callback with publish result payload + valid HMAC signature. Assert job status updated to "published" and analytics_24h_due_at is set.
    5. `test_analytics_poll_writes_performance`: Mock db.content_jobs.find returning jobs with analytics_24h_polled=False. POST /api/n8n/execute/poll-analytics-24h (with HMAC auth). Assert db.content_jobs.update_one called with performance_data.
    6. `test_strategy_approve_triggers_regeneration`: Mock db.strategy_recommendations.find_one (returns card with status "pending_approval" and generate_payload). POST /api/strategy/{rec_id}/approve. Assert 200 + generate_payload in response.
    7. `test_full_critical_path_sequential`: Chain all above in one test. Use a shared mock_db that accumulates state across steps. Assert the full journey completes without any mock raising an unexpected exception.

    Each test must be independently runnable. Use `pytest.mark.asyncio` where needed. All external services fully mocked — no MongoDB, Redis, Stripe, or LLM calls.
  </action>
  <acceptance_criteria>
    - File exists: backend/tests/test_e2e_critical_path.py
    - Contains class TestE2ECriticalPath
    - Contains at least 7 test methods covering signup -> onboard -> generate -> schedule -> publish -> analytics -> strategy -> approve
    - All tests mock external dependencies (no real DB/API calls)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_e2e_critical_path.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All 7+ E2E critical path tests pass. The full user journey from signup through second-cycle content generation is verified in automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dead link detection across media URLs and API endpoints</name>
  <files>backend/tests/test_dead_links.py</files>
  <behavior>
    - Test 1 (api_route_coverage): Every /api/* path referenced in frontend src/ files resolves to a registered FastAPI route — no orphan references
    - Test 2 (no_hardcoded_localhost): No frontend production code contains hardcoded localhost URLs — all use REACT_APP_BACKEND_URL env var
    - Test 3 (media_url_pattern_valid): All media URL patterns in routes/media.py and services/media_storage.py use R2_PUBLIC_URL prefix, not /tmp paths
    - Test 4 (route_registration_complete): All router files in backend/routes/ are registered in server.py via app.include_router()
  </behavior>
  <read_first>
    - backend/server.py (router registrations)
    - backend/routes/media.py (media URL patterns)
    - backend/services/media_storage.py (R2 URL construction)
    - backend/routes/uploads.py (upload URL handling)
  </read_first>
  <action>
    Create `backend/tests/test_dead_links.py` with static analysis tests:

    1. `test_frontend_api_references_resolve`: Use `pathlib.Path` to glob `frontend/src/**/*.js` and `frontend/src/**/*.jsx`. Extract all `/api/` path strings via regex `r'["\x27]/api/[a-z][a-zA-Z/\-:]*'`. Build set of referenced API base paths (strip path params like `:id`). Compare against FastAPI app's registered routes from `server.py`. Assert no unresolved references. Allow known exceptions (dynamic paths with variable segments).

    2. `test_no_hardcoded_localhost_in_frontend`: Glob all `frontend/src/**/*.{js,jsx}` files. Search for `http://localhost` or `https://localhost` in non-comment lines. Exclude `node_modules/`. Assert zero matches. Known exception: files containing `process.env.REACT_APP_` on the same line (development fallback pattern).

    3. `test_media_url_uses_r2_public_url`: Read `backend/routes/media.py`, `backend/routes/uploads.py`, `backend/services/media_storage.py`. Search for URL construction patterns. Assert no `/tmp/` path is returned as a URL to clients without an R2 fallback check. Verify `R2_PUBLIC_URL` is used in URL construction, or a 503 is raised when R2 is unavailable.

    4. `test_all_route_files_registered`: List all `.py` files in `backend/routes/` (excluding `__init__.py` and `__pycache__`). For each, check that `server.py` contains `include_router` referencing that module. Assert no orphan route files.

    Use `pathlib.Path` for all file operations (no subprocess/grep). Pattern from Phase 07 test_frontend_quality.py.
  </action>
  <acceptance_criteria>
    - File exists: backend/tests/test_dead_links.py
    - Contains at least 4 test functions
    - Tests use pathlib.Path for file scanning (not subprocess)
    - No hardcoded localhost URLs found in frontend production code
    - All route files are registered in server.py
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_dead_links.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>Dead link detection tests pass. Frontend API references resolve to real routes, no localhost leaks, media URLs use R2, all route files registered.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_e2e_critical_path.py tests/test_dead_links.py -x -v
```
</verification>

<success_criteria>
- E2E-01: Full critical path verified in automated test (signup -> onboard -> generate -> schedule -> publish -> analytics -> strategy -> approve -> regenerate)
- E2E-10: Dead link detection passes — no orphan API references, no localhost leaks, no /tmp media URLs, all routes registered
</success_criteria>

<output>
After completion, create `.planning/phases/16-e2e-audit-security-hardening-production-ship/16-01-SUMMARY.md`
</output>
