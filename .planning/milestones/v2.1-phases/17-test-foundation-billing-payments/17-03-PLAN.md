---
phase: 17-test-foundation-billing-payments
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "CI runs separate test jobs for billing, security, pipeline, and e2e domains"
    - "Billing CI job enforces 95% branch coverage on billing modules"
    - "Other domain CI jobs enforce 85% branch coverage on their modules"
    - "CI installs all test infrastructure packages including pytest-cov, pytest-randomly, mongomock-motor"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI matrix with domain-specific jobs and coverage thresholds"
      contains: "cov-fail-under"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "backend/.coveragerc"
      via: "pytest-cov reads .coveragerc for branch config"
      pattern: "--cov-branch"
---

<objective>
Configure CI matrix with separate jobs per test domain and per-domain coverage thresholds.

Purpose: Enforces coverage gates in CI so billing tests cannot merge below 95% branch coverage (per D-12) and other domains cannot merge below 85%. Separate jobs provide faster feedback and domain-specific failure isolation.

Output: Updated `.github/workflows/ci.yml` with 4 domain jobs plus the existing lint and frontend build jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-foundation-billing-payments/17-CONTEXT.md
@.planning/phases/17-test-foundation-billing-payments/17-01-SUMMARY.md

@.github/workflows/ci.yml
@backend/pytest.ini
@backend/.coveragerc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure CI matrix with domain-specific test jobs and coverage gates</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>
    **Per D-06: CI matrix with separate jobs per domain and per-domain coverage thresholds.**

    Replace the single `backend-test` job in `.github/workflows/ci.yml` with 4 domain-specific jobs. Keep `backend-lint` and `frontend-build` unchanged.

    The new jobs share a common setup (Python 3.11, install requirements, env vars for Mongo/Redis/JWT/Fernet) but each runs a different test subset with a different coverage threshold.

    **Job definitions:**

    1. **backend-test-billing** (FOUND-04, D-06, D-12):
       - Runs: `python -m pytest tests/billing/ tests/test_credits_billing.py tests/test_stripe_billing.py --cov=services/credits.py --cov=services/stripe_service.py --cov=routes/billing.py --cov-branch --cov-report=term-missing --cov-fail-under=95 -v --tb=short`
       - Coverage threshold: 95% (per D-12)
       - Services: mongo + redis

    2. **backend-test-security** (for Phase 18):
       - Runs: `python -m pytest tests/test_auth_core.py tests/test_oauth_flows.py tests/test_rate_limit.py tests/test_rate_limit_concurrent.py tests/test_n8n_security.py --cov=auth_utils.py --cov=routes/auth.py --cov=routes/auth_google.py --cov=middleware/security.py --cov-branch --cov-report=term-missing --cov-fail-under=85 -v --tb=short`
       - Coverage threshold: 85%
       - Services: mongo + redis

    3. **backend-test-pipeline** (for Phase 19):
       - Runs: `python -m pytest tests/test_pipeline_e2e.py tests/test_pipeline_integration.py tests/test_strategist.py tests/test_strategy_routes.py tests/test_lightrag_service.py tests/test_lightrag_integration.py tests/test_lightrag_isolation.py tests/test_media_orchestrator.py tests/test_media_pipeline_ledger.py tests/test_n8n_bridge.py tests/test_obsidian_service.py tests/test_obsidian_agents.py tests/test_obsidian_routes.py tests/test_analytics_social.py --cov=agents/ --cov=services/ --cov-branch --cov-report=term-missing --cov-fail-under=85 -v --tb=short`
       - Coverage threshold: 85%
       - Services: mongo + redis

    4. **backend-test-general** (catch-all for remaining tests):
       - Runs: `python -m pytest tests/ --ignore=tests/billing --ignore=tests/test_credits_billing.py --ignore=tests/test_stripe_billing.py --ignore=tests/test_auth_core.py --ignore=tests/test_oauth_flows.py --ignore=tests/test_rate_limit.py --ignore=tests/test_rate_limit_concurrent.py --ignore=tests/test_n8n_security.py --ignore=tests/test_pipeline_e2e.py --ignore=tests/test_pipeline_integration.py --ignore=tests/test_strategist.py --ignore=tests/test_strategy_routes.py --ignore=tests/test_lightrag_service.py --ignore=tests/test_lightrag_integration.py --ignore=tests/test_lightrag_isolation.py --ignore=tests/test_media_orchestrator.py --ignore=tests/test_media_pipeline_ledger.py --ignore=tests/test_n8n_bridge.py --ignore=tests/test_obsidian_service.py --ignore=tests/test_obsidian_agents.py --ignore=tests/test_obsidian_routes.py --ignore=tests/test_analytics_social.py -v --tb=short`
       - No coverage gate (catch-all)
       - Services: mongo + redis

    **Common env vars for all test jobs** (extracted from current ci.yml):
    ```yaml
    env:
      MONGO_URL: mongodb://localhost:27017
      DB_NAME: thookai_test
      REDIS_URL: redis://localhost:6379/0
      JWT_SECRET_KEY: ci-test-secret-key-not-for-production
      FERNET_KEY: 7X2PqyChA8TwDuzvdaeX_0Yh0SbmvZzxrjDXDwjCRCI=
      ENVIRONMENT: test
    ```

    **Common services for all test jobs:**
    ```yaml
    services:
      mongo:
        image: mongo:7.0
        ports: ["27017:27017"]
        options: --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })'" --health-interval 10s --health-timeout 5s --health-retries 5
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: --health-cmd "redis-cli ping" --health-interval 10s --health-timeout 5s --health-retries 5
    ```

    **Common steps for all test jobs:**
    ```yaml
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - name: Install dependencies
        run: pip install -r backend/requirements.txt
      - name: Run tests
        run: |
          cd backend
          [domain-specific pytest command]
    ```

    **NOTE:** The billing coverage gate (--cov-fail-under=95) will initially fail until Plan 04 and 05 write the comprehensive billing tests. This is intentional — it establishes the gate that later plans must satisfy. To prevent blocking PRs in the meantime, set the billing job to `continue-on-error: true` with a comment `# TODO: remove continue-on-error after Phase 17 billing tests are complete`.

    Write the complete YAML file ensuring proper indentation and valid syntax.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production && python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML valid" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "backend-test-billing" .github/workflows/ci.yml
    - grep -q "backend-test-security" .github/workflows/ci.yml
    - grep -q "backend-test-pipeline" .github/workflows/ci.yml
    - grep -q "backend-test-general" .github/workflows/ci.yml
    - grep -q "cov-fail-under=95" .github/workflows/ci.yml
    - grep -q "cov-fail-under=85" .github/workflows/ci.yml
    - python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" exits 0
  </acceptance_criteria>
  <done>CI matrix has 4 domain-specific test jobs. Billing job enforces 95% branch coverage gate. Security and pipeline jobs enforce 85%. YAML is valid.</done>
</task>

</tasks>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (valid YAML)
- `grep -c "backend-test-" .github/workflows/ci.yml` returns 4 (4 domain jobs)
- `grep "cov-fail-under=95" .github/workflows/ci.yml` finds billing threshold
</verification>

<success_criteria>
1. CI YAML is valid and contains 4 domain-specific test jobs
2. Billing job enforces 95% branch coverage on credits.py, stripe_service.py, billing.py
3. Security and pipeline jobs enforce 85% branch coverage
4. All jobs use the same Python 3.11 + mongo + redis service configuration
5. General job is a catch-all that runs remaining tests without coverage gate
</success_criteria>

<output>
After completion, create `.planning/phases/17-test-foundation-billing-payments/17-03-SUMMARY.md`
</output>
