---
phase: 19-core-features
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/core/test_lightrag_lambda_injection.py
  - backend/services/lightrag_service.py
  - backend/tests/core/test_lightrag_comprehensive.py
autonomous: true
requirements: [CORE-05, CORE-09]
must_haves:
  truths:
    - "User A's LightRAG query never returns documents belonging to User B — per-user isolation enforced at the query filter level"
    - "The lambda-scoped doc_filter_func string is replaced with a safe parameterized filter that cannot be injected"
    - "A user_id containing Python injection characters (quotes, parentheses, semicolons) does not break the filter or cause code execution"
    - "Inserting content for user A and querying as user B returns empty results"
    - "LightRAG health_check, insert_content, query_knowledge_graph all degrade gracefully when LightRAG is not configured"
    - "Embedding config assertion fails loudly on wrong model or wrong dimension"
  artifacts:
    - path: "backend/tests/core/test_lightrag_lambda_injection.py"
      provides: "TDD failing test for lambda injection cross-user bug, then verification after fix"
      min_lines: 80
    - path: "backend/services/lightrag_service.py"
      provides: "Fixed lightrag_service with safe user_id filtering (no lambda string interpolation)"
    - path: "backend/tests/core/test_lightrag_comprehensive.py"
      provides: "Comprehensive LightRAG isolation and integration tests"
      min_lines: 200
  key_links:
    - from: "backend/tests/core/test_lightrag_lambda_injection.py"
      to: "backend/services/lightrag_service.py"
      via: "Tests verify doc_filter_func no longer uses string interpolation with user_id"
      pattern: "doc_filter_func|lambda|user_id"
    - from: "backend/tests/core/test_lightrag_comprehensive.py"
      to: "backend/services/lightrag_service.py"
      via: "Tests verify all public functions with respx mocking"
      pattern: "health_check|insert_content|query_knowledge_graph"
---

<objective>
TDD fix the LightRAG lambda injection bug (CORE-09) and write comprehensive LightRAG isolation tests (CORE-05). The lambda-scoped doc_filter_func in query_knowledge_graph() uses f-string interpolation of user_id, which could leak cross-user data if user_id contains injection characters.

Purpose: CORE-09 requires a failing test first exposing the cross-user data leak via lambda scope, then a production fix. CORE-05 requires comprehensive per-user isolation verification. This plan follows RED-GREEN-REFACTOR TDD cycle.
Output: Fixed lightrag_service.py + ~30 new tests verifying isolation and all service functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/services/lightrag_service.py
@backend/tests/conftest.py
@backend/tests/test_lightrag_service.py
@backend/tests/test_lightrag_isolation.py
@backend/tests/test_lightrag_integration.py

<interfaces>
From backend/services/lightrag_service.py (current BUGGY code):
```python
# Line 138 — THE BUG: lambda string interpolation with user_id
"doc_filter_func": f"lambda meta: meta.get('user_id') == '{user_id}'"
# If user_id = "'; import os; os.system('rm -rf /'); '" this evaluates arbitrary code
# Even without code execution, user_id="user_A' or True or '" would match ALL documents

async def health_check() -> bool:
async def assert_lightrag_embedding_config() -> None:
async def insert_content(user_id: str, content: str, metadata: dict) -> bool:
async def query_knowledge_graph(user_id: str, topic: str, mode: str = "hybrid") -> str:
```
</interfaces>
</context>

<feature>
  <name>LightRAG Lambda Injection Fix</name>
  <files>backend/services/lightrag_service.py, backend/tests/core/test_lightrag_lambda_injection.py</files>
  <behavior>
    - Test 1: A user_id containing single quotes does NOT expand the lambda match scope (query param stays scoped to exact user_id)
    - Test 2: A user_id containing "' or True or '" does NOT match all documents (injection blocked)
    - Test 3: Normal user_id like "user_abc123" still produces correct filter targeting only that user
    - Test 4: The doc_filter_func field in the query payload uses a safe parameterized format, NOT f-string lambda interpolation
  </behavior>
  <implementation>Replace line 138 in lightrag_service.py: change the f-string lambda to a safe dict-based filter. Instead of sending a Python lambda string, send a structured filter dict that the LightRAG API can interpret without eval(). Use: `"doc_filter_func": {"field": "user_id", "operator": "==", "value": user_id}` or sanitize user_id by stripping/escaping dangerous characters before interpolation.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write failing test exposing lambda injection cross-user leak, then GREEN — fix production code</name>
  <files>backend/tests/core/test_lightrag_lambda_injection.py, backend/services/lightrag_service.py</files>
  <read_first>
    - backend/services/lightrag_service.py (full file — focus on line 138 doc_filter_func f-string)
    - backend/tests/test_lightrag_isolation.py (existing isolation tests — understand what's already covered)
    - backend/tests/test_lightrag_service.py (existing service tests — understand mock patterns)
    - backend/tests/conftest.py (respx_mock fixture)
  </read_first>
  <behavior>
    - Test: user_id with single quote injection does NOT produce a lambda that matches all documents
    - Test: user_id with "or True" injection does NOT produce a universally-matching filter
    - Test: Normal user_id produces a filter that matches exactly that user
    - Test: The query payload sent to LightRAG API does NOT contain raw f-string lambda with unescaped user_id
  </behavior>
  <action>
    **RED phase — write the failing test first:**

    Create `backend/tests/core/test_lightrag_lambda_injection.py`:

    ```python
    """TDD: Expose LightRAG lambda injection cross-user data leak (CORE-09).

    BUG: lightrag_service.py line 138 uses:
      f"lambda meta: meta.get('user_id') == '{user_id}'"

    A malicious user_id like "x' or True or '" would produce:
      lambda meta: meta.get('user_id') == 'x' or True or ''
    which matches ALL documents — cross-user data leak.
    """
    ```

    **TestLambdaInjectionBlocked (4 tests):**
    - `test_injection_with_single_quote_does_not_expand_match` — Use respx to capture the POST request to /query. Call query_knowledge_graph(user_id="x' or True or '", topic="test"). Inspect the captured request body JSON. Assert the doc_filter_func value does NOT contain unescaped single quotes that would alter the lambda logic. The filter must target ONLY the exact user_id string.
    - `test_injection_with_semicolon_does_not_execute_code` — user_id="'; import os; '". Assert the query payload's doc_filter_func is safe (no raw code execution path).
    - `test_normal_user_id_produces_correct_filter` — user_id="user_abc123". Assert doc_filter_func targets exactly "user_abc123".
    - `test_query_payload_does_not_use_raw_lambda_string` — Call query_knowledge_graph with any user_id. Capture request body. Assert the doc_filter_func field either (a) uses a structured dict filter, or (b) uses a sanitized user_id with dangerous characters stripped/escaped.

    Run tests — they should FAIL because current code uses f-string lambda interpolation.
    Commit: `test(19-03): add failing tests for lightrag lambda injection (CORE-09)`

    **GREEN phase — fix production code:**

    Edit `backend/services/lightrag_service.py`, line 138 area. Replace:
    ```python
    "doc_filter_func": f"lambda meta: meta.get('user_id') == '{user_id}'"
    ```
    With a safe alternative. Two options (pick whichever the LightRAG API supports):

    Option A — Structured filter dict:
    ```python
    "doc_filter_func": {"field": "user_id", "operator": "==", "value": user_id}
    ```

    Option B — Sanitized lambda (strip dangerous chars, validate user_id format):
    ```python
    import re
    safe_uid = re.sub(r"[^a-zA-Z0-9_-]", "", user_id)
    "doc_filter_func": f"lambda meta: meta.get('user_id') == '{safe_uid}'"
    ```

    Prefer Option A (structured dict) if the LightRAG REST API version supports it. If the API requires a lambda string, use Option B with strict sanitization AND add a validation check at the start of query_knowledge_graph:
    ```python
    if not re.match(r'^[a-zA-Z0-9_-]+$', user_id):
        logger.warning("Suspicious user_id rejected: %r", user_id)
        return ""
    ```

    Run tests again — they should now PASS.
    Commit: `fix(19-03): replace lambda string interpolation with safe user_id filter in lightrag_service`
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest tests/core/test_lightrag_lambda_injection.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend/tests/core/test_lightrag_lambda_injection.py returns 4 or more
    - python3 -m pytest tests/core/test_lightrag_lambda_injection.py exits 0 with 4+ passed
    - grep -v "f\"lambda" backend/services/lightrag_service.py shows no f-string lambda interpolation remaining (line 138 area replaced)
    - grep -q "safe_uid\|sanitize\|re.sub\|re.match\|operator.*==" backend/services/lightrag_service.py (evidence of safe filtering)
  </acceptance_criteria>
  <done>Lambda injection bug fixed via TDD. Failing tests written first exposing the cross-user data leak, then production code fixed. user_id with injection characters no longer expands match scope. Tests verify the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive LightRAG isolation and service tests</name>
  <files>backend/tests/core/test_lightrag_comprehensive.py</files>
  <read_first>
    - backend/services/lightrag_service.py (now fixed — read updated version after Task 1)
    - backend/tests/test_lightrag_service.py (existing 18 tests — identify gaps)
    - backend/tests/test_lightrag_isolation.py (existing 5 isolation tests — identify gaps)
    - backend/tests/test_lightrag_integration.py (existing 10 integration tests — routing contract tests)
    - backend/tests/conftest.py (respx_mock fixture)
    - backend/config.py (LightRAGConfig dataclass)
  </read_first>
  <action>
    Create `backend/tests/core/test_lightrag_comprehensive.py` with 25+ tests that close coverage gaps not addressed by existing test files.

    **Do NOT duplicate tests that already exist in test_lightrag_service.py, test_lightrag_isolation.py, or test_lightrag_integration.py.** Instead, focus on NEW coverage:

    **TestHealthCheck (3 tests):**
    - `test_health_check_returns_true_on_200` — respx mock GET /health returning 200. Assert True.
    - `test_health_check_returns_false_on_500` — respx mock returning 500. Assert False.
    - `test_health_check_returns_false_on_timeout` — respx raises httpx.TimeoutException. Assert False (graceful degradation).

    **TestInsertContent (5 tests):**
    - `test_insert_builds_correct_payload` — capture POST request body. Assert doc_id format is "{user_id}_{job_id}", tagged_content has CREATOR/PLATFORM/TYPE headers, metadata dict has all fields.
    - `test_insert_sends_api_key_header_when_configured` — settings.lightrag.api_key="test-key". Assert X-API-Key header present.
    - `test_insert_omits_api_key_header_when_not_configured` — api_key=None. Assert no X-API-Key header.
    - `test_insert_returns_false_on_http_error` — respx returns 500. Assert returns False (non-fatal).
    - `test_insert_returns_false_on_network_timeout` — respx raises TimeoutException. Assert False.

    **TestQueryKnowledgeGraph (6 tests):**
    - `test_query_builds_natural_language_query_with_user_id` — capture POST body. Assert query string contains user_id and topic.
    - `test_query_uses_hybrid_mode_by_default` — Assert param.mode="hybrid" in request body.
    - `test_query_uses_custom_mode_when_specified` — mode="local". Assert param.mode="local".
    - `test_query_returns_response_text_on_success` — respx returns {"response": "graph context"}. Assert returns "graph context".
    - `test_query_returns_empty_string_on_failure` — respx returns 500. Assert returns "".
    - `test_query_returns_empty_string_on_timeout` — respx raises TimeoutException. Assert returns "".

    **TestPerUserIsolation (5 tests):**
    - `test_insert_for_user_a_tags_only_user_a` — insert_content(user_id="user_A"). Capture request body. Assert metadata.user_id="user_A" and doc_id starts with "user_A_".
    - `test_query_for_user_a_filters_only_user_a` — query_knowledge_graph(user_id="user_A"). Capture request. Assert doc_filter targets "user_A" exclusively.
    - `test_query_for_user_b_does_not_match_user_a` — query(user_id="user_B"). Assert filter value is "user_B", not "user_A".
    - `test_sequential_queries_for_different_users_are_isolated` — query for user_A, then user_B. Capture both requests. Assert each has its own user filter.
    - `test_user_id_with_special_chars_is_sanitized` — user_id with dots/spaces. Assert either sanitized or rejected.

    **TestEmbeddingConfig (4 tests):**
    - `test_assert_embedding_valid_config_passes` — settings with correct model+dim. Assert no exception raised.
    - `test_assert_embedding_wrong_model_raises` — Assert AssertionError on wrong model name.
    - `test_assert_embedding_wrong_dim_raises` — Assert AssertionError on wrong dimension.
    - `test_assert_embedding_not_configured_logs_warning` — settings.lightrag.is_configured()=False. Assert function returns without error (warning logged).

    **TestGracefulDegradation (3 tests):**
    - `test_all_functions_return_default_when_not_configured` — is_configured=False. Assert health_check=False, insert_content=False, query_knowledge_graph="".
    - `test_insert_does_not_raise_on_any_exception` — mock httpx to raise arbitrary Exception. Assert returns False.
    - `test_query_does_not_raise_on_any_exception` — mock httpx to raise arbitrary Exception. Assert returns "".
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest tests/core/test_lightrag_comprehensive.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend/tests/core/test_lightrag_comprehensive.py returns 25 or more
    - python3 -m pytest tests/core/test_lightrag_comprehensive.py exits 0 with 25+ passed
    - grep -q "TestPerUserIsolation\|TestGracefulDegradation\|TestEmbeddingConfig" backend/tests/core/test_lightrag_comprehensive.py
  </acceptance_criteria>
  <done>LightRAG service fully tested: health check, insert, query with per-user isolation verified. Embedding config assertions tested. Graceful degradation confirmed for all failure modes. 25+ tests pass. Combined with Task 1, CORE-05 and CORE-09 both verified.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest tests/core/test_lightrag_lambda_injection.py tests/core/test_lightrag_comprehensive.py -v --tb=short -q 2>&1 | tail -10
```
All LightRAG tests pass. Lambda injection fix verified via TDD.
</verification>

<success_criteria>
- TDD cycle complete: failing test committed BEFORE fix, fix committed AFTER
- lightrag_service.py no longer has f-string lambda interpolation with raw user_id
- User_id injection characters (quotes, semicolons) are sanitized or blocked
- 29+ combined tests pass across both test files
- No test makes actual HTTP calls (all respx-mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/19-core-features/19-03-SUMMARY.md`
</output>
