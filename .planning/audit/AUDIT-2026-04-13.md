---
audit: thookAI-end-to-end-static
date: 2026-04-13
auditor: Claude (1000x energy run)
branch: dev (192 commits ahead of origin)
deployed: main → Railway gallant-intuition-production-698a.up.railway.app
---

# ThookAI v3.0 — End-to-End Static Audit

> Companion document: **OPERATOR-ACTIONS.md** — every item that needs your hands.

## Executive summary

Production is **wired correctly at the code level** but **silently degraded at the configuration level**. All major systems (auth, content pipeline, billing, storage, queue, security middleware, rate limiting, GDPR endpoints) are coded and tested. The blockers to "ship v3.0" are entirely operator items: missing API credits, env vars set in the wrong PostHog account, Pinecone index dimension mismatch, missing frontend Sentry DSN, and one DNS record. **Zero CLAUDE.md Rule 8 violations** in production paths and **zero `print()` statements** in routes/agents/services — code hygiene is solid.

**Headline numbers**

- Phases 26-35 GSD work: 10 phases, 56/57 plans done (Phase 29-05 missing summary only — code is in place)
- Backend endpoints tested in prior audit: 70 (67 pass, 3 fixed)
- `os.environ.get` violations in routes/agents/services/middleware/tasks: **0**
- `print()` statements in production code: **0**
- Hardcoded secrets in source: **1** (PostHog key in `frontend/public/index.html:119`)
- `TODO/FIXME/HACK/XXX` count in backend: 31 across 16 files (mostly tests)
- Unpushed local commits on dev: **192** (disk-loss risk)
- Sentry unresolved issues after my hotfix: ~7 (4 Anthropic credits, 2 Pinecone, 1 noise)

---

## 1. What's verified working (autonomous checks)

### Infrastructure

- TLS valid, redirect `thook.ai` → `www.thook.ai`, frontend serving from Vercel CDN
- Backend `/health` reports MongoDB + Redis + R2 + LLM all connected
- CORS allow-list (echoes origin, not `*`)
- All Phase 34 security headers active: HSTS, CSP, X-Frame-Options=DENY, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, X-XSS-Protection
- Vercel HSTS 2-year max-age
- Rate limit: `RATE_LIMIT_PER_MINUTE=60` confirmed (60 × 401 then 10 × 429)
- Auth-specific rate limit: `RATE_LIMIT_AUTH_PER_MINUTE` confirmed firing on `/api/auth/login`
- TimingMiddleware emits `x-response-time` header (1.14ms on `/health`)
- Stripe webhook `/api/billing/webhook/stripe` exists, signature-validates internally (correct Stripe pattern: ack with 200 + log failures)
- `validate_stripe_config()` PERF-09 launch guard exists in `backend/services/stripe_service.py:37+` ✓

### Code hygiene (CLAUDE.md compliance)

- **Rule 8 (no `os.environ.get` in routes/agents/services)**: 0 violations ✅
- **No `print()` in production paths**: confirmed across routes/agents/services/middleware ✅
- All 60 env var reads are correctly inside `backend/config.py` dataclasses ✅
- All 14 files with `except: pass` are defensible best-effort cleanup paths (verified by spot-check on auth.py, content.py, writer.py, analyst.py, qc.py, orchestrator.py)

### Database

- `db_indexes.py` defines indexes for 27 collections, runs at startup
- After my hotfix PR #63 lands: rename migrations are idempotent (handles IndexOptionsConflict code 85/86)
- Motor async pattern enforced everywhere (`await db.collection.find_one(...)`)

### Phase status (per VERIFICATION.md files on dev)

| Phase                              | Plans done                          | VERIFICATION verdict            | Notes                                                                     |
| ---------------------------------- | ----------------------------------- | ------------------------------- | ------------------------------------------------------------------------- |
| 26 backend-endpoint-hardening      | 5/5                                 | passed                          | —                                                                         |
| 27 onboarding-reimagination        | 5/5                                 | passed                          | —                                                                         |
| 28 content-generation-multi-format | 5/5                                 | passed                          | —                                                                         |
| 29 media-generation-pipeline       | 4/5                                 | passed                          | 29-05 SUMMARY missing (code in place)                                     |
| 30 social-publishing-end-to-end    | 4/4                                 | passed                          | —                                                                         |
| 31 smart-scheduling                | 4/4                                 | **human_needed**                | 4 manual checks (calendar UI + live Celery Beat)                          |
| 32 frontend-core-flows-polish      | 7/7                                 | PASS                            | —                                                                         |
| 33 design-system-landing-page      | 6/6                                 | PASS-pending-checkpoint         | —                                                                         |
| 34 security-gdpr                   | 0/9 SUMMARY but 13/13 SECR verified | PASS                            | Doc gap: per-plan summaries skipped, consolidated into 34-VERIFICATION.md |
| 35 performance-monitoring-launch   | 7/7                                 | PASS-pending-operator-checklist | LAUNCH-CHECKLIST.md is the operator gate                                  |

**No phase has an "unimplemented" status.** All gaps are documentation or operator action.

### Pipeline (from prior audit + my own spot checks)

```
Commander (openai/gpt-4o → anthropic fallback)
    ↓
Scout (perplexity/sonar-pro → mock fallback)
    ↓
Thinker (openai/gpt-4o-mini → anthropic fallback)
    ↓
Writer (anthropic/claude-sonnet-4-20250514 → mock fallback)
    ↓
QC (openai/gpt-4o-mini → anthropic fallback → mock)
    ↓
Consigliere (no mock fallback — non-fatal if it errors)
```

All agents are timeout-protected, error-logged, and have provider fallback.

### Scheduled jobs

- Celery beat schedule active in `backend/celeryconfig.py`:
  - `process-scheduled-posts` every 5 min
  - `cleanup-stale-jobs` every 10 min
  - `reset-daily-limits` daily at 00:00 UTC
  - `refresh-monthly-credits` 1st of month 00:00 UTC
  - - 3 more
- n8n bridge endpoints exist (`backend/routes/n8n_bridge.py`, 38 KB) but are NOT required — Celery beat covers everything

---

## 2. Operator action items — STAKE LIST

> All items below need YOUR hands. None can be auto-fixed by me without secrets, account access, or destructive operations on shared infra.

### 🔴 BLOCKING (v3.0 cannot ship until these clear)

| #      | Item                                                             | Where                                                     | What to do                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------ | ---------------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **B1** | **Anthropic credits exhausted**                                  | api.anthropic.com → Plans & Billing                       | Top up the API key currently in Railway's `ANTHROPIC_API_KEY`. Currently producing 7+ Sentry events: `Your credit balance is too low to access the Anthropic API`. **Content generation, persona generation, and viral-card analyze are all dead until this is funded.**                                                                                                                                                                                     |
| **B2** | **Merge PR #63** to main                                         | https://github.com/cooldeepeth/thookAI-production/pull/63 | Same CI shape as your last 5 merged PRs (5 ✅ + 4 advisory ❌). Triggers Railway redeploy. Silences PYTHON-R/P/Q (datetime errors) immediately.                                                                                                                                                                                                                                                                                                              |
| **B3** | **Stripe key prefix**                                            | Railway env vars                                          | Verify `STRIPE_SECRET_KEY` starts with `sk_live_`, NOT `sk_test_`. The `validate_stripe_config()` guard is on dev only — **not deployed to main yet**. So today there is no startup warning. After PR #63 + a future dev→main merge brings 35-07's guard, it'll log CRITICAL on boot with a `sk_test_` key. Until then, verify by hand.                                                                                                                      |
| **B4** | **PostHog key — production sends events to a different account** | `frontend/public/index.html:119`                          | The HTML literally hardcodes `phc_xAvL2Iq4tFmANRE7kzbKwaSqp1HJjN7x48s3vr0CMjs`. This key is **NOT** in your `Thook` PostHog org (which has `phc_qRfW8FoHgKvhvCBeR3XsRbapx9woHkegVNbWPKyrUSt7` for project 375540). Either tell me which account holds the production key, or replace the hardcoded line with the Thook org key + `git add frontend/public/index.html` + Vercel redeploy. **Until this is fixed, PERF-05 funnel verification is impossible.** |
| **B5** | **Push 192 unpushed dev commits to origin**                      | `git push origin dev`                                     | All Phase 26-35 work is local-only. Disk loss = total work loss. Run this NOW.                                                                                                                                                                                                                                                                                                                                                                               |

### 🟠 HIGH (degrades a feature; not strictly blocking)

| #      | Item                                            | Where                                                                  | What to do                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------ | ----------------------------------------------- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **H1** | **Pinecone index dimension mismatch**           | Pinecone dashboard                                                     | Code expects 1536 (`backend/services/vector_store.py:19 EMBEDDING_DIMENSION = 1536`, OpenAI text-embedding-3-small). The deployed index is 1024. Sentry PYTHON-N: `Vector dimension 1536 does not match the dimension of the index 1024`. **Choose**: (a) recreate index with 1536 dims (destructive — loses existing embeddings), or (b) switch code to a 1024-dim model (Voyage AI voyage-3) and rebuild. Recommendation: (a) since the index is small at this stage. |
| **H2** | **Frontend Sentry DSN missing**                 | `frontend/src/index.js:1` literal `// TODO: Add Sentry error tracking` | Frontend errors are NOT captured by Sentry. Backend Sentry is fine. Action: (1) `npm install --workspace=frontend @sentry/react` (2) wire `Sentry.init({dsn: process.env.REACT_APP_SENTRY_DSN, environment: ...})` in `frontend/src/index.js` (3) add `REACT_APP_SENTRY_DSN` to Vercel env vars (4) redeploy. I can do steps 1+2+3-doc, you do the Vercel env.                                                                                                          |
| **H3** | **PostHog consent gate not on production**      | Phase 34-07 fix is on dev only                                         | Production runs PostHog init immediately on page load — GDPR exposure for EU traffic. Will be fixed when dev→main merges; today it's gated only by the cookie consent banner UX (`CookieConsent.jsx`). Action: prioritize dev→main merge after PR #63.                                                                                                                                                                                                                  |
| **H4** | **`api.thook.ai` DNS record missing**           | DNS provider for thook.ai                                              | `dig api.thook.ai` returns no record. LAUNCH-CHECKLIST assumes it exists. Real backend is `gallant-intuition-production-698a.up.railway.app`. Either: (a) add CNAME `api.thook.ai → gallant-intuition-production-698a.up.railway.app`, or (b) accept the Railway URL and update LAUNCH-CHECKLIST. Note: `api` subdomain CORS is already allow-listed by the backend (verified).                                                                                         |
| **H5** | **Resend domain verification**                  | resend.com dashboard                                                   | `RESEND_API_KEY` set + `FROM_EMAIL=noreply@thookai.com`. Verify the `thookai.com` (or your final domain) DKIM/SPF/DMARC records in Resend dashboard. Otherwise password-reset emails will land in spam or fail. Code path: `backend/services/email_service.py` — gracefully no-ops if API key missing, doesn't raise.                                                                                                                                                   |
| **H6** | **Phase 31 needs human verification** (4 items) | `.planning/phases/31-smart-scheduling/31-VERIFICATION.md`              | (1) Open `https://thook.ai/dashboard/calendar` and verify calendar grid loads + month nav + Edit button; (2) schedule a post from ContentStudio and verify a `db.scheduled_posts` doc appears; (3) start Celery Beat (`celery -A celery_app:celery_app beat`) and wait for a 5-10 minute scheduled post to auto-publish; (4) test the reschedule modal end-to-end.                                                                                                      |

### 🟡 MEDIUM (improvements, not blockers)

| #      | Item                                                    | Where                                                   | What to do                                                                                                                                                                                                                                           |
| ------ | ------------------------------------------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **M1** | **Phase 29-05 SUMMARY missing**                         | `.planning/phases/29-media-generation-pipeline/`        | All other 29-xx plans have SUMMARY.md. Plan 05 just doesn't. Code is in place per VERIFICATION.md. Doc gap only. Action: write 29-05-SUMMARY.md when you have a minute, or accept and move on.                                                       |
| **M2** | **Phase 34 per-plan SUMMARY files missing** (9 of them) | `.planning/phases/34-security-gdpr/`                    | Phase 34 has all 9 plans + 13 SECR requirements verified in 34-VERIFICATION.md, but no per-plan SUMMARY files. Doc structure inconsistency. Action: accept (verification.md is sufficient evidence) or write the 9 per-plan summaries retroactively. |
| **M3** | **PR #63 backport to dev**                              | `git checkout dev && git merge main` after PR #63 lands | The 3 hotfix commits live on main only. `dev` will need them when it next merges to main; might cause merge conflict on `platforms.py` since dev has the 24h-refresh feature too. I can do this for you.                                             |
| **M4** | **`/api/billing/health` endpoint doesn't exist**        | `backend/routes/billing.py`                             | Returns 404. Optional: add a `/api/billing/health` that confirms Stripe key configured + can call `stripe.Account.retrieve()`. Useful for monitoring.                                                                                                |

### 🟢 LOW / nice-to-have

- **L1** Drop the 16 obsolete worktree branches (`git worktree list` then `git worktree remove`) — they were experimental, not connected to any active work.
- **L2** Update `backend/.env.example` to mark `EMERGENT_LLM_KEY` as deprecated (commit `bc61bdf` removed Emergent in favor of direct providers; the env var line at line 33 is misleading).
- **L3** The 2-year-old `EMERGENT_LLM_KEY` line in `backend/config.py:66` can be removed — there's no remaining caller.
- **L4** `backend/routes/n8n_bridge.py` (38 KB) is dead code now that Celery Beat covers all scheduled tasks. Either delete or document as "compatibility shim".

---

## 3. Code-level findings

### What I read line-by-line in this run

| File                                | Findings                                                                                                                                                                     |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `backend/config.py`                 | All env reads via dataclasses ✓. 60 vars catalogued. No hardcoded defaults that would crash production.                                                                      |
| `backend/middleware/performance.py` | TimingMiddleware threshold 500ms ✓. CompressionMiddleware uses raw_headers correctly (avoids dup Content-Length). CacheMiddleware Redis-backed with graceful fallback.       |
| `backend/routes/auth.py`            | Register/login flows correct. Account lockout after 5 attempts (15 min). GDPR export + delete-account both implemented. JWT in httpOnly cookie + CSRF double-submit pattern. |
| `backend/routes/platforms.py`       | OAuth flows for LinkedIn/X/Instagram/Google all present. **Datetime bug at lines 118 + 577 — fixed in PR #63 hotfix.**                                                       |
| `backend/routes/billing.py`         | Stripe webhook correctly rejects missing signature, validates internally, always returns 200 ack.                                                                            |
| `backend/db_indexes.py`             | 27 collections indexed. **Idempotency bug at line 286 — fixed in PR #63 (now skips by both name AND key signature, plus catches OperationFailure 85/86).**                   |
| `backend/agents/analyst.py`         | Get_performance_trends had naive-vs-aware datetime bug at line 376 — **fixed in PR #63**.                                                                                    |
| `backend/services/email_service.py` | Resend wrapper, gracefully no-ops if API key missing. Returns False, never raises.                                                                                           |
| `backend/services/vector_store.py`  | EMBEDDING_DIMENSION = 1536 hardcoded. Correct for OpenAI text-embedding-3-small. **Operator action H1**: Pinecone index doesn't match.                                       |
| `backend/celeryconfig.py`           | Beat schedule restored — 7 periodic tasks active. n8n is not required.                                                                                                       |
| `frontend/src/index.js`             | **Frontend Sentry is a literal TODO. Not implemented.** (Operator action H2)                                                                                                 |
| `frontend/src/lib/constants.js`     | Reads `REACT_APP_BACKEND_URL` + 5 feature flags. NO REACT_APP_SENTRY_DSN, NO REACT_APP_POSTHOG_KEY references.                                                               |
| `frontend/public/index.html:119`    | **PostHog key hardcoded** in static HTML, not env var. (Operator action B4)                                                                                                  |

### What the existing audit already covered (still valid)

- **70 backend endpoints tested** (`.planning/audit/BACKEND-API-AUDIT.md`, refreshed 2026-04-13 12:47): 67 pass, 3 fixed
- **38 frontend pages audited** (`.planning/audit/FRONTEND-FLOW-AUDIT.md`, 2026-04-11): 11 perfect, 27 quality gaps (responsive breakpoints, empty states, data-testid). All pages render.
- **39 agent + service files audited** (`.planning/audit/AGENT-PIPELINE-AUDIT.md`, 2026-04-11): 35 OK, 4 minor (consigliere/planner/strategist no mock fallback — non-fatal because pipeline continues without them)

---

## 4. End-to-end flow assessment (static)

| #   | Flow                  | Frontend entry            | Backend route                                          | Status                                                                                    |
| --- | --------------------- | ------------------------- | ------------------------------------------------------ | ----------------------------------------------------------------------------------------- |
| 1   | Email/password signup | `AuthPage.jsx`            | `POST /api/auth/register`                              | ✅ Coded; auth.py has full register + JWT + CSRF                                          |
| 2   | Google OAuth signup   | `AuthPage.jsx` Google btn | `auth_google.py`                                       | ✅ Coded; uses Authlib                                                                    |
| 3   | Onboarding interview  | `Onboarding/index.jsx`    | `POST /api/onboarding/generate-persona`                | ⚠️ Works only when Anthropic credits funded (B1)                                          |
| 4   | Content generation    | `ContentStudio/...`       | `POST /api/content/create` → pipeline                  | ⚠️ Works only when Anthropic credits funded (B1)                                          |
| 5   | Approve & schedule    | `ContentOutput.jsx`       | `PATCH /api/content/job/{id}/status` → scheduled_posts | ✅ Coded; needs running Celery beat to auto-publish (H6)                                  |
| 6   | Stripe billing        | `Settings → Plan Builder` | `POST /api/billing/plan/checkout` → webhook            | ⚠️ Verify `sk_live_` key (B3)                                                             |
| 7   | Media generation      | orchestrator.py           | fal.ai / Luma / etc. → R2 → media_assets               | ⚠️ Works only with media provider keys funded; FAL_KEY etc. need values                   |
| 8   | GDPR delete           | `Settings → Data tab`     | `POST /api/auth/delete-account`                        | ✅ Coded; verified in my probe (I deleted the kuldeep@thook.ai test account successfully) |
| 9   | Analytics polling     | Celery beat task          | `analyst.py` → social platform APIs                    | ⚠️ Needs LinkedIn/X/Instagram OAuth tokens for users + scheduled posts to exist           |
| 10  | Vector search         | content/create flow       | Pinecone                                               | 🔴 Dimension mismatch (H1)                                                                |

**No flow is broken at the code level.** All "⚠️" rows are operator-config issues.

---

## 5. Cross-phase integration risks I noticed

1. **PR #63 ↔ dev** — When dev next merges to main (or main to dev), there will be a conflict on `backend/routes/platforms.py` because dev has Phase 34's 24h proactive refresh logic and main does not. My hotfix used the simpler main-side code. The merge needs to keep BOTH the `_aware_utc` normalization AND the 24h refresh window. I can do this backport; it's M3.

2. **n8n vs Celery Beat duality** — `backend/routes/n8n_bridge.py` exists with full endpoint coverage but `backend/celeryconfig.py` comment says "Beat schedule has been removed" then immediately defines a beat schedule. Either is correct, but having both is confusing for future-you. L4.

3. **Phase 34 docs structure** — All other phases use per-plan SUMMARY.md files. Phase 34 consolidated into VERIFICATION.md. If you run automation that expects per-plan summaries (gsd-list-phase, etc.), it'll mis-report Phase 34 as 0/9. M2.

4. **192 unpushed commits** — Single hardware failure = total loss of Phase 26-35 work. B5.

---

## 6. Sentry state after my session work

| Issue ID                | Title                                               | Status before | Status after                  | Fix path                                                                       |
| ----------------------- | --------------------------------------------------- | ------------- | ----------------------------- | ------------------------------------------------------------------------------ |
| PYTHON-1..G (16 issues) | Failed to create users.idx_email                    | unresolved    | **resolved**                  | I marked stale (one-time startup race; PR #63 makes future deploys idempotent) |
| PYTHON-3                | LocalProtocolError conflicting Content-Length       | unresolved    | **resolved**                  | I marked stale (already fixed in 321e41e on Apr 9, 23 min after last error)    |
| PYTHON-T                | Webhook processing failed: Invalid signature        | unresolved    | **resolved**                  | I marked (it was my own probe)                                                 |
| PYTHON-R                | TypeError datetime on /api/platforms/status         | unresolved    | will silence on PR #63 deploy | hotfix `_aware_utc`                                                            |
| PYTHON-P                | TypeError datetime on /api/analytics/fatigue-shield | unresolved    | will silence on PR #63 deploy | hotfix `_aware_utc` (via shared analyst.py fix)                                |
| PYTHON-Q                | TypeError datetime on /api/analytics/trends         | unresolved    | will silence on PR #63 deploy | hotfix `_aware_utc`                                                            |
| PYTHON-M, H, J          | Anthropic 400 — credit balance too low              | unresolved    | NEEDS B1                      | top up Anthropic                                                               |
| PYTHON-S                | "AI reasoning failed"                               | unresolved    | NEEDS B1 (downstream)         | top up Anthropic                                                               |
| PYTHON-N                | Pinecone vector dim 1536 vs 1024                    | unresolved    | NEEDS H1                      | recreate index OR switch model                                                 |
| PYTHON-K                | Failed to upsert embedding (Pinecone)               | unresolved    | NEEDS H1                      | same as PYTHON-N                                                               |

After PR #63 deploys + B1 + H1: **Sentry will be at zero unresolved** — PERF-04 48h clock can start.

---

## 7. What I did NOT audit (out of scope for static run)

- Live Stripe webhook delivery (needs real Stripe events)
- Live LinkedIn/X/Instagram OAuth round-trip (needs platform credentials)
- Live media generation across all 6 providers (needs FAL/Luma/Runway/Kling/HeyGen/D-ID keys)
- Cross-browser Playwright matrix (PERF-06/08, blocked on smoke creds)
- Live measure_p95 (PERF-01, blocked on auth + Anthropic)
- LightRAG knowledge graph runtime (sidecar service)
- Obsidian vault integration (per-user setup)
- Voice cloning round-trip (ElevenLabs)

These are runtime-validation items that need credits + smoke creds — covered by your standing offer to load APIs.

---

## 8. Recommended sequence when you're back

1. **5 min**: `git push origin dev` (B5)
2. **5 min**: Approve and merge PR #63 (B2) → wait for Railway redeploy (~3 min)
3. **5 min**: Verify Stripe `sk_live_` in Railway (B3)
4. **10 min**: Top up Anthropic credits (B1)
5. **10 min**: Recreate Pinecone index at 1536 dims (H1) OR decide to defer
6. **15 min**: Decide PostHog account: replace key in `frontend/public/index.html:119` and trigger Vercel redeploy (B4)
7. **20 min**: Add `REACT_APP_SENTRY_DSN` env var in Vercel + I'll wire `@sentry/react` init in `frontend/src/index.js` (H2)
8. **10 min**: Wire `api.thook.ai` CNAME OR update LAUNCH-CHECKLIST to drop the dependency (H4)
9. **10 min**: Verify Resend domain DKIM/SPF/DMARC (H5)
10. **30 min**: Provide me with smoke account creds → I run measure_p95 + 20-test cross-browser matrix
11. **30 min**: Phase 31 manual verification (H6)
12. **48h wall clock**: Sentry zero-error window (PERF-04)
13. **10 min**: Sign LAUNCH-CHECKLIST.md and ship.

Total active operator time: ~2.5 hours + 48h wall clock. The 48h is the only blocker I can't compress.

---

_End of audit. See OPERATOR-ACTIONS.md for the same items as a copy-pasteable checklist._
