---
phase: 06-media-generation-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_media_generation.py
  - backend/agents/designer.py
  - backend/agents/voice.py
  - backend/agents/video.py
  - backend/tasks/media_tasks.py
autonomous: true
requirements:
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03

must_haves:
  truths:
    - "AI image generation runs asynchronously without blocking the FastAPI event loop"
    - "Voice clone creation via ElevenLabs API accepts sample URLs and returns a voice_id"
    - "Video generation dispatches to provider APIs and returns video_url on success"
    - "All polling loops use asyncio.wait_for with explicit timeouts"
  artifacts:
    - path: "backend/tests/test_media_generation.py"
      provides: "Tests for async image gen, voice clone, video gen"
      min_lines: 150
    - path: "backend/agents/designer.py"
      provides: "Async image generation with timeout protection"
      contains: "asyncio.wait_for"
    - path: "backend/agents/voice.py"
      provides: "Voice clone creation via ElevenLabs"
      contains: "create_voice_clone"
    - path: "backend/agents/video.py"
      provides: "Video generation with provider routing"
      contains: "generate_video"
  key_links:
    - from: "backend/tasks/media_tasks.py"
      to: "backend/agents/designer.py"
      via: "Celery task delegates to designer agent"
      pattern: "generate_image"
    - from: "backend/tasks/media_tasks.py"
      to: "backend/agents/video.py"
      via: "Celery task delegates to video agent"
      pattern: "generate_video"
    - from: "backend/agents/voice.py"
      to: "database"
      via: "voice_clone_id persisted to persona_engines"
      pattern: "db.persona_engines.update_one"
---

<objective>
Verify media generation agents (image, voice, video) work correctly: async execution without blocking, proper timeout handling, voice clone lifecycle, and video provider routing.

Purpose: Confirm MEDIA-01, MEDIA-02, MEDIA-03 requirements. The code already exists but has never been tested end-to-end in the stabilization context.
Output: Test file proving all media generation paths work, with any fixes applied to the agents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/agents/designer.py
@backend/agents/voice.py
@backend/agents/video.py
@backend/tasks/media_tasks.py
@backend/services/creative_providers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test and verify async media generation (image, voice, video)</name>
  <files>backend/tests/test_media_generation.py, backend/agents/designer.py, backend/agents/voice.py, backend/agents/video.py</files>
  <read_first>
    - backend/agents/designer.py (current image generation implementation)
    - backend/agents/voice.py (current voice generation and clone implementation)
    - backend/agents/video.py (current video generation implementation)
    - backend/tasks/media_tasks.py (Celery task wrappers)
    - backend/services/creative_providers.py (provider routing)
    - backend/tests/conftest.py (existing test fixtures)
  </read_first>
  <behavior>
    - Test 1: generate_image() with mocked provider returns result dict with generated=True, image_base64 present, does NOT block (completes within asyncio.wait_for timeout)
    - Test 2: generate_image() with no providers configured returns mock result with generated=False, mock=True, message present
    - Test 3: generate_image() with provider timeout returns generated=False, error contains "timeout"
    - Test 4: generate_image() with provider failure tries fallback provider
    - Test 5: generate_carousel() produces correct number of slides (cover + content + CTA)
    - Test 6: create_voice_clone() with mocked ElevenLabs API returns voice_id and persists to persona_engines
    - Test 7: create_voice_clone() with no API key returns error dict with status="failed"
    - Test 8: delete_voice_clone() removes voice_clone_id from persona_engines
    - Test 9: generate_voice_narration() with mocked provider returns audio_base64 and duration_estimate
    - Test 10: generate_video() with mocked provider returns video_url and generated=True
    - Test 11: generate_video() with no providers configured returns generated=False with error message
    - Test 12: generate_avatar_video() routes to HeyGen or D-ID based on available keys
    - Test 13: All image generation functions use asyncio.wait_for with timeout <= 90s
    - Test 14: Video polling loops do not run synchronously (wrapped in async with timeouts)
  </behavior>
  <action>
    Create backend/tests/test_media_generation.py with comprehensive tests for MEDIA-01, MEDIA-02, MEDIA-03.

    For MEDIA-01 (async image gen, per requirement):
    - Mock the external provider calls (httpx.AsyncClient, fal_client, openai.AsyncOpenAI) using unittest.mock.AsyncMock
    - Verify generate_image() is a coroutine function
    - Verify all provider functions (_generate_openai, _generate_fal, _generate_stability, etc.) use asyncio.wait_for with timeout parameter
    - Grep designer.py for "asyncio.wait_for" — must appear at least 3 times (openai, fal, replicate)
    - Test that when a provider mock raises asyncio.TimeoutError, the result contains error="generation_timeout"

    For MEDIA-02 (voice clone, per requirement):
    - Mock httpx.AsyncClient to simulate downloading audio samples from R2 URLs
    - Mock httpx.AsyncClient.post to ElevenLabs /v1/voices/add returning {"voice_id": "test_vid"}
    - Use a mock MongoDB (from conftest.py pattern) for db.persona_engines
    - Verify create_voice_clone stores voice_clone_id in persona_engines via $set
    - Verify delete_voice_clone unsets voice_clone_id, voice_clone_name, voice_clone_created_at fields
    - Test generate_speech_with_clone falls back to default voice when no clone exists

    For MEDIA-03 (video gen, per requirement):
    - Mock video provider APIs (Runway, Luma, etc.)
    - Verify generate_video() routes to the correct provider based on get_best_available_provider
    - Verify generate_avatar_video() prefers HeyGen when heygen_api_key is set
    - Test that _generate_runway polling loop has a bounded iteration count (120 iterations)
    - Verify video.py imports use asyncio patterns (not synchronous blocking)

    Import pattern for tests:
    ```python
    import asyncio
    import pytest
    from unittest.mock import AsyncMock, MagicMock, patch
    ```

    Use pytest.mark.asyncio for all async tests. Use monkeypatch or unittest.mock.patch to mock external dependencies.

    If any agent file needs fixes (e.g., missing timeout on a polling loop), fix it directly.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_media_generation.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_media_generation.py exists with >= 12 test functions
    - grep -c "async def test_" backend/tests/test_media_generation.py returns >= 12
    - grep -c "asyncio.wait_for" backend/agents/designer.py returns >= 3
    - grep "create_voice_clone" backend/agents/voice.py returns the function definition
    - grep "generate_video" backend/agents/video.py returns the main function definition
    - All tests in test_media_generation.py pass (exit code 0)
    - No test uses time.sleep or synchronous blocking calls
  </acceptance_criteria>
  <done>All 14 test behaviors pass. Image generation confirmed async with timeouts, voice clone lifecycle verified, video generation provider routing works.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Celery media tasks dispatch correctly to agents</name>
  <files>backend/tests/test_media_generation.py, backend/tasks/media_tasks.py</files>
  <read_first>
    - backend/tasks/media_tasks.py (Celery task wrappers for media generation)
    - backend/tests/test_media_generation.py (tests created in Task 1)
    - backend/services/credits.py (CreditOperation enum)
  </read_first>
  <action>
    Add tests to backend/tests/test_media_generation.py verifying the Celery task layer:

    1. Test generate_video_for_job task:
       - Mock db.content_jobs.update_one, db.media_assets.insert_one, db.persona_engines.find_one
       - Mock agents.video.generate_video to return {"generated": True, "video_url": "https://example.com/video.mp4", "provider": "luma"}
       - Mock services.credits.deduct_credits to return {"success": True}
       - Call the inner _generate() coroutine directly (not via Celery)
       - Verify db.content_jobs.update_one called with video_status="completed" and video_url set
       - Verify db.media_assets.insert_one called with type="video" and valid url

    2. Test generate_image Celery task:
       - Mock CreativeProvidersService.generate_image to return {"success": True, "image_url": "https://example.com/img.png"}
       - Mock deduct_credits to return {"success": True}
       - Verify db.content_jobs.update_one called with $push images

    3. Test credit deduction failure:
       - Mock deduct_credits to return {"success": False, "error": "Insufficient credits"}
       - Verify the task raises an Exception containing "Insufficient credits"
       - Verify no media generation API was called

    4. Test that media_tasks.py run_async helper creates a new event loop (does not use the main loop):
       - Verify run_async function signature creates asyncio.new_event_loop()
       - grep run_async in media_tasks.py for "new_event_loop"

    Use the same mock patterns established in Task 1.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_media_generation.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend/tests/test_media_generation.py returns >= 16 (12 from Task 1 + 4 from Task 2)
    - grep "new_event_loop" backend/tasks/media_tasks.py returns a match
    - grep "CreditOperation" backend/tasks/media_tasks.py returns matches showing credit deduction
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>Celery media task layer verified: tasks dispatch to correct agents, handle credit deduction, store results in DB, and use separate event loops.</done>
</task>

</tasks>

<verification>
All media generation tests pass:
```bash
cd backend && python -m pytest tests/test_media_generation.py -x -v
```
Full test suite still passes (no regressions):
```bash
cd backend && python -m pytest --timeout=120 -x
```
</verification>

<success_criteria>
- MEDIA-01: Tests prove generate_image is async, uses asyncio.wait_for, and does not block
- MEDIA-02: Tests prove create_voice_clone round-trip works (upload -> ElevenLabs -> persist voice_id)
- MEDIA-03: Tests prove generate_video routes to providers, generate_avatar_video routes to HeyGen/D-ID
- All tests pass, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-media-generation-analytics/06-01-SUMMARY.md`
</output>
