---
phase: 06-media-generation-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_uploads_media_storage.py
  - backend/routes/uploads.py
  - backend/services/media_storage.py
autonomous: true
requirements:
  - MEDIA-04
  - MEDIA-05

must_haves:
  truths:
    - "File uploads go to Cloudflare R2 when configured and return valid public URLs"
    - "Uploading without R2 configured in production returns HTTP 503 (not a silent /tmp save)"
    - "Media assets stored in db.media_assets have public_url fields using R2 base URL"
    - "Presigned upload URL flow (generate URL -> PUT file -> confirm) produces valid media asset records"
  artifacts:
    - path: "backend/tests/test_uploads_media_storage.py"
      provides: "Tests for R2 upload, 503 fallback, media asset CRUD"
      min_lines: 120
    - path: "backend/routes/uploads.py"
      provides: "Upload endpoint with R2/503 guard"
      contains: "503"
    - path: "backend/services/media_storage.py"
      provides: "R2 presigned URL generation and media asset storage"
      contains: "generate_upload_url"
  key_links:
    - from: "backend/routes/uploads.py"
      to: "backend/services/media_storage.py"
      via: "R2 client creation and put_object"
      pattern: "_r2_client"
    - from: "backend/services/media_storage.py"
      to: "database"
      via: "media_assets collection CRUD"
      pattern: "db.media_assets"
    - from: "backend/routes/uploads.py"
      to: "config"
      via: "settings.app.is_production check"
      pattern: "is_production"
---

<objective>
Verify file uploads go to R2 with proper 503 guard in production, and media assets have valid R2 URLs stored in the database.

Purpose: Confirm MEDIA-04 and MEDIA-05 requirements. Both uploads.py and media_storage.py already have the right code paths; we need tests proving them.
Output: Test file proving upload and media storage paths work correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/uploads.py
@backend/services/media_storage.py
@backend/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test R2 upload path and production 503 guard</name>
  <files>backend/tests/test_uploads_media_storage.py, backend/routes/uploads.py</files>
  <read_first>
    - backend/routes/uploads.py (current upload implementation with R2 and /tmp fallback)
    - backend/config.py (AppConfig.is_production property, R2Config)
    - backend/tests/conftest.py (existing test fixtures and patterns)
  </read_first>
  <behavior>
    - Test 1: upload_media with R2 configured uploads file to R2 and returns URL starting with R2 public URL
    - Test 2: upload_media without R2 in production mode returns HTTP 503 with detail containing "media_storage_unavailable"
    - Test 3: upload_media without R2 in dev mode falls back to /tmp and returns local path
    - Test 4: upload_media with invalid context_type returns HTTP 400
    - Test 5: upload_media with file exceeding MAX_BYTES (100MB) returns HTTP 400 with "too large"
    - Test 6: upload_media validates MIME type against context_type (e.g., video file for "image" context returns 400)
    - Test 7: upload_url endpoint stores link documents in db.uploads with context_type="link"
  </behavior>
  <action>
    Create backend/tests/test_uploads_media_storage.py with tests for MEDIA-04 and MEDIA-05.

    Test setup:
    - Use httpx.AsyncClient with FastAPI TestClient pattern (from conftest.py)
    - Mock _r2_client() in uploads.py to return a MagicMock boto3 client (for R2-available tests) or None (for R2-unavailable tests)
    - Mock settings.app.is_production to return True (for production 503 test) or False (for dev fallback test)
    - Mock db.uploads.insert_one as AsyncMock

    For Test 1 (R2 upload):
    - Mock _r2_client() to return a mock boto3 client
    - Mock settings.r2.r2_public_url to "https://pub-test.r2.dev"
    - Mock settings.r2.r2_bucket_name to "test-bucket"
    - POST /api/uploads/media with a test file (small PNG bytes, context_type="image")
    - Assert response 200, url starts with "https://pub-test.r2.dev/"
    - Assert mock_client.put_object was called with Bucket="test-bucket"

    For Test 2 (production 503):
    - Mock _r2_client() to return None
    - Mock settings.app.is_production to return True (patch AppConfig.is_production property)
    - POST /api/uploads/media with a test file
    - Assert response 503
    - Assert response JSON contains "media_storage_unavailable"

    For Test 3 (dev /tmp fallback):
    - Mock _r2_client() to return None
    - Mock settings.app.is_production to return False
    - POST /api/uploads/media with a test file
    - Assert response 200
    - Assert url in response starts with "/tmp/thookai_uploads/"

    Verify the 503 guard code exists in uploads.py:
    - grep "503" backend/routes/uploads.py must match
    - grep "is_production" backend/routes/uploads.py must match
    - grep "media_storage_unavailable" backend/routes/uploads.py must match

    Use auth mocking: patch get_current_user to return {"user_id": "test_user_123", "email": "test@example.com"}
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_uploads_media_storage.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_uploads_media_storage.py exists with >= 7 test functions
    - grep "503" backend/routes/uploads.py returns a match
    - grep "is_production" backend/routes/uploads.py returns a match
    - grep "media_storage_unavailable" backend/routes/uploads.py returns a match
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>R2 upload path verified: files go to R2 when configured, production returns 503 without R2, dev allows /tmp fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test media_storage.py presigned URL flow and media asset DB storage</name>
  <files>backend/tests/test_uploads_media_storage.py, backend/services/media_storage.py</files>
  <read_first>
    - backend/services/media_storage.py (presigned URL generation, confirm_upload, get_user_assets, delete_asset)
    - backend/tests/test_uploads_media_storage.py (tests created in Task 1)
  </read_first>
  <behavior>
    - Test 1: generate_upload_url with R2 configured returns upload_url, storage_key, and expires_in
    - Test 2: generate_upload_url without R2 raises HTTPException with status 503
    - Test 3: generate_upload_url with invalid file_type raises HTTPException with status 400
    - Test 4: generate_upload_url with invalid content_type for file_type raises HTTPException with status 400
    - Test 5: confirm_upload saves media asset to db.media_assets with correct fields (media_id, user_id, public_url, file_type, status="uploaded")
    - Test 6: confirm_upload public_url starts with settings.r2.r2_public_url
    - Test 7: get_user_assets returns only assets belonging to the user with status="uploaded"
    - Test 8: delete_asset removes from both R2 and MongoDB
    - Test 9: delete_asset with non-existent media_id raises HTTPException 404
    - Test 10: upload_bytes_to_r2 calls put_object and returns public URL
  </behavior>
  <action>
    Add tests to backend/tests/test_uploads_media_storage.py for the media_storage.py service layer (MEDIA-05).

    Test setup:
    - Mock get_r2_client() to return a MagicMock boto3 client (or None for unavailable tests)
    - Mock settings.r2.r2_public_url to "https://pub-test.r2.dev"
    - Mock settings.r2.r2_bucket_name to "test-bucket"
    - Mock settings.r2.has_r2() to return True or False
    - Mock db.media_assets as AsyncMock with find, find_one, insert_one, delete_one, count_documents

    For confirm_upload (Test 5-6):
    - Call confirm_upload with test params: user_id="u1", storage_key="u1/image/abc/test.png", file_type="image", filename="test.png", content_type="image/png", file_size_bytes=1024
    - Assert returned dict has media_id (UUID string), public_url starting with "https://pub-test.r2.dev/", status="uploaded"
    - Assert db.media_assets.insert_one was called

    For get_user_assets (Test 7):
    - Mock db.media_assets.find to return a cursor mock with to_list returning 2 test assets
    - Mock db.media_assets.count_documents to return 2
    - Call get_user_assets(user_id="u1")
    - Assert result has assets list with 2 items and total=2

    For delete_asset (Test 8-9):
    - Mock db.media_assets.find_one to return a test asset with storage_key
    - Mock get_r2_client().delete_object (for R2 deletion)
    - Mock db.media_assets.delete_one
    - Call delete_asset(user_id="u1", media_id="test_mid")
    - Assert both R2 delete_object and DB delete_one were called
    - For non-existent: mock find_one to return None, assert HTTPException 404

    For upload_bytes_to_r2 (Test 10):
    - Mock get_r2_client().put_object
    - Call upload_bytes_to_r2(storage_key="test/key", data=b"testdata", content_type="image/png")
    - Assert returned URL starts with settings.r2.r2_public_url
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_uploads_media_storage.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" backend/tests/test_uploads_media_storage.py returns >= 15 (7 from Task 1 + 10 from Task 2, minus overlap)
    - grep "confirm_upload" backend/services/media_storage.py returns the function definition
    - grep "get_user_assets" backend/services/media_storage.py returns the function definition
    - grep "delete_asset" backend/services/media_storage.py returns the function definition
    - grep "upload_bytes_to_r2" backend/services/media_storage.py returns the function definition
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>Media storage service verified: presigned URL generation, asset confirmation with R2 URLs, user asset listing, and deletion from both R2 and DB work correctly.</done>
</task>

</tasks>

<verification>
All upload and media storage tests pass:
```bash
cd backend && python -m pytest tests/test_uploads_media_storage.py -x -v
```
Full test suite still passes (no regressions):
```bash
cd backend && python -m pytest --timeout=120 -x
```
</verification>

<success_criteria>
- MEDIA-04: Tests prove production returns 503 without R2, dev allows /tmp, R2 path works
- MEDIA-05: Tests prove media assets in DB have valid R2 public URLs, presigned flow works end-to-end
- All tests pass, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-media-generation-analytics/06-02-SUMMARY.md`
</output>
