---
phase: 21-ci-strictness-httponly-cookie-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/e2e.yml
autonomous: true
requirements: [CI-01, CI-02, CI-03]

must_haves:
  truths:
    - "A deliberate test failure in any backend test job causes the CI run to fail with a red status"
    - "A deliberate test failure in the Playwright E2E job causes the CI run to fail with a red status"
    - "No job anywhere in ci.yml or e2e.yml has continue-on-error set to true"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with all continue-on-error removed"
      contains: "backend-test-security"
    - path: ".github/workflows/e2e.yml"
      provides: "E2E workflow with continue-on-error removed"
      contains: "playwright"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "backend test jobs"
      via: "pytest exit code propagation"
      pattern: "python -m pytest"
    - from: ".github/workflows/e2e.yml"
      to: "playwright job"
      via: "npx playwright test exit code"
      pattern: "npx playwright test"
---

<objective>
Remove all CI blind spots by deleting every `continue-on-error: true` directive from both CI workflow files, ensuring that any test failure in any job causes a hard red CI status.

Purpose: CI-01 through CI-03 require that no test job can silently pass while tests are broken. Currently 3 instances in ci.yml and 1 in e2e.yml mask real failures.
Output: Updated ci.yml and e2e.yml with zero continue-on-error directives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current continue-on-error locations in ci.yml -->
Line 152: step-level continue-on-error on "Run security tests" step (backend-test-security job)
Line 224: step-level continue-on-error on "Run pipeline and agent tests" step (backend-test-pipeline job)
Line 229: job-level continue-on-error on backend-test-general job

<!-- Current continue-on-error locations in e2e.yml -->
Line 12: job-level continue-on-error on playwright job
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove all continue-on-error from CI workflows</name>
  <files>.github/workflows/ci.yml, .github/workflows/e2e.yml</files>
  <read_first>
    - .github/workflows/ci.yml (full file — identify all continue-on-error lines)
    - .github/workflows/e2e.yml (full file — identify all continue-on-error lines)
  </read_first>
  <action>
    In `.github/workflows/ci.yml`:
    1. Remove `continue-on-error: true` from the "Run security tests" step in `backend-test-security` job (per CI-01). This is at line 152, indented under the step block.
    2. Remove `continue-on-error: true` from the "Run pipeline and agent tests" step in `backend-test-pipeline` job (per CI-01). This is at line 224, indented under the step block.
    3. Remove `continue-on-error: true` from the `backend-test-general` job definition (per CI-01). This is at line 229, indented at the job level.

    In `.github/workflows/e2e.yml`:
    4. Remove `continue-on-error: true` from the `playwright` job definition (per CI-02). This is at line 12, indented at the job level.

    After edits, verify with grep that ZERO instances of `continue-on-error` remain in either file.

    Do NOT change any other aspect of the workflow files — no step reordering, no new jobs, no flag changes.
  </action>
  <verify>
    <automated>grep -r "continue-on-error" .github/workflows/ci.yml .github/workflows/e2e.yml; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "continue-on-error" .github/workflows/ci.yml` returns 0
    - `grep -c "continue-on-error" .github/workflows/e2e.yml` returns 0
  </acceptance_criteria>
  <done>Zero continue-on-error directives remain in ci.yml and e2e.yml. All 4 backend test jobs and the Playwright job will now hard-fail on any test failure (CI-01, CI-02).</done>
</task>

<task type="auto">
  <name>Task 2: Verify CI workflows are syntactically valid and tests pass locally</name>
  <files>.github/workflows/ci.yml, .github/workflows/e2e.yml</files>
  <read_first>
    - .github/workflows/ci.yml (verify YAML structure intact after edits)
    - .github/workflows/e2e.yml (verify YAML structure intact after edits)
  </read_first>
  <action>
    1. Validate both workflow files parse as valid YAML (use `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` and same for e2e.yml). If python yaml module not available, use `python3 -c "import json, yaml; ..."` or a simple syntax check.

    2. Run the backend test suite locally to confirm existing tests still pass:
       ```
       cd backend && python -m pytest tests/ -x --tb=short -q 2>&1 | tail -20
       ```
       The `-x` flag stops on first failure. If any tests fail, investigate and note them but do NOT fix test failures in this plan — the goal is CI strictness, not test fixing. If tests fail, document which ones and why in the SUMMARY.

    3. Verify the YAML edits preserved correct indentation by checking that the job structure is valid:
       - `backend-test-security` job has steps with `run:` blocks
       - `backend-test-pipeline` job has steps with `run:` blocks
       - `backend-test-general` job has steps with `run:` blocks
       - `playwright` job in e2e.yml has steps with `run:` blocks

    Per CI-03: If any tests fail locally, the CI will now correctly surface those failures as red status — which is the desired behavior. Document any failures for follow-up but do not reintroduce continue-on-error.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/e2e.yml')); print('YAML valid')"</automated>
  </verify>
  <acceptance_criteria>
    - Both YAML files parse without errors
    - `grep -c "continue-on-error" .github/workflows/ci.yml .github/workflows/e2e.yml` returns 0 for both
  </acceptance_criteria>
  <done>CI workflows are syntactically valid YAML with zero continue-on-error. Any pre-existing test failures will now correctly block CI (CI-03 achieved). Test status documented in SUMMARY.</done>
</task>

</tasks>

<verification>
- `grep -r "continue-on-error" .github/workflows/` returns empty (zero matches)
- Both workflow files parse as valid YAML
- Workflow structure unchanged except for removed continue-on-error lines
</verification>

<success_criteria>
- CI-01: All 4 backend test jobs in ci.yml will hard-fail on test failure
- CI-02: Playwright job in e2e.yml will hard-fail on test failure
- CI-03: Achieved by removing the masking — real CI state is now visible
</success_criteria>

<output>
After completion, create `.planning/phases/21-ci-strictness-httponly-cookie-auth/21-01-SUMMARY.md`
</output>
