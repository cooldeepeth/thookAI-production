---
phase: 05-publishing-scheduling-billing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_publishing.py
  - backend/tests/test_platform_oauth.py
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
must_haves:
  truths:
    - "publisher.py sends HTTP POST to the correct LinkedIn API endpoint (https://api.linkedin.com/v2/ugcPosts) with Bearer auth header"
    - "publisher.py sends HTTP POST to the correct X/Twitter API endpoint (https://api.twitter.com/2/tweets) with Bearer auth header"
    - "publisher.py sends HTTP POST to the correct Instagram API endpoint (https://graph.facebook.com/) with access_token param"
    - "Scheduled posts with past scheduled_at are picked up by process_scheduled_posts and dispatched through the publisher"
    - "Platform OAuth connect returns valid auth URLs for each platform, disconnect removes stored tokens"
    - "Expired tokens are detected by get_platform_token and return None"
  artifacts:
    - path: "backend/tests/test_publishing.py"
      provides: "Publishing dispatch and scheduled post tests"
      min_lines: 80
    - path: "backend/tests/test_platform_oauth.py"
      provides: "Platform OAuth connect/disconnect/status tests"
      min_lines: 60
  key_links:
    - from: "backend/tasks/content_tasks.py"
      to: "backend/agents/publisher.py"
      via: "_publish_to_platform calls publish_to_platform"
      pattern: "from agents.publisher import publish_to_platform"
    - from: "backend/agents/publisher.py"
      to: "httpx.AsyncClient"
      via: "Real HTTP calls to LinkedIn/X/Instagram APIs"
      pattern: "httpx.AsyncClient"
    - from: "backend/routes/platforms.py"
      to: "db.platform_tokens"
      via: "Token storage with encryption"
      pattern: "_encrypt_token"
---

<objective>
Verify and test the publishing dispatch pipeline and platform OAuth flows.

Purpose: The publisher agent code and platform OAuth routes already exist with real API implementations. This plan creates comprehensive tests to verify that (a) _publish_to_platform in content_tasks.py properly delegates to agents/publisher.py which makes real HTTP requests to the correct platform API endpoints (not simulating), (b) process_scheduled_posts picks up due posts and publishes them, and (c) platform OAuth connect/disconnect/status flows work correctly with token encryption.

Output: Two test files covering the full publishing and OAuth surface area.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tasks/content_tasks.py
@backend/agents/publisher.py
@backend/routes/platforms.py
@backend/celeryconfig.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create publishing dispatch and scheduled post tests</name>
  <files>backend/tests/test_publishing.py</files>
  <read_first>
    - backend/tasks/content_tasks.py
    - backend/agents/publisher.py
    - backend/celeryconfig.py
    - backend/tests/conftest.py
  </read_first>
  <action>
Create `backend/tests/test_publishing.py` with the following tests.

IMPORTANT: To validate PUB-01/02/03, tests must mock `httpx.AsyncClient` (NOT `agents.publisher.publish_to_platform`) so that the real publisher.py HTTP dispatch code path runs end-to-end and we can assert the correct outgoing HTTP request URL and headers. This proves the actual API integration code works, not just that a mock was called.

1. **test_publish_to_platform_linkedin_sends_correct_request**: Patch `httpx.AsyncClient` so that `client.post()` captures the URL, headers, and JSON body. Call `_publish_to_platform(platform="linkedin", content="Test post", token={"access_token": "tok_test", "user_id": "u1"})`. Assert `client.post` was called with URL containing `api.linkedin.com/v2/ugcPosts` (or the LinkedIn UGC endpoint used in publisher.py). Assert headers contain `Authorization: Bearer tok_test`. Assert returns True. Assert no "[SIMULATED]" log line.

2. **test_publish_to_platform_x_sends_correct_request**: Same httpx mock pattern. Call with `platform="x"`. Assert URL contains `api.twitter.com/2/tweets` (or the X API endpoint used in publisher.py). Assert Bearer auth header present.

3. **test_publish_to_platform_instagram_sends_correct_request**: Same pattern. Call with `platform="instagram"`. Assert URL contains `graph.facebook.com` (or the Instagram Graph API endpoint used in publisher.py). Assert access_token is included (either as header or query param, depending on publisher.py implementation).

4. **test_publish_to_platform_publisher_http_failure_returns_false**: Patch `httpx.AsyncClient` to raise `httpx.HTTPStatusError` (or return a 500 response). Call `_publish_to_platform`. Assert returns False.

5. **test_publish_to_platform_publisher_network_error_returns_false**: Patch `httpx.AsyncClient` to raise `httpx.ConnectError` or similar network exception. Assert returns False.

6. **test_process_scheduled_posts_publishes_due_posts**: Mock `db.scheduled_posts.find` to return one post with `status="scheduled"` and `scheduled_at` in the past. Mock `db.platform_tokens.find_one` to return a valid token. Mock `_publish_to_platform` to return True (at this level, integration is already tested above). Call `process_scheduled_posts` (the async inner function). Assert the post status was updated to "published".

7. **test_process_scheduled_posts_fails_without_token**: Mock a due post but no platform token. Assert post status updated to "failed" with error "Platform not connected".

Import `_publish_to_platform` from `tasks.content_tasks`. Use `pytest.mark.asyncio` for async tests. Use `unittest.mock.AsyncMock` and `unittest.mock.patch` for mocking.

Before writing tests, read `backend/agents/publisher.py` carefully to identify the exact API endpoint URLs and HTTP call patterns used for each platform, then write assertions against those specific URLs.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_publishing.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_publishing.py` exists
    - Contains at least 7 test functions: `grep -c "async def test_" backend/tests/test_publishing.py` returns >= 7
    - All tests pass: `pytest tests/test_publishing.py -x` exits with code 0
    - Tests mock httpx, not the publisher function: `grep "httpx" backend/tests/test_publishing.py` returns matches
    - Tests assert correct API URLs: `grep "linkedin.com\|twitter.com\|graph.facebook" backend/tests/test_publishing.py` returns matches
    - Tests import from `tasks.content_tasks`: `grep "from tasks.content_tasks import" backend/tests/test_publishing.py`
  </acceptance_criteria>
  <done>7+ publishing tests pass, verifying real HTTP dispatch to correct LinkedIn/X/Instagram API endpoints with auth headers, error handling for HTTP and network failures, scheduled post processing, and no simulation in production path</done>
</task>

<task type="auto">
  <name>Task 2: Create platform OAuth connect/disconnect/status tests</name>
  <files>backend/tests/test_platform_oauth.py</files>
  <read_first>
    - backend/routes/platforms.py
    - backend/tests/conftest.py
  </read_first>
  <action>
Create `backend/tests/test_platform_oauth.py` with the following tests:

1. **test_platforms_status_returns_all_three**: GET `/api/platforms/status` with auth. Assert response has `platforms` dict with keys `linkedin`, `x`, `instagram`. Assert each has `connected` and `configured` fields.

2. **test_connect_linkedin_returns_auth_url**: GET `/api/platforms/connect/linkedin` with auth. Patch `LINKEDIN_CLIENT_ID` to a valid non-placeholder value (e.g., "test_client_id"). Assert response contains `auth_url` starting with `https://www.linkedin.com/oauth/v2/authorization`. Assert `state` is returned.

3. **test_connect_x_returns_auth_url**: GET `/api/platforms/connect/x` with auth. Patch `TWITTER_API_KEY` to "test_api_key". Assert response contains `auth_url` starting with `https://twitter.com/i/oauth2/authorize`. Assert PKCE challenge params present in URL.

4. **test_connect_instagram_returns_auth_url**: Similar for Instagram with META_APP_ID.

5. **test_disconnect_platform_removes_token**: First insert a mock token doc into `db.platform_tokens` for the test user. DELETE `/api/platforms/disconnect/linkedin`. Assert 200. Assert `db.platform_tokens.find_one` returns None for that user+platform.

6. **test_disconnect_nonexistent_returns_404**: DELETE `/api/platforms/disconnect/linkedin` when no token exists. Assert 404.

7. **test_get_platform_token_decrypts**: Import `get_platform_token`, `_encrypt_token` from `routes.platforms`. Insert a token doc with `access_token=_encrypt_token("real_token")` and non-expired `expires_at`. Call `await get_platform_token(user_id, "linkedin")`. Assert returns "real_token".

8. **test_get_platform_token_expired_returns_none**: Insert token with `expires_at` in the past and no `refresh_token`. Assert `get_platform_token` returns None. (Moved here from Task 1 because expiry enforcement lives in get_platform_token, not _publish_to_platform.)

9. **test_get_platform_token_expired_attempts_refresh**: Insert token with `expires_at` in the past but WITH a `refresh_token`. Mock the OAuth refresh HTTP call. Assert `get_platform_token` attempts to refresh the token before returning None.

Use the FastAPI TestClient or httpx.AsyncClient with the app. For auth, create a test JWT token or mock `get_current_user` dependency. Follow patterns in existing test files (read conftest.py for fixtures).
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_platform_oauth.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_platform_oauth.py` exists
    - Contains at least 8 test functions: `grep -c "def test_" backend/tests/test_platform_oauth.py` returns >= 8
    - All tests pass: `pytest tests/test_platform_oauth.py -x` exits with code 0
    - Tests verify token encryption: `grep "_encrypt_token\|_decrypt_token" backend/tests/test_platform_oauth.py`
    - Tests cover disconnect: `grep "disconnect" backend/tests/test_platform_oauth.py`
    - Tests cover expired token: `grep "expired" backend/tests/test_platform_oauth.py`
  </acceptance_criteria>
  <done>8+ platform OAuth tests pass, verifying connect URL generation for all 3 platforms, token encryption/decryption, disconnect flow, expired token detection, and token refresh attempt</done>
</task>

</tasks>

<verification>
```bash
cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_publishing.py tests/test_platform_oauth.py -v
```
All tests pass. Publishing tests verify actual HTTP dispatch to correct API endpoints (not just mock delegation). No test uses "[SIMULATED]" as expected behavior.
</verification>

<success_criteria>
- 15+ tests across 2 test files all pass
- Publishing dispatch verified at HTTP level: tests mock httpx.AsyncClient and assert correct API endpoint URLs and auth headers for LinkedIn, X, and Instagram
- Scheduled post processing verified: process_scheduled_posts picks up due posts and publishes
- OAuth flows verified: connect returns correct auth URLs, disconnect removes tokens, token encryption works
- Expired token handling verified in get_platform_token (not _publish_to_platform)
- No simulation in production path
</success_criteria>

<output>
After completion, create `.planning/phases/05-publishing-scheduling-billing/05-01-SUMMARY.md`
</output>
