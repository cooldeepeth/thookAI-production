---
phase: 04-content-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_pipeline_integration.py
  - backend/agents/thinker.py
  - backend/agents/writer.py
  - backend/agents/orchestrator.py
autonomous: true
requirements: [PIPE-02, PIPE-03, PIPE-04, PIPE-05]

must_haves:
  truths:
    - "LangGraph orchestrator builds and compiles without errors"
    - "UOM directives are fetched and injected into Thinker and Writer prompts"
    - "Fatigue shield data flows from persona_refinement into Thinker prompt as avoidance instructions"
    - "Writer queries Pinecone for similar past content and injects style examples into its prompt"
    - "When orchestrator is unavailable, legacy pipeline runs as fallback"
  artifacts:
    - path: "backend/tests/test_pipeline_integration.py"
      provides: "Unit tests proving PIPE-02 through PIPE-05 requirements"
      min_lines: 150
  key_links:
    - from: "backend/agents/pipeline.py"
      to: "backend/agents/orchestrator.py"
      via: "import run_orchestrated_pipeline with ImportError fallback"
      pattern: "from agents.orchestrator import run_orchestrated_pipeline"
    - from: "backend/agents/thinker.py"
      to: "backend/services/uom_service.py"
      via: "get_agent_directives call"
      pattern: "get_agent_directives.*thinker"
    - from: "backend/agents/thinker.py"
      to: "backend/services/persona_refinement.py"
      via: "fatigue_context parameter with _build_fatigue_prompt_section"
      pattern: "_build_fatigue_prompt_section"
    - from: "backend/agents/writer.py"
      to: "backend/services/vector_store.py"
      via: "_fetch_style_examples calls query_similar_content"
      pattern: "query_similar_content"
    - from: "backend/agents/learning.py"
      to: "backend/services/vector_store.py"
      via: "upsert_approved_embedding on content approval"
      pattern: "upsert_approved_embedding"
---

<objective>
Verify and test the content pipeline's advanced features: LangGraph orchestrator compilation, UOM directive injection, fatigue shield integration, and Pinecone vector store enrichment.

Purpose: All four subsystems (orchestrator, UOM, fatigue, vector store) have code in place but zero test coverage. Without tests, we cannot confirm they wire correctly. This plan creates focused unit tests that prove each integration point works, and fixes any issues found.

Output: `backend/tests/test_pipeline_integration.py` with tests covering PIPE-02 through PIPE-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/agents/pipeline.py
@backend/agents/orchestrator.py
@backend/agents/thinker.py
@backend/agents/writer.py
@backend/services/uom_service.py
@backend/services/vector_store.py
@backend/services/persona_refinement.py
@backend/agents/learning.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pipeline integration tests for orchestrator, UOM, fatigue, and vector store</name>
  <files>backend/tests/test_pipeline_integration.py</files>
  <read_first>
    backend/agents/pipeline.py
    backend/agents/orchestrator.py
    backend/agents/thinker.py
    backend/agents/writer.py
    backend/services/uom_service.py
    backend/services/vector_store.py
    backend/services/persona_refinement.py
    backend/agents/learning.py
  </read_first>
  <behavior>
    PIPE-02 Tests (LangGraph orchestrator):
    - Test 1: `build_content_pipeline()` returns a compiled graph object (mock langgraph imports if needed)
    - Test 2: `_run_agent_pipeline_inner` catches ImportError from orchestrator and falls back to legacy `run_agent_pipeline_legacy`
    - Test 3: Orchestrator `PipelineState` TypedDict has all required keys (job_id, user_id, platform, etc.)
    - Test 4: `quality_gate()` returns "pass" when `overall_pass=True`, "rewrite" when loop_count < MAX_QC_LOOPS, "accept" when loops exhausted
    - Test 5: `should_research()` returns True when commander output has `research_needed=True`, False otherwise

    PIPE-03 Tests (UOM directives):
    - Test 6: `run_thinker()` calls `get_agent_directives(user_id, "thinker")` and appends UOM CONSTRAINTS section to prompt when directives are returned
    - Test 7: `run_writer()` calls `get_agent_directives(user_id, "writer")` and appends ADAPTIVE STYLE DIRECTIVES section to prompt when directives are returned
    - Test 8: When `get_agent_directives` raises an exception, thinker/writer continue without UOM (non-fatal)

    PIPE-04 Tests (Fatigue shield):
    - Test 9: `_build_fatigue_prompt_section()` returns empty string when `shield_status="healthy"`
    - Test 10: `_build_fatigue_prompt_section()` returns constraint text including risk factor details when `shield_status="warning"` with risk_factors
    - Test 11: `run_thinker()` injects fatigue section into prompt when fatigue_context has non-healthy status
    - Test 12: In legacy pipeline, `get_pattern_fatigue_shield` is called before Thinker and result is passed as `fatigue_context`

    PIPE-05 Tests (Vector store / Pinecone):
    - Test 13: `_fetch_style_examples()` calls `query_similar_content(user_id, raw_input, top_k=3, similarity_threshold=0.65)` and returns formatted section
    - Test 14: `_fetch_style_examples()` returns empty string when vector store returns no results
    - Test 15: `_fetch_style_examples()` returns empty string and logs warning when vector store raises exception (non-fatal)
    - Test 16: `run_writer()` includes style_examples_section in prompt when vector store returns results
    - Test 17: In `learning.py`, `record_approval()` calls `upsert_approved_embedding()` with correct user_id, content_text, and content_id
  </behavior>
  <action>
    Create `backend/tests/test_pipeline_integration.py` with pytest and unittest.mock (AsyncMock, patch).

    Test structure:
    1. Import the functions under test directly from their modules
    2. Use `@pytest.mark.asyncio` for async tests
    3. Mock external dependencies (LlmChat, db, vector_store, uom_service, persona_refinement)
    4. For orchestrator tests: mock `langgraph.graph` to test `build_content_pipeline` and test `quality_gate`/`should_research` directly as pure functions
    5. For UOM tests: patch `services.uom_service.get_agent_directives` to return test directives like `{"risk_level": "low", "hook_complexity": "advanced", "max_options": 3}` for thinker, and `{"tone_intensity": "bold", "vocabulary_depth": "advanced", "content_length": "long", "emotional_energy": "high", "cta_aggressiveness": "strong"}` for writer
    6. For fatigue tests: call `_build_fatigue_prompt_section()` directly with test data
    7. For vector store tests: patch `services.vector_store.query_similar_content` to return test results with metadata including `content_preview` and `platform`
    8. For learning tests: patch `services.vector_store.upsert_approved_embedding` and verify it's called in `record_approval()`

    Key mocking patterns (matching existing test conventions in this repo):
    - `@patch("agents.thinker.LlmChat")` for LLM calls
    - `@patch("agents.thinker.chat_constructor_key", return_value="fake-key")`
    - `@patch("agents.thinker.openai_available", return_value=True)`
    - `AsyncMock` for all async functions

    For the orchestrator graph compilation test, since langgraph may not be installed locally, use a conditional skip:
    ```python
    pytest.importorskip("langgraph", reason="langgraph not installed")
    ```

    For pure function tests (quality_gate, should_research, _build_fatigue_prompt_section), test directly without mocks.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest tests/test_pipeline_integration.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_pipeline_integration.py` exists with at least 15 test functions
    - `grep -c "def test_" backend/tests/test_pipeline_integration.py` returns at least 15
    - `grep "quality_gate" backend/tests/test_pipeline_integration.py` finds tests for orchestrator quality gate
    - `grep "get_agent_directives" backend/tests/test_pipeline_integration.py` finds tests for UOM injection
    - `grep "_build_fatigue_prompt_section" backend/tests/test_pipeline_integration.py` finds tests for fatigue shield
    - `grep "query_similar_content" backend/tests/test_pipeline_integration.py` finds tests for vector store
    - `grep "upsert_approved_embedding" backend/tests/test_pipeline_integration.py` finds tests for learning embedding
    - All tests pass: `pytest tests/test_pipeline_integration.py` exits 0
  </acceptance_criteria>
  <done>15+ tests pass covering orchestrator logic, UOM injection, fatigue shield prompt building, vector store retrieval, and learning embedding storage</done>
</task>

<task type="auto">
  <name>Task 2: Fix any integration issues discovered by tests</name>
  <files>backend/agents/thinker.py, backend/agents/writer.py, backend/agents/orchestrator.py</files>
  <read_first>
    backend/tests/test_pipeline_integration.py
    backend/agents/thinker.py
    backend/agents/writer.py
    backend/agents/orchestrator.py
  </read_first>
  <action>
    After Task 1 tests are written and run, review any failures and fix the underlying code issues. Based on code review, anticipated fixes:

    1. **Thinker UOM logging**: Verify `logger.info("UOM constraints injected into Thinker prompt")` fires when directives are present. If the `openai_available()` check returns False before UOM can be injected, ensure the mock path also receives UOM data (currently `_mock_thinker` ignores UOM — this is acceptable behavior but should be documented in test).

    2. **Writer style examples**: `_fetch_style_examples()` in writer.py uses `commander_output.get("raw_input", ...)` to build the query text for vector store. If commander_output doesn't have `raw_input` (it's the user's raw input, not a commander field), the fallback is `primary_angle`. Verify this fallback path works. The actual raw_input should be passed through — check if `run_writer` receives it (it currently doesn't receive `raw_input` directly; it reconstructs from commander_output which may not have it). If `raw_input` is missing from commander_output, the vector store query may use the primary_angle instead, which is acceptable but less precise.

    3. **Orchestrator fallback in pipeline.py**: The `_run_agent_pipeline_inner` function catches `ImportError` for the orchestrator. Verify this works by checking the actual import path: `from agents.orchestrator import run_orchestrated_pipeline`. The test should confirm fallback happens correctly.

    If all tests pass on first run with no fixes needed, this task documents the verification in a brief comment at the top of the test file confirming all integration points are wired correctly.

    Do NOT change the pipeline's core behavior. Only fix genuine bugs where the wiring is broken (e.g., wrong variable names, missing parameters). If something is working as designed, leave it.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python3 -m pytest tests/test_pipeline_integration.py tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All tests in `test_pipeline_integration.py` pass (exit code 0)
    - All existing tests still pass (no regressions): `pytest tests/` exits 0
    - If fixes were made to thinker.py, writer.py, or orchestrator.py, each file still contains the key integration calls:
      - `grep "get_agent_directives" backend/agents/thinker.py` returns a match
      - `grep "get_agent_directives" backend/agents/writer.py` returns a match
      - `grep "_build_fatigue_prompt_section" backend/agents/thinker.py` returns a match
      - `grep "_fetch_style_examples" backend/agents/writer.py` returns a match
  </acceptance_criteria>
  <done>All pipeline integration tests pass with no regressions in the existing 165-test suite</done>
</task>

</tasks>

<verification>
- `cd backend && python3 -m pytest tests/test_pipeline_integration.py -v` — all 15+ tests pass
- `cd backend && python3 -m pytest tests/ -v` — all 165+ tests pass (no regressions)
- `grep -c "def test_" backend/tests/test_pipeline_integration.py` returns >= 15
</verification>

<success_criteria>
- PIPE-02: Tests verify orchestrator graph builds, quality_gate routes correctly, and fallback to legacy works
- PIPE-03: Tests verify UOM directives are fetched and injected into Thinker and Writer prompts
- PIPE-04: Tests verify fatigue shield constraints are built and injected into Thinker prompt
- PIPE-05: Tests verify Writer calls vector store for style examples and Learning stores embeddings on approval
- All tests pass, zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pipeline/04-01-SUMMARY.md`
</output>
