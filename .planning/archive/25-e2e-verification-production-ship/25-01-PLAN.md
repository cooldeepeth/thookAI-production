---
phase: 25-e2e-verification-production-ship
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx
  - backend/.env.example
autonomous: true
requirements: [SHIP-04, SHIP-06]

must_haves:
  truths:
    - "grep -rn 'console.log' frontend/src/ --include='*.js' --include='*.jsx' (excluding test files) returns zero results"
    - "Every variable in backend/.env.example has an inline comment explaining purpose and required/optional status"
    - "No hardcoded secret strings (API key patterns) appear in any non-test Python or JS file"
  artifacts:
    - path: "frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx"
      provides: "ContentOutput with console.log removed"
      contains: "onPublished={() => {}"
    - path: "backend/.env.example"
      provides: "Fully documented env reference"
      contains: "# Required"
  key_links:
    - from: "backend/.env.example"
      to: "backend/config.py"
      via: "every variable in .env.example maps to a field in config.py"
      pattern: "SHIP-04 compliance"
---

<objective>
Remove the two production console.log statements from ContentOutput.jsx and complete
.env.example documentation so every variable has an inline required/optional comment.

Purpose: Clears the SHIP-06 console.log finding and satisfies SHIP-04 before the ship
checklist is assembled in Plan 02. These are the only two tasks that modify production
source files in Phase 25.

Output: Clean ContentOutput.jsx (no console.log), fully annotated .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Key interface: the two console.log lines to remove -->
<!-- frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx -->
<!-- Line 480: console.log("Media updated:", media); -->
<!-- Line 593: <PublishPanel job={job} onPublished={() => console.log("Published/Scheduled")} /> -->

<!-- The .env.example already has most vars; the task is to add # Required / # Optional -->
<!-- inline comments to any variable that currently has no description comment.       -->
<!-- All n8n workflow IDs, Stripe Price IDs, and optional provider keys need marking. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove console.log from ContentOutput.jsx</name>
  <files>frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx</files>
  <read_first>frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx</read_first>
  <action>
    Two console.log calls must be removed (per SHIP-06):

    1. Line ~480: `console.log("Media updated:", media);`
       Remove this line entirely. It is inside a handler function; the function
       body should simply remain without the debug log.

    2. Line ~593: `onPublished={() => console.log("Published/Scheduled")}`
       Replace with `onPublished={() => {}}` — a no-op callback.
       Do NOT remove the onPublished prop; PublishPanel expects it.

    No other changes to this file. Do not reformat surrounding code.
  </action>
  <verify>
    <automated>grep -n "console\.log" frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    The grep above must output "CLEAN" (no matches).
    File must still parse: node --input-type=module --eval "import './frontend/src/pages/Dashboard/ContentStudio/ContentOutput.jsx'" should not throw syntax errors (or check via: cd frontend && npx react-scripts build --no-stats 2>&1 | grep -E "ERROR|SyntaxError" | head -5 || echo "OK").
  </acceptance_criteria>
  <done>ContentOutput.jsx contains zero console.log statements; the file is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate all undocumented vars in .env.example</name>
  <files>backend/.env.example</files>
  <read_first>backend/.env.example</read_first>
  <action>
    Read backend/.env.example top-to-bottom. For every variable that lacks a trailing
    inline comment explaining its purpose and required/optional status, add one.

    Format rule: `VAR_NAME=value     # [Required|Optional] — one-sentence description`

    Variables that already have a comment: leave them unchanged.

    Variables requiring annotation (currently blank or no inline comment):
    - All `STRIPE_PRICE_*` vars: `# Required for paid plan checkout (create in Stripe Dashboard > Products)`
    - `STRIPE_WEBHOOK_SECRET`: `# Required — validates Stripe webhook payloads (Stripe Dashboard > Webhooks)`
    - `RESEND_API_KEY`: `# Required for transactional email (password reset, agency invites) — resend.com/api-keys`
    - `FROM_EMAIL`: `# Required — sender address for all outgoing emails`
    - `N8N_API_KEY`: `# Optional — n8n API key for programmatic workflow management`
    - All `N8N_WORKFLOW_*` vars: `# Optional — n8n workflow ID, populated after creating the workflow in n8n UI`
    - `LIGHTRAG_API_KEY`: `# Optional — set if LightRAG auth is enabled on the sidecar`
    - `OBSIDIAN_BASE_URL`: `# Optional — Cloudflare Tunnel or ngrok URL for local Obsidian vault`
    - `OBSIDIAN_API_KEY`: `# Optional — Bearer token from Obsidian Local REST API plugin settings`
    - All `LINKEDIN_*`, `META_*`, `TWITTER_*` OAuth vars: `# Optional — required only if social platform publishing is enabled`
    - All video provider keys (`LUMA_API_KEY`, `KLING_API_KEY`, `RUNWAY_API_KEY`, `PIKA_API_KEY`,
      `HEYGEN_API_KEY`, `DID_API_KEY`): `# Optional — feature-gated video generation provider`
    - All voice provider keys (`ELEVENLABS_API_KEY`, `PLAYHT_API_KEY`, `PLAYHT_USER_ID`,
      `GOOGLE_TTS_API_KEY`): `# Optional — feature-gated voice/TTS provider`
    - `SENTRY_DSN`: `# Optional — Sentry error tracking DSN (sentry.io/settings/projects/)`
    - `FAL_KEY`: `# Optional — fal.ai image generation (fal.ai/dashboard); required for AI image features`

    Keep section headers (# ===== APP ===== etc.) unchanged.
    Do NOT reorder variables.
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
lines = open('backend/.env.example').readlines()
undocumented = [l.strip() for l in lines if re.match(r'^[A-Z][A-Z0-9_]+=', l) and '#' not in l]
if undocumented:
    print('MISSING DOCS:', undocumented[:5])
    sys.exit(1)
print('ALL DOCUMENTED')
"</automated>
  </verify>
  <acceptance_criteria>
    The Python check above prints "ALL DOCUMENTED". Every line matching `^[A-Z][A-Z0-9_]+=`
    must contain a `#` character (inline comment).
  </acceptance_criteria>
  <done>backend/.env.example has no undocumented variable lines; every var has Required or Optional annotation.</done>
</task>

</tasks>

<verification>
Run both automated checks:
1. `grep -rn "console\.log" frontend/src/ --include="*.js" --include="*.jsx" | grep -v "__tests__" | grep -v "\.test\."` → zero output
2. Python undocumented var check from Task 2 → "ALL DOCUMENTED"
3. `grep -rn "sk-ant-\|sk-proj-\|AIza\|AKIA" backend/ --include="*.py" | grep -v "__pycache__" | grep -v "tests/"` → zero results (confirm no hardcoded secrets)
</verification>

<success_criteria>
- ContentOutput.jsx: zero console.log statements
- backend/.env.example: every variable annotated with Required/Optional inline comment
- No hardcoded API key patterns in production Python files (confirmed via grep)
- SHIP-04 and SHIP-06 console.log sub-requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/25-e2e-verification-production-ship/25-01-SUMMARY.md`
</output>
