---
phase: 23-frontend-unit-test-suite
plan: "03"
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - frontend/src/__tests__/components/NotificationBell.test.jsx
  - frontend/src/__tests__/pages/StrategyDashboard.test.jsx
  - frontend/src/__tests__/pages/ContentStudio.test.jsx
  - frontend/src/__tests__/pages/Sidebar.test.jsx
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "StrategyDashboard renders strategy cards from mocked API and approve/dismiss buttons are interactive"
    - "ContentStudio renders the input panel and generate button with mocked API for job creation"
    - "Sidebar renders nav links and shows the logged-in user's credit balance from mocked /api/billing/credits"
    - "NotificationBell renders the bell button and shows dropdown with notifications from mocked API"
    - "frontend-test CI job in .github/workflows/ci.yml runs on every push/PR and blocks merge on failure"
  artifacts:
    - path: "frontend/src/__tests__/components/NotificationBell.test.jsx"
      provides: "8 behavior tests for NotificationBell component"
      min_lines: 60
    - path: "frontend/src/__tests__/pages/StrategyDashboard.test.jsx"
      provides: "8 behavior tests for StrategyDashboard"
      min_lines: 60
    - path: "frontend/src/__tests__/pages/ContentStudio.test.jsx"
      provides: "8 behavior tests for ContentStudio"
      min_lines: 60
    - path: "frontend/src/__tests__/pages/Sidebar.test.jsx"
      provides: "6 behavior tests for Sidebar"
      min_lines: 50
    - path: ".github/workflows/ci.yml"
      provides: "frontend-test job running `npx craco test --watchAll=false`"
      contains: "frontend-test"
  key_links:
    - from: "frontend/src/__tests__/components/NotificationBell.test.jsx"
      to: "frontend/src/hooks/useNotifications.js"
      via: "MSW handler override for /api/notifications"
      pattern: "server\\.use"
    - from: ".github/workflows/ci.yml"
      to: "frontend/ test command"
      via: "npm test -- --watchAll=false"
      pattern: "frontend-test"
---

<objective>
Write 30 component/page tests for StrategyDashboard, ContentStudio, Sidebar, and NotificationBell — all behavior-verified (no snapshot-only tests). Then add the `frontend-test` CI job to `.github/workflows/ci.yml` that runs these tests on every push and PR.

Purpose: Satisfy TEST-03 (30 more tests bringing total to 78+), TEST-04 (CI job), TEST-05 (component coverage).
Output: 4 test files + CI job update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-frontend-unit-test-suite/23-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From frontend/src/components/NotificationBell.jsx:
- data-testid="notifications-btn" on the bell button
- data-testid="sidebar-backdrop" on the overlay div in Sidebar.jsx
- data-testid="sidebar" on the aside element
- Renders NotificationBell which internally uses useNotifications hook (fetches /api/notifications + /api/notifications/count)
- Shows unreadCount badge when unreadCount > 0
- Opens/closes dropdown on bell click
- Renders displayNotifications.slice(0,10) list items

From frontend/src/pages/Dashboard/StrategyDashboard.jsx:
- Uses useStrategyFeed hook (fetches /api/strategy?status=pending_approval and history)
- Uses useNotifications hook
- Renders SkeletonCard when loading=true
- Renders ActiveCard for each card in activeCards
- Approve button: data attribute or role "button" with text "Approve" or "Generate"
- Dismiss button: role "button" with text "Dismiss"
- Requires react-router-dom (useNavigate) and framer-motion

From frontend/src/pages/Dashboard/ContentStudio/index.jsx:
- Platform selector buttons for linkedin, x, instagram
- Text area for rawInput
- Generate/submit button
- Polls /api/content/job/:id
- Requires react-router-dom (useSearchParams, useNavigate)

From frontend/src/pages/Dashboard/Sidebar.jsx:
- data-testid="sidebar" on <aside>
- data-testid="sidebar-backdrop" on overlay
- Uses useAuth() from AuthContext
- Fetches /api/billing/credits on mount
- Shows user tier badge
- Nav links rendered with NavLink from react-router-dom

Router/Context wrapper needed for all page tests:
```jsx
import { MemoryRouter } from 'react-router-dom';
import { AuthProvider } from '@/context/AuthContext';

function TestWrapper({ children }) {
  return (
    <MemoryRouter>
      <AuthProvider>{children}</AuthProvider>
    </MemoryRouter>
  );
}
```

For components using framer-motion — mock it to prevent animation issues in jsdom:
```js
jest.mock('framer-motion', () => ({
  motion: {
    div: ({ children, ...props }) => <div {...props}>{children}</div>,
    span: ({ children, ...props }) => <span {...props}>{children}</span>,
  },
  AnimatePresence: ({ children }) => <>{children}</>,
}));
```

For components using lucide-react icons — they work as-is in jsdom.

For MSW EventSource mock in tests that use useNotifications (StrategyDashboard uses it):
```js
global.EventSource = jest.fn(() => ({
  addEventListener: jest.fn(),
  close: jest.fn(),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationBell and Sidebar component tests (14 tests)</name>
  <files>
    frontend/src/__tests__/components/NotificationBell.test.jsx,
    frontend/src/__tests__/pages/Sidebar.test.jsx
  </files>
  <behavior>
    NotificationBell (8 tests):
    - renders_bell_button: bell button with data-testid="notifications-btn" is in the document
    - no_badge_when_zero: when unreadCount=0, no badge/count element is visible
    - badge_shows_count: when /api/notifications/count returns unread_count=3, badge shows "3"
    - dropdown_opens_on_click: clicking notifications-btn renders the notification dropdown
    - dropdown_closes_on_second_click: clicking notifications-btn again closes the dropdown
    - empty_state_shown: when notifications=[], dropdown shows "No notifications" or similar empty message
    - notification_item_rendered: when API returns 1 notification with message "Job completed", that text appears in dropdown
    - mark_all_read_button: "Mark all read" button triggers POST /api/notifications/read-all

    Sidebar (6 tests):
    - renders_sidebar: element with data-testid="sidebar" is in document
    - nav_links_rendered: at least 5 nav link labels are visible (Dashboard, Strategy, Content Studio, etc.)
    - credits_displayed: after /api/billing/credits resolves with credits=80, a number "80" appears in sidebar
    - logout_button_present: button or link with text "Logout" or "Log out" is visible
    - mobile_backdrop: when isOpen=true, data-testid="sidebar-backdrop" is in document
    - no_backdrop_when_closed: when isOpen=false, data-testid="sidebar-backdrop" is NOT in document
  </behavior>
  <action>
    Create `frontend/src/__tests__/components/NotificationBell.test.jsx`:
    - Mock EventSource globally at top of file (before imports)
    - Use TestWrapper (MemoryRouter + AuthProvider) since NotificationBell uses hooks that call apiFetch
    - For badge test: use `server.use()` to override `/api/notifications/count` to return `{ unread_count: 3 }`
    - For notification item test: use `server.use()` to override `/api/notifications` to return a notification with `message: 'Job completed'`
    - Click interactions: use `userEvent.setup()` from `@testing-library/user-event`
    - Do NOT test internal hook implementation — test only rendered output and interactions

    Create `frontend/src/__tests__/pages/Sidebar.test.jsx`:
    - Render `<Sidebar isOpen={false} onClose={jest.fn()} />` inside TestWrapper
    - For mobile backdrop test: render with `isOpen={true}`
    - Use `server.use()` to set `/api/auth/me` to return a valid user so AuthProvider populates user
    - For credits test: `await waitFor(() => expect(screen.getByText('80')).toBeInTheDocument())`
    - Import `userEvent` for logout button click

    Both files must import `server` from `@/mocks/server` and use `server.use()` for per-test overrides.
    Both use `import { http, HttpResponse } from 'msw'`.
    No snapshot tests (`toMatchSnapshot` is forbidden in this phase).
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/frontend && CI=true npx craco test --watchAll=false --testPathPattern="__tests__/(components|pages/Sidebar)" 2>&1 | grep -E "Tests:|PASS|FAIL"</automated>
  </verify>
  <done>14 tests pass. "Tests: 14 passed" in output. No snapshot assertions. No real network calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StrategyDashboard and ContentStudio tests + CI job (16 tests + CI)</name>
  <files>
    frontend/src/__tests__/pages/StrategyDashboard.test.jsx,
    frontend/src/__tests__/pages/ContentStudio.test.jsx,
    .github/workflows/ci.yml
  </files>
  <behavior>
    StrategyDashboard (8 tests):
    - renders_without_crash: component mounts without throwing
    - loading_skeleton_shown: while API is in-flight, loading skeleton is rendered
    - empty_state_shown: when activeCards=[], empty state message or "no recommendations" text is shown
    - cards_rendered: when API returns 2 active cards, 2 card elements are rendered
    - card_shows_platform: card with platform='linkedin' shows LinkedIn platform icon or label
    - approve_button_present: each active card has an interactive approve-type button
    - dismiss_button_present: each active card has a dismiss-type button
    - approve_calls_api: clicking approve on a card triggers POST /api/strategy/:id/approve

    ContentStudio (8 tests):
    - renders_without_crash: component mounts without throwing
    - platform_selector_present: linkedin/x/instagram platform buttons are rendered
    - default_platform_linkedin: "LinkedIn" platform is selected/active by default
    - input_textarea_present: a textarea for content input is in the document
    - generate_button_present: a "Generate" or submit button is in the document
    - platform_switch: clicking the "X" platform button makes it the active platform
    - generate_triggers_api: after filling input and clicking Generate, POST /api/content/generate is called
    - prefill_from_url: when URL has ?prefill=Hello, textarea is pre-filled with "Hello"

    CI job: add a `frontend-test` job to `.github/workflows/ci.yml`:
    - Job name: "Frontend Tests"
    - Runs on: ubuntu-latest
    - Steps: checkout, setup-node@v4 (node 18, cache npm, cache-dependency-path frontend/package-lock.json), npm ci (in frontend/), run tests
    - Test command: `CI=true npm test -- --watchAll=false`
    - No coverage gate (first iteration — tests must pass, not a coverage requirement)
    - Must be listed in the CI file at the same level as backend-lint, backend-test-*, frontend-build jobs
  </behavior>
  <action>
    Create `frontend/src/__tests__/pages/StrategyDashboard.test.jsx`:
    - Mock framer-motion at top: `jest.mock('framer-motion', ...)` (see interfaces block)
    - Mock EventSource globally (StrategyDashboard uses useNotifications which opens SSE)
    - Wrap in TestWrapper (MemoryRouter + AuthProvider)
    - For loading skeleton test: render component but do NOT await — check immediately for skeleton element (check for `animate-pulse` class or aria-busy or a specific testid if one exists)
    - For empty state: default handlers return `{ cards: [] }` — just waitFor the empty message
    - For cards rendered: use `server.use()` to override `/api/strategy` to return 2 cards with distinct `recommendation_id` values and `platform: 'linkedin'`
    - For approve_calls_api: spy on the POST handler using `server.use()` with a callback that records the call, then click the approve button and `waitFor` the spy to be called

    Create `frontend/src/__tests__/pages/ContentStudio.test.jsx`:
    - Wrap in TestWrapper
    - For generate_triggers_api test: use `userEvent.type()` to fill the textarea with "Test topic", then click the Generate button, then `waitFor` the POST to /api/content/generate
    - For prefill test: render with MemoryRouter initialEntries `['/dashboard/studio?prefill=Hello']` and use `useSearchParams`-aware render
    - ContentStudio uses `useSearchParams` — MemoryRouter with initialEntries covers this

    Update `.github/workflows/ci.yml`:
    Read the current file first. Add the `frontend-test` job after the existing `frontend-build` job. The job must NOT use `continue-on-error` (per Phase 21 CI strictness). The full job:
    ```yaml
      frontend-test:
        name: Frontend Tests
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - name: Set up Node 18
            uses: actions/setup-node@v4
            with:
              node-version: "18"
              cache: npm
              cache-dependency-path: frontend/package-lock.json

          - name: Install dependencies
            run: |
              cd frontend
              npm ci

          - name: Run frontend tests
            run: |
              cd frontend
              CI=true npm test -- --watchAll=false
    ```

    No snapshot tests. All behavior tests use `screen.getBy*` / `screen.queryBy*` assertions.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/frontend && CI=true npx craco test --watchAll=false --testPathPattern="__tests__/pages/(StrategyDashboard|ContentStudio)" 2>&1 | grep -E "Tests:|PASS|FAIL"</automated>
  </verify>
  <done>16 tests pass for StrategyDashboard + ContentStudio. CI job added to ci.yml. `grep "frontend-test" .github/workflows/ci.yml` returns the job definition. No `continue-on-error` on the job.</done>
</task>

</tasks>

<verification>
Full suite verification after both Plans 02 and 03:

```bash
cd /Users/kuldeepsinhparmar/thookAI-production/frontend
CI=true npx craco test --watchAll=false 2>&1 | grep -E "Test Suites:|Tests:"
```
Expected output:
- "Test Suites: 8 passed, 8 total" (4 from Plan 02, 4 from Plan 03)
- "Tests: 78 passed, 78 total" (48 from Plan 02, 30 from Plan 03)
Minimum acceptable: 45+ tests across 10+ test files

CI job check:
```bash
grep -A 20 "frontend-test:" /Users/kuldeepsinhparmar/thookAI-production/.github/workflows/ci.yml
```
Must show job with `npm test -- --watchAll=false` and no `continue-on-error`.

No snapshot assertions in any test file:
```bash
grep -r "toMatchSnapshot\|toMatchInlineSnapshot" /Users/kuldeepsinhparmar/thookAI-production/frontend/src/__tests__/
```
Must return empty.
</verification>

<success_criteria>
- 30 component/page tests pass in 4 files
- No snapshot-only tests — every test verifies behavior via screen queries or API call assertions
- StrategyDashboard tests cover: render, loading, empty, cards shown, approve/dismiss interaction
- ContentStudio tests cover: render, platform switch, generate triggers API, URL prefill
- Sidebar tests cover: render, nav links, credits display, logout button, mobile backdrop
- NotificationBell tests cover: render, unread badge, dropdown open/close, empty state, mark-all-read
- `frontend-test` CI job added with no continue-on-error flag
- Total across all 3 plans: 78+ tests, 8 test files (well above 45+ / 10+ requirement)
</success_criteria>

<output>
After completion, create `.planning/phases/23-frontend-unit-test-suite/23-03-SUMMARY.md`
</output>
