---
phase: 07-platform-features-admin-frontend-quality
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_frontend_quality.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Mobile sidebar in Sidebar.jsx toggles open/closed via isOpen prop and onClose callback, with responsive Tailwind classes"
    - "ErrorBoundary component catches render errors and shows recovery UI with a refresh button"
    - "Empty state components in ContentLibrary, Campaigns, and Templates show friendly CTAs when data lists are empty"
    - "AuthContext handles 401 responses by clearing token and redirecting to /auth"
    - "All frontend pages have valid imports, no undefined component references, and consistent API URL patterns"
  artifacts:
    - path: "backend/tests/test_frontend_quality.py"
      provides: "Static analysis tests verifying frontend code quality patterns"
      min_lines: 100
  key_links:
    - from: "frontend/src/pages/Dashboard/Sidebar.jsx"
      to: "isOpen/onClose props"
      via: "React props"
      pattern: "isOpen|onClose"
    - from: "frontend/src/components/ErrorBoundary.jsx"
      to: "getDerivedStateFromError"
      via: "React lifecycle"
      pattern: "getDerivedStateFromError|componentDidCatch"
    - from: "frontend/src/context/AuthContext.jsx"
      to: "localStorage"
      via: "token management"
      pattern: "thook_token|localStorage"
---

<objective>
Verify frontend quality requirements (mobile sidebar, error boundary, empty states, 401 handling, no console errors) via static analysis tests that grep and parse frontend source files.

Purpose: Frontend code exists but has not been verified for quality patterns. Since we cannot run a browser in CI, we use file-level static analysis to confirm: responsive classes exist on Sidebar, ErrorBoundary catches errors, empty states have CTAs, AuthContext handles 401, and all pages have valid imports.
Output: backend/tests/test_frontend_quality.py with static analysis tests for UI-01 through UI-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/pages/Dashboard/Sidebar.jsx
@frontend/src/components/ErrorBoundary.jsx
@frontend/src/context/AuthContext.jsx
@frontend/src/pages/Dashboard/ContentLibrary.jsx
@frontend/src/pages/Dashboard/Campaigns.jsx
@frontend/src/pages/Dashboard/Templates.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static analysis tests for mobile sidebar, error boundary, and empty states</name>
  <files>backend/tests/test_frontend_quality.py</files>
  <read_first>
    frontend/src/pages/Dashboard/Sidebar.jsx
    frontend/src/components/ErrorBoundary.jsx
    frontend/src/pages/Dashboard/ContentLibrary.jsx
    frontend/src/pages/Dashboard/Campaigns.jsx
    frontend/src/pages/Dashboard/Templates.jsx
  </read_first>
  <behavior>
    UI-01 Mobile Sidebar:
    - Sidebar.jsx accepts isOpen prop (grep confirms "isOpen" in function signature or destructured props)
    - Sidebar.jsx accepts onClose prop (grep confirms "onClose")
    - Sidebar.jsx has responsive Tailwind classes: at least one of "md:hidden", "lg:block", "fixed", or mobile-related responsive breakpoint class
    - Sidebar.jsx contains an overlay or backdrop element for mobile (a click handler on an overlay div)

    UI-02 Error Boundary:
    - ErrorBoundary.jsx exports a class component with getDerivedStateFromError
    - ErrorBoundary.jsx has componentDidCatch for error logging
    - ErrorBoundary.jsx renders a recovery UI with a button that calls window.location.reload()
    - ErrorBoundary is imported and used in App.js or Dashboard/index.jsx

    UI-03 Empty States:
    - ContentLibrary.jsx has conditional rendering for empty state (e.g., "No content" or "Get started" or "Create your first" text)
    - Campaigns.jsx has conditional rendering for empty state
    - Templates.jsx has conditional rendering for empty state
    - Each empty state has a CTA element (button or link with action text)
  </behavior>
  <action>
    Create backend/tests/test_frontend_quality.py using Python's pathlib to read frontend source files and assert patterns exist.

    ```python
    import pytest
    from pathlib import Path

    FRONTEND_ROOT = Path(__file__).parent.parent.parent / "frontend" / "src"
    ```

    1. TestMobileSidebar (UI-01):
       - Read Sidebar.jsx content as string
       - Assert "isOpen" appears in content (prop name)
       - Assert "onClose" appears in content (prop name)
       - Assert at least one responsive class: check for "md:" or "lg:" or "xl:" Tailwind breakpoint prefix in content
       - Assert "fixed" or "absolute" appears (mobile overlay positioning)
       - Assert at least one click handler exists near close/overlay logic (onClick appears in file)

    2. TestErrorBoundary (UI-02):
       - Read ErrorBoundary.jsx content
       - Assert "getDerivedStateFromError" in content
       - Assert "componentDidCatch" in content
       - Assert "hasError" in content (state flag)
       - Assert "window.location.reload" in content (recovery action)
       - Read App.js content — assert "ErrorBoundary" is imported (check for "import" line with "ErrorBoundary") OR check Dashboard/index.jsx for ErrorBoundary usage

    3. TestEmptyStates (UI-03):
       - Read ContentLibrary.jsx — assert it contains at least one of: "empty", "no content", "get started", "create your first", "nothing here" (case-insensitive search)
       - Read Campaigns.jsx — same check for empty state text
       - Read Templates.jsx — same check for empty state text
       - For each file, verify there's a button or CTA near the empty state: search for "button" or "Button" or "onClick" or "Link" or "href" appearing in the file

    All tests should be simple synchronous pytest tests (no async needed — just file reads).
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_frontend_quality.py::TestMobileSidebar tests/test_frontend_quality.py::TestErrorBoundary tests/test_frontend_quality.py::TestEmptyStates -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_frontend_quality.py exists
    - grep "class TestMobileSidebar" backend/tests/test_frontend_quality.py returns a match
    - grep "class TestErrorBoundary" backend/tests/test_frontend_quality.py returns a match
    - grep "class TestEmptyStates" backend/tests/test_frontend_quality.py returns a match
    - grep "isOpen" backend/tests/test_frontend_quality.py returns a match
    - grep "getDerivedStateFromError" backend/tests/test_frontend_quality.py returns a match
    - All tests pass with 0 failures
  </acceptance_criteria>
  <done>Mobile sidebar has responsive props/classes, error boundary catches and recovers, empty states show CTAs in ContentLibrary/Campaigns/Templates</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Static analysis tests for 401 handling and frontend page integrity</name>
  <files>backend/tests/test_frontend_quality.py</files>
  <read_first>
    frontend/src/context/AuthContext.jsx
    frontend/src/App.js
    frontend/src/pages/Dashboard/index.jsx
    backend/tests/test_frontend_quality.py
  </read_first>
  <behavior>
    UI-04 401 Redirect:
    - AuthContext.jsx stores token in localStorage under key "thook_token"
    - AuthContext.jsx has a logout function that clears the token
    - AuthContext.jsx or a global fetch interceptor checks for 401 status
    - If no explicit 401 handling in AuthContext, the ProtectedRoute in App.js redirects unauthenticated users to /auth

    UI-05 No JS Console Errors:
    - All Dashboard pages import from valid paths (no typos in import paths)
    - All page components in Dashboard/index.jsx are imported and have corresponding files
    - No obvious undefined variable references (all imported components are used in JSX)
    - REACT_APP_BACKEND_URL is consistently used (not mixed with hardcoded localhost URLs)
  </behavior>
  <action>
    Append two test classes to backend/tests/test_frontend_quality.py:

    4. TestAuthRedirect (UI-04):
       - Read AuthContext.jsx content
       - Assert "thook_token" in content (localStorage key)
       - Assert "logout" in content (function name)
       - Assert "localStorage.removeItem" or "localStorage.clear" in content (token cleanup on logout)
       - Read App.js content
       - Assert "ProtectedRoute" in content (route guard component)
       - Assert "/auth" in content (redirect destination for unauthenticated users)
       - Assert "Navigate" in content (React Router redirect component)

    5. TestFrontendPageIntegrity (UI-05):
       - Read Dashboard/index.jsx
       - Extract all import statements (lines starting with "import")
       - For each local import (starting with "./" or "@/"), verify the target file exists in the filesystem
       - Read App.js — verify all imported page components have corresponding files
       - Search all Dashboard/*.jsx files for "REACT_APP_BACKEND_URL" or "BACKEND_URL" usage — verify no hardcoded "http://localhost" URLs appear (except in fallback patterns like `|| "http://localhost"`)
       - Verify no page file has `undefined` as a standalone word that isn't in a comment or typeof check (basic sanity)

    For the import verification: use a simple regex to extract import paths, resolve them relative to the file location, and check Path.exists(). Account for aliases like "@/" mapping to "src/".
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_frontend_quality.py::TestAuthRedirect tests/test_frontend_quality.py::TestFrontendPageIntegrity -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "class TestAuthRedirect" backend/tests/test_frontend_quality.py returns a match
    - grep "class TestFrontendPageIntegrity" backend/tests/test_frontend_quality.py returns a match
    - grep "thook_token" backend/tests/test_frontend_quality.py returns a match
    - grep "ProtectedRoute" backend/tests/test_frontend_quality.py returns a match
    - All tests pass with 0 failures
  </acceptance_criteria>
  <done>401 handling verified: ProtectedRoute guards dashboard, logout clears token, redirect to /auth. All frontend pages have valid imports and consistent API URL patterns.</done>
</task>

</tasks>

<verification>
All 5 UI requirements have passing static analysis tests:
- UI-01: Mobile sidebar has responsive props and Tailwind classes
- UI-02: Error boundary catches errors and shows recovery UI
- UI-03: Empty states with CTAs in ContentLibrary, Campaigns, Templates
- UI-04: 401 redirect via ProtectedRoute and AuthContext logout
- UI-05: All frontend pages have valid imports and no hardcoded URLs

```bash
cd backend && python -m pytest tests/test_frontend_quality.py -x -v
```
</verification>

<success_criteria>
- backend/tests/test_frontend_quality.py exists with 5 test classes
- All tests pass (0 failures)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-features-admin-frontend-quality/07-04-SUMMARY.md`
</output>
