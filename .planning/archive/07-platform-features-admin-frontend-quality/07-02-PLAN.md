---
phase: 07-platform-features-admin-frontend-quality
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_sharing_notifications_webhooks.py
autonomous: true
requirements:
  - FEAT-06
  - FEAT-07
  - FEAT-08
  - FEAT-09

must_haves:
  truths:
    - "Persona share endpoint generates a share_token and returns a share_url at /creator/{token}"
    - "Public persona endpoint at /api/persona/public/{token} returns the persona card without requiring auth"
    - "Viral card endpoint at /api/viral-card/analyze accepts pasted posts and returns a persona card with card_id and share_url"
    - "SSE notification stream at /api/notifications/stream returns text/event-stream with heartbeat and notification payloads"
    - "Webhook registration at /api/webhooks creates endpoint with HMAC secret and test ping delivers payload"
  artifacts:
    - path: "backend/tests/test_sharing_notifications_webhooks.py"
      provides: "Tests for FEAT-06 through FEAT-09"
      min_lines: 200
  key_links:
    - from: "backend/routes/persona.py"
      to: "db.persona_shares"
      via: "insert_one / find_one"
      pattern: "db\\.persona_shares"
    - from: "backend/routes/viral_card.py"
      to: "db.viral_cards"
      via: "insert_one"
      pattern: "db\\.viral_cards"
    - from: "backend/routes/notifications.py"
      to: "services/notification_service"
      via: "function imports"
      pattern: "get_notifications|get_unread_count|mark_read|mark_all_read"
    - from: "backend/routes/webhooks.py"
      to: "services/webhook_service"
      via: "function imports"
      pattern: "register_webhook|list_webhooks|delete_webhook|test_webhook"
---

<objective>
Verify persona sharing, viral card generation, SSE notifications, and outbound webhooks endpoints work correctly via automated tests.

Purpose: These four features have full backend implementations but no tests proving correctness. Tests validate that persona sharing generates tokens, viral cards analyze posts, SSE streams deliver events, and webhook CRUD + test delivery works.
Output: backend/tests/test_sharing_notifications_webhooks.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/persona.py
@backend/routes/viral_card.py
@backend/routes/notifications.py
@backend/routes/webhooks.py
@backend/services/notification_service.py
@backend/services/webhook_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test persona sharing and viral card endpoints</name>
  <files>backend/tests/test_sharing_notifications_webhooks.py</files>
  <read_first>
    backend/routes/persona.py
    backend/routes/viral_card.py
    backend/tests/conftest.py
  </read_first>
  <behavior>
    FEAT-06 Persona Sharing:
    - POST /api/persona/share with authenticated user who has a persona creates share_token and returns share_url containing "/creator/"
    - POST /api/persona/share with user who has no persona returns 404 with "Persona not found"
    - POST /api/persona/share when active share exists returns existing token (no duplicate)
    - GET /api/persona/public/{token} returns persona card data without auth
    - GET /api/persona/public/{token} with invalid token returns 404
    - DELETE /api/persona/share/{share_id} revokes the share

    FEAT-07 Viral Card:
    - POST /api/viral-card/analyze with 100+ chars of posts_text returns card with card_id starting "vc_" and share_url "/discover/"
    - POST /api/viral-card/analyze with < 100 chars returns 400 "at least 100 characters"
    - POST /api/viral-card/analyze with invalid platform returns 400
    - GET /api/viral-card/{card_id} retrieves previously generated card
    - GET /api/viral-card/nonexistent returns 404
  </behavior>
  <action>
    Create backend/tests/test_sharing_notifications_webhooks.py with two test classes:

    1. TestPersonaSharing:
       - Use httpx.AsyncClient with ASGITransport(app=app)
       - Override get_current_user dependency: {"user_id": "test-share-user"}
       - Patch "database.db" — mock db.persona_engines.find_one to return a persona doc with user_id, card, voice_fingerprint fields
       - Mock db.persona_shares.find_one to return None (no existing share) on first call
       - Mock db.persona_shares.insert_one as AsyncMock
       - Mock db.users.find_one to return {"user_id": "test-share-user", "subscription_tier": "pro"}
       - Test POST /api/persona/share with body {"expiry_days": 30} → assert 200, response has "share_token", "share_url" containing "/creator/"
       - Test POST /api/persona/share when persona_engines.find_one returns None → assert 404
       - Test GET /api/persona/public/{token} — mock db.persona_shares.find_one to return share doc, mock db.persona_engines.find_one to return persona → assert 200 with persona card data
       - Test GET /api/persona/public/bad-token → assert 404

    2. TestViralCard:
       - NO auth required (public endpoint)
       - Patch "database.db" — mock db.viral_cards.insert_one as AsyncMock
       - Patch "services.llm_client.LlmChat" to avoid real LLM calls — mock the entire LlmChat class and its send_message method to return a JSON string with the expected card fields (writing_voice_descriptor, personality_archetype, etc.)
       - Test POST /api/viral-card/analyze with body {"posts_text": "A" * 150, "platform": "linkedin"} → assert 200, response has "card_id" starting with "vc_", "share_url" containing "/discover/"
       - Test POST /api/viral-card/analyze with body {"posts_text": "short", "platform": "linkedin"} → assert 400
       - Test POST /api/viral-card/analyze with invalid platform → assert 400
       - Mock db.viral_cards.find_one to return card doc → Test GET /api/viral-card/{card_id} → assert 200

    Import pattern:
    ```python
    import pytest
    from httpx import AsyncClient, ASGITransport
    from unittest.mock import AsyncMock, patch, MagicMock, PropertyMock
    from server import app
    from auth_utils import get_current_user
    ```
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_sharing_notifications_webhooks.py::TestPersonaSharing tests/test_sharing_notifications_webhooks.py::TestViralCard -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "class TestPersonaSharing" backend/tests/test_sharing_notifications_webhooks.py returns a match
    - grep "class TestViralCard" backend/tests/test_sharing_notifications_webhooks.py returns a match
    - grep "share_token" backend/tests/test_sharing_notifications_webhooks.py returns a match
    - grep "vc_" backend/tests/test_sharing_notifications_webhooks.py returns a match
    - All tests in these two classes pass with 0 failures
  </acceptance_criteria>
  <done>Persona sharing and viral card endpoints verified: share tokens generated, public view works, viral card analysis returns card with share URL</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test SSE notifications and outbound webhooks</name>
  <files>backend/tests/test_sharing_notifications_webhooks.py</files>
  <read_first>
    backend/routes/notifications.py
    backend/services/notification_service.py
    backend/routes/webhooks.py
    backend/services/webhook_service.py
    backend/tests/test_sharing_notifications_webhooks.py
  </read_first>
  <behavior>
    FEAT-08 SSE Notifications:
    - GET /api/notifications returns list of notifications for authenticated user
    - GET /api/notifications?unread_only=true filters to unread only
    - POST /api/notifications/{id}/read marks notification as read, returns success
    - POST /api/notifications/read-all marks all as read, returns updated count
    - GET /api/notifications/count returns unread_count integer
    - GET /api/notifications/stream returns response with Content-Type text/event-stream

    FEAT-09 Outbound Webhooks:
    - POST /api/webhooks with url + events creates webhook and returns secret
    - GET /api/webhooks returns list of user's webhooks
    - DELETE /api/webhooks/{id} removes webhook
    - POST /api/webhooks/{id}/test sends test ping
    - GET /api/webhooks/events returns list of supported event types including "job.completed"
  </behavior>
  <action>
    Append two test classes to backend/tests/test_sharing_notifications_webhooks.py:

    3. TestSSENotifications:
       - Override get_current_user: {"user_id": "test-notif-user"}
       - Patch "services.notification_service.get_notifications" as AsyncMock returning [{"notification_id": "n1", "type": "job_completed", "message": "Content ready", "read": False, "created_at": "2025-06-01T00:00:00Z"}]
       - Patch "services.notification_service.get_unread_count" as AsyncMock returning 3
       - Patch "services.notification_service.mark_read" as AsyncMock returning True
       - Patch "services.notification_service.mark_all_read" as AsyncMock returning 5
       - Test GET /api/notifications → assert 200, response has "notifications" list and "count"
       - Test POST /api/notifications/n1/read → assert 200 with "marked as read"
       - Test POST /api/notifications/read-all → assert 200 with "updated" field
       - Test GET /api/notifications/count → assert 200 with "unread_count" == 3
       - Test GET /api/notifications/stream → assert response status 200 and content-type header contains "text/event-stream" (use stream=True on httpx, read first chunk or just check headers)

    4. TestOutboundWebhooks:
       - Override get_current_user: {"user_id": "test-webhook-user"}
       - Patch "services.webhook_service.register_webhook" as AsyncMock returning {"webhook_id": "wh_123", "url": "https://example.com/hook", "events": ["job.completed"], "secret": "whsec_test123", "created_at": "2025-06-01T00:00:00Z"}
       - Patch "services.webhook_service.list_webhooks" as AsyncMock returning [{"webhook_id": "wh_123", "url": "https://example.com/hook", "events": ["job.completed"]}]
       - Patch "services.webhook_service.delete_webhook" as AsyncMock returning True
       - Patch "services.webhook_service.test_webhook" as AsyncMock returning {"success": True, "status_code": 200}
       - Test POST /api/webhooks with body {"url": "https://example.com/hook", "events": ["job.completed"]} → assert 201 with "webhook_id" and "secret"
       - Test GET /api/webhooks → assert 200 with "webhooks" list
       - Test DELETE /api/webhooks/wh_123 → assert 200 with "deleted": True
       - Test POST /api/webhooks/wh_123/test → assert 200 with "success": True
       - Test GET /api/webhooks/events → assert 200 with "events" list containing dict with name "job.completed"
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_sharing_notifications_webhooks.py::TestSSENotifications tests/test_sharing_notifications_webhooks.py::TestOutboundWebhooks -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "class TestSSENotifications" backend/tests/test_sharing_notifications_webhooks.py returns a match
    - grep "class TestOutboundWebhooks" backend/tests/test_sharing_notifications_webhooks.py returns a match
    - grep "text/event-stream" backend/tests/test_sharing_notifications_webhooks.py returns a match
    - grep "job.completed" backend/tests/test_sharing_notifications_webhooks.py returns a match
    - All tests in these two classes pass with 0 failures
  </acceptance_criteria>
  <done>SSE notifications stream verified (heartbeat, event delivery, CRUD), webhook registration/test/delete verified with HMAC secret generation</done>
</task>

</tasks>

<verification>
All 4 FEAT requirements (06-09) have passing automated tests:
- FEAT-06: Persona sharing creates token, public view renders card
- FEAT-07: Viral card analyzes posts, generates shareable card
- FEAT-08: SSE notifications stream, list, mark-read, count
- FEAT-09: Webhook CRUD, test ping, supported events

```bash
cd backend && python -m pytest tests/test_sharing_notifications_webhooks.py -x -v
```
</verification>

<success_criteria>
- backend/tests/test_sharing_notifications_webhooks.py exists with 4 test classes
- All tests pass (0 failures)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-features-admin-frontend-quality/07-02-SUMMARY.md`
</output>
