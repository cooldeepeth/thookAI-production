---
phase: 07-platform-features-admin-frontend-quality
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_platform_features.py
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - FEAT-05

must_haves:
  truths:
    - "Content repurposing endpoint accepts a job_id and target platforms, calls bulk_repurpose, and returns adapted content"
    - "Campaign CRUD endpoints create, list, update, and delete campaign groupings owned by the authenticated user"
    - "Template marketplace lists seed templates filtered by category and hook_type, and use-template pre-fills generation"
    - "Content export endpoint returns CSV with date-range filtering and single-job export returns text/json"
    - "Post history import accepts batch posts array and calls process_bulk_import for persona training"
  artifacts:
    - path: "backend/tests/test_platform_features.py"
      provides: "Comprehensive tests for FEAT-01 through FEAT-05"
      min_lines: 200
  key_links:
    - from: "backend/routes/repurpose.py"
      to: "agents/repurpose.bulk_repurpose"
      via: "async function call"
      pattern: "bulk_repurpose"
    - from: "backend/routes/campaigns.py"
      to: "db.campaigns"
      via: "Motor async CRUD"
      pattern: "db\\.campaigns\\."
    - from: "backend/routes/templates.py"
      to: "db.templates"
      via: "Motor async queries"
      pattern: "db\\.templates\\."
    - from: "backend/routes/content.py"
      to: "csv.writer"
      via: "StreamingResponse export"
      pattern: "generate_csv"
    - from: "backend/routes/onboarding.py"
      to: "agents/learning.process_bulk_import"
      via: "async function call"
      pattern: "process_bulk_import"
---

<objective>
Verify all platform feature endpoints (repurpose, campaigns, templates, export, import) work correctly via automated tests.

Purpose: These five features have backend code written but no tests proving they work end-to-end. Each test exercises the route handler with mocked dependencies to confirm request validation, database operations, and response shapes are correct.
Output: backend/tests/test_platform_features.py with tests covering FEAT-01 through FEAT-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/repurpose.py
@backend/routes/campaigns.py
@backend/routes/templates.py
@backend/routes/content.py
@backend/routes/onboarding.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test repurpose, campaigns, and template marketplace endpoints</name>
  <files>backend/tests/test_platform_features.py</files>
  <read_first>
    backend/routes/repurpose.py
    backend/routes/campaigns.py
    backend/routes/templates.py
    backend/tests/conftest.py
  </read_first>
  <behavior>
    FEAT-01 Repurpose:
    - POST /api/content/repurpose with valid job_id + target_platforms calls bulk_repurpose and returns success
    - POST /api/content/repurpose with invalid platform returns 400 with "Invalid platforms" detail
    - GET /api/content/repurpose/preview/{job_id} returns preview without creating jobs

    FEAT-02 Campaigns:
    - POST /api/campaigns with name creates campaign with campaign_id starting with "camp_", status "active"
    - GET /api/campaigns returns list of user's campaigns (not other users')
    - PUT /api/campaigns/{id} updates name/status fields
    - DELETE /api/campaigns/{id} removes the campaign
    - POST /api/campaigns with invalid platform returns 400
    - GET /api/campaigns/{id} for non-existent campaign returns 404

    FEAT-03 Templates:
    - GET /api/templates returns list of templates with category and hook_type fields
    - GET /api/templates?category=thought_leadership filters by category
    - POST /api/templates/{id}/use returns template content suitable for generation form pre-fill
    - GET /api/templates/{id} returns single template details
  </behavior>
  <action>
    Create backend/tests/test_platform_features.py with three test classes:

    1. TestRepurposeEndpoints:
       - Use httpx.AsyncClient with app from server (import app from server)
       - Use app.dependency_overrides[get_current_user] = lambda: {"user_id": "test-user-feat"} for auth bypass (per Phase 06 decision)
       - Patch "agents.repurpose.bulk_repurpose" to return {"success": True, "jobs": [{"job_id": "rp_1", "platform": "x"}]}
       - Test POST /api/content/repurpose with body {"job_id": "job_123", "target_platforms": ["x", "instagram"]}
       - Assert 200 response with "success" and "jobs" keys
       - Test invalid platform returns 400

    2. TestCampaignEndpoints:
       - Patch "database.db" with AsyncMock for campaigns collection
       - Mock db.campaigns.insert_one to return AsyncMock()
       - Mock db.campaigns.find_one to return a campaign doc with campaign_id, user_id, name, status
       - Mock db.campaigns.find().sort().skip().limit().to_list() for list endpoint
       - Test POST /api/campaigns creates campaign (assert response has campaign_id starting with "camp_")
       - Test GET /api/campaigns returns list
       - Test GET /api/campaigns/{id} returns single campaign
       - Test PUT /api/campaigns/{id} updates fields
       - Test DELETE /api/campaigns/{id} returns success
       - Test POST with invalid platform returns 400

    3. TestTemplateEndpoints:
       - Patch "database.db" for templates collection
       - Mock db.templates.find() chain to return sample templates with fields: template_id, title, category, hook_type, platform, content_structure, upvotes, uses_count
       - Test GET /api/templates returns list
       - Test GET /api/templates?category=thought_leadership filters correctly (verify query passed to find includes {"category": "thought_leadership"})
       - Test GET /api/templates/{id} returns single template
       - Test POST /api/templates/{id}/use returns content for pre-fill

    All tests use pytest.mark.asyncio decorator. Import patterns:
    ```python
    import pytest
    from httpx import AsyncClient, ASGITransport
    from unittest.mock import AsyncMock, patch, MagicMock
    from server import app
    from auth_utils import get_current_user
    ```
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_platform_features.py::TestRepurposeEndpoints tests/test_platform_features.py::TestCampaignEndpoints tests/test_platform_features.py::TestTemplateEndpoints -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_platform_features.py exists
    - grep "class TestRepurposeEndpoints" backend/tests/test_platform_features.py returns a match
    - grep "class TestCampaignEndpoints" backend/tests/test_platform_features.py returns a match
    - grep "class TestTemplateEndpoints" backend/tests/test_platform_features.py returns a match
    - grep "bulk_repurpose" backend/tests/test_platform_features.py returns a match
    - grep "camp_" backend/tests/test_platform_features.py returns a match
    - All tests in these three classes pass with 0 failures
  </acceptance_criteria>
  <done>Repurpose, campaign CRUD, and template marketplace endpoints all verified via passing tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test content export and post history import endpoints</name>
  <files>backend/tests/test_platform_features.py</files>
  <read_first>
    backend/routes/content.py
    backend/routes/onboarding.py
    backend/tests/test_platform_features.py
  </read_first>
  <behavior>
    FEAT-04 Content Export:
    - GET /api/content/job/{job_id}/export?format=text returns PlainTextResponse with Content-Disposition header
    - GET /api/content/job/{job_id}/export?format=json returns JSON with Content-Disposition header
    - GET /api/content/export/bulk?format=csv returns CSV StreamingResponse with header row
    - GET /api/content/export/bulk?format=csv&from_date=2025-01-01&to_date=2025-12-31 applies date filter
    - GET /api/content/export/bulk for user with no jobs returns CSV with only header row

    FEAT-05 Post History Import:
    - POST /api/onboarding/import-history with valid posts array calls process_bulk_import
    - POST /api/onboarding/import-history with empty posts returns 400 or appropriate error
  </behavior>
  <action>
    Append two test classes to backend/tests/test_platform_features.py:

    4. TestContentExportEndpoints:
       - Patch database.db for content_jobs collection
       - Mock db.content_jobs.find_one to return a job with fields: job_id, user_id, platform, status, final_content (dict with "text" key), raw_input, created_at (datetime)
       - Test GET /api/content/job/test-job/export?format=text → assert response status 200, Content-Type contains "text/plain", Content-Disposition contains "thookai-test-job.txt"
       - Test GET /api/content/job/test-job/export?format=json → assert 200, Content-Disposition contains ".json"
       - Test GET /api/content/job/nonexistent/export → assert 404
       - Mock db.content_jobs.find().sort().to_list() returning 2 sample jobs for bulk export
       - Test GET /api/content/export/bulk?format=csv → assert 200, Content-Type contains "text/csv", Content-Disposition contains "thookai-export.csv"
       - Test GET /api/content/export/bulk?format=csv&from_date=2025-01-01&to_date=2025-12-31 → assert 200 and query includes created_at filter

    5. TestPostHistoryImport:
       - Patch "agents.learning.process_bulk_import" as AsyncMock returning {"processed": 3, "skipped": 0}
       - Test POST /api/onboarding/import-history with body {"posts": [{"text": "post 1", "platform": "linkedin"}, {"text": "post 2", "platform": "x"}, {"text": "post 3", "platform": "linkedin"}], "source": "manual"} → assert 200 with processed count
       - Verify process_bulk_import was called with correct user_id and valid_posts list

    Important: Read content.py lines 860-1005 to understand the _extract_content_text helper and CSV generation logic. The bulk export uses StreamingResponse — test the response status and headers, not the streamed body content.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production/backend && python -m pytest tests/test_platform_features.py::TestContentExportEndpoints tests/test_platform_features.py::TestPostHistoryImport -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "class TestContentExportEndpoints" backend/tests/test_platform_features.py returns a match
    - grep "class TestPostHistoryImport" backend/tests/test_platform_features.py returns a match
    - grep "thookai-export.csv" backend/tests/test_platform_features.py returns a match
    - grep "process_bulk_import" backend/tests/test_platform_features.py returns a match
    - All tests in these two classes pass with 0 failures
  </acceptance_criteria>
  <done>Content export (single + bulk CSV) and post history import endpoints verified via passing tests</done>
</task>

</tasks>

<verification>
All 5 FEAT requirements (01-05) have passing automated tests:
- FEAT-01: Repurpose endpoint calls bulk_repurpose correctly
- FEAT-02: Campaign CRUD operations work with proper validation
- FEAT-03: Template marketplace lists/filters/uses templates
- FEAT-04: Export produces CSV/text/JSON with proper headers
- FEAT-05: Import history calls process_bulk_import with valid posts

```bash
cd backend && python -m pytest tests/test_platform_features.py -x -v
```
</verification>

<success_criteria>
- backend/tests/test_platform_features.py exists with 5 test classes
- All tests pass (0 failures)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/07-platform-features-admin-frontend-quality/07-01-SUMMARY.md`
</output>
