---
phase: 20-frontend-e2e-integration
plan: 04
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - e2e/billing.spec.ts
  - e2e/agency.spec.ts
  - e2e/helpers/mock-api.ts
autonomous: true
requirements: [E2E-03, E2E-04]

must_haves:
  truths:
    - "Billing E2E test covers plan selection, Stripe checkout redirect, subscription activation, credit usage, and upgrade flow"
    - "Agency E2E test covers workspace creation, member invitation, context switching, and content generation as a workspace member"
    - "Stripe checkout is mocked via page.route() — no real Stripe calls"
    - "All agency RBAC boundaries are verified: viewer cannot publish, editor can generate, owner can manage members"
  artifacts:
    - path: "e2e/billing.spec.ts"
      provides: "Billing flow E2E test"
      contains: "billing"
      min_lines: 100
    - path: "e2e/agency.spec.ts"
      provides: "Agency workspace E2E test"
      contains: "workspace"
      min_lines: 100
  key_links:
    - from: "e2e/billing.spec.ts"
      to: "e2e/helpers/mock-api.ts"
      via: "import mockStripeCheckout"
      pattern: "import.*mockStripeCheckout"
    - from: "e2e/billing.spec.ts"
      to: "frontend/src/pages/Dashboard"
      via: "navigates to billing/settings pages"
      pattern: "page.goto.*dashboard"
    - from: "e2e/agency.spec.ts"
      to: "frontend/src/pages/Dashboard/AgencyWorkspace"
      via: "navigates to agency workspace pages"
      pattern: "page.goto.*dashboard"
---

<objective>
Create Playwright E2E tests for the billing flow (plan selection through upgrade) and agency workspace flow (creation through team content generation). Both specs use Playwright's built-in route mocking for Stripe and API calls.

Purpose: E2E-03 (billing E2E) and E2E-04 (agency workspace E2E)
Output: Two comprehensive Playwright specs covering billing and agency journeys
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-frontend-e2e-integration/20-01-SUMMARY.md
@frontend/src/App.js
@backend/routes/billing.py
@backend/routes/agency.py
@backend/services/stripe_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create billing E2E spec (E2E-03)</name>
  <files>e2e/billing.spec.ts, e2e/helpers/mock-api.ts</files>
  <read_first>
    - e2e/helpers/auth.ts (from Plan 01 — signUp/logIn helpers)
    - e2e/helpers/mock-api.ts (from Plan 01 — mockStripeCheckout)
    - e2e/fixtures.ts (from Plan 01 — authenticatedPage fixture)
    - backend/routes/billing.py (understand billing endpoints: checkout, portal, credits, subscription status)
    - backend/services/stripe_service.py (understand checkout session creation, price IDs)
    - backend/services/credits.py (understand TIER_CONFIGS, credit deduction)
    - frontend/src/pages/Dashboard/Settings.jsx (understand billing/settings UI — plan selection, upgrade buttons)
    - frontend/src/pages/Dashboard/ (look for billing-related components)
  </read_first>
  <action>
1. Update `e2e/helpers/mock-api.ts` to add:
   - `mockBillingEndpoints(page)` — intercepts:
     - `GET /api/billing/subscription` returns `{ subscription_tier: 'free', credits: 100, status: 'active' }`
     - `POST /api/billing/checkout` returns `{ checkout_url: 'https://checkout.stripe.com/test_session_123' }` (never actually navigates to real Stripe)
     - `GET /api/billing/credits` returns `{ credits: 100, tier: 'free' }`
     - `POST /api/billing/portal` returns `{ portal_url: 'https://billing.stripe.com/test_portal' }`
   - `mockSubscriptionActive(page, tier)` — overrides subscription endpoint to return `{ subscription_tier: tier, credits: 500, status: 'active' }`
   - `mockCreditDeduction(page, remaining)` — intercepts credit check to return `{ credits: remaining }`

2. Create `e2e/billing.spec.ts`:

   **`test.describe.serial('E2E-03: Billing Flow', () => { ... })`**

   **`test('free tier user sees upgrade options', async ({ page }) => { ... })`**
   - Log in as a free-tier test user
   - Apply `mockBillingEndpoints(page)`
   - Navigate to settings/billing page
   - Assert plan cards or upgrade buttons are visible
   - Assert current plan shows "Free"

   **`test('selecting Pro plan initiates Stripe checkout', async ({ page }) => { ... })`**
   - Apply `mockBillingEndpoints(page)`
   - Navigate to billing/pricing page
   - Click "Upgrade to Pro" or equivalent button
   - Intercept the navigation to Stripe checkout URL
   - Assert the checkout request was made to `/api/billing/checkout` with `{ plan: 'pro', interval: 'monthly' }` (or similar)
   - Verify the response contains a checkout_url starting with `https://checkout.stripe.com`

   **`test('subscription becomes active after checkout callback', async ({ page }) => { ... })`**
   - Apply `mockSubscriptionActive(page, 'pro')` (simulates post-checkout state)
   - Navigate to dashboard or billing page
   - Assert subscription status shows "Pro" and "Active"
   - Assert credit balance shows the Pro tier allocation (e.g., 500 credits)

   **`test('credit usage decrements balance', async ({ page }) => { ... })`**
   - Apply mocks for content generation (consume credits) and credit check (returns reduced balance)
   - Trigger a content generation action
   - After generation, check credit display updates (credits decreased)
   - Assert credits never go negative

   **`test('upgrade from Pro to Studio', async ({ page }) => { ... })`**
   - Apply `mockSubscriptionActive(page, 'pro')` initially
   - Navigate to billing page
   - Click "Upgrade to Studio" button
   - Mock checkout for Studio tier
   - Assert checkout request includes Studio price ID reference
   - Apply `mockSubscriptionActive(page, 'studio')` for post-upgrade state
   - Verify UI updates to show "Studio" tier

   CRITICAL: All Stripe interactions are mocked. The test NEVER navigates to a real Stripe URL. Use `page.route('**/checkout.stripe.com/**', route => route.fulfill({ status: 200, body: '<html>Mock Stripe</html>' }))` to catch any accidental navigation.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production && npx playwright test e2e/billing.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/billing.spec.ts` exists with 5 serial billing tests
    - Tests cover: free tier display, checkout initiation, subscription activation, credit usage, upgrade
    - All Stripe URLs intercepted via `page.route()` — no real Stripe navigation
    - `e2e/helpers/mock-api.ts` exports `mockBillingEndpoints`, `mockSubscriptionActive`, `mockCreditDeduction`
    - `npx playwright test e2e/billing.spec.ts` exits 0
  </acceptance_criteria>
  <done>Billing E2E spec validates the complete payment lifecycle with mocked Stripe: plan selection, checkout, activation, credit usage, and upgrade</done>
</task>

<task type="auto">
  <name>Task 2: Create agency workspace E2E spec (E2E-04)</name>
  <files>e2e/agency.spec.ts, e2e/helpers/mock-api.ts</files>
  <read_first>
    - e2e/helpers/auth.ts (signUp/logIn helpers)
    - e2e/helpers/mock-api.ts (current state after Task 1 additions)
    - backend/routes/agency.py (workspace creation, member invite, role management endpoints)
    - frontend/src/pages/Dashboard/AgencyWorkspace/ (understand workspace UI components)
    - frontend/src/pages/Dashboard/Sidebar.jsx (understand workspace context switching)
  </read_first>
  <action>
1. Update `e2e/helpers/mock-api.ts` to add:
   - `mockAgencyEndpoints(page)` — intercepts:
     - `POST /api/agency/workspaces` returns `{ workspace_id: 'ws-e2e-001', name: 'E2E Agency', owner_id: 'user-e2e' }`
     - `GET /api/agency/workspaces` returns `[{ workspace_id: 'ws-e2e-001', name: 'E2E Agency', role: 'owner' }]`
     - `POST /api/agency/workspaces/ws-e2e-001/invite` returns `{ status: 'invited', email: 'member@test.io' }`
     - `GET /api/agency/workspaces/ws-e2e-001/members` returns member list with roles
   - `mockWorkspaceContext(page, workspaceId)` — intercepts requests to include workspace context header

2. Create `e2e/agency.spec.ts`:

   **`test.describe.serial('E2E-04: Agency Workspace', () => { ... })`**

   **`test('owner creates a new workspace', async ({ page }) => { ... })`**
   - Log in as agency-tier test user (mock subscription to 'agency')
   - Apply `mockAgencyEndpoints(page)`
   - Navigate to agency/workspace management page
   - Find "Create Workspace" button and click
   - Fill workspace name: "E2E Test Agency"
   - Submit the form
   - Wait for workspace creation response
   - Assert workspace appears in sidebar or workspace list
   - Assert user has "Owner" role displayed

   **`test('owner invites a member', async ({ page }) => { ... })`**
   - Apply agency mocks
   - Navigate to workspace members/settings page
   - Find "Invite Member" button or input
   - Enter member email: `member-e2e@test.io`
   - Select role: "editor"
   - Click invite/send button
   - Wait for invite API response
   - Assert member appears in the members list with "editor" role and "invited" status

   **`test('member context switch shows workspace content', async ({ page }) => { ... })`**
   - Log in as the invited member (mock login response with workspace membership)
   - Mock workspace list to include the test workspace
   - Navigate to dashboard
   - Find workspace switcher in sidebar
   - Select "E2E Test Agency" workspace
   - Wait for context switch (URL change or content reload)
   - Assert workspace name appears in the header/sidebar
   - Assert content shown is scoped to the workspace (mock workspace-scoped content list)

   **`test('member can generate content in workspace context', async ({ page }) => { ... })`**
   - Continue in workspace context from previous test
   - Apply content generation mocks with workspace-scoped response
   - Navigate to content studio
   - Generate content (same flow as critical path but within workspace)
   - Assert generated content is tagged with workspace ID
   - Assert content appears in workspace content list

   **`test('viewer cannot access publish actions', async ({ page }) => { ... })`**
   - Log in as a viewer-role member (mock workspace membership with role: "viewer")
   - Apply agency mocks
   - Navigate to content studio within workspace
   - Assert that publish/approve buttons are either hidden or disabled
   - If a publish button exists, click it and assert an error or permission denied message
   - This verifies RBAC enforcement at the UI level

   IMPORTANT: Agency workspace may not have full frontend UI implemented. If the AgencyWorkspace directory is empty or stub, the test should focus on:
   - API-level verification via `page.route()` intercepting and asserting correct request payloads
   - Navigation to the agency page and verifying basic rendering (even if just a "Coming Soon" page)
   - Document any missing UI as a known gap in the test
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production && npx playwright test e2e/agency.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/agency.spec.ts` exists with 5 serial agency workspace tests
    - Tests cover: workspace creation, member invitation, context switching, workspace content generation, RBAC viewer restriction
    - All API calls mocked via `page.route()` — no real backend agency calls needed
    - `e2e/helpers/mock-api.ts` exports `mockAgencyEndpoints`, `mockWorkspaceContext`
    - `npx playwright test e2e/agency.spec.ts` exits 0
  </acceptance_criteria>
  <done>Agency workspace E2E spec validates the complete team workflow: create workspace, invite members, switch context, generate content as member, and verify viewer RBAC restriction</done>
</task>

</tasks>

<verification>
- `npx playwright test e2e/billing.spec.ts e2e/agency.spec.ts --reporter=list` shows all tests passing
- `grep -c "test(" e2e/billing.spec.ts` shows 5 tests
- `grep -c "test(" e2e/agency.spec.ts` shows 5 tests
- `grep -r "waitForTimeout" e2e/billing.spec.ts e2e/agency.spec.ts` returns 0 results
- `grep -c "page.route" e2e/billing.spec.ts` shows 5+ route mocks
</verification>

<success_criteria>
- Billing E2E validates: free tier display, checkout initiation, subscription activation, credit usage, upgrade
- Agency E2E validates: workspace creation, member invite, context switching, team generation, RBAC enforcement
- All external calls (Stripe, LLM) are mocked for deterministic execution
- No flaky patterns (waitForTimeout banned)
- Combined 10 new E2E tests from this plan
</success_criteria>

<output>
After completion, create `.planning/phases/20-frontend-e2e-integration/20-04-SUMMARY.md`
</output>
