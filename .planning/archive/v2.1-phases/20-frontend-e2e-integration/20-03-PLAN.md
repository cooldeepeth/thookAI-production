---
phase: 20-frontend-e2e-integration
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - e2e/critical-path.spec.ts
  - e2e/helpers/test-data.ts
autonomous: true
requirements: [E2E-02]

must_haves:
  truths:
    - "Playwright executes the full critical path: signup -> onboard -> generate -> schedule -> publish -> analytics -> strategy -> approve"
    - "All E2E steps pass against the live dev stack with mocked LLM responses"
    - "No step uses waitForTimeout — only waitForSelector, waitForURL, waitForResponse"
    - "Test uses deterministic mock LLM for content generation (not real API calls)"
  artifacts:
    - path: "e2e/critical-path.spec.ts"
      provides: "Full critical path E2E test"
      contains: "critical path"
      min_lines: 150
    - path: "e2e/helpers/test-data.ts"
      provides: "Deterministic test data for E2E flows"
      min_lines: 30
  key_links:
    - from: "e2e/critical-path.spec.ts"
      to: "e2e/helpers/auth.ts"
      via: "import signUp and logIn helpers"
      pattern: "import.*auth"
    - from: "e2e/critical-path.spec.ts"
      to: "e2e/helpers/mock-api.ts"
      via: "import mockLLMResponse and mockOnboardingLLM"
      pattern: "import.*mock-api"
    - from: "e2e/critical-path.spec.ts"
      to: "frontend/src/App.js"
      via: "navigates to routes defined in App.js (/auth, /onboarding, /dashboard)"
      pattern: "page.goto.*/(auth|onboarding|dashboard)"
---

<objective>
Implement the full critical path E2E test covering the complete user journey: signup, onboard (persona creation), content generation, scheduling, publishing simulation, analytics viewing, strategy recommendation, and one-click approve. This is the most important E2E test — it proves the core product works end-to-end.

Purpose: E2E-02 — Critical path E2E (signup -> onboard -> generate -> schedule -> publish -> analytics -> strategy -> approve)
Output: A comprehensive Playwright spec that validates the complete user journey with deterministic mocks
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-frontend-e2e-integration/20-01-SUMMARY.md
@frontend/src/App.js
@frontend/src/pages/AuthPage.jsx
@frontend/src/pages/Onboarding
@frontend/src/pages/Dashboard
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test data helpers and critical path spec structure</name>
  <files>e2e/helpers/test-data.ts, e2e/critical-path.spec.ts</files>
  <read_first>
    - e2e/helpers/auth.ts (from Plan 01 — understand signUp/logIn signatures)
    - e2e/helpers/mock-api.ts (from Plan 01 — understand mock helper signatures)
    - e2e/fixtures.ts (from Plan 01 — understand custom test fixture)
    - frontend/src/pages/AuthPage.jsx (understand form field names/selectors for signup/login)
    - frontend/src/pages/Onboarding/ (understand onboarding wizard flow — steps, buttons, form fields)
    - frontend/src/pages/Dashboard/ContentStudio/ (understand content generation form fields)
    - frontend/src/pages/Dashboard/DashboardHome.jsx (understand dashboard layout)
    - frontend/src/pages/Dashboard/Analytics.jsx (understand analytics page)
    - frontend/src/pages/Dashboard/StrategyDashboard.jsx (understand strategy feed)
  </read_first>
  <action>
1. Create `e2e/helpers/test-data.ts`:
   - Export `TEST_USER` object: `{ email: `e2e-critical-${Date.now()}@test.io`, password: 'TestPass123!', name: 'E2E Critical Path' }`
   - Export `MOCK_PERSONA` object: a minimal persona engine object with `card`, `voice_fingerprint`, `content_identity` fields matching the schema in CLAUDE.md Section 5
   - Export `MOCK_CONTENT_JOB` object: `{ job_id: 'e2e-job-001', status: 'reviewing', platform: 'linkedin', draft: 'AI is transforming content creation...', final_content: 'AI is transforming content creation for creators and founders.' }`
   - Export `MOCK_SCHEDULE_RESULT` object: `{ schedule_id: 'e2e-sched-001', status: 'scheduled', scheduled_at: new Date(Date.now() + 86400000).toISOString() }`
   - Export `MOCK_STRATEGY_CARD` object: `{ rec_id: 'e2e-rec-001', topic: 'AI content trends', signal: 'trending_topic', why_now: 'AI content creation is trending on LinkedIn this week', platform: 'linkedin' }`

2. Create `e2e/critical-path.spec.ts`:

   Import `test, expect` from `@playwright/test` (or from `../fixtures` if custom fixture needed).
   Import helpers from `./helpers/auth`, `./helpers/mock-api`, `./helpers/test-data`.

   **`test.describe('E2E-02: Critical Path', () => { ... })`**

   Use `test.describe.serial()` so tests run in order and share state (later steps depend on earlier ones).

   **Step 1: `test('signup creates account and redirects', async ({ page }) => { ... })`**
   - Navigate to `/auth`
   - Wait for the auth form to be visible
   - Find and click the signup tab/toggle (inspect AuthPage.jsx to find the correct selector)
   - Fill email, password, name fields
   - Click submit button
   - `await page.waitForURL('**/onboarding**')` OR `**/dashboard**` depending on whether user is redirected to onboarding or dashboard
   - Assert URL contains expected path

   **Step 2: `test('onboarding wizard generates persona', async ({ page }) => { ... })`**
   - Apply `mockOnboardingLLM(page)` to intercept persona generation API call
   - Navigate to `/onboarding` (or continue from signup redirect)
   - Complete each onboarding step:
     - For each question, find the text input or selection and provide an answer
     - Click "Next" or equivalent button after each answer
     - Use `page.waitForSelector` to confirm each step loads
   - After final step, wait for persona generation API response via `page.waitForResponse(resp => resp.url().includes('/api/onboarding'))`
   - Assert redirect to dashboard after persona is created

   **Step 3: `test('content generation produces draft', async ({ page }) => { ... })`**
   - Apply `mockLLMResponse(page, MOCK_CONTENT_JOB)` to intercept generation API
   - Navigate to content studio page within dashboard
   - Find the content generation form (platform selector, content type, raw input textarea)
   - Select "LinkedIn" as platform
   - Type "AI trends in content creation" in the input
   - Click generate button
   - Wait for response via `page.waitForResponse(resp => resp.url().includes('/api/content'))`
   - Assert a draft appears on the page (content card, modal, or inline preview)

   **Step 4: `test('content can be scheduled', async ({ page }) => { ... })`**
   - Mock the schedule API endpoint via `page.route('**/api/content/*/schedule', ...)`
   - Find the schedule/approve button on the generated content
   - Click schedule
   - Wait for schedule confirmation UI element
   - Assert schedule success indicator is visible

   **Step 5: `test('analytics page loads with data', async ({ page }) => { ... })`**
   - Mock analytics endpoint via `page.route('**/api/analytics/**', ...)` returning sample analytics data
   - Navigate to analytics page within dashboard
   - Wait for analytics data to render
   - Assert at least one chart or metric is visible on the page

   **Step 6: `test('strategy dashboard shows recommendations', async ({ page }) => { ... })`**
   - Mock strategy endpoint via `page.route('**/api/strategy**', ...)` returning `[MOCK_STRATEGY_CARD]`
   - Navigate to strategy dashboard page
   - Wait for recommendation card to appear
   - Assert card contains "AI content trends" text and "why now" rationale

   **Step 7: `test('one-click approve triggers generation', async ({ page }) => { ... })`**
   - Mock the strategy approve endpoint via `page.route('**/api/strategy/*/approve', ...)`
   - Mock the content generation endpoint (same as Step 3)
   - Find the "Approve" button on the strategy recommendation card
   - Click approve
   - Wait for response confirming generation was triggered
   - Assert generation-in-progress indicator appears (spinner, loading state, or success toast)

   **CRITICAL PATTERNS (from research):**
   - NEVER use `page.waitForTimeout()` — always use `waitForSelector`, `waitForURL`, `waitForResponse`
   - Use `page.route()` for API mocking — deterministic, fast, no real LLM calls
   - Each test should be resilient to async rendering: use `expect(locator).toBeVisible({ timeout: 10000 })`
   - Use data-testid selectors where possible: `page.getByTestId('...')` — if selectors are ambiguous, fall back to `page.getByRole()` or `page.getByText()`
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production && npx playwright test e2e/critical-path.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/critical-path.spec.ts` exists with 7 serial test steps
    - Test covers: signup, onboarding, content generation, scheduling, analytics, strategy, approve
    - No `waitForTimeout` calls in the spec (grep returns 0 matches)
    - All API calls are mocked via `page.route()` for deterministic execution
    - `e2e/helpers/test-data.ts` exports `TEST_USER`, `MOCK_PERSONA`, `MOCK_CONTENT_JOB`, `MOCK_SCHEDULE_RESULT`, `MOCK_STRATEGY_CARD`
    - `npx playwright test e2e/critical-path.spec.ts` exits 0 (all 7 steps pass)
  </acceptance_criteria>
  <done>Critical path E2E test validates the complete user journey from signup through one-click strategy approve with deterministic mocks</done>
</task>

<task type="auto">
  <name>Task 2: Harden critical path spec with error scenarios and retries</name>
  <files>e2e/critical-path.spec.ts</files>
  <read_first>
    - e2e/critical-path.spec.ts (the spec created in Task 1)
    - playwright.config.ts (check retry config)
  </read_first>
  <action>
1. Add error boundary tests within the critical path spec (separate `test.describe` block):

   **`test.describe('E2E-02: Error Resilience', () => { ... })`**

   **`test('shows error toast on generation API failure', async ({ page }) => { ... })`**
   - Log in as test user
   - Mock `/api/content/generate` to return 500 with `{ detail: 'LLM service unavailable' }`
   - Attempt content generation
   - Assert an error toast or error message is visible on the page
   - Assert the page does not crash (no white screen)

   **`test('handles auth token expiry gracefully', async ({ page }) => { ... })`**
   - Log in as test user
   - Mock all API calls to return 401 (simulating expired token)
   - Navigate to dashboard
   - Assert redirect to `/auth` page (or login modal appears)

   **`test('onboarding handles LLM timeout', async ({ page }) => { ... })`**
   - Mock onboarding persona generation to take 30s (simulate timeout via `page.route` with delayed fulfillment)
   - Start onboarding flow
   - Assert a loading indicator is visible during the wait
   - After mock resolves, assert persona is created OR timeout error is shown

2. Review all `page.route()` calls in the spec — ensure each mock route handler calls `route.fulfill()` (not `route.continue()`) to prevent real API hits.

3. Add a `test.afterEach` hook that captures a screenshot on failure:
   ```typescript
   test.afterEach(async ({ page }, testInfo) => {
     if (testInfo.status !== testInfo.expectedStatus) {
       await page.screenshot({ path: `test-results/failure-${testInfo.title}.png` });
     }
   });
   ```

4. Verify no `waitForTimeout` exists anywhere in the file — use grep.
  </action>
  <verify>
    <automated>cd /Users/kuldeepsinhparmar/thookAI-production && grep -c "waitForTimeout" e2e/critical-path.spec.ts; echo "Expected: 0 occurrences"</automated>
  </verify>
  <acceptance_criteria>
    - 3 additional error resilience tests added to `critical-path.spec.ts`
    - Error scenarios: API 500, auth 401 redirect, LLM timeout
    - `afterEach` hook captures screenshots on failure
    - Zero `waitForTimeout` calls in the entire spec
    - All route mocks use `route.fulfill()` (not `route.continue()`)
  </acceptance_criteria>
  <done>Critical path spec hardened with error scenarios, screenshot-on-failure, and zero waitForTimeout usage confirmed</done>
</task>

</tasks>

<verification>
- `npx playwright test e2e/critical-path.spec.ts --reporter=list` shows all tests passing
- `grep -r "waitForTimeout" e2e/` returns 0 results
- `wc -l e2e/critical-path.spec.ts` shows 150+ lines
- `grep -c "test(" e2e/critical-path.spec.ts` shows 10 tests (7 happy path + 3 error)
</verification>

<success_criteria>
- Complete critical path E2E covering the full user journey from signup to strategy approve
- All LLM/external API calls mocked for deterministic, fast execution
- Error resilience tests prove the UI degrades gracefully
- No flaky patterns (no waitForTimeout, all waits are event-driven)
- Screenshots captured automatically on failure for debugging
</success_criteria>

<output>
After completion, create `.planning/phases/20-frontend-e2e-integration/20-03-SUMMARY.md`
</output>
