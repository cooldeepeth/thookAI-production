[pytest]
asyncio_mode = auto
addopts = -p randomly
filterwarnings =
    error::RuntimeWarning
markers =
    integration: requires Docker and external services (deselect with '-m "not integration"')
norecursedirs =
    tests/load
